# NoWhere 일일 검토 보고 — 2026-09-10 (KST, 11:02)

> 작성: 마스터 역할 일일 검토 / 기준 문서: docs/PRODUCT_VISION.md · OPERATIONS.md · ORDER_FLOW.md · ROADMAP.md · TASKS.md · CHANGELOG.md
> 검토·보고·파일 저장만 수행, 코드·문서 내용 변경 없음.
> 직전 저장: 본 파일(01:02 검토본) — 이후 구간(09-10 01:02 ~ 11:02) 검사. **신규 커밋 0건(HEAD `c0c4a71` 유지). 코드 편집 2건(NotificationsView 01:03, OrderCard 01:37) + UI 보고서 1건(0103) + 11:01:11 로컬 빌드 산출물·배포 패키지 재생성. 검증 신규 사실: 지연돼 있던 찜·빠른매칭·태그 검색·추천 백엔드 테스트를 이번 검토에서 직접 실행 → 전부 통과. 신규 발견 1건(연결 운행 카드 조건 근거 미노출 — 문서 불일치).**

## ① 프로젝트 상태 요약

- **HEAD `c0c4a71`(09-09 17:20, perf) 유지 — 신규 커밋 0건.** origin/main 대비 **ahead 26**(변동 없음). **push·CI(ci.yml) 실동작 여전히 0회.**
- **미커밋 상태: 총 70건(수정 50 · 미추적 20)** — 직전(수정 50 · 미추적 20)과 동일 규모. 소스·테스트·마이그레이션·모델이 전부 미커밋 상태로 중간 스냅샷 부재.
- **구간 내 코드 편집 2건** (01:02 이후 → 11:02 까지 약 10시간 중 실제 편집은 01:03·01:37 두 건, 이후 01:37→11:02 약 9시간 25분 무편집):
  - `frontend/src/views/NotificationsView.vue`(01:03) — UI 보고서 0103의 문구 재문안: `처리할 일(…)로 이동` → `처리할 일(…) 화면으로 이동`. 템플릿 텍스트만 변경, 로직 영향 없음.
  - `frontend/src/components/orders/OrderCard.vue`(01:37) — 카드에 **찜 하트(`favoriteable`/`favorited` props, `favorite-change` emit) · 조건 별점(`ScoreStars`, 100점→5점 환산·근거 모달) · 태그 칩(`tag-search` emit / 타 화면은 `localStorage:nowhere:market:pendingTag` 경유)** 통합. 배선 확인: [MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1221-L1256) · [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue#L145-L147) 정상 연결, OrderCard 단독 import 오류 없음.
- **11:01:11 로컬 빌드 산출물·배포 패키지 재생성** — `public/index.html`·`public/assets/*`(gitignore 대상)와 `.deploy/deploy_frontend.tar`(1.95MB)가 11:01:11 갱신. **로컬 빌드 + 패키징까지 확인되며 서버 반영 여부는 미확인**(본 검토는 SSH·배포 미수행). 옛 백엔드 패키지 `deploy_backend.tar`는 08-29 이후 그대로.
- **검증 신규 사실(이번 검토에서 직접 실행)**:
  - `OrderFavoriteTest` + `MarketQuickMatchListTest` → **10 passed (49 assertions)**
  - `OrderApiTest` + `RecommendationApiTest` → **72 passed (428 assertions)**
  - ⇒ 직전 보고까지 "테스트 실행 기록 없음"으로 남아 있던 **찜 · 빠른매칭 · 태그 검색 · 홈 추천(동점 정렬·시간 여유) 백엔드 검증이 실제로 통과**함을 확인(미실행 대기 항목 해소).
- **상태 마커(문서 기준, 변동 없음)**: TASKS Current = 품질 블록(Q-3·Q-5·Q-6). IMPROVEMENT_PLAN Q-5 `[~]` 유지. Phase D·상용화 = 사용자 지시 대기. 신기능(찜·태그 검색·빠른매칭·시간 여유·동점 정렬)은 TASKS Current·예정 목록에 없는 개발 → 완료 후 Q-6 문서 반영 필요.
- **문서 잔존 모순(유지)**: IMPROVEMENT_PLAN §1-행1·§2-2 자동 만료 메모 ↔ Phase C-1 완료 상충, §2-1 '전면 미구현' 12항목 미정리, **A-5 문구·동작 불일치(④ 신규 참조)**.

## ② 진행 중·대기 중 작업

- **진행 중(미커밋, 구현은 사실상 완결)**: 찜 블록 — 백엔드(`OrderFavorite`·`OrderFavoriteService`·마이그레이션) + 마켓/찜 목록 카드 하트 + 상세 히어로 하트 + `/favorites` + 조회 시점 자동 정리 + 취소·배차 시 찜 기사 안내 알림. **남은 것: 커밋(사용자 지시 후)뿐** — 백엔드 테스트는 이번에 통과 확인.
- **진행 중(미커밋)**: 태그 검색(카드 태그 칩 → 마켓 검색 + LIKE 검색 + `tags` Attribute 전환) / 마켓 '내 조건'(빠른매칭: 가까운 서비스 시각순·시작 운행 제외) / 홈 추천(더보기·플래시 가드·동점 정렬·'시간 여유 충분' 근거) — 테스트 통과 확인, 브라우저 실동작(비교 UX)만 미확인.
- **이번 구간 커밋: 없음** — HEAD c0c4a71 유지, ahead 26.
- **대기(유지)**: push → CI 최초 실동작 · Q-5 브라우저 점검 · Q-6 문서 완결 · Phase D · 상용화 · 미커밋 70건 일괄 커밋.

## ③ 해야 할 기능 추가·개발 항목

- **신규(검토 발견)**: **연결 운행(복귀) 카드의 조건 근거 노출 배선 누락** — 백엔드는 복귀 행에 `match_score`·`match_reasons`를 부착하지만([OrderListService.php](file:///d:/wwwroot/nowhere/app/Services/Order/OrderListService.php#L702)), 마켓의 복귀 섹션 OrderCard는 `:show-match-reasons`를 받지 않아 점수·근거가 표시되지 않음(④ 참조).
- **검증 대기 해소 후 남은 작업**: 찜·태그 검색·빠른매칭·추천 후속분 → **문서(Q-6) 반영 + 커밋**.
- **예정 기능(문서 기준, 변동 없음)**: Q-3 CI 최초 실행(push 후) / Q-5 브라우저 점검 마무리 / Q-6 문서 완결(신기능 상태 반영 포함) / Phase D(개인화 2~3단계·동선 최적화·오늘 수익 목표·배차 엔진 — 운영 데이터 축적 후) / Settlement 고도화(정산서·세금계산서) / 상용화(고정 도메인·SSL·GitHub 리포 private·비밀번호 로그인 비활성화) / 2~3차 부가 기능(쇼핑·중고거래·항공편·주차·여행지).
- **UI 후보(갱신)**: ~~NotificationsView '처리할 일(…)로 이동' 문구~~ → **0103에서 수정 완료(후보 종료)**. 유지 후보 — OrderFavoritesView eyebrow `--danger`(빨간 하트 통일 의도와 공존, 안정화 후 재검토) · QuickMatchPanel `.qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 · coin+'원' 이중 표기 · '정산 대기중' 캐노니컬 · ChatImageSheet 3분할 · UiChip 기본 스타일 · MyMarketView FAB '＋' 글리프. 0103 표준 스캔(용어·보조용언·소형 폰트·`rel="noopener"`) 신규 위반 0건.

## ④ 버그 수정·개선 필요 항목

- **이번 구간 수정 2건**: ① NotificationsView 문구 조사 어색(괄호 뒤 '로') 정리 (0103). ② OrderCard 카드 통합(찜·별점·태그) — 신규 기능 배선.
- **신규 발견(개선 필요) — 연결 운행 카드 조건 근거 미노출**: [MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1221-L1231)의 복귀 섹션 OrderCard에 `:show-match-reasons`가 없어(`:favoriteable`·`:favorited`만 전달) [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L159)의 `condOpenable`이 false → 복귀 행은 별점·근거가 렌더되지 않음. 반면 [IMPROVEMENT_PLAN.md](file:///d:/wwwroot/nowhere/docs/IMPROVEMENT_PLAN.md#L154) A-5는 "복귀(return-routes) 행에도 match_score·match_reasons 부착, OrderCard에 `:show-match-reasons` 전달"로 **완료 처리**되어 있어 문서·코드 불일치. (전체 목록 카드는 [L1251](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1250-L1251)에서 정상 전달.) 수정 여부·우선순위는 사용자 판단 필요.
- **검토 관찰(유지)**: ① 찜 정리는 `cancelled`(전원)·`accepted`(배차, 새 수행자 제외) 훅 + 조회 시점 자동 정리로 커버. **숨김(`is_hidden`)·`admin_hold`(보류)·조건 변경 안내(`notifyChanged` 호출 지점)·draft 복귀 경로는 미검증.** ② `tags` 캐스트 → Attribute 전환([Order.php L505](file:///d:/wwwroot/nowhere/app/Models/Order.php#L505-L513), `JSON_UNESCAPED_UNICODE` 저장) 영향 — 기존 저장분(MySQL)·`array` 캐스트 의존 코드 회귀 확인 필요(테스트는 통과). ③ 미커밋 70건 지속, 중간 스냅샷 커밋 부재.
- **런타임 검증(부분 해소)**: 백엔드 테스트는 이번에 통과 확인했으나 **브라우저 실동작(찜 하트 상태 동기화·태그 칩 이동·빠른매칭 정렬·동점 정렬) 미확인**, CI 실동작 0회 유지.
- **유지 — 코드 후보**: 역할 리터럴 `'Driver'` → `ROLE_DRIVER` 치환, Q-5 후보군.
- **리포 위생**: 미추적 20건 — UI 보고서 10건(1401~0103, 0103·0013 신규) + 신규 소스/테스트(OrderFavorite·OrderFavoriteService·마이그레이션·ScoreStars·OrderFavoritesView·MarketWaveSeeder·OrderFavoriteTest·MarketQuickMatchListTest) + `docs/daily-review/2026-09-10.md`. `.edge-ride-profile2/` 디스크 삭제 잔여.

## ⑤ 마스터 관점의 우선순위·다음 행동 추천

1. **(신규) 연결 운행 카드 조건 근거 노출 여부 결정** — A-5 문서는 완료로 기록, 코드는 미배선. 의도적 제외라면 문서 정정, 의도한 노출이라면 `:show-match-reasons` 한 줄 추가로 정합. (코드 변경은 지시 후)
2. **미커밋 기능 블록 마감** — 백엔드 검증 통과 확인 완료 → 11:01 빌드 산출물 기준 **브라우저 실동작 확인 후 커밋**(커밋은 사용자 지시 후).
3. **찜 안내 훅 잔여 경로 점검** — `notifyChanged`(조건 변경) 호출 지점, 숨김·보류·draft 복귀 시 찜 처리 일관성.
4. **태그 검색 UX 확인** — 태그 칩 → 마켓 검색(같은 화면 emit / 타 화면 pendingTag), 한글 매칭(SQLite·MySQL), `tags` Attribute 전환 회귀.
5. **신기능 상태를 문서(Q-6)에 반영**(사용자 지시 후) — 찜·태그 검색·빠른매칭·시간 여유·동점 정렬은 TASKS Current(품질 블록)에 없는 개발 → IMPROVEMENT_PLAN/TASKS 정합 정리.
6. **미커밋 70건 일괄 커밋 + 기록물 커밋**(사용자 지시 후) — 소스 블록 / UI 보고서 10건 / 신규 테스트 3종 / MarketWaveSeeder / 일일 검토 파일(09-09·09-10).
7. **push → CI 최초 실행**(사용자 지시 후) — ahead 26 push 후 ci.yml 첫 통과 확인.
8. **Q-5 마무리** — MarketWaveSeeder(3,000건) 마켓 부하·스크롤 + 반응형·다크모드 브라우저 전수 점검(`[~]` → `[x]`).

## ⑥ 오늘 해야 할 일

- [ ] **(신규) 연결 운행 카드 조건 근거 미노출 처리 결정** — MarketView 복귀 섹션 OrderCard `:show-match-reasons` 추가 vs A-5 문서 정정 (사용자 판단)
- [ ] **브라우저 실동작 확인** — 찜 하트 토글·찜 목록 `/favorites`·태그 칩 검색 이동·빠른매칭 정렬·홈 동점 정렬 (11:01 빌드 산출물 기준)
- [ ] **미커밋 소스 일괄 커밋** — 기존 배치 + UI 접근성 8건 + 홈 + 빠른매칭 + 찜 블록 + 후속분(태그 검색·찜 알림·OrderCard 통합) (커밋은 사용자 지시 후)
- [ ] 찜 이탈 경로 잔여 확인 — `notifyChanged`(조건 변경) 호출 지점·숨김/보류/draft 복귀 시 찜 처리 (취소·배차·조회 정리는 구현됨)
- [ ] 태그 검색 한글 매칭 확인 — SQLite/MySQL 양쪽 + `tags` Attribute 전환 후 기존 동작 회귀 여부 (테스트는 통과)
- [ ] **기록물 커밋** — UI 보고서 10건(1401~0103) + MarketWaveSeeder + 신규 테스트 3종 + 일일 검토 파일(09-09·09-10) (사용자 지시 후)
- [ ] **push → CI 최초 실행 확인** — ahead 26 push 후 ci.yml 통과 (push는 사용자 지시 후)
- [ ] **Q-5 반응형·다크모드 브라우저 점검** — MarketWaveSeeder(3,000건) 마켓 부하·스크롤 + 전 화면 `[~]` 마무리
- [ ] 배포 패키지 확인 — 11:01:11 재생성된 `.deploy/deploy_frontend.tar`의 서버 반영 여부 확인 (배포는 사용자 지시 후)
- [ ] **Q-6 문서 반영** — 찜·태그 검색·빠른매칭·시간 여유·동점 정렬 구현 상태 + A-5 불일치·자동 만료 모순·§2-1 목록 정리 (문서 변경은 사용자 지시 후)
- [ ] OrderFavoritesView eyebrow `--danger` 컬러 재검토 — 찜 기능 안정화 후 의미 오용/의도 구분
- [ ] `.edge-ride-profile2/` 디스크 삭제 — gitignore 반영 완료, 디스크 잔여 (사용자 확인 후)
- [ ] 다음 작업 후보 확정 — 신기능·Q-6 완결 후 Phase D vs 상용화 (사용자 지시 대기)

---

*직전 저장 보고서(09-10 01:02본) 대비 **변경 있음**: ① **신규 커밋 0건**(HEAD `c0c4a71`·ahead 26 유지) ② **코드 편집 2건** — NotificationsView(01:03, 문구 재문안) · **OrderCard(01:37, 찜 하트·조건 별점·태그 칩 카드 통합)** 이후 01:37→11:02 무편집 ③ **UI 보고서 0103 1건 신규(01:03)** — '처리할 일(…)로 이동' 문구 수정 + build 통과, 반복 후보 1건 해소 ④ **11:01:11 로컬 빌드 산출물 + `.deploy/deploy_frontend.tar` 재생성**(서버 반영 미확인) ⑤ **검증 신규 사실 — 이번 검토에서 직접 실행: OrderFavoriteTest+MarketQuickMatchListTest 10 passed, OrderApiTest+RecommendationApiTest 72 passed**(직전까지 '테스트 미실행' 대기 항목 해소) ⑥ **신규 발견 1건 — 연결 운행(복귀) 카드에 `:show-match-reasons` 미배선으로 조건 근거 미노출, IMPROVEMENT_PLAN A-5 완료 기록과 불일치**. ⑥ 대기 항목(일괄 커밋·push→CI·Q-5·Q-6·다음 블록 확정)은 전부 변동 없음.*
