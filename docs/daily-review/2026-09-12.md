# NoWhere 일일 검토 보고 — 2026-09-12 (KST, 23:01)

> 작성: 마스터 역할 일일 검토 / 기준 문서: docs/PRODUCT_VISION.md · OPERATIONS.md · ORDER_FLOW.md · ROADMAP.md · TASKS.md · CHANGELOG.md
> 검토·보고·파일 저장만 수행, 코드·문서 내용 변경 없음.
> 직전 저장: `daily-review/2026-09-12.md`(22:01 검토본) — 이후 구간(09-12 22:01 ~ 23:01) 검사. **코드·커밋·문서 신규 변경 0건. 운영 DB 재동기화(22:58:56)만 발생 → 신규 작업 없음.**

## 직전 보고서 대비 변경 요약 (22:01 → 23:01)

- **변경 없음(코드·커밋·문서) + 운영 데이터만 재증가(DB 재동기화 22:58:56).**
  - HEAD `c2efb55`(09-11 18:11) **그대로** · `origin/main...HEAD` = **`0 0`**(미푸시 0) · **신규 커밋 0건**.
  - 워킹트리 **69건 유지**(tracked **47** · untracked **22**) · `git diff --shortstat` **47 files / +1897 / −291**(22:01과 동일, **변동 없음**).
  - 소스 최신 수정 시각도 **09-12 20:30**(`RecommendationApiTest.php`·`OrderWorkspaceListBuilder.php`) 그대로 — 22:01~23:01 사이 **코드 파일 변경 0건**(그 이후 최신 mtime은 직전 보고서 22:02분).
  - 문서 mtime **전부 미갱신**: TASKS 09-11 18:11 · CHANGELOG 09-11 19:22 · ORDER_FLOW·OPERATIONS 09-11 18:51 · DEPLOY 09-11 15:36 · API 09-11 12:13 · IMPROVEMENT_PLAN·PRODUCT_VISION 09-10 · ROADMAP 09-09.
  - **서버 → 로컬 DB 재동기화(22:58:56)** — 운영 인제스천 지속으로 재고만 증가. **신청·운행·정산은 여전히 0건.**
    - `orders` **227 → 244건**: **draft 78 → 82 · published 130 → 135 · cancelled 19 → 27**.
    - `order_ingestions` **158 → 169건 전부 `processed`**.
    - `order_terms` **mapped 32 · ignored 3 유지 · pending 87 → 92**(미매핑 +5).
    - `order_groups` **40 → 45건**(이름 다수가 중국어 유입 원문).
    - published 135건 실측: **`vehicle_type` 미지정 79 · `amount_value` 미지정 63** · **`service_datetime` 전건 NULL(135)** · 구분 **pickup 57 / sending 62 / point 16**.
    - **`claimed_at` 0 · `order_claims` 0 · `order_offers` 0 · `settlements` 0** — 재고 135건 대비 기사 신청 0건.
  - A-5(복귀 카드 조건 근거) `:show-match-reasons` 전역 **3건 유지**([HomeView.vue L803·L826](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L803) · [MarketView.vue L1251](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1251)) — 복귀 섹션 미배선 **39회차**.
  - 테스트는 **코드 무변경이므로 재실행하지 않음** — 직전 실행(21:02) 결과 유지: 백엔드 **434 passed(2099 assertions)** · 프런트 **101 passed(17 files)**.

## ① 프로젝트 상태 요약

- **HEAD `c2efb55`(09-11 18:11) — 미푸시 0커밋** + **워킹트리 69건 미커밋**(47 tracked + 22 untracked) · `git diff --shortstat` = 47 files / **+1897 / −291**.
  - 미커밋 7갈래: ① Task 23(자기 수행 정산 0) ② 커뮤니티 설문·맛집 ③ 인제스천·용어 매핑 ④ 인제스천 원본 기록 + 단건 publish ⑤ 공개 요건 완화 + 구분 추정/`시내` + `orders:publish-ready` ⑥ 금액 미지정 완료 게이트 + `orders:apply-terms` 매시 스케줄 ⑦ 셋트 카드 조건 점수·근거 + 유입 원문 셋트명 한글 대체.
- **운영 데이터가 또 전진**: 서버 인제스천 지속으로 **마켓 published 130 → 135건**, 초안 78 → 82건, 원문 수집 158 → 169건 전부 처리. **"팔 수 있는 재고"는 135건인데 신청은 여전히 0건** — 병목은 확정적으로 **기사 유입(수요)** 이다.
- **spec([tasks.md](file:///d:/wwwroot/nowhere/.trae/specs/achieve-first-revenue/tasks.md))** — Task 1~23 전부 `[x]` + SubTask 22.6. 미착수/보류는 Task 22.4 "이름·도메인 확정 및 전면 교체" 1건. **20:19~20:30 신규 작업 spec 미등재.**
- **문서 정합**: [ORDER_FLOW.md L50](file:///d:/wwwroot/nowhere/docs/ORDER_FLOW.md#L50)은 여전히 "출발지·도착지·**차량·구분·금액** 필수"로 공개 요건 완화와 충돌 · [CHANGELOG.md](file:///d:/wwwroot/nowhere/docs/CHANGELOG.md#L24-L70)는 **09-11 절까지만**이고 09-12 절 없음(Task 22·23·인제스천·용어 매핑·단건 publish·공개 요건 완화·완료 금액 게이트·셋트 점수 **전부 미기재**). [ROADMAP.md](file:///d:/wwwroot/nowhere/docs/ROADMAP.md)는 2026-09-09 갱신 lag 지속.
- **잔존 모순(39회차)**: [IMPROVEMENT_PLAN.md L162](file:///d:/wwwroot/nowhere/docs/IMPROVEMENT_PLAN.md#L162)는 복귀(return-routes) 행에 `match_score`·`match_reasons` 부착 + `:show-match-reasons` 전달을 완료(2026-09-08)로 기록하나, 실제 배선은 전역 3건뿐이고 [복귀 섹션 L1220-L1234](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1220-L1234)에는 없다. (셋트·일반 카드는 배선됨.)
- **품질 기준선**: 백엔드 **434 passed(2099 assertions)** · 프런트 **101 passed(17 files)** — 코드 무변경으로 회귀 없음.

## ② 진행 중·대기 중 작업

- **진행 중(미커밋) — 일곱 갈래가 워킹트리에 쌓여 있다(69건, 22:01과 동일).**
  - ① Task 23(자기 수행 정산 전액 0) — 코드·테스트·문서 정합 완료, 09-11 19:01부터 미커밋.
  - ② 커뮤니티 설문조사·여행지 맛집 — CHANGELOG 기록됨, TASKS/spec/ROADMAP 미등재.
  - ③ 위챗 중국어 인제스천 + 용어 매핑 — 미커밋.
  - ④ 인제스천 원본 기록 배선 + 단건 `store` `publish` 옵션 — 문서·spec 미등재.
  - ⑤ 공개 요건 완화 + `ServiceTypeInferrer` + `orders:publish-ready`·`orders:apply-terms` — 문서·spec·TASKS 전부 미등재.
  - ⑥ 금액 미지정 완료 게이트 + `orders:apply-terms` 매시 스케줄 — 문서·spec 미등재.
  - ⑦ 셋트 카드 조건 점수·근거 + 유입 원문 셋트명 한글 대체(20:19~20:30) — 테스트 3건 동반, 문서·spec 미등재.
  - 일곱 갈래 모두 커밋 전 → **워킹트리 드리프트 위험 확대**(69건).
- **운영 실행 상태(사용자 주도로 판단됨)**: 서버 인제스천이 지속 가동(원문 169건 전부 처리)되고 22:58에 **DB 재동기화**가 또 수행됐다. 결과: **draft 82 / published 135 / cancelled 27.**
- **push 완료 상태 유지**: 09-11 4커밋이 원격 반영(`0 0`). CI 결과 확인은 여전히 열려 있다(Q-3) — **로컬에 `gh` CLI가 없어 이 환경에서는 확인 불가**, GitHub 웹에서 확인해야 한다.
- **여전히 열린 경로(등록 탭 3개 병존)**: 셋트(묶음)·AI(붙여넣기)·직접 입력 탭 3개 그대로. "1개 흐름 통합" 미착수.
- **단건 공개 이원화(유지)**: 단건 등록은 API에 `publish`가 있지만 프런트는 여전히 2단계(생성 → `apiTransitionOrder(id,'published')`)로 호출([useOrderForm.js L175-L176](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderForm.js#L175-L176)).
- **실질 병목 1개로 확정 — 기사 유입**: 재고 135건이 마켓에 있는데 `order_claims` 0 · `order_offers` 0 · 신청 0건. **공급은 준비됐고 수요가 0이다.** 앞단 조건은 HTTPS 고정 주소(Task 22 — NoWhere 도메인 확보 불가 확인 → 이름 교체 여부 결정 필요).
- **대기(유지)**: 미커밋 7갈래 커밋 · Q-3 CI 최초 결과 확인(웹) · Q-5 반응형·다크모드 점검 · Q-6 잔여(ROADMAP·CHANGELOG·신규 작업 등재) · Phase D · 상용화(도메인·SSL·리포 private·비밀번호 로그인 비활성화) · `.edge-ride-profile2/` 디스크 삭제 · `.deploy/_t.php` 임시 파일 정리.

## ③ 해야 할 기능 추가·개발 항목

- **기사 유입 착수(최우선 · 전환점)** — 재고 135건 확보, 신청 0건. 남은 일은 **기사를 실제로 들여보내는 것**뿐이다. 선행: HTTPS 고정 주소(이름·도메인 결정) → [DEPLOY.md §7](file:///d:/wwwroot/nowhere/docs/DEPLOY.md#L111-L164) Named Tunnel → 재빌드 → 아이폰 확인.
- **공개 요건 완화 문서 정합(최우선 · 저비용)** — [ORDER_FLOW.md L50](file:///d:/wwwroot/nowhere/docs/ORDER_FLOW.md#L50)·[API.md L129](file:///d:/wwwroot/nowhere/docs/API.md#L129)·[TASKS.md L86](file:///d:/wwwroot/nowhere/docs/TASKS.md#L86)을 "노선+일시 필수, 차량·구분·금액은 미지정 허용('차량 무관'·'요금 협의'·'구분 미지정')"으로 정정.
- **금액 미지정 완료 게이트 문서화** — 완료 시 실제 수익 필수 규칙([OrderTransitionService.php L98-L105](file:///d:/wwwroot/nowhere/app/Services/Order/OrderTransitionService.php#L98-L105))이 ORDER_FLOW·API.md 어디에도 없다. 정산·수수료 성립 조건으로 명문화 필요.
- **셋트 조건 점수·이름 대체 규칙 문서화** — ① 셋트 카드가 대표 다리(`firstOrderId`) 기준으로 점수를 낸다는 규칙 ② 유입 원문 셋트명을 표시 계층에서 한글 노선 요약으로 대체한다는 규칙 — 둘 다 RECOMMENDATION·UI·ORDER_FLOW 어디에도 없다.
- **`point`('시내') 서비스 구분 문서화** — PRODUCT_VISION·ORDER_FLOW·API.md·RECOMMENDATION 어디에도 없다. 실데이터 published 16건 존재. 매칭·추천 조건과의 관계 정의 필요.
- **신규 커맨드 정책 정리(절반 해소)** — `orders:apply-terms`는 **매시 자동 등록(해소)**, `orders:publish-ready`는 **여전히 스케줄 미등록**(수동). 자동화 범위·수동 유지 여부 명시 필요.
- **`ServiceTypeInferrer` 추정 품질 검증** — 공항↔공항은 null, 시내↔시내는 `point`. '인천/김포'를 공항으로 간주하는 규칙이라 **시내 '인천'·'김포' 지역이 공항으로 오추정**될 수 있다 → published 135건 표본 점검 필요.
- **인제스천/용어 실패·누적 조회/재처리 수단(후속 · 시급)** — `order_terms` 미매핑 **87 → 92건** 지속 증가(+5/시간). **실패 원본 조회·재처리 화면/커맨드가 없고**, 누적 정리·회전 정책도 없다.
- **Q-3 CI 최초 실행 결과 확인(최우선 · 웹 확인)** — push로 트리거됨. 로컬 `gh` CLI 부재로 조회 불가 → GitHub Actions 웹 확인, 실패 시 즉시 수정.
- **단건 등록 `publish` 프런트 배선(후속)** — 백엔드는 생겼으나 프런트는 2단계 호출 유지 → 통일 여부 결정.
- **미커밋 7갈래 문서 등재** — 커뮤니티 설문·맛집 / 인제스천·용어 매핑 / 단건 publish / 공개 요건 완화 / Task 23 / 금액 미지정 완료 게이트 + apply-terms 스케줄 / 셋트 조건 점수·이름 대체를 TASKS `Current`·spec Task·ROADMAP에 등재 필요(구현 완료분).
- **이름·도메인 확정(최우선 · 사용자 전용, Task 22.4 보류 항목)** — 도메인 부재 확정 → 이름 교체 여부 결정. 실사용자 0명인 지금이 교체 비용 최저.
- **HTTPS 고정 주소 확보(최우선 · 사용자 전용)** — 도메인 + Let's Encrypt 또는 Cloudflare Named Tunnel. 절차 [DEPLOY.md §7](file:///d:/wwwroot/nowhere/docs/DEPLOY.md#L111-L164).
- **복귀 동선 계측 연결(Task 19·20 후속)** · **문구 해석 정확도 향상(LLM 키 연결)** · **공유 링크 정책 마감(토큰 만료·회수/재발급)** — 유지.
- **문서 명시 후속(2축 준비)**: 등록자 등급(플랜) 요율 테이블(무료 5%/Basic 4%/Pro 3%/Business 2% — [PRODUCT_VISION.md L142-L154](file:///d:/wwwroot/nowhere/docs/PRODUCT_VISION.md#L142-L154)) · 수금 자동화(PG 가상계좌·웹훅) · 요율 변경 이력 조회 · 전역 요율 변경 UI.
- **인프라 후속(유지)**: 스케줄러 cron 모니터링/경보 · `schedule.log` 로테이션 · API 스모크를 배포 필수 단계로 고정. **스케줄 7종 유지(`apply-terms` 매시 실행 감시 필요).**
- **UI 신규 후보(누적 · 유지)**: `theme-color` 테마 미추종 · 동적 안내 `aria-live` 0건 · `user-scalable=no`(WCAG 1.4.4) · `.order-card__fav` 터치 영역 24×18(WCAG 2.5.8 미달) · iOS Safari 입력 확대 미대응 · 셋트 카드 `set-card__cond` 터치 영역 16px 높이 기준 미달 가능성.
- **예정 기능(문서 기준)**: Q-3 CI 확인 / Q-5 마무리 / Q-6 잔여 / Phase D(개인화 2~3단계·동선 최적화·오늘 수익 목표·배차 엔진) / Settlement 고도화(정산서·세금계산서) / 2~3차 부가 기능.

## ④ 버그 수정·개선 필요 항목

- **[주의] 표시 계층만 한글 대체 — 저장 데이터는 원문 그대로** — [setDisplayName()](file:///d:/wwwroot/nowhere/app/Support/Orders/OrderWorkspaceListBuilder.php#L158-L180)은 셋트 **카드 이름만** 바꾼다. `order_groups.name`에는 중국어 원문이 남으므로 **검색·정렬·알림 문구·관리자 화면·API 소비처에서 원문이 계속 노출**될 수 있다. 실데이터 45개 그룹 이름이 대부분 원문이다.
- **[미검증] 셋트 행 점수 산정 기준의 부작용** — 셋트 점수는 **대표 다리 1건(firstOrderId)** 기준이라 나머지 다리 조건이 다르면 카드의 '조건 N%'가 실제 셋트 전체와 어긋날 수 있다. 추천 정렬(`min_match_score` 60% 필터)에는 영향이 없다고 주석에 명시돼 있으나 **정렬 순위에는 영향** → 실데이터 육안 확인 필요.
- **[중요] `orders:apply-terms`가 매시 자동 실행되며 위험도 상승** — 19:45부터 **매시 정각 자동으로 저장 데이터를 소급 치환**한다([ApplyOrderTerms.php L36-L87](file:///d:/wwwroot/nowhere/app/Console/Commands/ApplyOrderTerms.php#L36-L87) · [console.php](file:///d:/wwwroot/nowhere/routes/console.php)). **사전 오매핑 시 1시간마다 대량 데이터가 조용히 바뀌고 복원 수단이 없다**(감사 로그 외). `order_terms` pending이 **87 → 92**로 늘어 오매핑 위험도 함께 커졌다 → 실행 전 백업 + 드라이런 규칙화 필요.
- **[유지] 금액 미지정 완료 게이트가 등록자·기사의 흐름을 막을 수 있다** — 계약 금액이 없는 운행은 완료 시 실제 수익이 없으면 **완료 전이 자체가 422로 실패**한다([L98-L105](file:///d:/wwwroot/nowhere/app/Services/Order/OrderTransitionService.php#L98-L105)). published 135건 중 **금액 미지정 63건(47%)** 이 여기 해당.
- **인제스천 미매핑 누적 지속** — `order_terms` pending **87 → 92건**(1시간에 +5). 잘못된 payload 반복 시 무한 누적(회전·정리·재처리 없음) → 정리 정책 필요.
- **마켓 노출 품질 저하 확대** — published 135건 중 **79건이 '차량 무관'(59%)·63건이 '요금 협의'(47%)**. 노선·일시 외 정보가 거의 없는 카드가 과반 → 추천 노출·정렬에서 불리해지지 않는지 확인 필요.
- **`orders:publish-ready`는 등록자 의도와 무관하게 초안을 공개한다** — 요건(노선+일시)만 충족하면 `draft`→`published` 전이([PublishReadyOrders.php L28-L74](file:///d:/wwwroot/nowhere/app/Console/Commands/PublishReadyOrders.php#L28-L74)). 등록자가 일부러 초안으로 남긴 건도 공개될 수 있다 → 정책 확인 필요(현재는 `admin_hold`·지난 운행만 제외).
- **문서-코드 충돌(유지)** — publish 필수 항목(ORDER_FLOW L50·API.md L129·TASKS L86) 불일치 + `point`('시내') 미기재 + 완료 금액 게이트 미기재 + 셋트 점수·이름 대체 규칙 미기재.
- **저장 파이프라인 정규화 부작용(유지)** — `OrderCreator::normalize()`가 모든 등록 경로의 값을 변환한다. 사전 오류의 영향 범위가 '문구 해석'에서 '저장 데이터'로 넓어졌다.
- **태그 승격이 값을 지우지 않는다(유지)** — `帮划客路` 표기는 태그로 옮겨지지만 지명 값에는 원문이 남는다(설계 의도) → 카드 노출 확인 필요.
- **단건 등록 공개 경로 이원화(유지)** — API `publish`와 프런트 2단계 병존 → 실패 시 초안 잔류 가능성.
- **신규 기능 8건이 문서 추적 밖(유지)** — 완료 판정 기준 공백.
- **미커밋 누적 69건(지속)** — 일곱 갈래가 섞여 변경 경계가 흐려졌다. 기능별 커밋 분리 시급.
- **자기 수행 운행 수금 순환은 해소(코드 기준)** — Task 23으로 전액 0 마감 확정. 단 **미커밋**.
- **[해소] `DEN-` 테스트 재고·추천 오염** — 동기화 DB 기준 **0건 유지**.
- **[해소] 셋트 카드 조건 근거 부재** — `match_score`·`match_reasons`가 셋트 행에도 붙고 모달로 노출된다(테스트 통과).
- **[확인 필요] `service_datetime` 전건 NULL(135/135)** — publish 요건은 date+time 기준으로 통과하나 파생 컬럼은 전부 NULL → 이 컬럼을 참조하는 코드/쿼리가 있는지 점검 필요.
- **[미해소 · 최우선] HTTPS 고정 주소 부재가 홍보 유입의 단일 병목** — Quick Tunnel은 재시작마다 주소가 바뀐다. 이름 교체 결정이 선행돼야 한다.
- **[미해소 · 유지] 복귀 경로 계측 공백** — 복귀 유입이 `behavior_events`에 남지 않아 효과 측정 불가.
- **[미해소 · 유지] 공개 payload의 내부 식별자 노출 — spec 문구와 불일치.** [PublicOrderController::payload](file:///d:/wwwroot/nowhere/app/Http/Controllers/Api/PublicOrderController.php#L41-L68)는 `row.id`·`row.key`·`claimedAt`·`claimBatchId`를 그대로 내려보낸다.
- **[미해소 · 39회차] 연결 운행(복귀) 카드 조건 근거 미노출** — 문서는 완료 기록, 코드는 미배선(셋트·일반 카드는 배선됨).
- **[미해소 · 10회차] 홍보 랜딩 유입 경로 단절 위험** — 도메인 루트(`/`)의 비로그인 응답은 여전히 Blade [welcome.blade.php](file:///d:/wwwroot/nowhere/resources/views/welcome.blade.php)([routes/web.php L9-L15](file:///d:/wwwroot/nowhere/routes/web.php#L9-L15)).
- **[미해소 · 유지] 운영 서버 코드 드리프트 재발 방지 미고정** — 배포 시 일괄 동기화 + 계약·스모크 검증 필수화 규칙 없음. 신규 스크립트 2종(publish_ready·verify_market)도 절차 문서 미반영.
- **배포 후 미검증 경계 조건(유지)**: ① 기존 `settlements` 행 `fee_rate`·`collection_status` default ② `admin_hold`·분쟁 상태와 수금/출금 게이트 상호작용 ③ 수금 확인 취소(paid → pending) 필요 여부 ④ 등록자 개별 요율 해제 시 진행 중 정산 영향 ⑤ 자기 수행 `not_required` 행이 출금 재원·수금 목록에서 실제 제외되는지.
- **런타임 검증 미확인(유지)**: 셋트 카드 조건 별·근거 모달 렌더 · 중국어 셋트명 → 한글 노선 요약 표시 · 금액 미지정 완료 흐름(도착 → 금액 모달 → 완료) · `orders:apply-terms` 매시 자동 실행 결과 · `/welcome`·`/s/order/{token}` OG 미리보기 · 관리자 '수금 확인'·'수수료 매출' · Task 23 자기 수행 다이얼로그→정산 0 마감 · 커뮤니티 설문·맛집 · 관리자 '미매핑 용어' 탭 · 중국어 등록→저장 자동 변환 · 인제스천 기록 · 단건 `publish` 분기 · Task 18·19·20·21 · 공개 요건 완화 후 초안→공개 흐름 · '차량 무관'/'요금 협의'/'시내' 표기 · `orders:publish-ready --dry-run`/실행 · UI 최신 회차 육안.
- **리포 위생(개선)**: 미추적 22건 = 커뮤니티·인제스천/용어 매핑·신규 커맨드/추정기·본 보고서(정상). `.edge-ride-profile2/` 디스크 삭제, `.deploy/_t.php` 임시 파일 정리 잔여(gitignore 반영됨).
- **유지 — 코드 후보**: 역할 리터럴 `'Driver'` → `ROLE_DRIVER` 치환(Q-5 후보군).

## ⑤ 마스터 관점의 우선순위·다음 행동 추천

1. **기사 유입 착수(최우선 · 전환점)** — 공급은 끝났다(**published 135건, 신청 0건**). 병목은 **HTTPS 고정 주소**(이름·도메인 결정)와 기사 모집. 이름 결정 → Named Tunnel → 재빌드 → 아이폰 확인.
2. **미커밋 69건 정리 — 기능별 커밋 분리(즉시)** — ① Task 23 ② 커뮤니티 ③ 인제스천+용어 매핑 ④ 인제스천 원본+단건 publish ⑤ 공개 요건 완화 + 구분 추정/`시내` + `publish-ready` ⑥ 금액 미지정 완료 게이트 + apply-terms 스케줄 ⑦ 셋트 조건 점수 + 셋트명 한글 대체. 7갈래로 늘며 드리프트 위험이 실질 위험으로 커졌다.
3. **`orders:apply-terms` 자동 실행 안전장치 마련** — 매시 자동 소급 치환 + `order_terms` pending 92건. **실행 전 백업 + 드라이런/오매핑 표본 검증**을 규칙화하고 실제 치환 로그를 확인한다.
4. **Q-3 CI 최초 실행 결과 확인** — push로 트리거됨. **이 환경엔 `gh` CLI가 없으므로 GitHub Actions 웹에서 확인**하고, 실패 시 즉시 수정.
5. **`order_terms` pending 92건 정리 정책(시급)** — 실패 원본 조회/재처리 + 누적 회전 정책 필요(+5/시간 증가 중).
6. **금액 미지정 완료 게이트 영향 점검** — published 135건 중 63건(47%)이 금액 미지정. 완료 시 422로 막히는 경로가 실사용에서 문제되지 않는지 확인.
7. **신규 작업 문서 등재 + 충돌 정정(즉시·저비용)** — publish 요건 완화 · `point`('시내') · 완료 금액 게이트 · 셋트 점수·이름 대체 규칙 · 커맨드 2종 정책 · spec/TASKS 등재.
8. **셋트명 한글 대체의 나머지 노출 경로 점검** — 표시 계층만 대체했으므로 검색·정렬·알림·관리자 화면에서 원문이 새는지 확인. 필요하면 저장 시점 정규화 검토.
9. **셋트 조건 점수 실데이터 육안 확인** — 대표 다리 기준 점수가 셋트 전체와 어긋나 보이는 카드가 있는지, 정렬이 기대대로인지.
10. **`orders:publish-ready` 정책 확정** — 초안 자동 공개 허용 범위, 스케줄러 등록 여부, 등록자 '보류' 플래그 필요성.
11. **`ServiceTypeInferrer` 오추정 점검(저비용)** — '인천'·'김포' 시내 지역이 공항으로 오추정되는지 표본 확인(published `point` 16건).
12. **단건 등록 공개 경로 통일 결정(저비용)** — 프런트 `publish: true` 1회 호출 vs 2단계 유지.
13. **첫 수익 실현 1건** — 재고 135건이므로 **기사 계정으로 신청→승인→운행→완료→자동 정산→입금·수금 확인→수수료 매출**까지 한 바퀴. 현재 전 구간 0건.
14. **정규화·공개 실동작 검증** — 중국어 등록 → 저장 변환 + 태그 승격 + `order_terms` 적재 / 초안→공개 → '차량 무관'·'요금 협의'·'시내' 카드 렌더 육안.
15. **A-5 불일치 종결(39회차)** — 복귀 섹션 `:show-match-reasons` 추가 vs 문서 정정.
16. **다음 블록 확정** — 기사 유입(홍보·HTTPS) vs 수익 축 확장(플랜 등급·PG 수금 자동화) vs 품질 블록(Q-3·Q-5) vs 커뮤니티 2차 (사용자 지시 대기).

## ⑥ 오늘 해야 할 일

- [ ] **(1순위 · 전환점) 기사 유입 착수** — 마켓 published 135건 확보·신청 0건 → 기사 모집 + HTTPS 고정 주소(이름·도메인 결정) 선행
- [ ] **(2순위 · 즉시) 미커밋 정리 — Task 23 커밋** — 8파일(`Settlement.php`·`SettlementService.php`·`useOrderDetail.js`·`RegistrantSettlementView.vue`·`OrderSelfDriveApiTest.php`·`ORDER_FLOW.md`·`OPERATIONS.md`·spec tasks.md)
- [ ] **(3순위 · 즉시) 커뮤니티 설문·맛집 커밋** — 설문 투표·`community_survey_votes`·`PlaceCard`/`SurveyBlock`·`CommunityPostEditor`·`routes/api.php`·`CommunityApiTest`·CHANGELOG 등
- [ ] **(4순위 · 즉시) 인제스천·용어 매핑 커밋** — `OrderIngestion`·`OrderTerm`·`OrderTermCollector`·`OrderTermService`·`OrderTermController`·`ChineseTextNormalizer`·`OrderCreator::normalize`·마이그레이션·테스트 3본·`AdminOperationController`·`AdminView.vue` 등
- [ ] **(5순위 · 즉시) 인제스천 원본 기록 + 단건 publish 커밋** — `OrderController.php`(recordIngestion 3종·store `publish`·`original_summary`) + `OrderApiTest.php` 3건
- [ ] **(6순위 · 즉시) 공개 요건 완화 + 구분 추정/`시내` + `publish-ready` + 완료 게이트/스케줄 커밋** — `Order.php`·`OrderListRowBuilder.php`·`ServiceTypeInferrer.php`·`PublishReadyOrders.php`·`ApplyOrderTerms.php`·`OrderTransitionService.php`·`OrderDetailView.vue`·`routes/console.php`·프런트 5곳·테스트 4본
- [ ] **(7순위 · 즉시) 셋트 조건 점수·셋트명 한글 대체 커밋** — `OrderListService.php`·`OrderWorkspaceListBuilder.php`·`SetGroupCard.vue`·`RecommendationApiTest.php` (+301줄)
- [ ] **(8순위 · 최우선) `orders:apply-terms` 자동 실행 안전장치** — 매시 자동 소급 치환 → 실행 전 DB 백업·드라이런·오매핑 표본 확인 + 로그 점검 (pending 92건으로 위험 증가)
- [ ] **(9순위) Q-3 CI 최초 실행 결과 확인** — GitHub Actions 웹에서 [ci.yml](file:///d:/wwwroot/nowhere/.github/workflows/ci.yml)(Pint→Pest→vitest→빌드→check:refs) 결과 확인(로컬 `gh` CLI 부재). 실패 시 즉시 수정
- [ ] **(10순위 · 시급) `order_terms` pending 92건 정리 정책** — 미매핑 실패 원본 조회/재처리 수단 + 누적 회전·정리 규칙 결정
- [ ] **(11순위) 셋트명 원문 노출 경로 점검** — 표시 계층만 대체 → 검색·정렬·알림·관리자 화면·API 소비처에서 중국어 원문이 새는지 확인
- [ ] **(12순위) 셋트 조건 점수 육안 확인** — 대표 다리 기준 별·근거 모달이 셋트 전체를 대표하는지, 추천 정렬이 기대대로인지(published 135건 표본)
- [ ] **(13순위) 금액 미지정 완료 게이트 영향 점검** — published 135건 중 금액 미지정 63건(47%) → 완료 시 422 차단 경로·UI 유도 충분성 확인
- [ ] **(14순위 · 즉시·저비용) 문서-코드 충돌 정정** — publish 필수 항목 3곳([ORDER_FLOW L50](file:///d:/wwwroot/nowhere/docs/ORDER_FLOW.md#L50)·[API.md L129](file:///d:/wwwroot/nowhere/docs/API.md#L129)·[TASKS L86](file:///d:/wwwroot/nowhere/docs/TASKS.md#L86)) + `point`('시내') + 완료 금액 게이트 + 셋트 점수·이름 대체 규칙 (문서 변경은 사용자 지시 후)
- [ ] **(15순위) `orders:publish-ready` 정책 확정** — 초안 자동 공개 허용 범위 · 스케줄러 등록 여부 · 등록자 '보류' 플래그 필요성
- [ ] **(16순위 · 결정 필요) 이름·도메인 확정** — NoWhere 도메인 부재 확정 → 이름 교체 여부 결정 (spec Task 22.4 보류 항목)
- [ ] **(점검) `ServiceTypeInferrer` 오추정 점검** — '인천'·'김포' 시내 지역이 공항으로 오추정되는지 확인 · 공항↔공항 null 유지 (published `point` 16건 표본)
- [ ] **(점검) `service_datetime` 전건 NULL 확인** — published 135건 파생 컬럼이 전부 NULL인데 이 컬럼을 참조하는 코드/쿼리가 있는지 점검(요건 검사는 date+time 기준)
- [ ] **(결정) 단건 등록 공개 경로 통일** — 프런트 `publish: true` 1회 호출 vs 2단계 유지 ([useOrderForm.js L175-L176](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderForm.js#L175-L176))
- [ ] **마켓 품질 확인** — published 135건 중 '차량 무관' 79·'요금 협의' 63이 추천·정렬에서 불리해지지 않는지, 기사에게 카드가 어떻게 보이는지 육안
- [ ] **첫 수익 실현 1건** — 기사 계정으로 마켓 135건 중 1건 신청 → 승인 → 운행 → 완료 → (24h) 자동 정산 → 입금 → 관리자 '수금 확인' → '수수료 매출' 확인 (현재 claims 0·settlements 0)
- [ ] **정규화·공개 실동작 확인** — 중국어 운행 등록 → 저장 값 한국어 변환 + `帮划客路` 태그 승격 + `order_terms` 미매핑 적재(pending 92) 육안 / 초안 → `orders:publish-ready --dry-run` → 실행 → 마켓 노출
- [ ] **용어 직접 등록·매핑 실동작 확인** — 관리자 '용어 추가' 모달 → 생성(201)/갱신(200) · 매핑 → 다음 유입 자동 반영 + 기존 운행·일정 소급 치환 · 감사 로그 육안
- [ ] **신규 완료 흐름 육안 확인** — 금액 미지정 운행 상세 → 도착 단계 → 금액 모달 → 실제 수익 입력 → 완료 · 금액 미입력 시 버튼 비활성 확인
- [ ] **커뮤니티 설문·맛집 실동작 확인** — 설문 작성 → 투표(1인 1표·선택 변경·마감 후 불가) · 맛집 카드 렌더 · 글 수정 모달 분기
- [ ] **Task 23 자기 수행 실동작 확인** — 직접 수행 경고 다이얼로그 → 공개 중단 → 완료 → 정산 `0/0/0`·`not_required` → 등록자 정산 '자기 수행' 라벨 · 관리자 수금 목록 미노출
- [ ] **도메인 확보 즉시 HTTPS 전환 실행** — [DEPLOY.md §7](file:///d:/wwwroot/nowhere/docs/DEPLOY.md#L111-L164): Cloudflare Named Tunnel → `.env`(APP_URL·FRONTEND_URL·SESSION_SECURE_COOKIE) → `VITE_SITE_URL` 재빌드 → 아이폰 확인 7항목
- [ ] **복귀 동선 실동작 확인 + 계측 연결** — 공유 링크 → 가입/로그인 → 복귀 · 비로그인 딥링크 → 로그인 → 복귀 · 세션 만료(401) → 복귀 + 복귀 경로를 `behavior_events`에 기록
- [ ] **아이폰 PWA 실동작 확인** — 설치 안내·아이콘명 `NoWhere`·인앱 브라우저 분기·알림 토글 (Task 21·22.6, HTTPS 확보 후)
- [ ] **Task 18 등록 화면 실동작 확인** — 추가 입력 접기/자동 펼침·배지 · 공개 요건 안내(요건 완화 반영 여부) · 결과 카드 요약 · 묶음 등록 `publish` → 마켓 노출
- [ ] **문구 해석 API 교체** — DeepSeek/Qwen 등 도달 가능 엔드포인트로 교체 → `parsed_by=ai` 정확도 향상 (설정 위주)
- [ ] **운영 스모크·검증 재실행** — `.deploy/smoke_endpoints.sh` · `verify_api_contract.sh` · [publish_ready.sh](file:///d:/wwwroot/nowhere/.deploy/publish_ready.sh)·[verify_market.sh](file:///d:/wwwroot/nowhere/.deploy/verify_market.sh) · `/up` · 마켓 total
- [ ] **배포 절차 규칙화(갱신)** — 신규 커맨드 2종(apply-terms 자동·publish-ready 수동)·백업·마켓 검증을 배포 필수 단계로 고정
- [ ] **스케줄러 모니터링 보강(강화)** — **스케줄 7종** 유지(`apply-terms` 매시) · `storage/logs/schedule.log` 확인 · `auto_settle`/`expire-claims` 재확인 · 미동작 감지 수단
- [ ] **문서 정합 잔여(Q-6 마감)** — ROADMAP 갱신(수금·자기 수행·Task 19~23·커뮤니티·인제스천·용어·단건 publish·공개 요건 완화·완료 게이트·셋트 점수) · CHANGELOG 09-11/09-12 절 보강
- [ ] **공유 링크 후속 결정** — 토큰 만료·회수/재발급 정책 · 유입 계측 연결
- [ ] **랜딩 유입 경로 확정** — 루트(`/`) → `/welcome` 리다이렉트 vs 홍보 링크 고정 (10회차)
- [ ] **A-5 복귀 행 조건 근거 배선 결정** — MarketView 복귀 섹션 `:show-match-reasons` 추가 vs 문서 정정 (39회차)
- [ ] **공개 payload 정합** — `row.id`·`row.key` 노출 유지 시 spec·테스트 문구 정정, 제거 시 대체 식별자 검토
- [ ] **수금 경계 조건 점검** — 기존 `settlements` `fee_rate`/`collection_status` default · `admin_hold` 상호작용 · 수금 되돌림 · 개별 요율 해제 · 자기 수행 `not_required` 제외 실확인
- [ ] **Task 16 미사용 잔재 정리 결정** — `StoreStructuredOrderRequest`·`orders.original_summary`/`structured_payload`
- [ ] **Task 12·14 실동작 확인** — 랜딩 등록자 CTA → 가입 화면 '등록자' 기본 선택 · HTTP 접속 시 알림 안내 문구
- [ ] **배지·접근성 전역 표준 결정** — `:focus-visible` 포커스 링(`outline: none` 16곳) · `n-badge` 색/크기·대비 · `theme-color` 테마 연동 · `aria-live` 도입
- [ ] **UI 신규 후보 결정** — 찜 하트 터치 영역 24×18 → 24×24 · OrderFavoritesView 아이브로우 `--danger` 조정 · iOS Safari 입력 확대 대응 · 셋트 조건 버튼 터치 영역(16px)
- [ ] **Q-5 반응형·다크모드 브라우저 점검** — 마켓 부하·스크롤 + 전 화면 `[~]` 마무리
- [ ] `.edge-ride-profile2/` 디스크 삭제 · `.deploy/_t.php` 임시 파일 정리 (사용자 확인 후)
- [ ] 다음 작업 후보 확정 — 기사 유입(홍보·HTTPS) vs 수익 축 확장 vs 품질 블록(Q-3·Q-5) vs 커뮤니티 2차 vs Phase D (사용자 지시 대기)
- [x] ~~로컬 테스트 회귀 확인~~ — 코드 무변경이라 재실행 생략 · 직전 실행(21:02) 결과 유지: `php artisan test` **434 passed(2099 assertions)** · `npm test`(vitest) **101 passed(17 files)**
- [x] ~~직전 보고서 대비 변경 감지~~ — **코드·커밋·문서 신규 변경 0건(= 신규 작업 없음)** · **DB 재동기화(22:58)로 운영 데이터만 증가**
- [x] ~~git 상태 점검~~ — HEAD `c2efb55`(09-11 18:11) · `origin/main...HEAD` = **`0 0`**(미푸시 0) · 워킹트리 69건(47 tracked + 22 untracked) · `git diff --shortstat` 47 files / **+1897 / −291**(22:01과 동일)
- [x] ~~spec Task 확인~~ — Task 1~23 전부 `[x]` + SubTask 22.6 · 유일 미착수 = Task 22.4 보류(이름·도메인) · 20:19~20:30 작업 spec 미등재
- [x] ~~문서 mtime 확인~~ — 이 구간 문서 변경은 본 보고서뿐. TASKS 09-11 18:11 · CHANGELOG 09-11 19:22 · ORDER_FLOW·OPERATIONS 09-11 18:51 · DEPLOY 09-11 15:36 · API 09-11 12:13 · IMPROVEMENT_PLAN·PRODUCT_VISION 09-10 · ROADMAP 09-09 lag
- [x] ~~A-5 배선 확인~~ — `:show-match-reasons` 전역 3건 유지(HomeView L803·L826 · MarketView L1251 '전체 운행'), 복귀 섹션 L1220-L1234 미배선 (39회차)
- [x] ~~소스 변경 감지~~ — 22:01~23:01 소스 파일 변경 **0건**(최신 수정 09-12 20:30 `RecommendationApiTest.php`·`OrderWorkspaceListBuilder.php` 20:30·`OrderListService.php` 20:19·`SetGroupCard.vue` 20:19)
- [x] ~~운영 데이터 실측~~ — 동기화 DB(22:58) 기준 `orders` **244**(**draft 82·published 135·cancelled 27**) · `DEN-` 0건 · published 중 `vehicle_type` 미지정 79·`amount_value` 미지정 63·`service_datetime` 전건 NULL(135)·구분 pickup 57/sending 62/point 16 · `order_terms` mapped 32/ignored 3/**pending 92** · `order_ingestions` 169 전부 processed · `order_groups` 45 · **claimed 0·claims 0·offers 0·settlements 0**

---

*직전 저장 보고서(09-12 22:01본) 대비 **코드·커밋·문서 신규 변경 0건 — 신규 작업 없음**. HEAD `c2efb55` 유지(`origin/main...HEAD` = `0 0`) · 워킹트리 **69건 유지**(47 tracked + 22 untracked) · `git diff --shortstat` **+1897/−291** 동일 · 소스 최신 수정 **20:30** 그대로 · 문서 mtime 전부 미갱신. 이 구간 유일한 변화는 **서버 DB 재동기화(22:58:56)** 로 운영 데이터가 다시 늘어난 것: `orders` **227 → 244**(draft 82·**published 135**·cancelled 27) · `order_ingestions` **169건 전부 processed** · `order_terms` **pending 87 → 92** · `order_groups` **45** · published 135건 중 `vehicle_type` 미지정 **79**·`amount_value` 미지정 **63**·`service_datetime` 전건 NULL·`point` **16** · **`claimed_at` 0·`order_claims` 0·`order_offers` 0·`settlements` 0**. **공급 135건 vs 수요 0건** → 병목은 여전히 '기사 유입'. 문서 미갱신으로 publish 필수 항목 3곳·`point`·완료 게이트·셋트 점수/이름 규칙 미문서 유지. A-5 `:show-match-reasons` 전역 3건(**39회차**). 다음 병목: **기사 유입(최우선) / 미커밋 69건 7갈래 커밋 분리 / `apply-terms` 자동 실행 안전장치 / Q-3 CI 결과(웹) / `order_terms` pending 92 정리 / 셋트명 원문 노출 경로 점검 / 문서 등재 + 충돌 정정 / 이름·도메인 → HTTPS**. 다음 검토는 커밋 정리·CI 결과·이름 결정·기사 신청 발생 여부를 감시한다.*
