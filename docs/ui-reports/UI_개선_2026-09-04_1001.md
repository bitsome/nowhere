# UI 개선 보고서 — 2026-09-04 10:01

## 실행 결과 요약
- **수정 1건** — 최근 전체 읽기 이력이 없던 ReviewsView의 다크 모드 활성 탭 흰 글자 저대비 해소(채움 위 어두운 글자 `#07120e` 표준).

## (a) 실행 시각
- 2026-09-04 10:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 보고서에서 부분 언급만 되던 화면**):
  - `views/ReviewsView.vue`(전체 480줄 — 리뷰: 받은 리뷰/내가 쓴 리뷰 세그먼트 탭·평점 요약(점수+별점 분포)·리뷰 목록·운행 연결 행)
  - `views/MoreView.vue`(전체 314줄 — 더보기: 프로필 히어로·VIP·XP 바·오늘 통계 3카드·내 운행/커뮤니티/운영/설정/계정 메뉴)
  - `views/settings/SettingsView.vue`(전체 161줄 — 설정 카드 그리드·로그아웃)
  - `views/settings/SettingsNotificationsView.vue`(전체 106줄 — 브라우저 알림 토글)
  - `views/settings/SettingsAppearanceView.vue`(전체 111줄 — 화면 모드 토글·앱 설치)
  - `views/settings/SettingsVehiclesView.vue`(전체 203줄 — 내 차량 목록·등록/수정 모달)
- 전역 대조: `assets/base.css`(`--accent` 라이트 `#1f1f1f`/다크 `#63e2b7` L38·L69 — 이번 수정의 근거 토큰), 전 앱 탭/칩 활성 상태 규칙(`brand-soft`+brand 텍스트 vs solid 채움) 전수
- 전역 감사(grep): `var(--accent)` 채움 사용처 전수(15건 — 활성 탭·아바타·`.btn--primary`뿐), 채움 위 어두운 글자 `#07120e` 표준 적용처 전수(29건 — 앱 전역 관용 확인), `html.dark` 활성 탭 오버라이드 쌍 전수
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_0903.md`·`0104.md`·`0001.md`·`2026-09-03_2303.md` 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 09:03 이후 첫 실행)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 / brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **ReviewsView 활성 탭 다크 글자 저대비 (신규 발견·적용)**: `.reviews-tabs__item--active`(L264-267)는 **solid 채움 `background: var(--accent)` + 흰 글자**인데, `--accent`는 라이트 `#1f1f1f`(흰 글자 정상)·다크 `#63e2b7`(밝은 틸 — 흰 글자 저대비)로 모드에 따라 값이 바뀐다. 이는 그동안 전 화면에서 정리된 **"brand/밝은 채움 위 흰 글자" 저대비 결함과 같은 계열**이며, 이 파일은 최근 전체 읽기 이력이 없어 미처리로 남아 있었다. 내부 건수 필 `.reviews-tabs__count`(`rgba(255,255,255,.25)`+흰 글자)도 동일한 문제를 공유.
2. **MoreView·Settings 계열 클린 (신규 전체 확인)**: MoreView 히어로 — CTA("회원정보" brand 틴트+brand 텍스트) 1개뿐으로 화면당 Primary 1개 원칙 준수, "내 운행/운행 기록/내 마켓/처리할 일" 메뉴 용어·분리(활성=내 운행, 기록=완료) 정상, `.more-hero__vip` `1px 6px/10px/400` 배지 스펙 준수(금색 그라디언트는 프리미엄 배지 관용). SettingsView 카드 그리드 5종·모달 푸터 Primary("저장") 1개, SettingsNotificationsView/SettingsAppearanceView 토글 행·알림 문구 한국어, SettingsVehiclesView "검증/기본" n-tag·EmptyState("등록된 차량이 없습니다") 문구 정상. 신규 위반 없음.
3. **ReviewsView 나머지 클린**: 평점 요약 점수(brand)·별점 분포 바(brand)·리뷰 카드·"운행 보기" 연결 행(아이콘+라벨+화살표, hover brand) 한국어·"운행" 용어 정상, 받은/쓴 리뷰 탭 카운트는 "탭 칩 내부 숫자 관용". EmptyState 문구 정상. `html.dark` 오버라이드가 전혀 없던 파일이라 이번 추가한 2곳이 유일한 다크 쌍이 됨.
4. **전역 캐너리 통과**: `var(--accent)` 채움+흰 글자는 활성 탭(이번 수정)·아바타 3곳(base.css `chat-list__avatar`·ChatList `chat-list__avatar`·MessageBubble `cb-avatar`)·base.css `.btn--primary`뿐 — 나머지는 전부 기존 관용/후보 분류 항목. `#07120e` 표준 29건 유지 확인.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/ReviewsView.vue` | `html.dark .reviews-tabs__item--active` 추가 — `color: #07120e`(채움 위 어두운 글자 표준) + 근거 주석("다크는 --accent가 밝은 틸(#63e2b7)이라 흰 글자 대비 약함, 라이트는 어두운 채움이라 흰 글자 유지") |
| `views/ReviewsView.vue` | `html.dark .reviews-tabs__item--active .reviews-tabs__count` 추가 — 배경 `rgba(255,255,255,.25)` → `rgba(7,18,14,.14)`, 글자 `#fff` → `#07120e` + 주석 — 다크 활성 탭 내부 건수 필 대비 해소 |

`<style scoped>` CSS 추가만(템플릿 구조·script·import 무변경). 라이트 모드 시각·구조·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ReviewsView `.review-item__avatar`(var(--accent)+흰 이니셜)·MoreView `.more-hero__avatar`(brand-gradient+흰 글자)·ChatList/MessageBubble 아바타**: 아바타 관용 — 기존 회차 반복 분류 유지(전역 아바타 글자색 설계 후보로만 기록).
- **base.css `.btn--primary`(var(--accent)+흰 글자)·`.chat-list__avatar`**: legacy 공용 정의 — "brand 위 글자색" 전역 설계 후보(09:03 이래 반복 미적용).
- **ReviewsView 활성 탭 라이트 solid `#1f1f1f` 채움 vs 다른 화면 활성 탭 `brand-soft`+brand 텍스트 관용**: grayscale-first 강조 스타일의 독립 구현 — 앱 탭 활성 상태 통일(대형) 후보 기록(시각 변경 아님, 미적용).
- **MoreView `.more-hero__vip` 금색 그라디언트(`#f7b731/#f2994a`)·SettingsView `.settings-card__icon` brand-gradient+흰 아이콘**: 프리미엄 배지·아이콘 관용(기존 분류 유지).
- **SettingsView `small` 11.5px 등 비정수 폰트·탭 카운트 `reviews-tabs__count` 11px/700**: 앱 전역 힌트/칩 내부 숫자 관용 — 유지.
- **OrderDetailView·OrderCreateView 등 대형 화면의 미검토 구간·ProfileView naive 세그먼트·FAB 흰 글자 아이콘·`.v8-noti` 데드 CSS**: 기존 대형/설계 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 943ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: ReviewsView `html.dark .reviews-tabs__item--active`(`#07120e`)·건수 필 다크 쌍 추가 확인.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 규칙·주석 추가뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
