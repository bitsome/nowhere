# UI 개선 보고서 — 2026-09-05 21:02

## 실행 결과 요약
- **변경 없음** — 20:02(52회차) 이후 `frontend/src` 변경 0건(mtime 스캔·git 상태 확인)으로 신규 기능 코드가 없어 전수 열람 대상이 없음. 오늘 회차 중 신규 기능 도입(지원·추적 등)으로 배선이 더해졌으나 전면 재열람 이력이 밀려 있던 **마켓(MarketView) 화면 전체**를 신선 시점 점검(script+템플릿+scoped CSS+드로어 전역 CSS)하고, 부분 확인에 그쳤던 **알림센터(NotificationsView) 템플릿**을 이어 확인했으나 **명확한 소형 결함을 발견하지 못해 수정 없음**. 직전 회차(20:02) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 21:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 20:02 이후 `frontend/src` 변경 0건 (`Get-ChildItem LastWriteTime > 20:02` 결과 없음, git status는 기존 파일 재구성 이력성 변경뿐으로 신규 작업분 없음)
- **직접 열람(신선 시점 점검)**: `views/MarketView.vue`(전체 1338줄 — script L1-684: 필터/검색 상태 저장·퀵 필터(신규/임박/긴급·오늘/내일·금액 5~10만)·서비스·정렬·위치 3단·새 운행 감지/하이라이트·30초 폴링·SSE·keep-alive 재조회 / 템플릿 L686-1020: 서비스 퀵 칩·필터 칩+건수 배지·초기화·내 운행·노선 검색 버튼, 적용 필터 요약 칩, 필터 모달·노선 검색 모달, 오류 alert, 스켈레톤·EmptyState, 연결 운행(return)·전체 운행(set+list) 섹션, 페이지네이션, 빠른매칭 FAB·드로어 / scoped CSS L1021-1338 + 드로어 전역 CSS L1340-1382) · `views/NotificationsView.vue`(템플릿 L225-373 — 49회차에서 script 분류 로직·v8 CSS를 본 뒤 잔여 템플릿 확인: 지금 처리할 알림 카드·필터 탭·최근 알림·날짜 그룹/토글·목록 행)
- **전역 감사(grep)**: `font-size: 7/8/9px` 0건 / `원원`·`formatWon(...)원` 단위 중복 0건 / `주문번호|order_no|order_number` 노출 — AdminView 운영 개입 2건(L1771·L2030, 관리자 콘솔 설계상 유지)뿐 / UI 노출 "드라이버|오더" 0건(매치는 전부 코드 주석)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_2002.md`(52회차)·`1904.md`(51회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
- **신규 없음**: 이번 시간대 신규 기능 코드 0건 + 마켓 화면 신선 점검에서도 명확한 소형 결함 미발견.
- 점검 결과 정합 확인 사항:
  1. **필터 칩 행·배지 체계 정합**: 서비스 퀵 칩(픽업/샌딩/랜딩)은 상단 즉시 필터, 나머지는 '필터' 모달 진입 + 건수 배지(`.market-filters__badge` — brand 채움 위 `#07120e` 다크 텍스트 10px/400 표준). hover는 `@media (hover:hover)`로 터치 잔류 포커스 방지 — 기존 관례 준수. 적용 필터 요약 칩(`.market-active__chip`)은 개별 클릭 해제 + 와일드카드(`%`) 미노출 처리로 사용자 문구 정합.
  2. **필터 모달 구성**: 날짜(전체/오늘/내일 + datetime-local) → 시간대 → 서비스 유형 → 특별 보기 → 출발지/도착지(3단 셀렉트 + 즐겨찾기 별표) → 차량(2단) → 최소 탑승 → 금액(퀵 칩 + 직접 입력) → 정렬 — '정보 → 선택' 순서·색상 무채색 유지. '금액 퀵 보기'는 정렬과 중복이라 제거된 상태(퀵=금액 → 정렬 이전 마이그레이션 L111-115) 정합. 모달 footer의 취소/초기화/적용 = Primary '적용' 1개 원칙 준수.
  3. **연결 운행/전체 운행 섹션 + 추적 배선**: return(연결)/set/단일 목록은 헤더 라벨 없는 의도적 설계(CSS 주석 L1333 '헤더 라벨은 두지 않는다' — 추천 카드가 문맥상 자기 설명적)로 유지. tracking prop(scope:'market', section: return/set/list, rank) 배선 정확, 추천 카드 제외 로직(`is_matched_to_me`·returnRouteKeys·set 분리)으로 목록 중복 없음.
  4. **새 운행 감지/하이라이트**: 필터·검색 없이 1페이지를 볼 때만 토스트, 신규 key는 6초 하이라이트 후 제거 — 알림 피로도 원칙 부합. 폴링은 visibility hidden 시 중지, onActivated 조용한 재조회 — 리소스 정합.
  5. **알림센터 템플릿 정합(49회차 잔여 확인)**: '지금 처리할 알림' 카드는 우선 그룹(읽지 않은 request형)만, 내부 항목은 색 계열(warn=앰버/price=앰버/danger=레드) 배경 구분 + 요금 제안 인라인 금액/처리 이동 버튼은 '알림은 안내, 처리는 액션 센터' 역할 분리 유지. 날짜 그룹 토글(오늘=기본 펼침, 필터 변경 시 초기화)·읽지 않음 dot·상태 배지 tone(`v8-priority-status--*`) 49회차 확인과 동일.
  6. **전역 캐너리 통과**: 단위 중복·10px 미만 폰트·기술 식별자(관리자 콘솔 제외)·UI "드라이버/오더" 잔존 모두 0건.

## (d) 실제 적용한 수정
- 없음 (변경 없음).

## (e) 적용하지 않은 항목과 이유 (후보)
- **MarketView scoped 데드 CSS `.page-head`·`.page-head__desc`(L1022-1034)**: 템플릿에서 사용 0건 — 렌더링 영향 없는 코드 정리로 UI 개선에 해당하지 않아 미적용(기존 '데드 CSS' 후보 분류에 통합 기록).
- **마켓 필터 칩 등의 클릭형 칩 패딩·굵기(6px 12px·11px·600 등)**: 배지 스펙(1px 6px·10px·400)과 다른 콘텐츠 칩 계층 — 09-04 16:01 분류 유지(반복 미적용).
- **빠른매칭 FAB(54px brand 채움 + 흰 아이콘)**: 아이콘 관용 패턴 — 기존 'brand 위 글자색 전역 설계' 후보 분류 유지(반복 미적용).
- **기존 반복 후보(레벨 타이틀 "드라이버" data/levels.js↔LevelTable.php, 홈 추천 조건% 미노출, LoginView·RegisterView auth CSS 중복, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마, v-chip/verify-chip 패딩 차이, 문의 placeholder 문구, ChatList '운행' 칩 유지 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 없음에도 현재 작업 트리 정합성 확인차 `npm run build`(frontend, vite v8.2.1) 실행 — **통과**, 4362 modules, built in 948ms. MarketView·NotificationsView 청크 정상 생성.
- import 누락 점검: 코드 수정 없음 → 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
