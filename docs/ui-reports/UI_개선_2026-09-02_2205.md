# UI 개선 보고서 — 2026-09-02 22:05

## (a) 실행 시각
- 2026-09-02 22:05 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 컴포넌트: `components/chat/ChatList.vue`, `components/chat/ChatThread.vue`, `components/chat/MessageBubble.vue`, `components/orders/QuickMatchPanel.vue`, `components/common/SegmentedGroup.vue`, `components/common/EmptyState.vue`, `components/common/LevelBadge.vue`, `components/ui/UiChip.vue`
- 뷰: `views/ChatView.vue`, `views/NotificationsView.vue`(칩 색 패턴 대조), `views/community/CommunityView.vue`·`CommunityPostView.vue`, `views/settings/*.vue`(5파일), `views/orders/OrderDetailView.vue`(일부), `views/DashboardView.vue`(일부), `views/auth/LoginView.vue`·`RegisterView.vue`
- CSS/기타: `assets/base.css`(토큰·아바타 대조), `data/levels.js`(레벨 타이틀), `utils/colors.js`(대조)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_2101.md` 및 그 이전 보고서(적용 내역 전수 확인)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "오더"→"운행" / 배지 스펙 1px 6px·10px·400 / v8 상황별 색)

## (c) 발견한 개선 포인트
1. **ChatList 유형 칩 — 라이트 모드 대비 결함 (신규 발견)**: `.chat-list__type-chip--yellow`/`--red`가 다크 전용 밝은 색(`#ffd071`/`#ff8a8a`) 단일 선언이라 라이트 모드(흰 배지 위)에서 거의 읽히지 않음. 또한 `--green` 칩은 민트 틴트 배경에 `color:var(--brand)`(라이트=파랑)라 배경과 텍스트 색상이 어긋남. **동일한 v8 상태 칩 계열은 09:14 회차에서 NotificationsView에 "라이트 색 + html.dark 밝은색" 쌍으로 이미 정리됐으나, ChatList는 10:02에 폰트/굵기만 수정되고 색 쌍 정리는 누락된 잔존 건.**
2. **레벨 타이틀 "드라이버" 노출 (신규 발견)**: `data/levels.js` `LEVEL_LIST` 타이틀(`신입 드라이버`~`슈퍼 드라이버`)과 백엔드 제공 `level.title`이 MoreView 히어로·LevelBadge 가이드 모달·ProfileView·UserPageView에 그대로 노출. 역할 용어("기사") 통일 관점의 후보. 단, 백엔드 `LevelTable`과 동기화 필요(주석 명시)라 서버 변경 없이 진행 불가.
3. **기타 재확인 결과 클린**: 낡은 fallback hex·`--text-secondary` 0건, UI 노출 "오더"/"히스토리"/"유저" 0건(코드 식별자·주석만), 영문 placeholder 0건, `#e5484d` 등 의미색 하드코딩·다크 brand 위 흰 글자 등은 기존 후보 유지(아래 e 참조). 배지 스펙(1px 6px/10px/400) 잔존 위반 0건(전 파일 grep 확인).
4. **ChatThread 상태 칩·QuickMatchPanel 칩**: 이미 `html.dark` 대비 처리 또는 토큰(`var(--brand)`) 사용으로 양 모드 안전 — 수정 불필요.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/chat/ChatList.vue` | 유형 칩 3종을 NotificationsView v8 상태 칩과 동일한 "라이트 기본색 + `html.dark` 밝은색" 쌍으로 분리 — `--green` 라이트 `#0e9d8d`(기존 var(--brand) 파랑 불일치 해소), `--yellow` 라이트 `#9a6a00`, `--red` 라이트 `#c03a3f`; 다크는 `var(--brand)`/`#ffd071`/`#ff8a8a` 그대로 유지(픽셀 동일). 배경 틴트·구조·동작 변경 없음. |

순수 `<style scoped>` CSS 속성 변경 1건 — 의미·레이아웃·상호작용 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ChatList "승인 요청"(pending) 칩이 green 계열**: 알림센터 규칙상 "요청/확인 필요"는 옐로우 계열이지만, 채팅 목록 칩은 마지막 메시지 유형 표식이라 동일 매핑을 적용하려면 의미 검증(보는 사람 관점)이 필요 — 디자인 결정 사항(후보).
- **`data/levels.js` 레벨 타이틀 "드라이버"**: 백엔드 `LevelTable`과 반드시 동기 유지해야 하며, 실제 표시는 백엔드 `level.title` 의존 — 서버 작업 동반이라 로컬 프론트만으로는 불일치가 생겨 보류(후보).
- **아바타류 다크 모드 mint 배경 + 흰 글자**(ChatList/MessageBubble/ReviewsView/커뮤니티 feed-avatar 등, `#fff` → `#07120e` 필요 후보): 21:01 보고서의 "--on-brand 전역 설계" 후보와 동일 건 — 전역 일괄 결정 필요, 반복 미적용.
- **`#e5484d`·`#f0a500`·`#ff8a8a` 계열 의미색 하드코딩**(피드 좋아요 활성·MarketView 즐겨찾기 등): 색상 체계 결정 후 일괄 토큰화 대상(기존 보고서 후보 유지).
- **ReviewsView·CommunityView 탭/카운트 `1px 7px`**: 활성 탭 오버레이 대비 문제로 탭 디자인과 함께 재검토(기존 후보 유지).
- **settings-back 버튼 5개 파일 중복**: 공용 컴포넌트화는 구조 변경 — 후보.
- **EmptyState 5색·커뮤니티 카테고리 6색 grayscale 통일**: 전역 디자인 결정(기존 후보 유지).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 883ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `ChatList.vue`에 라이트 기본색 3종 + `html.dark` 오버라이드 3종 존재, 다크 모드 기존 색(`#ffd071`/`#ff8a8a`/`var(--brand)`) 보존 확인.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 1건뿐이며 템플릿·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
