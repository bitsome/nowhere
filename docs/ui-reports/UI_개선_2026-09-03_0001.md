# UI 개선 보고서 — 2026-09-03 00:01

## (a) 실행 시각
- 2026-09-03 00:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰: `views/ProfileView.vue`(프로필 히어로 인증 배지·레벨 혜택 칩), `views/orders/OrderCreateView.vue`(일정 그룹 헤더·카운트 칩), `views/orders/RideHistoryView.vue`(동일 그룹 패턴 대조), `views/orders/MyMarketView.vue`(주석 확인), `views/community/CommunityView.vue`·`CommunityPostView.vue`(영상 배지), `views/NotificationsView.vue`(대조 참조), `views/HomeView.vue`(대조 참조)
- CSS: `assets/base.css`(전역 주석·공용 인증/상태 행·알림·채팅 공용 영역)
- 전역 감사(grep): fallback hex(`var(--x, #…)`), 배지-유사 padding(`1px 7/8px`, `2px 6~10px`, `3px/4px/5px/6px + 1x px`), `11px/600~800` 조합, 영문 placeholder, UI 노출 영문("VIP"만 브랜드 유지), **인코딩 깨짐 주석(mojibake — `\uF900-\uFAFF` 전수, `?`/`?뚮┝`/`梨꾪똿` 계열)**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_2301.md` 및 그 이전 보고서 8건(적용 내역 전수 확인 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색)

## (c) 발견한 개선 포인트
1. **base.css 주석 3곳 인코딩 깨짐 (신규 발견)**: `/* 紐⑤컮???붾㈃ ?뺣?/異뺤냼 李⑤떒 + ?붾툝??以?諛⑹? */`(L66), `/* ?뚮┝ ?⑤꼸 / 紐⑸줉 */`(L264), `/* 梨꾪똿 */`(L369) — EUC-KR/과거 인코딩 잔재로 보이는 깨진 한글 주석. 그동안 한글 grep에 잡히지 않아 미정리 상태로 남아 있던 잔존 건. 각각 이어지는 CSS 블록(html/body 공통 기본, 알림 패널/목록, 채팅 목록)과 대응되도록 한글 주석으로 정리.
2. **배지 스펙 위반 잔존 — 공용 `.verify-badge` (신규 발견)**: base.css "공용 인증/상태 행" 블록의 `.verify-badge`가 `3px 10px / 11px / 600` — 동일 블록의 `.verify-row__done`·`__pending`은 10:02 회차에서 이미 스펙(1px 6px/10px/400)으로 정리됐으나 `.verify-badge`만 누락. 프로필 히어로(ProfileView)의 "차량 인증"/"면허 인증" 정보 라벨로 노출되는 배지로, 23:01의 `.claim-group__badge` 정리와 같은 계열 잔존 건.
3. **배지 스펙 위반 잔존 — 일정 그룹 카운트 칩 (신규 발견)**: OrderCreateView `.schedule-group__count`("N건" 카운트)가 `3px 10px / 11px / 600` — 그동안 배지-유사 grep(`1px 7/8px`, `2px 6~9px`)에 안 걸려 누락. 같은 카운트 칩 계열(NotificationsView `.v8-total`·`.v8-day__count` 등)은 이미 스펙으로 정리된 상태라 잔존 위반.
4. **기타 재확인 결과 클린**: 낡은 fallback hex 0건, 영문 placeholder 0건, UI 노출 영문 0건("VIP"는 브랜드 명칭으로 유지), UI 노출 "드라이버"/"오더"/"히스토리"/"유저" 0건(주석만), `v-if="false"`/TODO/FIXME 0건.
5. **배지-유사 요소 재분류(수정 불필요 확인)**: ProfileView `.level-benefit`(3px 10px/11px/600)·커뮤니티 `.video-player__badge`(3px 10px/11px/600, 미디어 오버레이)·base.css `.chat-type-sep`(3px 12px/11px/700)는 콘텐츠 칩/미디어 오버레이/구분 라벨로 배지 스펙 대상이 아님이 재확인 — 후보로만 기록(아래 e).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `assets/base.css` | 깨진 한글 주석 3곳 정리 — L66 공통 기본(모바일 터치·글자 크기 조정·본문 폰트), L264 "알림 패널 / 목록", L369 "채팅 목록" (이어지는 CSS 블록과 대응하는 주석으로 교체) |
| `assets/base.css` | `.verify-badge` `3px 10px/11px/600` → `1px 6px/10px/400` — 배지 스펙(1px 6px/10px/400) 준수. 색상(`--brand-soft`/`--brand`)·gap·구조·동작 변경 없음 |
| `views/orders/OrderCreateView.vue` | `.schedule-group__count` `3px 10px/11px/600` → `1px 6px/10px/400` — 배지 스펙 준수. 색상(`--brand-soft`/`--brand`)·구조·동작 변경 없음 |

모두 `<style scoped>`/주석 텍스트 변경으로, 의미·레이아웃·상호작용 무변경(스펙 위반 잔존 정리 2건 + 인코딩 깨짐 주석 3건).

## (e) 적용하지 않은 항목과 이유 (후보)
- **ProfileView `.level-benefit`(3px 10px/11px/600)**: 레벨 혜택을 나열하는 콘텐츠 칩(가변 길이 텍스트)으로, UiChip(4px 7px/11px/700)과 동일 계열 — "내용 칩은 배지 스펙 대상 아님" 기존 판단 유지. 전역 시각 변화가 크면 후보로 재검토.
- **커뮤니티 `.video-player__badge`(3px 10px/11px/600)**: "숏츠/영상" 오버레이 라벨로 반투명 검정 위 흰 글자(이미지 위 가독성) — 미디어 오버레이 칩은 배지 스펙 대상 아님(후보 유지).
- **base.css `.chat-type-sep`(3px 12px/11px/700)**: 채팅 유형 구분 라벨(전폭 센터 칩)로 콘텐츠 구분자 — 배지 대상 아님(기존 22:05 판단 유지).
- **ReviewsView·CommunityView 탭/카운트 `1px 7px`**: 활성 탭 위 흰 오버레이 카운트로 10px/400 적용 시 대비 약화 — 탭 디자인과 함께 재검토(기존 후보 유지).
- **ProfileView `n-tag` VIP·roleLabel, MoreView `.more-hero__vip`와 `.verify-badge` 나란히 배치 시 크기 차**: n-tag(naive-ui 기본 스펙)와 커스텀 배지의 혼재는 별도 설계 사항(후보).
- **공용 `UiChip.vue`(11px/700, padding 4px 7px)**: 클릭형 콘텐츠 칩으로 배지 스펙 대상 아님(기존 후보 유지).
- **`.verify-row__done`·`__pending` `#2ea043` vs `--status-completed`(#18a058)·VerifiedBadge 색**: 토큰화 시 라이트 색 미세 변화 수반 — 색상 체계 결정 후 일괄 적용(기존 후보 유지).
- **다크 모드 brand 위 흰 글자(`--on-brand`)·OrderDetailView 의미색 하드코딩·confetti·EmptyState 5색·커뮤니티 카테고리 6색 grayscale 통일**: 전역 설계/디자인 결정 사항(기존 보고서 후보 유지, 반복 미적용).
- **data/levels.js 레벨 타이틀 "드라이버"**: 백엔드 `LevelTable.php`와 동기화된 값 — 교차 변경 필요(기존 후보 유지).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 871ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: base.css에서 mojibake(`\uF900-\uFAFF`·`?뚮┝`·`梨꾪똿` 계열) 잔존 0건, `.verify-badge`·`.schedule-group__count` 모두 `1px 6px`/`10px`/`400`로 변경 확인, 배지-유사 padding(`3px 10px`) 잔존은 콘텐츠 칩·미디어 오버레이(후보)뿐.
- import 누락 점검: 이번 변경은 `<style>` CSS 속성 2건 + 주석 텍스트 3건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
