# UI 개선 보고서 — 2026-09-05 16:03

## 실행 결과 요약
- **수정 1건** — 15:04(47회차) 이후 `frontend/src` 변경 12건(15:12~15:14 **고객지원(B-4) 기능 도입**: SupportView 신규 + 지원 API + 라우트 + MoreView 진입점 + AdminView '고객지원' 탭 / 15:50~15:53 **행동 신호(노출·클릭) 수집 도입**: tracking.js·behavior API 신규 + OrderCard/SetGroupCard/HomeView/MarketView 추적 배선) 전수 열람. 새 기능은 대체로 한국어/"운행" 용어/색 의미/배지 체계를 잘 지켜 정합적. 유일한 명확한 소형 결함: **SupportView 공지 탭 카드의 레드 '공지' 배지 — 공지 전용 목록에서 모든 카드에 동일 라벨이 반복되고, 레드는 문제=의미색이라 공지에 오용되며 FAQ 카드(Q. 프리픽스, 배지 없음)와 비대칭** → 배지·관련 CSS 제거로 수정. 직전 회차(15:04) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 16:03 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 15:04 이후 `frontend/src` 변경 12건 — `views/settings/SettingsView.vue`(15:04:37, 47회차 수정 반영분) · `api/support.js`(15:12:29, 신규) · `views/SupportView.vue`(15:12:58, 신규) · `router/index.js`(15:13:12, support 라우트) · `views/MoreView.vue`(15:13:20, 고객지원 진입점) · `views/AdminView.vue`(15:14:40, 고객지원 탭 추가) · `api/behavior.js`(15:50:50, 신규) · `utils/tracking.js`(15:50:59, 신규) · `components/orders/OrderCard.vue`(15:51:20, 추적 배선) · `components/orders/SetGroupCard.vue`(15:51:30, 추적 배선) · `views/HomeView.vue`(15:52:24, 추적 배선) · `views/MarketView.vue`(15:53:06, 추적 배선)
- **직접 열람**: SupportView(전체, 템플릿+scoped CSS) · `api/support.js`·`api/behavior.js`(전체) · `utils/tracking.js`(전체 — 세션당 중복 노출 차단·디바운스 일괄 전송·실패 무시 설계 정상) · OrderCard·SetGroupCard(추적 prop/click/impression 배선 + 카드 템플릿/CSS) · HomeView(히어로 클릭·노출 watch L493-549 + 추천 3섹션 추적 prop L735-770) · MarketView(return/set/list 섹션 추적 prop L950-980) · AdminView(고객지원 탭 4종 — 공지/FAQ/문의 관리 탭·답변/작성 모달 L1559-1677·L2131-2192, DAILY_ITEMS L557-565, 칩 색 체계) · MoreView(고객지원 섹션 L148-153) · router/index.js(support 라우트 L57) · `app/Services/Support/SupportService.php`(kind_label·created_at 포맷 확인) · `app/Models/SupportPost.php`(kindOptions = 공지/FAQ) · `assets/base.css`(sk-line·색 변수 — --status-completed #18a058/#27946a) · App.vue(keep-alive include·하단 내비) · `utils/icons.js`(help/arrow-back/chevron-down/send/inbox 등록 확인)
- **전역 감사(grep)**: `formatWon(...)원`·원 단위 중복 0건 / font-size 10px 미만 0건 / "오더|주문번호|order_no" 노출 0건 / 신규 아이콘 전부 등록 확인(help L187·arrow-back L126·chevron-down L129·send L130·inbox L176)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1504.md`(47회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **공지 탭 레드 배지 중복 + 의미색 오용(→수정)**: [SupportView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SupportView.vue#L134-L142)의 공지 카드가 `post.kind_label`('공지') 배지를 danger 틴트(레드 10% 배경 + 레드 텍스트)로 반복 노출. 공지 탭은 `kind: 'notice'`로 필터링되어 모든 카드의 라벨이 동일한 '공지' — 정보 가치 없는 중복 장식이자, 레드는 문제(취소·거절·신고) 의미색이라 공지에 부적합. 같은 화면 FAQ 카드는 Q. 프리픽스 + 배지 없음으로 처리되어 비대칭이며, AdminView는 kind별 탭 분리로 배지를 쓰지 않음(사용자 화면에서만의 잔재). → 공지 카드의 배지 span과 `.support-card__badge` CSS 제거(카드 행 = 제목 + 날짜 + 셰브론, FAQ 행 = Q. 제목 + 셰브론과 동일 계열로 정리).
2. **고객지원 화면 전체 정합성**: [SupportView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SupportView.vue)는 탭 3종(공지/FAQ/1:1 문의) 분리, 문의 폼(제목·내용) → 제출 = 화면당 Primary 버튼 하나(우측 하단), 문의 후 입력 초기화 + 내 문의 재조회, 답변 박스는 브랜드 틴트 + '운영팀 답변' 라벨 — '정보→이유→행동'·미니멀 원칙 준수. '뒤로' 버튼 + page-head 구조도 다른 전체 페이지(알림 등)와 유사. 문의 상태 칩(open=앰버+다크 텍스트, answered=민트) 색 의미(대기=옐로우·완료=민트) 정합.
3. **AdminView 고객지원 탭**: 공지/FAQ 관리(작성·수정·삭제, kind별 탭 분리로 배지 불필요) + 1:1 문의 처리(상태 필터 기본 open + '답변' 버튼 — 답변 시 알림 전달 안내, 답변 후 목록 재조회). 상태 칩(open=앰버 #f0a800+다크, answered=민트+백색)은 AdminView 기존 v-chip·op-status 체계(민트+백색 텍스트 관례)와 동일해 정합. 작성·답변 모달은 기존 admin-modal 패턴 준수.
4. **행동 신호 수집 정합성**: tracking.js는 카드 mount 시 impression(같은 scope:section:orderId 세션당 1회, keep-alive 재마운트·탭 전환 중복 방지)·클릭 시 click(중복 제한 없음), 1.2초 디바운스 + 40건 일괄 + pagehide flush + 미인증/실패 무시 설계. 홈(chain/single/set + 히어로)·마켓(return/set/list) 배선은 rank·scope·section 매핑 정확. OrderCard·SetGroupCard 모두 화면에 표시되는 카드만 v-if로 마운트되어 노출 신호 오염 없음. 추적 전용 prop이라 카드 레이아웃·복잡도 영향 없음.
5. **전역 캐너리 통과**: 단위 중복 0건 / 10px 미만 폰트 0건 / 기술 식별자 노출 0건 / 신규 아이콘 전부 등록 확인(런타임 렌더 정상).

## (d) 실제 적용한 수정
- [SupportView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SupportView.vue#L134-L142) — 공지 카드의 중복 레드 배지 제거(템플릿 + CSS):
  - 전: `<span class="support-card__badge">{{ post.kind_label }}</span>` (모든 공지 카드에 동일 '공지' 라벨, danger 틴트 반복) + `.support-card__badge { background: color-mix(... var(--danger) 10% ...); color: var(--danger); ... }` CSS 블록
  - 후: 배지 span 제거 — 카드 행은 `제목(flex) · 날짜 · 셰브론`만 유지. `.support-card__badge` CSS도 삭제(잔재 없음 확인).
- 그 외 수정 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
- **신규 문의 상태 칩 answered(민트+백색 10px 텍스트) 대비**: 라이트 #18a058 위 백색 ≈ 3.35:1로 AA 미달이나, AdminView 기존 민트 칩(승인/답변 완료 — v-chip·op-status·verify-chip) 전부 동일 조합이고 46회차부터 '전역 의미색·상태 칩 정책과 함께 검토' 후보로 반복 분류 — 이번 신규 칩도 그 관례와 일관되므로 별도 수정하지 않고 전역 정책 후보로 유지. (사용자 카드 OrderCard는 완료=민트+다크 텍스트로 예외 존재 — 통일은 대형 정리 과제)
- **일일 운영 대시보드에 '문의 접수(open 티켓)' 카운트 미포함**: DAILY_ITEMS(L557-565)에 신고/보류/승인 대기 등은 있으나 고객지원 open 문의는 없음 — 운영 우선순위(🟡 확인 필요)에 넣으려면 백엔드 daily 카운트 API 확장이 필요해 기능 변경에 해당, UI 후보로만 기록.
- **1:1 문의 본문 placeholder '(운행 번호·날짜 포함 시 확인이 빠릅니다)'**: 사용자 UI에 운행 번호가 노출되지 않아 안내로 부적합할 수 있으나, 문의 처리 속도를 위한 일반 안내로 해석 가능 — '날짜·노선' 중심 문구로의 조정은 복사 정책 결정(후보).
- **AdminView 상단 탭 목록·고객지원 섹션 노출 범위**: 공지/FAQ는 전 역할 공통, 1:1 문의도 등록자·기사 모두 필요 — 게이팅 없음이 적절(유지).
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, 홈 추천 카드 조건% 미노출, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) **통과** — 4362 modules, built in 977ms. SupportView 청크 정상 생성.
- import 누락 점검: 수정이 SupportView 템플릿 span 1개 제거 + scoped CSS 블록 삭제에 국한 — 신규 import 없음, 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
