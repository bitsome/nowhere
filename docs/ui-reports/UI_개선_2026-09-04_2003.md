# UI 개선 보고서 — 2026-09-04 20:03

## 실행 결과 요약
- **변경 없음** — 19:01(37회차) 이후 `frontend/src` 소스 6건 mtime 변경(19:31~19:33 5개 뷰 + 20:01 ChatList)이 감지되어, 19:01 이후 첫 실행분으로 이들 **6개 파일을 전수 재열람 + 전역 캐너리 감사**한 결과 신규 적용 대상 미발견. 편집 배치의 성격(일부 파일 줄 수 변동: MarketView 1411→1375)은 19:31~19:33 세션에서 정리·수정이 있었음을 시사하나, 현재 콘텐츠는 기존 표준(배지 스펙·`#07120e`·한국어·"운행" 용어·Primary 1개 원칙)을 모두 준수 — 이전 회차 적용분 유지 확인. ChatList(20:01 터치)도 콘텐츠는 09-03 14:05 이후 표준 그대로.

## (a) 실행 시각
- 2026-09-04 20:03 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **19:31~19:33 편집 배치(5개 뷰 — 19:01 이후 변경분, 전수 재열람)**:
  - `views/HomeView.vue`(전체 1203줄 — script L1-532·템플릿 L534-745·scoped CSS L747-1142·전역 탭 CSS L1144-1203: 히어로·빈 히어로·추천 탭(단일/왕복/셋트)·왕복 체인 그룹(랭크·일괄요청중 집계/CTA)·단일/셋트 카드·EmptyState 3종·빠른 메뉴·스켈레톤 — 19:01 전수 구조와 동일)
  - `views/MarketView.vue`(전체 1375줄 — script L1-684·템플릿 L686-1017·scoped CSS L1018-1331·전역 드로어 CSS L1334-1375: 서비스 유형/필터/검색/내 운행 칩·적용 필터 요약 칩·필터 모달(날짜·시간대·유형·특별 보기·출발/도착·차량·인원·금액·정렬)·노선 검색 모달·연결 운행/전체 운행 섹션·페이지네이션·빠른매칭 FAB+드로어. 16:01(1411줄) 대비 36줄 축소 — 섹션 헤더 라벨 미사용 CSS 주석("헤더 라벨은 두지 않는다" L1326-1329) 일관)
  - `views/orders/MyMarketView.vue`(전체 1071줄 — 공개/진행중/정산/요청 탭·하위 카테고리(보냄/받음·전체/예약/운행중)·정산 일괄 CTA·일괄요청 보냄 그룹(시간 구분선·만료 카운트다운)·휴지통 모달·검색 모달·등록 FAB)
  - `views/orders/RideHistoryView.vue`(전체 808줄 — 상태 탭(전체/완료/정산/취소)·목록/캘린더·일별/월별/연별 그룹·요약 히어로 3칸·히스토리 카드)
  - `views/orders/OrderCreateView.vue`(전체 1250줄 — 목록 스케줄(탭·목록/캘린더·일/월 그룹)·등록 폼(AI 구조화/직접 입력/셋트)·템플릿·구조화 결과·셋트 일정·저장 CTA)
- **ChatList(20:01 터치 — 신규 재열람)**: `components/chat/ChatList.vue`(전체 262줄 — 오늘/어제/지난주 분류 칩+건수·대화 카드·유형 칩(승인/시간/요금/취소)·운행 칩·경로 요약)
- 전역 감사(grep): `font-size` 6~9px 0건, "오더/주문번호/order_number" .vue UI 노출 0건
- 기존 적용 유지 확인(grep): HomeView 전역 탭 건수 칩 `#07120e`(L1201)·랭크/집계 배지(10px/400)·일괄요청 CTA `#07120e`(L1029)·히어로 CTA 다크 `#07120e`(L964), MarketView 필터 건수 배지 `#07120e`(L1208), MyMarketView 정산 CTA `#07120e`(L1047)·휴지통 건수 배지 10px/400, RideHistoryView·OrderCreateView 캘린더 today `#07120e`(각 L766·L1213), ChatList 활성 칩 카운트 `#07120e`(L226)·유형 칩 라이트/다크 쌍(09-02 22:05 적용분) — 전부 존재
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1901.md`(37회차 — HomeView 전수)·`1801.md`(36회차)·`1701.md`(35회차)·`1601.md`(35회차 MarketView 1411줄 전수)·`1301.md`(OrderCreateView 캘린더 적용) 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 19:01 이후 첫 실행)
- 작업 트리 확인: 19:01 이후 `frontend/src` 수정 6건(위 목록, LastWriteTime 실측 19:31:32~19:33:13·20:01:14) — 이번에 전수 확인 완료
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand·상태색 채움 위 글자 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **편집 배치 5뷰 클린 (19:31~19:33 변경분 전수 재확인)**: HomeView(히어로 눈썹 brand 11px/700·보상 라벨 10px/600·경로 20px/800·태그 칩 1px 6px/10px·CTA grayscale-first 라이트 `var(--text)`채움/다크 brand+`#07120e`·화면당 Primary 1개(일괄요청/매칭 설정) — 19:01 결론 재현)·MarketView(칩 행·필터 모달 Primary "적용" 1개+취소/초기화 보조·노선 검색 Primary "검색" 1개·빈 상태 "가져올 수 있는 운행이 없습니다"·"운행" 용어)·MyMarketView(섹션 배지 10px/400·정산 CTA brand+`#07120e`·요청 만료 카운트다운 danger 의미색·휴지통 전체 삭제 danger ghost+버튼·검색/휴지통 모달 Primary 각 1개)·RideHistoryView(상태 배지 `statusTextColor` 로직·캘린더 today `#07120e`·건수/배지 스펙)·OrderCreateView(탭 radio·폼 저장 Primary 1개(모드별 "운행 등록"/"셋트 등록" 분기)·캘린더 today `#07120e`) — 신규 위반 없음.
2. **ChatList 클린 (20:01 터치분)**: 분류 칩 건수(비활성 10px/600·활성 brand+`#07120e` L226)·유형 칩 3종 라이트(#0e9d8d/#9a6a00/#c03a3f)+다크 쌍·"운행" 칩(1px 6px/10px/400)·빈 문구 "이 기간의 대화가 없습니다." — 09-02 22:05·09-03 14:05 적용분 그대로. 아바타 `--accent`+흰 글자(L242)는 기존 관용 분류.
3. **전역 캐너리 통과**: 10px 미만 폰트 0건, "오더/주문번호" UI 노출 0건. 19:01 이후 변경 배치·터치 모두 표준 위반 없음 — 신규 잔존 없음.

## (d) 실제 적용한 수정
- **없음** (변경 없음 — 편집 배치 5뷰·ChatList 전수 재열람 + 전역 캐너리 모두 신규 위반 미발견, 기존 적용 내역 반복 없음)

## (e) 적용하지 않은 항목과 이유 (후보)
- **MarketView 1411→1375줄 축소분(16:01 대비 36줄)**: 19:31~19:33 편집 세션의 정리·수정으로 추정되나 현재 콘텐츠는 표준 준수 — 역추적 diff 불가(미커밋 작업 트리·스냅샷 없음)하므로 추가 변경 없음.
- **HomeView·CommunityView·ChatList 탭 칩 내부 건수(1px 5px/600)**: 칩 내부 카운트 관용 패턴 — 배지 스펙(1px 6px/400)은 독립 배지용 — 정상 분류(미적용).
- **FAB·아바타·빠른매칭 아이콘 흰색(`var(--brand)` 위 `#fff`, ChatList L242 포함)**: 아이콘/아바타 관용 — "brand 위 글자색" 전역 설계 후보로 기존 분류 유지(반복 미적용).
- **OrderCreateView 중복 CSS 정의(`.schedule-list` L882 gap:18px↔L1071 gap:10px·`.create-tags` L941↔L1004)**: 후행 정의가 우선되어 실제 노출은 단일 — 정리 대상이나 동작 변경 위험 소지로 큰 변경 분류(신규 후보 기록).
- **OrderCreateView 폼 블록 들여쓰기 불일치(L258~641)·MarketView/MyMarketView/RideHistoryView 상태 탭 패턴 중복**: 시각·동작 영향 없는 코드 포맷 — 리팩터링 후보 유지(미적용).
- **기존 후보(HomeView 탭 카운트 vs 표시 개수 불일치·OrderDetailView `.detail-hero`·AdminView `page-head__desc`·`/v8-noti` 데드 CSS·RegisterView CSS 복제·EmptyState/UiChip variant 색·naive 전역 테마·SettingsVerificationView 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 없음 → `npm run build` 미실행(빌드 불필요, 수정 시에만 빌드 검증 방침 유지 — 마지막 빌드는 34회차 15:06 성공 확인분).
- 수정 후 grep 검증: 해당 없음(변경 0건). 표준 유지 grep은 (b)·(c)에 기록 — 전부 존재.
- import 누락 점검: 코드 변경 없음 — 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
