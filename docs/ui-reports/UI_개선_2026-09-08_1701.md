# UI 개선 보고서 — 2026-09-08 17:01

## 실행 결과 요약
- **수정 1건 적용** — 직전 실행(09-08 16:01) 보고서 이후 `frontend/src` 변경분 **5건**(ProfileView 16:08 · SettingsView 16:07 · SettingsVerificationView 16:12 · AdminView 16:22 · api/operations.js 16:15) 중 vue 화면 4건을 검토했다(OrderCreateView·BaseIcon·LevelBadge 16:04 저장분은 16:01 보고서의 적용분 자체임을 확인 — 신규 코드 아님). 16:01 이후 변경분은 설정 화면 분리(ProfileView 리팩토링 + 설정 허브/인증/차량 페이지 신설)와 관리자 화면 대규모 변경으로, 대부분 이미 UI 원칙에 부합했다. 이번 실행은 ProfileView에 남은 **낡은 HTML 주석 블록 8줄(이동 전 구조 설명·중복 '상세 탭' 주석)** 을 데드 마크업으로 판단해 제거했다. 그 외 신규 위반(기술 식별자·용어·소형 폰트) 0건.

## (a) 실행 시각
- 2026-09-08 17:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 16:01 이후 수정분)**: `frontend/src` 전체 재귀 — **6건**: `ProfileView.vue`(16:08, git 추적) · `SettingsView.vue`(16:07, 신규 미추적) · `SettingsVerificationView.vue`(16:12, 신규 미추적) · `api/operations.js`(16:15, 신규 — API 계층이라 vue/CSS 검토 대상 아님, 존재만 확인) · `AdminView.vue`(16:22) · `OrderCreateView.vue`/`BaseIcon.vue`/`LevelBadge.vue`(16:04 — 16:01 보고서 적용분 자체로 신규 코드 아님 확인).
- **직접 판독(검토 대상)**:
  - `views/ProfileView.vue`(전체 + `git diff HEAD`로 16:08 변경분 분석) — 변경분: "유저→사용자·드라이버→기사" 용어 정리, 등록자 '업체 인증' 카드 추가(Q-4), `.page-shell` 통합(기존 자체 width·media CSS 제거), 리뷰 시각 `formatTime()` 적용, 레벨 바 다크모드 대응, 세그먼트 탭 활성 글자색 #ffffff→#07120e(양호한 개선). 잔존 문제로 낡은 주석 블록 식별.
  - `views/settings/SettingsView.vue`(신규 — 설정 허브: 프로필/차량/알림/인증/화면 카드 + 로그아웃)
  - `views/settings/SettingsVerificationView.vue`(신규 — 역할별 차량·면허/사업자·계좌 인증 신청 화면)
  - `views/settings/SettingsVehiclesView.vue`(기사 차량 관리 — 같은 패턴 비교용)
  - `assets/base.css`(700~791행 — 공용 `.verify-*` 규칙 재사용 확인)
  - `views/AdminView.vue`(16:22 대규모 변경 — 화면 수만 줄 초과로 전수 판독 대신 후보 기록)
- **전역 규칙 감사(grep)**:
  - 템플릿 기술 식별자(`주문번호|주문 번호|오더번호`): **0건** · `orderId` 12건 매칭은 전부 JS 코드 식별자(props/함수 인자/상태) — 규칙상 유지, 템플릿 노출 없음
  - `font-size: 7~9px`: **0건**
  - `role === 'Driver'` 리터럴: **8곳**(ProfileView 24·43행 / HomeView 27 / MoreView 27 / MarketView 30 / App.vue 243 / SettingsVehiclesView 28 / QuickMatchPanel 30 — 기존 후보 유지)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1601.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **ProfileView 낡은 HTML 주석 블록(이번 실행 적용)**: 113~120행에 이동 전 구조를 설명하던 잔여 주석 6개 — `<!-- 내 실적 — '요약' 탭으로 이동 -->`, `<!-- 상세 탭 — 유형별로 분리 (요약 / 차량·근무 / 활동 / 설정) -->`, `<!-- 인증 상태 — '설정' 탭으로 이동 -->`, `<!-- 바로가기 — '요약' 탭으로 이동 -->`, `<!-- 상세 탭 — 차량·근무 / 활동 / 설정 -->` 등. 실제 탭은 '요약/근무/활동' 3개뿐인데 주석이 옛 이름('차량·근무'·'상세 탭')과 옛 구조를 참조해 **현재 코드와 불일치**하고, '상세 탭' 주석은 2건 중복·내용도 상충. 순수 주석(렌더링 영향 없음)인 데드 마크업 — 구조는 인접 실제 코드로 즉시 파악 가능해 제거로 정보 손실 없음.
2. **신규 설정 화면 — UI 원칙 부합 재확인**: SettingsView(허브 카드 그리드)·SettingsVerificationView(인증 상태 카드)·SettingsVehiclesView(차량 목록) 모두 `.page-shell` + 공용 `.verify-*`(base.css 708~791) 재사용. SettingsVerificationView의 상태 칩(`verify-chip--done/pending/rejected/none`)은 v8 시안 의미색(완료=틸·대기=앰버 #f0a800·거절=레드·미인증=그레이)에 정확히 부합. 설정 허브·자식 페이지의 뒤로가기('설정')·`page-head` 규칙도 일관 — 위반 없음.
3. **ProfileView 16:08 변경분 — 수정 필요 없음**: 세그먼트 탭 활성 글자색 `#07120e` 두 모드 공통 적용(주석 513행에 근거 명시)은 brand 라이트/다크에서 대비 확보한 올바른 처리. `formatWon`(useProfileSettings, 콤마만)·`formatTime`(ISO → 'M월 D일') 사용도 문맥 정상. 데드 import·CSS 없음(스타일 블록 전체가 템플릿과 1:1 매칭, 확인 완료).
4. **신규 위반 — 0건**: 기술 식별자·용어·소형 폰트 감사 모두 위반 없음. 16:01 보고서 이후 신규 결함 없음.
5. 기존 반복 후보 상태 변화 없음(16:01 기록분 그대로, 단 `role === 'Driver'`는 11곳 → **8곳**으로 감소 — SettingsView가 ROLE_DRIVER import로 전환한 영향).

## (d) 실제 적용한 수정
- **ProfileView.vue — 낡은 HTML 주석 8줄 제거**(113~120행의 이동 전 구조 설명·중복 '상세 탭' 주석 일괄 삭제). `<n-card>`(히어로) 종료와 `<n-tabs>` 시작 사이 빈 줄만 남김. 주석 제거로 렌더링·동작 변화 없음 — 로컬 전용 작업. (ProfileView 16:08 변경분은 사용자 작업으로 이번 실행 수정 없음)

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(16:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이(SettingsVerificationView의 verify-chip 2px 8px/font-weight 700도 해당 — 상태 칩으로 배지 스펙 대상 아님 유지) · 문의 placeholder 문구(SupportView) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 8곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지.
- **AdminView.vue 16:22 대규모 변경(신규 후보)**: 관리자 화면이 3천 줄 이상 증감으로 재구축됨(관리자 시스템 작업으로 추정). 화면이 크고 작업 진행 중일 가능성이 높아(16:22 수정) 전수 판독·수정은 이번 범위에서 보류 — 다음 실행에서 AdminView 중심 점검 후보로 기록.
- **SettingsView 카드 타이포 단차**: 카드 제목(strong) 11px / 설명(small) 11.5px로 설명이 제목보다 크게 보임 — ProfileView 바로가기(strong 11px·small 11px)와 미세 단차 있으나, 대부분 앱이 11~11.5px 소형 폰트 체계를 쓰고 제목이 bold라 시인성 문제 없음. 디자인 판단 필요 — 대형 아님으로 후보 유지.
- 이번 실행 신규 감사에서 위 1건 외 적용할 신규 결함 없음(위 (c) 4 참조).

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, `✓ built in 1.04s`. ProfileView가 포함된 index/main 청크 및 각 라우트 청크(AdminView·OrderDetailView 등) 정상 생성.
- import 누락 점검: 이번 수정은 템플릿 주석 제거만(스크립트·템플릿 동작 코드·import 변경 없음) — 영향 없음. ProfileView 스타일 블록과 템플릿 클래스 매칭, 스크립트 import(computed·onMounted·ref·watch 등) 사용처 사전 확인 완료 — 누락 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
