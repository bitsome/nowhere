# UI 개선 보고서 — 2026-09-08 13:01

## 실행 결과 요약
- **수정 1건 적용** — 직전 실행(09-08 12:01) 보고서 작성 이후 `frontend/src`에서 **9개 파일 변경**(12:34~12:49, 로딩 스켈레톤 도입·커뮤니티 글 작성 리팩토링 등 개발 진행분). 신규 코드 검토 결과 **HomeView.vue에 base.css 전역 스켈레톤(`.sk-*`)과 동일한 scoped CSS 중복 정의(38줄)를 발견·제거**했다. 이외 신규 위반(기술 식별자·용어·소형 폰트·영문 라벨·배지 스펙) 0건.

## (a) 실행 시각
- 2026-09-08 13:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 12:01 이후 수정분)**: `frontend/src` 전체 재귀 — **9건** 수정:
  - `views/HomeView.vue`(12:49) · `components/orders/OrderDetailChat.vue`(12:48, 신규) · `views/ChatView.vue`(12:48) · `composables/usePostComposer.js`(12:46) · `components/community/CommunityPostEditor.vue`(12:46) · `views/AdminView.vue`(12:44) · `views/orders/OrderDetailView.vue`(12:34) · `data/roles.test.js`(12:40) · `utils/koreanHolidays.test.js`(12:40)
  - 테스트 2건을 제외한 UI 7건을 전수 검토.
- **직접 판독**: HomeView.vue(템플릿 전체·스켈레톤 스타일), ChatView.vue(전체 — 대화 목록 스켈레톤), OrderDetailChat.vue(전체 — 운행 상세 인라인 대화창), CommunityPostEditor.vue(전체 — 글 수정 모달), usePostComposer.js(전체), `assets/base.css` 스켈레톤 섹션(645~705), `views/community/CommunityPostView.vue` 스켈레톤 CSS(630~654).
- **전역 규칙 감사(grep)**:
  - 기술 식별자 템플릿 노출(`오더|주문번호|…`): **1건** — SupportView.vue:189 placeholder(기존 반복 후보, 신규 아님)
  - `font-size: 7~9px`: **0건**
  - `.sk-card/.sk-line/.sk-chip/.sk-circle/@keyframes sk-shimmer` **scoped 재정의**: HomeView.vue(제거 대상) · CommunityPostView.vue(패딩 14px 의도적 로컬 오버라이드, 유지)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1201.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **HomeView.vue 스켈레톤 scoped CSS 중복(이번 실행 신규)**: 금시간 추가된 홈 로딩 스켈레톤(`.home-skeleton`)이 공용 `.sk-card/.sk-line/--sm/--md/--lg/.sk-chip`, `@keyframes sk-shimmer`, `prefers-reduced-motion` 미디어 블록을 **base.css 전역 정의(645~705)와 동일한 값으로 scoped 재정의**(기존 851~888·914~919). 로컬 고유 정의는 `.sk-line--title`·`.sk-line--cta`뿐. 같은 시간대에 추가된 ChatView 스켈레톤은 전역 클래스를 정상 재사용(중복 없음)해 HomeView만 예외 패턴.
2. **금시간 신규 UI 컴포넌트 검토 결과 — 위반 없음**: OrderDetailChat(등록자 대화)의 상대 이름 칩은 배지 스펙(padding 1px 6px / 10px / 400)과 정확히 부합, 전송 버튼 아이콘 우선 38px 원형 단일 Primary. CommunityPostEditor(글 수정)는 카테고리 칩·취소/저장 2액션이지만 저장 disabled 처리로 혼선 없음. ChatView·AdminView·OrderDetailView 스켈레톤 블록은 용어·기술 식별자·색상 위반 없음.
3. 기존 반복 후보 상태 변화 없음(12:01 기록분 그대로).

## (d) 실제 적용한 수정
- **HomeView.vue — 전역과 중복되는 스켈레톤 scoped CSS 38줄 제거**(scoped `.sk-card`, `.sk-line`, `.sk-line--sm/md/lg`, `.sk-chip`, `@keyframes sk-shimmer`, `prefers-reduced-motion` 블록 삭제). 렌더링 결과 동일: 남은 요소들은 base.css 전역 정의(main.js에서 무조건 import)로 동일 스타일을 받고, 홈 전용 크기 변형 `.sk-line--title`·`.sk-line--cta`만 scoped로 유지(주석으로 전역 의존 명시). 데드였던 `.sk-chip`(HomeView 템플릿 미사용) 정의도 함께 제거.
- 순수 CSS 중복 제거로 렌더링·동작 변화 없음 — 로컬 전용 작업.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(12:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView '(운행 번호·날짜 포함 시…)' — 사용자가 운행 번호를 알 수 없어 안내 실효성 문제) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · App.vue 데드 CSS `.bottom-nav__item--button` · ChatView.vue 미사용 `ref` import(이번 시간 수정으로 해소 여부 재확인 시 신규 아님) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 11곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지(렌더링 영향 없거나 대형·디자인·제품 결정 수반, 반복 미적용).
- **CommunityPostView.vue `.sk-card` scoped 정의**: base.css(패딩 16px) 대비 패딩 14px의 **의도적 로컬 오버라이드**로 판단, 순수 중복이 아니어서 유지(이번 시간 변경분 아님).
- 금시간 신규 스켈레톤(ChatView·AdminView·OrderDetailView)·글 수정 모달·OrderDetailChat: 위반 항목 없어 후보로 분류할 신규 결함 없음.

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, 974ms. HomeView 분할 CSS 번들은 중복 제거 후에도 정상 생성.
- import 누락 점검: 이번 실행은 scoped 스타일 블록 삭제만(스크립트·템플릿 변경 없음) — 추가 import 대상 없음. 스켈레톤 클래스는 base.css 전역(main.js:5 무조건 import) 의존으로 런타임 스타일 유지 확인.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
