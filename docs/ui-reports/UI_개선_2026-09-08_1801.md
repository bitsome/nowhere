# UI 개선 보고서 — 2026-09-08 18:01

## 실행 결과 요약
- **변경 없음(수정 0건)** — 직전 실행(09-08 17:01) 보고서 이후 `frontend/src` 변경분 **5건**(ProfileView 17:04 · api/orders.js 17:59 · views/orders/ActionCenterView 18:00 · composables/useOrderDetail.js 18:01 · views/orders/OrderDetailView 18:01)을 검토했다. 이 중 ProfileView 17:04는 17:01 보고서의 적용분(낡은 주석 제거) 자체의 저장 시각으로 신규 코드가 아님을 확인. 나머지 4건은 운행 상세 로직의 `useOrderDetail.js` 컴포저블 분리 + `views/orders/` 이동을 포함한 일련의 운행 상세 화면 작업(17:59~18:01, 실행 직전 진행된 편집 세션)으로, 검토 결과 **기술 식별자·용어·소형 폰트·배지 스펙·단일 Primary 등 UI 원칙 위반 0건** — 적용할 신규 결함이 없어 코드 수정 없이 종료(편집 세션이 방금 끝난 파일이라 충돌 리스크도 고려).

## (a) 실행 시각
- 2026-09-08 18:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 17:01 이후 수정분)**: `frontend/src` 전체 재귀 — **5건**: `ProfileView.vue`(17:04) · `api/orders.js`(17:59, API 계층 — vue/CSS 검토 대상 아님, 사용 함수만 존재 확인) · `views/orders/ActionCenterView.vue`(18:00) · `composables/useOrderDetail.js`(18:01) · `views/orders/OrderDetailView.vue`(18:01).
- **직접 판독(검토 대상)**:
  - `views/ProfileView.vue`(113~120행 영역) — 17:01 보고서 적용분(낡은 주석 제거 후 `</n-card>` ~ `<n-tabs>` 사이 빈 줄)이 그대로 유지됨. 120행의 `<!-- 요약 — 내 실적 + 자주 쓰는 메뉴 -->`는 실제 탭과 일치하는 정상 주석. 17:04 저장 시각은 해당 적용분 저장분 — 신규 코드 아님.
  - `views/orders/ActionCenterView.vue`(전체, script+template+style 전수) — 처리할 일 화면. 승인 요청/요금 제안/채팅 요청 섹션 구조, `.page-shell` 통합, 거절 사유 모달. 배지(`.actions-head__count`·`.actions-sub__badge`·`.actions-card__count`·`.actions-card__indicator`·`.chat-item__tag`) 전부 padding 1px 6px / 10px / 400 스펙 준수. `.actions-btn--ok`(brand)·`--no`(danger 라이트)·`--chat`(아웃라인)로 역할 구분 명확, 의미색만 사용. 칩 색 이슈(채팅 요청 time_change·route_change·payment_change가 brand색, cancel만 red)는 기존 후보와 동일 상태(아래 (e)).
  - `views/orders/OrderDetailView.vue`(템플릿 380~1140행·주동작 영역 중심 전수, style은 관련 규칙 대조) — 운행 상세 화면. 컴포저블 분리 후에도 히어로(노선/금액/상태 태그) → 진행 스테퍼 → 상태 안내 → 정보 → 액션 구조 유지. 상태별 액션은 `primaryAction`(상태 전이 단일 Primary, label `→ 상태명`) + 보조(채팅/수정)로 **화면당 Primary 하나** 원칙 준수. 가져오기 신청/거절은 신청자 카드 footer에 거절(secondary)+승인(primary) — 동일 행동 쌍이라 유지 적절. 모달 내 Primary/error 버튼은 context 내 1건씩. 기술 식별자(주문번호 등) 템플릿 노출 0건(JS `order_id` 참조 326행은 코드 비교용).
  - `composables/useOrderDetail.js`(script 전반 + 640~720행 액션 로직) — `primaryAction`(isClaimantPending → '수락대기중' 표시용 → 다음 전이 → 리뷰는 별도 버튼), `statusButtonColor`의 CSS var → 실제 색 변환은 컬러 프롭 한계에 대한 정당한 처리. 노출 문구 '가져오기/가져오기 요청/내 운행으로 가져오기'로 운행 용어 일관. 위반 없음.
- **전역 규칙 감사(grep)**:
  - 템플릿 기술 식별자(`주문번호|주문 번호|오더번호|운행 번호`): **1건** — `SupportView.vue` 문의 placeholder 문구(기존 후보 유지, 아래 (e)) · `font-size: 7~9px`: **0건**
  - `views/orders/*` 소형 폰트·배지 위반: **0건**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1701.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **신규 위반 — 0건**: 17:01 이후 변경분(운행 상세 화면 컴포저블 분리·이동)에서 기술 식별자·용어·소형 폰트·배지 스펙·단일 Primary 원칙 모두 충족. useOrderDetail.js 분리 후에도 화면 액션 구조(정보 → 안내 → 단일 주동작 → 보조)가 유지되어 미니멀 원칙 훼손 없음.
2. **ProfileView 17:04 = 직전 보고서 적용분 재확인**: 낡은 주석 제거가 반영된 상태 그대로(113행 `<n-tabs>` 시작, 120행 탭 주석만 유지). 반복 적용 대상 없음.
3. **신규 결함 발생 요인 없음**: api/orders.js는 `apiAcceptOffer·apiRejectOffer·apiActions` 등 호출부(ActionCenterView/useOrderDetail)와 시그니처 일치 확인 — UI 영향 없음. 빌드 정상(아래 (f)).
4. 기존 반복 후보 상태 변화 없음(17:01 기록분 그대로).

## (d) 실제 적용한 수정
- **없음** — 이번 실행은 검토만 수행. 수정할 신규 결함이 없었고, 검토 대상 파일 4건(17:59~18:01)이 실행 직전 편집된 진행 중 코드로 판단되어 무리한 개입을 피함.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(17:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색(time_change·route_change·payment_change가 brand색 — 알림 의미색 체계상 '요금·시간 변경=옐로우 확인 필요'인데 brand로 렌더, 전역 색 설계와 연결되어 소형 수정 아님) · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView '(운행 번호·날짜 포함 시…)' — 이용자가 운행 번호를 알 수 없는 경우 안내 실효성 문제) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 8곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) · SettingsView 카드 타이포 단차 — 기존 분류 유지.
- **AdminView.vue 16:22 대규모 변경(17:01 후보 이월)**: 관리자 화면 재구축 작업분 — 화면이 크고 작업 진행 중일 가능성이 높아 다음 실행에서 AdminView 중심 점검이 우선 후보. 이번 실행 변경분에는 해당 없음.
- 이번 실행은 위 (c)와 같이 신규 결함이 없어 신규 미적용 항목 없음.

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, `✓ built in 2.33s`. OrderDetailView(54.62 kB)·ActionCenterView·ProfileView 등 최근 변경 뷰 청크가 정상 생성 — 18:01 시점의 작업분도 컴파일 오류 없음을 확인(로컬 검증).
- import 누락 점검: 이번 실행은 코드 수정이 없어 해당 없음. (참고로 useOrderDetail.js → OrderDetailView 분리분은 빌드 통과로 import/참조 정합성 확인됨.)
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
