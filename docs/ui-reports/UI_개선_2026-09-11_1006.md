# UI 개선 보고서 — 2026-09-11 10:06

## 실행 결과 요약
- **코드 수정 2건(1개 파일)** — 이번 회차는 `frontend/src` 의 **scoped 스타일과 전역(텔레포트) 스타일의 클래스 이름 충돌**을 처음으로 전수 감사했다. Vue 3 의 `<style scoped>` 는 `data-v-*` 속성으로 범위를 좁히는데, **`<Teleport>` 로 body 에 옮겨진 노드도 그 속성을 그대로 유지**한다. 그래서 한 컴포넌트 안에서 **scoped 규칙과 전역 규칙이 같은 클래스 이름을 쓰면 두 규칙이 같은 요소에 동시에 적용**된다. `QuickMatchPanel.vue` 가 정확히 이 형태(패널의 "원큐 설정" 블록과 조건 모달 본문이 둘 다 `.qmp-quick`)여서, **모달 본문이 패널 블록의 구분선·여백을 함께 받고, 패널 블록이 모달의 flex gap 을 함께 받는 상호 오염**이 빌드 산출물에서 재현됐다.
  1. **`frontend/src` 밖에서 모달 누수**: [QuickMatchPanel.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L912-L916) 의 scoped `.qmp-quick`(`border-top:1px solid var(--border); margin-top:12px; padding-top:12px`)이 **텔레포트된 조건 모달 본문에도 적용**되어, 카드 제목 바로 아래에 의도하지 않은 **가로 구분선 + 12px 여백**이 생겼다.
  2. **패널 블록의 여백 팽창**: 전역 [.qmp-quick](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1047) (`display:flex; flex-direction:column; gap:18px`)이 모달용으로 추가됐지만, 같은 클래스인 **패널의 "원큐 설정" 블록에도 적용**되어 제목↔프리셋 사이 간격이 의도(≈19px)보다 **gap 18px 만큼 더 벌어졌다**(p 기본 margin + row `margin-top:8px` 에 gap 이 가산).
  → 패널 블록의 클래스 이름을 `qmp-quick*` → **`qmp-preset*`** 으로 분리해 두 규칙이 서로의 요소를 건드리지 않게 했다.
- 추가로 **빠른매칭 토글 스위치의 상태 미고지**를 함께 교정: [.qmp-switch](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L529-L538) 는 `<button>` 이라 켜짐/꺼짐을 스크린리더가 읽지 못했다. `role="switch"` + `:aria-checked="matchEnabled"` 를 붙였다.
- 실행 전 상태 점검: HEAD `f240340`, `frontend/src` 최신 mtime = **01:31:11**(`useOrderForm.js`) → 직전 보고서(01:01) 이후 **타 세션 편집 2건**(01:05 `NotificationsView.vue` = 01:01 회차 적용분, 01:31 `useOrderForm.js` = AI 구조화 폴백 안내 문구)이 있었고 **둘 다 UI 결함 없음**으로 판정(아래 (b)·(e)).
- 전역 표준 스캔 신규 위반 0건: `오더`/`주문번호` **0건**, 미정의 CSS 변수(`var(--…)` 대비 `--…:` 정의) **0건**, `!important` **0건**, 배지 규격(1px 6px/10px/400) 위반 **0건**.
- `npm run build` 통과(`✓ built in 8.70s`, 오류·경고 0건), `vitest run` **13 files / 73 tests 통과**, `check:refs`(템플릿 참조) **OK**. 분류·클래스 이름 변경뿐이라 import·동작 무변경.
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-11 10:00~10:07 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-11_0101.md`(적용: 알림 `priorityCls` 에 `report: 'danger'` 누락 교정) · `2026-09-11_0001.md`(적용: 실시간 카운트다운 `tabular-nums` 5곳) · `2026-09-10_2301.md`(적용: `color-scheme`·`dvh`·`overscroll-behavior`) — 적용 내역·미적용 후보 전수 대조 후 **반복 적용 없음**. 역대 보고서 그렙(`qmp-quick`·`qmp-switch`·`QuickMatchPanel`·`텔레포트`·`data-v`) 결과 **"scoped ↔ 전역 클래스 충돌" 축은 09-02~09-11 보고서에 등장하지 않는 신규 감사 축**임을 확인(09-04 09:03 회차가 QuickMatchPanel 1080줄을 전체 열람했으나 이 충돌은 미기록).
- **실행 전 상태 점검**: `Get-Date`(10:00) + `git log --oneline -3`(HEAD `f240340`) + `git status --short` + `Get-ChildItem frontend/src -Recurse | Sort LastWriteTime -Descending` → 01:01(직전 회차) 이후 타 세션 편집 2건 확인.
- **신규 축 1 — scoped/전역 클래스 충돌**: `frontend/src` 전수에서 `<style>`(전역) 블록을 가진 컴포넌트를 추리고, 그 안의 클래스 이름이 같은 파일의 scoped 규칙과 겹치는지 대조 → [QuickMatchPanel.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1040-L1046) 의 전역 블록(모달 텔레포트용) 주석 "body에 텔레포트되므로 전역 스타일 필요 (scoped 적용 안 됨)" 이 **사실과 다름**(scoped 는 `data-v` 속성으로 텔레포트 노드에도 적용됨)을 확인하고, `.qmp-quick` 이 패널·모달 양쪽에서 쓰이는 것을 특정.
- **빌드 산출물 교차 검증**: `dist/assets/MarketView-*.css` 에서 두 규칙이 동시에 존재함을 확인 — `.qmp-quick[data-v-2c66678e]{border-top:1px solid var(--border);margin-top:12px;padding-top:12px}` + `.qmp-quick{flex-direction:column;gap:18px;display:flex}`.
- **신규 축 2 — 커스텀 토글의 상태 시맨틱**: `role="switch"`/`aria-checked` 그렙 → `frontend/src` **0건**. 네이티브 `<select>`/`<textarea>` **0건**, 토글 성격 컨트롤은 [QuickMatchPanel `.qmp-switch`](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L529-L538)(자체 제작) 1곳뿐이며 나머지(테마·알림 토글)는 `n-switch`(naive-ui, 시맨틱 내장)임을 확인.
- **중복 셀렉터 전수(신규 계수, 수정 없음)**: 파일별 동일 셀렉터 블록 2회 이상 정의 스캔 → 검출 대부분이 `@media (prefers-reduced-motion)`·`@media (hover: hover)` 내부 중첩 규칙(정상)과 `@media` 밖 전역 텔레포트 블록(위 축 1)이었다. 순수 중복은 `base.css` 의 `body` 2블록(85·91 — 분리 의도 유지)뿐으로 **추가 결함 없음**.
- **미정의 CSS 변수 전수(신규 감사, 수정 없음)**: `var(--…)` 사용 이름 집합과 `--…:` 정의 집합 대조 → 미정의는 `--x`·`--cx`·`--cy`·`--confetti-color`·`--score-star-size` 5종뿐이고 **전부 `:style` 인라인 바인딩으로 주입되는 동적 변수**(정상). `--status-` 는 `colors.js` 주석·`.replace('var(--status-'…)` 문자열에서 검출된 **정규식 오탐**(실사용 아님).
- **하드코딩 배경색 다크 대응 전수(신규 감사, 수정 없음)**: scoped CSS 의 `background: #hex` 55건 판독 → 대부분 라이트/다크 쌍 존재([UiCard](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiCard.vue#L49-L64)·[UiChip](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiChip.vue#L33-L92)·[NotificationsView](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L470-L509)·[UserPageView](file:///d:/wwwroot/nowhere/frontend/src/views/community/UserPageView.vue#L520-L551)) 이거나 흰 글자 위 고정색 배지(대비 유지). **누락 결함 0건**.
- **타 세션 신규 편집 검토(수정 없음)**: [NotificationsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L119-L120)(01:05, 직전 회차 `priorityCls` 적용분 — `report: 'danger'` 잔존 확인) · [useOrderForm.js](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderForm.js#L153-L164)(01:31, AI 구조화 실패 시 서버 규칙 파서 폴백을 `structured.parsed_by === 'local'` 로 판별해 안내 문구 분기) — **로직 변경이며 템플릿·CSS 상 신규 UI 결함 없음**.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀·의미색), 프로젝트 배지 표준(1px 6px/10px/400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출).

## (c) 발견한 개선 포인트
1. **scoped ↔ 전역 클래스 이름 충돌로 모달에 구분선이 새어 들어감(신규 발견·적용)**: Vue 3 의 scoped CSS 는 클래스 선택자에 `[data-v-*]` 를 덧붙여 범위를 좁히는데, **`<Teleport>` 는 노드의 속성을 유지한 채 위치만 옮기므로 텔레포트된 모달 본문에도 같은 `data-v` 가 붙는다**. 개발자는 "scoped 적용 안 됨"으로 보고 모달용 전역 스타일을 추가했지만, 실제로는 **두 규칙이 동시에 적용**된다. 그 결과 [.qmp-quick](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1047) 클래스 하나를 공유하는 **패널 블록과 모달 본문이 서로의 스타일을 받아** ① 모달 제목 아래에 불필요한 가로 구분선·12px 여백이 생기고 ② 패널 "원큐 설정" 블록의 제목↔프리셋 간격이 gap 18px 만큼 벌어졌다. 값이 아니라 **스타일 범위(scope)** 의 문제라 정적 화면·단일 파일 판독으로는 드러나지 않는다.
2. **빠른매칭 토글의 상태 미고지(신규 발견·적용)**: [.qmp-switch](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L529-L538) 는 자체 제작 `<button>` 이라 `aria-label="빠른 매칭"` 만 있고 **켜짐/꺼짐 상태를 노출하지 않는다**. `n-switch` 를 쓰는 다른 토글과 달리 시맨틱이 없어, 스크린리더 사용자는 토글의 현재 상태를 알 수 없다.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/orders/QuickMatchPanel.vue` | ① scoped 블록의 `.qmp-quick`·`.qmp-quick__title`·`.qmp-quick__row`·`.qmp-quick__row::-webkit-scrollbar`·`.qmp-quick__btn`·`.qmp-quick__btn b`·`.qmp-quick__btn span`·`.qmp-quick__btn:hover:not(:disabled)`·`.qmp-quick__btn:disabled` (9개 셀렉터)를 **`qmp-preset*` 로 개명**(L912-L959) — 패널 "원큐 설정" 블록과 모달의 `.qmp-quick` 을 분리해 scoped 규칙이 모달로 새지 않게 함(이유 주석 2줄) |
| `components/orders/QuickMatchPanel.vue` | ② 템플릿의 원큐 설정 블록 클래스 4곳을 `qmp-preset`/`qmp-preset__title`/`qmp-preset__row`/`qmp-preset__btn` 로 개명(L545~L552, 이유 주석 1줄) |
| `components/orders/QuickMatchPanel.vue` | ③ `.qmp-switch` 에 `role="switch"` + `:aria-checked="matchEnabled"` 추가(L533-534) — 켜짐/꺼짐을 보조기술에 고지 |

- 공통: **클래스 이름·ARIA 속성 변경만** 수행(`<script>` 로직·props·emit·import·표시 텍스트·DOM 구조 무변경). 이미 정의돼 있던 스타일 규칙의 **셀렉터 이름만 바꿔 재사용**하므로 새 스타일 정의 0건.
- 파일당 편집은 **순차** 로 진행(동일 파일 병렬 편집 시 선행 수정이 원복되는 기존 교훈 준수) 후, 그렙으로 **잔존 확인** — `qmp-quick__title`·`qmp-quick__row`·`qmp-quick__btn` **0건**, scoped `.qmp-quick {` **0건**, `qmp-preset` 계열 9곳 + 템플릿 4곳, 전역 `.qmp-quick` 는 모달용 1곳([L1047](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1047))만 유지.
- 적용 후 확인(빌드 산출물): scoped `.qmp-quick[data-v-…]` **0건**(모달 누수 소멸), `.qmp-preset[data-v-43b5c2fb]{border-top…}` 신설, 전역 `.qmp-quick{flex-direction:column;gap:18px;display:flex}` 1건 유지(모달 전용).

## (e) 적용하지 않은 항목과 이유 (후보)
1. **iOS Safari 입력 확대(input zoom) 미대응(신규 관찰·후보)**: 앱은 모바일 PWA 인데 본문 폰트가 11~12px 이고 입력 요소도 같은 크기라([ChatThread](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatThread.vue#L346)·[ChatRequestSheet](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatRequestSheet.vue#L327-L337)·[SettlementView](file:///d:/wwwroot/nowhere/frontend/src/views/SettlementView.vue#L187-L189) `.settle-input`), **iOS Safari 는 16px 미만 입력에 포커스하면 화면을 자동 확대**해 레이아웃이 흔들리고 입력 후에도 축소되지 않는 경우가 있다. 표준 대응은 모바일에서 입력 폰트를 16px 로 올리는 것이지만 **앱 전체 인풋 크기가 눈에 띄게 커지는 시각 변경**이라 이번 "작은 교정" 범위를 넘는다 → 후보(터치 기기 사용자 보고가 있으면 우선 처리 권장).
2. **`role="switch"` 미부여 커스텀 토글 잔여(1번 축 검증 결과 없음)**: `frontend/src` 의 토글 성격 컨트롤은 `.qmp-switch` 1곳뿐이었고 이를 교정했으므로 **잔여 0건**(테마·알림 토글은 `n-switch` 로 시맨틱 내장) — 기록만 남김.
3. **`.qmp-quick`(모달) 전역 블록의 존재 자체(신규 관찰·후보)**: scoped 가 텔레포트 노드에도 적용되므로, 모달용 스타일을 전역 블록으로 둘 필요는 원래 없다(전역이 다른 화면에까지 새는 위험). 다만 지금은 모달 전용 클래스가 하나뿐이고 전역 규칙도 1건이라, **전역 블록을 scoped 로 흡수하는 리팩터링**은 범위가 커서 후보로만 기록(이번 회차는 패널 쪽만 분리해 상호 오염을 제거).
4. **`NotificationsView` KIND 의 `tagCls` 미사용(신규 관찰·후보)**: [KIND](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L42-L55) 11종이 `tagCls` 를 갖고 있으나 템플릿은 `tag`(라벨)+[TAG_VARIANT](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L85-L97)(칩 색)만 쓴다 — **죽은 데이터**다. UI 표시에 영향이 0인 순수 정리라 이번 회차는 후보(제거 시 표시 불변).
5. **href 없는 `<a>` 행(우선처리 카드·UiListRow 4곳)**: 접근성상 버튼/링크가 맞지만 카드 내부에 별도 버튼이 있어 중첩 인터랙티브가 되고 Enter 버블링 문제가 재현된다 → 09-10·09-11 여러 회차 분류 유지(구조 변경).
6. **`role="button"` 카드의 Space 키 미지원(3곳)**: [HomeView](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L686-L693)·[OrderCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L302-L309)·[SetGroupCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L77) 는 `@keydown.enter` 만 처리 — WAI-ARIA button 패턴은 Space 도 요구하나 스크롤·버블링 기존 구조 문제와 같은 뿌리 → 후보.
7. **기존 반복 후보(상태 재확인, 유지)**: FAB 3곳(`var(--brand)`+흰 글리프) · `--accent` 아바타 흰 이니셜(09-07 "임의 수정 금지") · `:focus-visible` 미정의/`outline: none` · UiChip 하드코딩 팔레트(칩 규격 4px·11px/700 은 배지 표준 대상 아님) · 알림 화면 카운트 배지 radius 혼재(9px/7px/999px) · 관리자 상태 칩 굵기 잔여분 · base.css 레거시 `.btn`/`.card`/`.input` 죽은 CSS · `data/levels.js` "드라이버" 타이틀 · PWA 매니페스트 용어("드라이버 오더 마켓") · `theme-color`/매니페스트 색 테마 연동 · `aria-live` 도입 · `user-scalable=no` · `transition: all` · 소수점 폰트(11.5px 등) · `border-radius: 14px` 하드코딩 · `@media` 브레이크포인트 토큰화 · 알림 본문 `word-break: break-all` · `formatWon` 명칭 혼재 · coin+`원` 이중 표기 · MoreView 정산/처리할 일 아이콘 중복 · "내 마켓" 아이콘 표기 불일치 · SettingsVehiclesView 로딩 스켈레톤 부재 · 관리자 모달 `60vh`/`50vh` · MarketView `n-drawer` 80vh · HeaderBar 라우트 표 데드 엔트리(`my-orders`) — 각각 전역 디자인 결정 또는 순수 정리 작업으로 09-03~09-11 여러 회차에서 분류된 항목 → 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 8.70s`, 오류·경고 0건). 청크는 종전과 동일하게 생성(`MarketView` 42.03 kB · `HomeView` 16.67 kB · `index` 272.66 kB 등).
- **`vitest run` 통과** — `Test Files 13 passed (13)`, `Tests 73 passed (73)`. **`npm run check:refs` OK**("모든 템플릿 참조가 정의되어 있습니다").
- import 누락 점검: 변경이 **클래스 이름·ARIA 속성뿐**이라 import·심볼·컴포넌트 등록 영향 **0건**. `role`/`aria-checked` 는 표준 ARIA 속성이고 `matchEnabled` 는 이미 스크립트에 존재하는 반응형 값이다.
- 빌드 산출물 검증(`dist/assets/MarketView-BvTQmr44.css`): scoped `.qmp-quick[data-v-…]` **0건**(모달 누수 소멸), `.qmp-preset[data-v-43b5c2fb]{border-top:1px solid var(--border);margin-top:12px;padding-top:12px}` 신설, 전역 `.qmp-quick{flex-direction:column;gap:18px;display:flex}` **1건 유지**(모달 전용).
- 런타임 위험 점검: ① 패널 "원큐 설정" 블록은 이제 **scoped `.qmp-preset` 규칙만** 받아 제목↔프리셋 간격이 원래 의도(≈19px)로 돌아오고, 모달 본문은 **전역 `.qmp-quick`(flex·gap 18px)만** 받아 제목 아래 불필요한 구분선·여백이 사라진다. ② 두 요소는 서로 다른 클래스가 되어 **상호 오염 경로가 소멸**한다. ③ `role="switch"`+`aria-checked` 는 시각 표현에 영향이 없고(모양은 기존 CSS 그대로) 보조기술 고지만 추가된다. 레이아웃(간격 교정분 제외)·색·모션 변화 0.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
