# UI 개선 보고서 — 2026-09-05 19:04

## 실행 결과 요약
- **변경 없음** — 18:02(50회차) 이후 `frontend/src` 변경 0건(mtime 스캔·git 상태 확인)으로 신규 기능 코드가 없어 전수 열람 대상이 없음. 오늘 회차 중 아직 심층 점검되지 않은 **채팅(운행 업무센터) 화면 전체**를 신선 시점 점검(ChatView·ChatList·ChatThread·MessageBubble·ChatRequestEvent·ChatRequestSheet·ChatImageSheet 템플릿+scoped CSS)했으나 **명확한 소형 결함을 발견하지 못해 수정 없음**. 후보로 고려한 '운행' 칩 2건(ChatList 목록·ChatThread 상단 카드)은 근거 확인 후 유지 판정(아래 e-1). 직전 회차(18:02) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 19:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 18:02 이후 `frontend/src` 변경 0건 (`Get-ChildItem LastWriteTime > 18:02` 결과 없음, git status는 파일 재구성 이력성 변경뿐으로 신규 작업분 없음)
- **직접 열람(신선 시점 점검 — 채팅 기능 전수)**: `views/ChatView.vue`(전체 — 목록/스레드 전환·빈 상태 문구) · `components/chat/ChatList.vue`(전체 — 분류 칩 오늘/어제/지난주·타입 칩 색 매핑·'운행' 칩·경로 줄) · `components/chat/ChatThread.vue`(script L1-167 + 템플릿 L169-297 — 운행 정보 카드·신고·요청/이미지 버튼·컴포저·새 메시지 배지) · `components/chat/MessageBubble.vue`(전체 — 말풍선 그룹·이미지·삭제 롱프레스) · `components/chat/ChatRequestEvent.vue`(전체 — 요청 카드 수락/거절·결과 상태) · `components/chat/ChatRequestSheet.vue`(전체 — 요청 메뉴 6종·폼) · `components/chat/ChatImageSheet.vue`(전체 — 사진/보관함 탭·업로드·드래그 정렬)
- **서버측 교차 확인**: `app/Http/Controllers/Api/ChatController.php`(index/store) · `app/Services/Chat/ChatService.php` `conversations()`(L39-88 — `order` 직렬화에 `order_id` nullable → 운행 미연동 일반 대화 존재 가능 확인)
- **전역 감사(grep)**: `font-size: 7/8/9px` 0건 / `formatWon(...)원`·`원원` 단위 중복 0건 / `주문번호|오더|order_no` 노출 0건 / UI "드라이버" 잔존 — 레벨 타이틀(`data/levels.js`, 기존 반복 후보)뿐, 역할 라벨·다이얼로그·메시지 잔존 0건
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1802.md`(50회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
- **신규 없음**: 이번 시간대 신규 기능 코드 0건 + 채팅 기능 신선 점검에서도 명확한 소형 결함 미발견.
- 점검 결과 정합 확인 사항:
  1. **타입 칩 색 체계 정합**: ChatList·ChatRequestEvent의 타입 칩(승인 요청=민트 계열, 시간/경로/요금 변경=옐로우, 취소/거절=레드)과 요청 카드 라벨 색은 알림센터 의미색 체계와 대응 — 다만 "승인 요청(pending) 민트"는 09-02 22:05부터 '보는 사람 관점 의미 검증 필요' 반복 후보(디자인 결정)로 기존 분류 유지.
  2. **'운행' 칩 2건은 정보성 유지 판정**: ChatList의 `.chat-list__order-chip`(운행)과 ChatThread 상단 `.chat-order-card__tag`(운행)는 48회차에서 제거한 '공지' 배지(탭 컨텍스트가 전건 동일 라벨)와 달리, **ChatService `conversations()`가 `order_id` nullable 일반 대화도 목록에 포함**해 운행 연동 대화 구분 표식으로 정보 가치가 있음 → 제거하지 않음(아래 e-1). 칩 스펙(1px 6px·10px·400)도 09-02 수정분 유지 상태.
  3. **채팅 화면 Primary 액션·구성 원칙**: 스레드 컴포저(입력창 + Primary 보내기 1개), 요청 바텀시트(전송 버튼 1개), 이미지 시트(전송 1개 + 보조 버튼 분리) — '한 화면 Primary 버튼 하나' 준수. 요청 메뉴 6종은 유형(승인/시간/픽업 위치/목적지/요금/취소) 분리로 '필요한 선택지' 제공, 취소만 레드 강조.
  4. **말풍선·카드 대비 처리 정합**: 내 말풍선(민트/틸 brand 채움)과 요청/이미지 전송 Primary 버튼 모두 밝은 채움 위 `#07120e` 다크 텍스트 표준 적용, 거절/삭제는 danger 흰 텍스트 — v8 대비 규칙 일관. 상태 배지·라벨(1px 6px·10px·400) 전부 배지 스펙 준수, 빈 상태·로딩 스켈레톤 문구도 한국어 표준.
  5. **전역 캐너리 통과**: 단위 중복·10px 미만 폰트·기술 식별자 노출·UI "드라이버" 잔존(레벨 타이틀 제외) 모두 0건.

## (d) 실제 적용한 수정
- 없음 (변경 없음).

## (e) 적용하지 않은 항목과 이유 (후보)
- **ChatList '운행' 칩 · ChatThread 운행 카드 '운행' 태그**: 48회차 '공지' 배지 제거(전건 동일·탭이 결정적)와 달리, 이 칩은 운행 연동 대화와 `order_id` null 일반 대화(현재 진입점은 운행 발화뿐이나 API·모델상 가능)를 구분하는 정보 표식. 목록 전체가 운행 대화뿐인 현재 시점엔 반복 장식처럼 보일 수 있으나, 일반 1:1 채팅 도입 시 필요한 구분자라 제거하지 않고 유지(제거 여부는 일반 채팅 도입 시 재검토 후보).
- **채팅 목록 분류 라벨 미세 불일치(탭 '지난주' 내 '이번 주' 섹션 헤더 가능)**: 이번 주 중 2일 전 대화가 '지난주' 탭에 들어가면서 내부에 '이번 주' 헤더로 표시될 수 있음 — 실사용 충돌이 크지 않고, 탭 라벨 변경은 용어 정책(복사) 결정 사항(후보).
- **레벨 타이틀 "드라이버"(data/levels.js ↔ LevelTable.php)**: 사용자 노출·백엔드 동기 값 — 교차 변경 필요(기존 반복 후보 유지).
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, 홈 추천 조건% 미노출, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마, v-chip/verify-chip 패딩 차이, 문의 placeholder 문구 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 없음에도 현재 작업 트리 정합성 확인차 `npm run build`(frontend, vite v8.2.1) 실행 — **통과**, 4362 modules, built in 933ms. ChatView 청크 정상 생성.
- import 누락 점검: 코드 수정 없음 → 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
