# UI 개선 보고서 — 2026-09-10 22:01

## 실행 결과 요약
- **코드 수정 1건(12개 규칙 / 11개 파일)** — 이번 회차는 `frontend/src` 의 **채움(fill) 위 글자·아이콘 색 계약**을 전수 감사해, **`--brand-gradient` 채움 위 흰 글자/아이콘이 남아 있는 12곳이 다크 모드에서 대비 ≈1.6:1로 사실상 보이지 않는다**는 실제 결함을 찾아 앱 표준(어두운 글자 `#07120e`)으로 교정했다.
  1. **`--brand-gradient` 위 흰 글자·아이콘 12곳 다크 대비 교정(신규)**: [LoginView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/auth/LoginView.vue#L170) `.login-mark` · [RegisterView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/auth/RegisterView.vue#L208) `.login-mark` · [ForgotPasswordView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/auth/ForgotPasswordView.vue#L205) `.reset-mark` · [LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L181) `.lp-mark` · [SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue#L194) `.sp-mark` · [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L415) `.profile-hero__avatar` · [동일 파일](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L744) `.community-entry__icon` · [CommunityView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L764) `.feed-avatar` · [CommunityPostView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityPostView.vue#L331) `.feed-avatar` · [UserPageView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/UserPageView.vue#L337) `.user-hero__avatar` · [SetGroupCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L209) `.set-card__avatar` · [SettingsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsView.vue#L149) `.settings-card__icon`. 다크 모드에서 `--brand-gradient` 는 **밝은 틸(#63e2b7 → #3ca97e)** 로 바뀌는데 이 위에 흰 글자/아이콘이 남아 **대비 1.61:1(밝은 쪽)~2.93:1(어두운 쪽)** 이었다 — 로고 "N"·아바타 이니셜·커뮤니티 진입 아이콘이 배경에 묻힌다. 앱은 이미 `#07120e` 를 **brand 채움 위 표준**으로 30여 곳에 적용하고 있어([CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L106-L110)·[ReviewsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ReviewsView.vue#L269-L273)·[MessageBubble.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/MessageBubble.vue#L183-L184) 등) 이 12곳만 표준에서 빠져 있었다. 같은 패턴(**다크 전용 override**)으로 교정 → **다크 대비 11.9:1 / 6.5:1** 로 회복.
- 실행 전 상태 점검: HEAD `f240340`, `frontend/src` 최신 mtime = **21:53:21** → 직전 회차(21:01 보고서, 최신 mtime 20:07:59 기록) **이후 21:51~21:53에 OrderCard 공유 버튼·`icons` `share`·MyMarketView 연동이 추가**돼 있었다(본 회차 작업 아님 — 검토만 수행, 재적용 없음).
- 전역 표준 스캔 신규 위반 0건: "오더"/주문번호 0건, 단일 자릿수 font-size 0건, `!important` 0건, `target="_blank"` rel 누락 0건, `id=`/`for=` 0건(연결 오류 불가).
- `npm run build` 통과(`✓ built in 999ms`, 오류·경고 0건). CSS 규칙·주석 추가만이라 import·DOM·스크립트 무변경.
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 22:01~22:12 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_2101.md`(적용: `<n-input>` 네이티브 속성 `input-props` 이관 10건 + `inputmode` 4건) · `2026-09-10_2001.md`(적용: 커뮤니티 댓글 시간 `!important` 2건 제거) · `2026-09-10_1901.md`(적용: 반복 펄스 모션 감소 4곳·셋트 임박 배지 그림자 토큰화) — **반복 적용 없음**(이번 축은 "채움 위 글자색 계약"으로 역대 보고서에 전수 감사 기록이 없다).
- **실행 전 상태 점검**: `git log --oneline -3`(HEAD `f240340`) + `git status --short` + `Get-ChildItem frontend/src -Recurse | Sort LastWriteTime -Descending | Select -First 15` → 최신 21:53:21(OrderCard)·21:52:30(icons.test)·21:52:11(MyMarketView)·21:51:17(icons). 직전 회차 이후 신규 편집이 있었으므로 **해당 파일은 검토 대상**으로 삼고 수정은 가하지 않았다.
- **채움(fill) 사용처 전수 1 — `--brand-gradient`**: `background: var(--brand-gradient)` 그렙 → 정의 2건([base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L17)·[동일 파일](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L48), 라이트 `#36adff→#2f54eb` / 다크 `#63e2b7→#3ca97e`) + 사용 13건. 이 중 글자/아이콘이 올라가는 **12곳**을 개별 판독(모두 `color: #ffffff`, 다크 override 0건) — 위 (c)-1 목록. 나머지 1곳은 글자 없는 진행바([ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L638) `.level-bar__fill`)로 대상 아님.
- **채움(fill) 사용처 전수 2 — 기존 표준 대조군(`#07120e`)**: `#07120e` 그렙 → **앱 전체 30여 곳**이 이미 "밝은 brand 채움 위 어두운 글자" 표준을 적용 중임을 확인([MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1513-L1526) 배지 · [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue#L228-L235) · [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L505-L508) · [RideHistoryView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/RideHistoryView.vue#L779-L780) · [OrderCreateView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue#L1151-L1152) · [MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L1053) · [QuickMatchPanel.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1027-L1028) · [ChatImageSheet.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatImageSheet.vue#L938-L942) · [ChatList.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatList.vue#L242-L243) · [ChatThread.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatThread.vue#L349-L350) · [AdminView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/AdminView.vue#L3018-L3028) 등).
- **다크 전용 교정 선례 판독(교정 방식 결정 근거)**: [CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L106-L110) · [ReviewsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ReviewsView.vue#L269-L273) — 주석에 **"라이트는 어두운 채움이라 흰 글자 유지, 다크만 채움 위 어두운 글자(#07120e) 표준 적용"** 이 명시돼 있고, [SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue#L349-L353) 도 동일 취지를 주석으로 남기고 있다. 즉 이번 교정은 **새 규약 도입이 아니라 기존 규약의 미적용 잔여분 정렬**이다.
- **`--accent`·`--brand` 채움 잔여 후보 판독**: `html.dark .market-fab`([MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1483-L1485))은 다크에서 **그림자만** 조정하고 글리프는 흰색 유지, [CommunityView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L1100) `.community-fab`·[MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L722) `.my-market-fab` 도 동일(FAB 3곳) → 기존 반복 후보 유지.
- **신규 공유 기능 검토(수정 없음)**: [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L214-L224)(`showShare` = `shareable && !selectable && isPublished && order.userId === auth.user?.id`) · [동일 파일](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L386-L415)(`.order-card__actions` 묶음·`aria-label="공유"`·`@click.stop.prevent`) · [MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L19-L22)(`useOrderShare` 연동) · [useOrderShare.js](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderShare.js#L18-L56)(`share(orderId)` 시그니처 일치·`navigator.share`→클립보드→링크 표시 폴백) · [icons.js](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L59)(`ShareSocialOutline` 등록) — **시그니처·이벤트명·아이콘명·접근성 이름 모두 정합, 결함 없음**.
- **분류 계약 전수 대조(위반 0)**: `target="_blank"` 3곳 전부 `rel="noopener…"` 보유([CommunityView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L301-L306) · [CommunityPostView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityPostView.vue#L185-L190) · [OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L714-L716)) · `id="…"`/`for="…"` **0건**(라벨 연결 깨질 여지 없음) · `aria-label` 63건 유지.
- **ARIA 탭 계약**: [HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L744-L758) · [ChatList.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatList.vue#L168-L182) 가 `role="tablist"`+`role="tab"`+`aria-selected` 를 쓰지만 화살표 키 내비게이션·`aria-controls` 는 없다 → 후보.
- **타이포 스케일 계약**: `font-size` 소수 px 그렙 → **21곳**(`11.5px` 14 · `10.5px` 6 · `12.5px` 1). 예: [EmptyState.vue](file:///d:/wwwroot/nowhere/frontend/src/components/common/EmptyState.vue#L57) `.empty-state--compact .empty-state__hint` · [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L590) · [QuickMatchPanel.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1139) → 후보.
- **기타 전역 그렙**: `!important` **0건**(20:01 교정 유지) · `word-break` 28건(대부분 `break-word`, `break-all` 2건 — [base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L366) · [AdminView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/AdminView.vue#L3933)) · `:focus-visible`/`outline: none` 16건(11파일) · `loading="lazy"` 14건 · `font-variant-numeric: tabular-nums` 1건([OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L2301)).
- **부수 검토**: [stores/theme.js](file:///d:/wwwroot/nowhere/frontend/src/stores/theme.js#L6-L18)(OS 설정 `prefers-color-scheme` 미반영) · [SettingsAppearanceView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsAppearanceView.vue#L26-L34) · [EmptyState.vue](file:///d:/wwwroot/nowhere/frontend/src/components/common/EmptyState.vue) · [UiChip.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiChip.vue)(`4px 7px`/11px/700 — 칩 규격 후보) · [index.html](file:///d:/wwwroot/nowhere/frontend/index.html#L23-L36)(`<title>` "골라드립니다" vs `og:title` "골라드려요" 문체 혼재, `og:image` 상대 경로).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀·의미색), 프로젝트 배지 표준(1px 6px / 10px / 400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출).

## (c) 발견한 개선 포인트
1. **다크 모드에서 브랜드 마크·아바타의 흰 글자/아이콘이 배경에 묻힘(신규 발견·적용)**: `--brand-gradient` 는 라이트에서 파랑(`#36adff→#2f54eb`), **다크에서 밝은 틸(`#63e2b7→#3ca97e`)로 반전**된다. 그런데 로그인·가입·비밀번호 재설정 마크, 랜딩/공유 화면 로고, 프로필 아바타, 커뮤니티 피드 아바타, 셋트 카드 아바타, 설정 아이콘 등 **12곳이 `color: #ffffff` 를 그대로 유지**해, 다크 모드에서 흰 글자가 밝은 틸 위에 얹혀 **대비 1.61:1**(밝은 끝)·**2.93:1**(어두운 끝)이 된다. 흰 글자 기준 최소 가독선(4.5:1)은 물론 3:1에도 못 미쳐 **로고 "N"과 아바타 이니셜이 사실상 사라져 보인다**. 값이 아니라 **다크 전환 시 채움과 글자가 함께 뒤집히지 않은 문제**라 눈으로는 "원래 그런가 보다" 넘어가기 쉽다.
2. **동일 증상이 앱 표준으로 이미 확립돼 있음(신규 교차 확인)**: 저장소에는 `#07120e` 가 **"밝은 brand 채움 위 어두운 글자"** 표준으로 30여 곳에 적용돼 있고, 주석에도 그 취지가 명시돼 있다([CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L106-L110) "라이트는 어두운 채움이라 흰 글자 유지, 다크만 채움 위 어두운 글자 표준 적용", [SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue#L349-L353) "앱의 다른 Primary CTA와 동일하게 어두운 글자로 예외 처리"). 즉 이번 12곳은 **새 디자인 결정이 아니라 표준 미적용 잔여분**이며, 같은 방식(다크 전용 `color: #07120e`)으로 맞추면 다크 대비가 **11.9:1 / 6.5:1** 로 회복된다.
3. **라이트 모드의 그라디언트 위 흰 글자도 4.5:1 미달(신규 계수·후보)**: 라이트 그라디언트 밝은 끝 `#36adff` 위 흰 글자는 **2.45:1**(표준 `#07120e` 를 쓰면 7.79:1). 다만 라이트는 "파란 그라디언트 + 흰 로고"가 브랜드 룩으로 자리 잡았고 로고타입은 WCAG 대비 예외 대상이라, 이번 회차는 **다크 전용**으로만 교정하고 라이트 2모드 통일은 후보로 남긴다(아바타 이니셜까지 포함하면 2모드 적용이 옳을 수 있음 — 소유자 결정 필요).
4. **FAB 3곳 `--brand` 채움 + 흰 아이콘(기존 후보 재확인·수치 정밀화)**: [MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1462-L1485) `.market-fab` · [CommunityView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L1100) `.community-fab` · [MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L722) `.my-market-fab` 는 다크(#63e2b7)에서 **1.61:1**, 라이트(#36adff)에서 **2.45:1** 로 두 모드 모두 기준 미달이다. 1·2번과 같은 뿌리지만 이 항목은 09-03 이후 여러 회차에 "전역 brand 위 글자색 설계 결정 건"으로 분류돼 있어 **분류 유지**(반복 미적용).
5. **소수점 폰트 스케일 21곳(신규 계수·후보)**: `11.5px`(14) · `10.5px`(6) · `12.5px`(1)가 섞여 있어 서브픽셀 렌더링 환경(D PR 1x)에서 11/12px 로 반올림돼 화면마다 다르게 보일 수 있다. 다만 11.5px 이 14곳에서 일관되게 쓰인 **의도된 미세 조정**으로 보여 일괄 정수화는 시각 변화가 커 후보.
6. **base.css 알림 본문 `word-break: break-all`(신규 관찰·후보)**: [base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L366) `.notify-item__message` 만 `break-all` 이라 영문·숫자 단어가 중간에서 끊긴다(같은 파일 다른 곳은 `break-word`). 다만 한글은 음절 단위 줄바꿈이 자연스러워 실사용 영향이 작고, 알림 패널 폭(280px)에서의 동작을 눈으로 봐야 해 후보.
7. **테마가 OS 설정을 따르지 않음(신규 관찰·후보)**: [theme.js](file:///d:/wwwroot/nowhere/frontend/src/stores/theme.js#L6-L9) 는 `localStorage.theme === 'dark'` 만 보므로, 기기를 다크로 쓰는 첫 방문자도 항상 라이트로 시작한다(설정 화면 카피도 "어두운 화면으로 전환합니다" 토글 단일). `prefers-color-scheme` 연동은 **동작·카피 변경**이라 후보.
8. **문서 메타 문체 혼재(신규 관찰·후보)**: [index.html](file:///d:/wwwroot/nowhere/frontend/index.html#L24-L36) 의 `<title>`·`description` 은 "골라드립니다"(합니다체), `og:title`·`twitter:title` 은 "골라드려요"(해요체)로 갈린다. 카피 문체 결정 사안이고 `frontend/src` 밖이라 후보.
9. **탭 ARIA 부분 구현(신규 관찰·후보)**: [HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L744-L758)·[ChatList.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatList.vue#L168-L182) 는 `role="tab"` 을 선언했지만 화살표 키 이동·`tabindex` 로빙·`aria-controls` 가 없어 ARIA 탭 패턴을 절반만 따른다(스크린리더는 탭으로 안내하지만 키보드 동작은 일반 버튼). 역할을 낮추거나(`aria-pressed` 토글 버튼) 키보드 계약을 채우는 두 방향 모두 **구조 변경**이라 후보.
10. **기존 반복 후보(상태 재확인, 유지)**: 하단 네비·ChatList `n-badge` 색/크기 통일 · `semantic.badgeRed` 사문화 · 커뮤니티 피드 카드 키보드 접근 · 알림 화면 카운트 배지 radius 혼재(9px/7px/999px) · 카피 문체 혼재 · `transition: all` 1곳 · `--accent` 아바타 흰 글자 · UiChip 하드코딩 6색 팔레트(`4px 7px`·11px·700) · 커뮤니티 카테고리 색 체계 · "내 마켓" 아이콘 표기 불일치 · MoreView 정산/처리할 일 아이콘 중복 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · coin+`원` 이중 표기 · 관리자 화면 상태 칩 규격 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · HeaderBar 라우트 표 데드 엔트리 · 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종 · 말줄임표/모달 푸터 문구 혼재 · `new Date(created_at_iso)` 방어 · SettingsVehiclesView 로딩 스켈레톤 부재 · HomeView 탭 카운트 불일치 · `role === 'Driver'` 리터럴 치환 · `:focus-visible` 미정의 다수 / `outline: none` 16곳 · 공유 크롤러 Blade 링크 저대비 · 스피너 모션 감소 미적용 · `border-radius: 14px` 하드코딩 15건 · `@media` 브레이크포인트 토큰화 · `tabular-nums` 잔여 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/auth/LoginView.vue` | `html.dark .login-mark { color: #07120e; }` 추가(L170) — 다크 틸 그라디언트 위 "N" 대비 회복 |
| `views/auth/RegisterView.vue` | `html.dark .login-mark` 동일 교정(L208) |
| `views/auth/ForgotPasswordView.vue` | `html.dark .reset-mark` 동일 교정(L205) |
| `views/LandingView.vue` | `html.dark .lp-mark` 동일 교정(L181) — 로고 마크(30px/20px) |
| `views/SharedOrderView.vue` | `html.dark .sp-mark` 동일 교정(L194) — 공유 랜딩 헤더·푸터 로고 |
| `views/ProfileView.vue` | `html.dark .profile-hero__avatar`(L415) · `html.dark .community-entry__icon`(L744) 2건 — 이니셜·흰 아이콘 글리프 |
| `views/community/CommunityView.vue` | `html.dark .feed-avatar`(L764) — 피드 아바타 이니셜(36px) |
| `views/community/CommunityPostView.vue` | `html.dark .feed-avatar`(L331) |
| `views/community/UserPageView.vue` | `html.dark .user-hero__avatar`(L337) — 프로필 히어로 아바타(72px) |
| `components/orders/SetGroupCard.vue` | `html.dark .set-card__avatar`(L209) |
| `views/settings/SettingsView.vue` | `html.dark .settings-card__icon`(L149) — 흰 아이콘 글리프 |

- 공통: **`<style scoped>` 에 다크 전용 `color` override 1개 + 이유 주석 1줄**만 추가. 기존 라이트 규칙(`color: #ffffff`)과 컴포넌트 구조·템플릿·스크립트·import 무변경 → **라이트 모드 렌더는 완전히 동일**하고 다크 모드만 개선된다.
- 파일당 단일 편집으로 원복 위험 없음(ProfileView 만 2건 → 순차 별도 편집). 수정 후 그렙으로 **12개 규칙 전부 잔존 확인**([LoginView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/auth/LoginView.vue#L170) … [SetGroupCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L209)), 교정 전 다크 override 존재 0건 → **교정 후 `--brand-gradient` 채움 위 흰 글자 잔여 0건**(글자 없는 `.level-bar__fill` 제외).
- 대비 수치(교정 전 → 후, WCAG 대비): 밝은 틸 `#63e2b7` 기준 **1.61:1 → 11.9:1**, 어두운 틸 `#3ca97e` 기준 **2.93:1 → 6.5:1**.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **라이트 모드 그라디언트 위 흰 글자(3번)**: 밝은 끝 `#36adff` 위 흰 글자 2.45:1(표준 적용 시 7.79:1)로 기준 미달이지만, "파란 그라디언트 + 흰 로고"가 브랜드 룩이고 로고타입은 WCAG 대비 예외다. 2모드 통일은 **브랜드 시각 변경**이라 소유자 결정이 필요 → 후보.
2. **FAB 3곳 흰 글리프(4번)**: 1번과 같은 뿌리지만 09-03 이후 "전역 brand 위 글자색 설계 결정 건"으로 반복 분류된 항목이라 분류 유지(반복 미적용).
3. **소수점 폰트 스케일 21곳(5번)**: 11.5px 이 14곳에서 일관 사용된 의도된 미세 조정으로 보이고, 일괄 정수화는 14개 파일에 걸친 시각 변화라 후보.
4. **`.notify-item__message` `word-break: break-all`(6번)**: 한글은 음절 줄바꿈이 자연스러워 실사용 영향이 작고, 알림 패널 폭에서의 시각 확인이 필요 → 후보.
5. **테마 `prefers-color-scheme` 연동(7번)**: 동작(첫 방문자 테마)과 설정 화면 카피를 함께 바꾸는 제품 결정 → 후보.
6. **`index.html` 메타 문체 혼재·`og:image` 상대 경로(8번)**: `frontend/src` 밖 파일이고 문체·절대 URL 정책 결정 사안 → 후보.
7. **탭 ARIA 완성(9번)**: `role="tab"` 을 유지하며 키보드 계약을 채우는 것도, 역할을 낮추는 것도 구조 변경 → 후보.
8. **`n-badge` 색/크기 통일 · `semantic.badgeRed` · 커뮤니티 카드 키보드 접근 · 알림 배지 radius · 카피 문체 · `transition: all` · `:focus-visible`/`outline: none` · base.css 레거시 `.btn`/`.card` · `tabular-nums` 잔여**: 각각 전역 디자인 결정 또는 순수 정리 작업 성격으로 09-03~09-10 여러 회차에서 분류된 항목 → 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 999ms`, 오류·경고 0건) — 수정 반영 후 실행. `LoginView`·`RegisterView`·`ForgotPasswordView`·`LandingView`·`SharedOrderView`·`ProfileView`·`CommunityView`·`CommunityPostView`·`UserPageView`·`SetGroupCard`·`SettingsView` 청크 정상 재생성.
- import 누락 점검: 변경이 **scoped CSS 규칙·주석 추가뿐**이라 import·심볼·컴포넌트 등록 영향 **0건**. 새 선택자는 기존 클래스 1개 + `html.dark` 조합이라 템플릿·DOM 변경이 없다(scoped 컴파일 시 `html.dark .x[data-v-…]` 로 확장되어 `html.dark` 클래스 토글과 정상 매칭 — 기존 30여 곳과 동일 방식).
- 빌드 산출물 검증: `dist/assets` 에서 교정 12개 규칙 전부 반영 확인 — [LoginView-CsEAjl-l.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/LoginView-CsEAjl-l.css) · [RegisterView-DrigzlWU.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/RegisterView-DrigzlWU.css) · [ForgotPasswordView-77ZtPkd1.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/ForgotPasswordView-77ZtPkd1.css) · [LandingView-muiOZQ5I.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/LandingView-muiOZQ5I.css) · [SharedOrderView-BgMTlC69.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/SharedOrderView-BgMTlC69.css) · [ProfileView-5CUX9bEa.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/ProfileView-5CUX9bEa.css)(2건) · [CommunityView-fnAqrwGt.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/CommunityView-fnAqrwGt.css) · [CommunityPostView-CxJJKvNX.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/CommunityPostView-CxJJKvNX.css) · [UserPageView-4sK3kO1u.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/UserPageView-4sK3kO1u.css) · [SetGroupCard-DUTdbE71.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/SetGroupCard-DUTdbE71.css) · [SettingsView-CPYuFKcD.css](file:///d:/wwwroot/nowhere/frontend/dist/assets/SettingsView-CPYuFKcD.css).
- 런타임 위험 점검: **다크 모드에서만 적용되는 색 변경**이라 라이트 사용자 화면은 1px도 바뀌지 않는다. 다크에서는 해당 마크 위 글자·아이콘만 어두워지고 배경·크기·여백·모션은 그대로여서 레이아웃 회귀 시나리오가 없다. CSS 명시도는 `html.dark`(0,1,1)+클래스(0,1,1)로 기존 규칙(0,1,0)을 정상적으로 이긴다.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
