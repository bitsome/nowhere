# UI 개선 보고서 — 2026-09-03 14:05

## (a) 실행 시각
- 2026-09-03 14:05 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰: `views/settings/SettingsView.vue`·`SettingsNotificationsView.vue`(전체 — 최근 미검토 화면), `views/community/CommunityView.vue`·`views/MarketView.vue`(활성 탭/필터 카운트 칩 영역), `views/auth/LoginView.vue`(`.login-submit` 크기 대조), `views/ProfileView.vue`·`MoreView.vue`·`ReviewsView.vue` 등 white-on-brand 잔존 분류
- 컴포넌트: `components/chat/MessageBubble.vue`(전체 — 내 말풍선 대비 신규 발견), `components/chat/ChatThread.vue`(새 메시지 점프 배지 신규 발견), `components/chat/ChatList.vue`(분류 칩 카운트), `components/orders/SetGroupCard.vue`(전체), `components/orders/OrderDetailChat.vue`(전체), `components/community/CommunityPostEditor.vue`(액션 영역), `components/common/LevelBadge.vue`·`EmptyState.vue`·`SegmentedGroup.vue`·`VerifiedBadge.vue`·`ScrollTopButton.vue`(전체 — 최근 미검토 공용 컴포넌트), `components/ui/UiChip.vue`·`UiListRow.vue`·`UiSection.vue`(전체)
- 전역 감사(grep): `background: var(--brand)` + `color:#fff/#ffffff` 전수(40곳 분류), chat 컴포넌트 white-on-brand 전수(MessageBubble·ChatThread·ChatList·ChatImageSheet), `font-weight:700/800` 80건 스캔(헤더/수치용 정상), UI 노출 "드라이버"/"오더" 잔존 재확인
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1301.md`·`1207.md`·`1103.md`(후보 전수 — 반복 적용 없음, 이번 회차 미적용 후보는 후보로만 유지)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first), `assets/base.css`(라이트 `--brand:#36adff`/다크 `--brand:#63e2b7` 확인)

## (c) 발견한 개선 포인트
1. **활성 카운트 칩 white-on-brand 3곳 (13:01 신규 후보 → 이번 회차 적용)**: 활성 탭/필터의 작은 개수 칩이 `background: var(--brand)`(라이트 파랑·다크 틸 모두 밝음) + 흰 글자(≈1.9:1 저대비). CommunityView `.community-tabs__item--active .community-tabs__count`(L596)·MarketView `.market-filters__badge`(L1210)·ChatList `.chat-chip--active .chat-chip__count`(L227). 직전 회차들(12:07~13:01)이 같은 코드베이스의 **brand 채움 + 흰 글자** 요소(ChatRequestEvent·ChatRequestSheet·HomeView claim 등)를 전부 `#07120e` 어두운 글자로 통일한 것과 동일 표준으로, 수치 칩 3곳도 같은 일관 처리로 해소.
2. **채팅 내 말풍선 `.cb-bubble--mine` white-on-brand (신규 발견·적용)**: `MessageBubble.vue` 내 말풍선이 `background: var(--brand); color:#fff` — 라이트 #36adff·다크 #63e2b7 모두 밝아 11px 본문 텍스트의 대비가 가장 약한 요소 중 하나. 앞서 고친 동일 계열 버튼(요청 승인 카드·요청 보내기 CTA)과 동일 표준(`#07120e`) 적용.
3. **ChatThread 새 메시지 점프 배지 `.chat-thread__jump` white-on-brand (신규 발견·적용)**: "새 메시지 도착" 알약(브랜드 채움 + 흰 글자 11px/700) — 동일 저대비. brand 채움 표준(`#07120e`) 적용.
4. **아바타 이니셜 --accent 위 흰 글자 잔존 (기존 후보 재확인)**: `MessageBubble.vue` `.cb-avatar`(L175)·`ChatList.vue` `.chat-list__avatar`(L244) — 라이트 `--accent`=검정(#1f1f1f)이라 정상, 다크 `--accent`=틸(#63e2b7)에서만 흰 글자 저대비. 원형 아바타 관용 패턴으로 전역 "brand 위 글자색" 결정 후 일괄 재평가(13:01 후보와 동일).
5. **ChatImageSheet 흰 아이콘 11곳**: 사진 위 삭제/선택 오버레이 아이콘 등 `color:#fff` — 이미지 위 흰 아이콘은 관용 패턴(사진 배경이 어두울 수 있어 흰색 유지가 오히려 대비 확보), 미적용.
6. **기타 재확인 결과 클린**: 배지 스펙(1px 6px/10px/400) 위반 0건, UI 노출 "드라이버"/"오더" 0건(코드 주석만), Settings·공용 컴포넌트(LevelBadge/VerifiedBadge/SegmentedGroup/ScrollTopButton/UiChip/UiListRow/UiSection) 한국어·placeholder 클린, 검증 배지·EmptyState·셋트 카드 배지 표준 준수 확인.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/chat/ChatList.vue` | `.chat-chip--active .chat-chip__count` 글자색 `#fff` → `#07120e` + 근거 주석 — 활성 분류 칩의 개수 칩(brand 채움) 저대비 해소 |
| `views/community/CommunityView.vue` | `.community-tabs__item--active .community-tabs__count` 글자색 `#ffffff` → `#07120e` — 활성 카테고리 탭 개수 칩 저대비 해소 |
| `views/MarketView.vue` | `.market-filters__badge` 글자색 `#ffffff` → `#07120e` + 주석 갱신 — 필터 개수 배지(brand 채움) 저대비 해소 |
| `components/chat/ChatThread.vue` | `.chat-thread__jump` 글자색 `#fff` → `#07120e` + 근거 주석 — 새 메시지 도착 알약 저대비 해소 |
| `components/chat/MessageBubble.vue` | `.cb-bubble--mine` 글자색 `#fff` → `#07120e` + 근거 주석 — 내 말풍선 본문 저대비 해소 |

전부 `<style scoped>` CSS 글자색 교체(템플릿·script·import 무변경) — 의미·레이아웃·상호작용 무변경. 값은 같은 코드베이스의 brand 채움 텍스트 표준(`#07120e`, 직전 회차 ChatRequestEvent·ChatRequestSheet·HomeView claim과 동일)을 따름.

## (e) 적용하지 않은 항목과 이유 (후보)
- **채팅 아바타·대화 목록 아바타(--accent 다크=틸 위 흰 이니셜)**: 원형 아바타 관용 패턴 — 13:01 후보 유지(전역 "brand 위 글자색" 설계 결정 후 일괄 재평가).
- **ChatImageSheet 흰 아이콘 11곳**: 사진 위 오버레이 아이콘 관용 패턴 — 미적용(신규 확인 항목으로 후보 기록).
- **ChatThread `.chat-order-card__status` 상태 알약 흰 글자(신규 확인)**: 인라인 상태색 배경별(초록·앰버·레드 등) 대비가 상태마다 달라 흰 글자 유지가 안전한 경우가 있어 상태색 팔레트와 함께 별도 검토 필요 — 후보 기록.
- **라이트 naive-ui primary 초록(#18a058) 전역 불일치·다크 primary 흰 글자·ProfileView naive 세그먼트 탭·ReviewsView 활성 탭 white-on-brand**: naive 테마 오버라이드 전역 설계 필요 — 기존 대형 후보 유지(12:07~13:01).
- **등록 FAB('＋')·brand-gradient 아이콘 박스 흰 아이콘(MoreView·SettingsView·CommunityView 등)**: 관용 패턴 — 기존 후보 유지.
- **HomeView 탭 카운트 불일치·`.login-submit` CTA 크기(11px/600)·SettingsVerificationView primary 2개·data/levels.js "드라이버"**: 기존 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 929ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: ChatList L228·CommunityView L598·MarketView L1220·ChatThread L304·MessageBubble L184 모두 `color: #07120e` 존재 — 5/5.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 5건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
