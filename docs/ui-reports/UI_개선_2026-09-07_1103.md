# UI 개선 보고서 — 2026-09-07 11:03

## 실행 결과 요약
- **수정 0건(변경 없음)** — 직전 실행(60회차, 09-07 10:06) 보고서 작성(10:07) 이후 `frontend/src` 변경은 **ChatView.vue(10:10:34)·App.vue(10:10:42) 2건뿐**이고, 이 2건의 델타는 채팅 '모두 읽음'·안 읽음 배지 후속(대화방을 벗어나면 닫고 채팅 탭 이탈 직후 목록·배지를 갱신 — 기능/동기화 로직)으로 **UI 결함은 발견되지 않음**. 신규 관찰 2건(App.vue 데드 CSS `.bottom-nav__item--button`·ChatView 미사용 `ref` import)은 렌더링 변화 없는 코드 정리라 기존 방침대로 후보에만 기록. 60회차 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-07 11:03 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 09-07 10:07(60회차 보고서 작성) 이후 `frontend/src` 변경 2건
  1. `views/ChatView.vue`(10:10:34) — '모두 읽음' 버튼 폴링·SSE 반영 경로 재확인
  2. `App.vue`(10:10:42) — 채팅 화면 이탈 시 대화방 닫기 + 목록·하단 채팅 배지 갱신 watch(기능 로직), 하단 네비/알림 배너 구조
- **직접 열람(전수)**: 위 2개 파일 전체(script+template+style), `stores/chats.js`(markAllRead·loadConversations·reloadMessages 배선), `api/chats.js`(read-all), `stores/notifications.js`(markAllRead 병행 관용 대조)
- **대조·감사(grep)**: `frontend/src` 신규 위반 스캔 — 7~9px `font-size`(0건), UI 노출 "주문번호/오더 번호/드라이버" 라벨(0건 — 잔존 '드라이버'는 주석·`data/levels.js` 레벨 타이틀, 기존 후보), `bottom-nav__item--button` 사용처(정의 1건뿐)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-07_1006.md`(60회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
10:10 델타(채팅 읽음 동기화 후속) 전수 점검 결과 — **수정할 명확·소형 결함 없음**. 확인된 정합 사항:
1. **App.vue 채팅 이탈 watch**: `route.name` 이탈 시 `chats.close()`(하단 네비 복원) → 직후 `loadConversations()`로 안 읽음 수를 즉시 반영 — 하단 채팅 배지가 옛 안 읽음 수를 남기던 문제를 화면 전환 시점에 해소하는 기능 로직(UI 결함 아님). 60회차 보고서가 적은 "대화를 읽고 탭을 빠져나갈 때 하단 배지에 옛 안 읽음 수가 남는다" 항목의 전역 해소책으로 판단 — UI 표준 위반 없음.
2. **하단 네비 채팅 배지 조건**: `chats.unreadTotal > 0 && route.name !== 'chat'` — 대화방(전체 화면) 및 채팅 목록에서는 배지 미노출, 다른 탭에서만 노출. 미니멀·정보 우선 원칙 위배 없음.
3. **ChatView.vue 재확인**: `.page-shell` 조건부(목록만 14px 여백)·폴링 3s·SSE 신호 경로·스켈레톤/빈 상태 모두 60회차 결론과 동일 상태. 신규 위반 없음.
4. **신규 관찰 — App.vue 데드 CSS `.bottom-nav__item--button`**(L340-346): 템플릿 사용 0건(더보기 항목은 router-link로 전환 완료) — 과거 `<button>` 잔재. 렌더링 영향 없음.
5. **신규 관찰 — ChatView.vue 미사용 import `ref`**(L2): script에 `ref()` 호출 0건 — 불필요 import 잔존.
6. 전역 스캔: 7~9px 폰트·기술 식별자 UI 노출 0건 재확인 — 기존 기준 유지.

## (d) 실제 적용한 수정
- **없음(변경 없음)** — 10:10 델타는 기능/동기화 로직으로 UI 결함 없음. 데드 CSS·미사용 import는 렌더링 변화 없는 코드 정리라 기존 방침대로 미적용(후보 기록).

## (e) 적용하지 않은 항목과 이유 (후보)
- **App.vue 데드 CSS `.bottom-nav__item--button`(L340-346)**: 템플릿 사용 0건 — 비노출 정리는 UI 개선 아님(기존 '데드 CSS' 분류, 신규 기록).
- **ChatView.vue 미사용 `ref` import(L2)**: script 내 `ref()` 호출 0건 — 렌더링 영향 없음(코드 정리, 신규 기록).
- **기존 반복 후보(61회차 기록분 유지)**: 채팅 목록 탭 선택 초기화·'지난주' 칩/그룹 라벨 단차·승인 요청 칩 색(그린 vs 확인 필요 옐로우)·HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출 · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구 · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) — 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) — **통과**, 4362 modules, built in 941ms. 10:10 수정분(App.vue watch·ChatView.vue)이 60회차 빌드(10:07) 이후 변경된 상태였으므로 이번 실행에서 재검증 — ChatView 청크(23.78 kB CSS·68.75 kB JS) 포함 모든 청크 정상 생성.
- import 누락 점검: 이번 실행 수정 0건. 10:10 변경 파일의 import를 점검 — App.vue: `vue`(computed/onBeforeUnmount/onMounted/ref/watch)·stores(auth/chats/driver/notifications/theme/ui)·`naiveThemeOverrides`·ChatListener/HeaderBar/NotificationListener/BaseIcon/ScrollTopButton·`connectEventStream` 전부 사용처 확인(누락 0건, 미사용 없음). ChatView.vue: `onBeforeUnmount/onMounted`·`useChatsStore`·ChatList/ChatThread/EmptyState 사용 확인 — 단 `ref`는 미사용(위 e-2 기록).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
