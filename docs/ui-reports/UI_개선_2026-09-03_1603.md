# UI 개선 보고서 — 2026-09-03 16:03

## (a) 실행 시각
- 2026-09-03 16:03 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 읽기): `views/settings/SettingsProfileView.vue`(전체 — 최근 심층 검토에서 빠진 화면), `views/orders/RideHistoryView.vue`(상태 배지 글자색 로직 `statusTextColor` L261-268·`.history-card__status` L660 대조)
- 컴포넌트: `components/chat/ChatThread.vue`(연결 운행 카드 상태 태그 — script computed·템플릿 인라인 스타일·scoped CSS), `components/orders/OrderCard.vue`(`.status-badge` 클래스 글자색 규칙 L530-551 대조), `components/orders/SetGroupCard.vue`(`.status-badge` 동일 규칙 대조)
- 유틸/CSS: `utils/colors.js`(`statusColorVar`), `assets/base.css`(`--status-*` 라이트/다크 토큰 대조)
- 전역 감사(grep): 상태 배지 인라인 배경(`:style` background + statusColorVar 계열) 전수 — ChatThread 1곳만 잔존 확인, UI 노출 "드라이버"/"오더"/"주문번호" 전수(코드 주석·`data/levels.js` 동기 데이터만), 영문 placeholder(0건 — ChatImageSheet 바인딩 표현식만)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1501.md`·`1405.md`·`1301.md`·`1207.md`·`1103.md`(적용 내역·후보 전수 — 반복 적용 없음. 14:05 보고서의 "ChatThread 상태 알약 흰 글자 — 별도 검토 필요 후보"가 이번 회차 적용 대상으로 판정됨)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **ChatThread 연결 운행 상태 태그 — 라이트 모드 흰 글자 on 밝은 상태색 (신규 판정·적용)**: 대화방 상단 연결 운행 카드의 상태 태그 `.chat-order-card__status`가 배경을 상태색(`statusColorVar`)으로 채우면서 라이트 글자색은 `#fff` 고정 + 다크만 `#101418`로 예외 처리된 상태였다. 라이트 모드에서 `--status-published`(#36adff 파랑)·`--status-trading`(#ffa940 앰버)·`--status-driving`(#13c2c2 시안)·`--status-completed`(#18a058 초록) 등 밝은 배경 위 흰 10px 글자는 대비 약 1.9~3.4:1로 저대비. 같은 코드베이스의 동일 용도 상태 배지(OrderCard·SetGroupCard `.status-badge--published/driving/trading/acceptance-pending` → `#101418`, RideHistoryView `statusTextColor`가 완료·요금협의·공개·운행중·승인대기에서 어두운 글자)는 이미 상태별 글자색 규칙을 갖고 있어 **화면 간 불일치** 상태였다. 14:05 보고서에서 "상태색 팔레트와 함께 별도 검토 필요" 후보로 남았으나, 형제 배지의 기존 표준(RideHistoryView `statusTextColor`와 동일 상태 목록)이 확립돼 있어 동일 규칙 적용이 일관된 소규모 수정으로 판정.
2. **SettingsProfileView 클린 (신규 확인)**: 프로필 수정 화면은 primary "저장" 1개(화면당 Primary 1개 원칙)·라벨/placeholder 한국어·`n-alert` 오류·성공 분리·n-card 1겹·불필요한 식별자 없음 — 수정 불필요.
3. **기타 재확인 클린**: 배지 스펙(1px 6px/10px/400) 위반 0건, UI 노출 "드라이버"/"오더"/주문번호 0건(코드 주석·백엔드 동기 `data/levels.js`만), 영문 placeholder 0건, 상태 배지 인라인 스타일 전수 결과 ChatThread 외 잔존 white-on-bright 0건(OrderCard·SetGroupCard는 클래스 규칙 보유).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/chat/ChatThread.vue` | 상태 태그 글자색 computed `orderStatusTextColor` 추가 — `published/trading/driving/completed/acceptance_pending`은 `#101418`, 나머지는 `#ffffff` (RideHistoryView `statusTextColor` L265-268과 동일 규칙·동일 상태 목록) |
| `components/chat/ChatThread.vue` | 상태 태그 인라인 스타일에 `color: orderStatusTextColor` 추가 — 라이트 밝은 상태색 위 흰 글자 저대비 해소, 다크 모드도 형제 화면과 동일하게 상태별 글자색 적용 |
| `components/chat/ChatThread.vue` | `.chat-order-card__status` CSS에서 고정 `color:#fff` 제거 + 다크 전용 `html.dark { color:#101418 }` 블록 제거 — 글자색은 상태 밝기에 따라 인라인 지정으로 일원화(주석 갱신) |

템플릿 인라인 스타일 1곳 + script computed 1곳(기존 `computed` import 사용, 신규 import 없음) + scoped CSS 정리. 의미·레이아웃·패딩·동작 무변경 — 글자색만 상태별로 RideHistoryView·OrderCard 표준과 동일하게 일원화.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ChatImageSheet 흰 아이콘 11곳**: 사진 위 오버레이 아이콘 관용 패턴 — 기존 후보 유지.
- **채팅 아바타·대화 목록 아바타(--accent 다크=틸 위 흰 이니셜)·FAB('＋')·brand-gradient 아이콘 박스·Login/Register 그라디언트 CTA**: 원형 아바타/브랜드 관용 — 전역 "brand 위 글자색" 설계 결정 후 일괄 재평가(기존 후보 유지).
- **라이트 naive-ui primary 초록(#18a058)·다크 primary 흰 글자 전역 불일치**: 전역 naive 테마 오버라이드 필요 — 대형 후보(12:07~15:01 유지).
- **SetGroupCard "N" 신규 배지·ProfileView naive 세그먼트 활성 탭·ReviewsView 활성 탭 white-on-brand**: naive/관용 패턴 — 기존 후보 유지.
- **HomeView 탭 카운트 불일치·`.login-submit` CTA 크기(11px/600)·SettingsVerificationView primary 2개·data/levels.js "드라이버"·OrderDetailView 평점 칩 등 콘텐츠 칩**: 기존 후보 유지(반복 미적용).
- **"운행시작·정산완료·운행중" 등 붙여쓰기 상태 키워드**: 상태값/스테퍼 라벨 관행 — 정상 분류(미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 922ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `orderStatusTextColor`가 ChatThread script(L36)·템플릿 인라인 스타일(L172)에 존재, `.chat-order-card__status` CSS에서 `color:#fff`·`html.dark …#101418` 제거 확인 — 상태 배지 인라인 배경 전수 중 white-on-bright 잔존 0건.
- import 누락 점검: 이번 변경은 script에 기존 import된 `computed`만 사용하는 computed 추가 + 템플릿 인라인 스타일 1곳 + scoped CSS 정리 — 신규 import 추가·제거 없음, 빌드 통과로 런타임 참조 검증 완료. **import 누락 없음.**
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
