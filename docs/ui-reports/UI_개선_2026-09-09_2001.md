# UI 개선 보고서 — 2026-09-09 20:01

## 실행 결과 요약
- **코드 수정 2건(2개 파일)** — 직전 실행(19:02) 이후 `frontend/src` 신규 변경은 없음(19:13 이후 mtime 0건, 상태 안정). 직전 보고서 적용 내역·후보와 겹치지 않는 신규 항목만 직접 수정했다.
- 신규 적용 2유형: ① DashboardView 동일 선택자 중복 정의 1건 제거(`.dash-columns` — 후반 블록이 전반 블록과 완전 동일한 중복, 제거해도 표시 불변) ② NotificationsView 템플릿 사용처 0건인 데드 CSS 제거(`.v8-noti`·`.v8-noti:last-child`·`.v8-noti:hover` — 과거 UiListRow 도입 전 잔재).
- 발견 근거: 파일별 동일 CSS 선택자 중복 전수 스캐너(node 스크립트)로 5개 파일 후보를 발굴하고, 각각 템플릿 사용처·선언 일치를 직접 확인해 명백한 건만 적용.
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 20:01~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_1902.md`·1809 — 적용 내역·반복 후보 전수 대조, 반복 적용 없음. 실행 전 `git status`·mtime 검사로 19:02 이후 소스 변경 0건 확인(타 세션 간섭 없음).
- **CSS 선택자 중복 전수 스캔(node 스크립트, `frontend/src` 전체 .vue)** — 동일 파일 내 동일 선택자 복수 정의 5개 후보 발굴: DashboardView(`.settle-item`·`.settle-item__label`·`.settle-item__value`·`.dash-columns`), ChatImageSheet(`.img-sheet__archive-delete-selected`), QuickMatchPanel·MarketView(`@media (hover: hover)` 복수 블록).
- **직접 판독**: `DashboardView.vue`(200~358 템플릿·569~778 CSS), `NotificationsView.vue`(전량 836행), `useOrderDetail.js`(195~234), `useOrderSchedule.js`(전량), `MyMarketView.vue`(25~54), `AdminView.vue`(650~709), `NotFoundView.vue`(전량).
- **전역 그렙**: `대기중` 계열(운행중·진행중 등 포함) · `오더/주문번호/order_id` · `'운행 중' vs '운행중'` 용례 · `font-size: 1~9px` 소형 폰트 · 보조용언 붙여쓰기(`~드릴게요/~드려요/~봐주세요/~주세요`) · `aria-label` 부착 현황 · 잔여 `v8-noti` 참조.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피 표준, 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙).

## (c) 발견한 개선 포인트
1. **DashboardView `.dash-columns` 동일 정의 중복(명백한 CSS 결함)**: [DashboardView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/DashboardView.vue) CSS 671행과 770행이 `display:grid / minmax(280px,1fr) / gap:16px`로 완전 동일. 후행 블록을 지워도 표시 불변 → **적용**(후행 블록 제거, 전반 블록 유지).
2. **NotificationsView `.v8-noti` 데드 CSS(명백한 잔재)**: [NotificationsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue) `.v8-noti`·`.v8-noti:last-child`·`.v8-noti:hover` 3블록이 템플릿 사용처 0건(그렙 확인, 목록 행은 UiListRow 사용). 과거 목록 직전 구현 잔재 → **적용**(제거).
3. **동일 선택자 복수 정의 4건(검토 후 보류)**: DashboardView `.settle-item`·`__label`·`__value`(576·729행)는 **의도된 재사용**(전반=정산 현황 그리드 카드용 column+패딩, 후반=건당 평균 목록 row용). 단 같은 scoped CSS에서 동일 클래스에 상충 선언이 섞여 cascade가 두 배치에 모두 적용되는 **잠재적 표시 결함 후보**로 확인됨(두 배치 모두 flex-direction:column·패딩·배경을 공유). 표시 결과 판단이 필요해 즉시 수정하지 않고 후보로 기록. ChatImageSheet `.img-sheet__archive-delete-selected`(3개로 분할된 부분 규칙)·QuickMatchPanel/MarketView `@media (hover:hover)` 복수 블록은 기능상 정상 분리로 판단(병합 이득 없음).
4. 신규 표준 위반(띄어쓰기·보조용언·기술 식별자·소형 폰트·용어) 0건 — 직전 보고서 상태 유지. `'정산 대기중'`(useOrderDetail.js:214·DashboardView.vue:245 등)은 기존 백엔드 캐노니컬 후보로 유지.

## (d) 실제 적용한 수정
1. `views/DashboardView.vue` — `.dash-columns` 후행 중복 정의 제거(기존 770~774행). 전반 정의(671행) 1건만 유지, 표시 불변.
2. `views/NotificationsView.vue` — 데드 CSS `.v8-noti`·`.v8-noti:last-child`·`.v8-noti:hover` 3블록 제거(기존 681~696행). 템플릿 사용처 0건이라 표시 불변.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **DashboardView `.settle-item` 클래스 상충 재사용(신규, 구조적)**: 정산 현황 그리드(column 박스)와 '건당 평균 매출' 리스트(row)가 같은 `.settle-item`을 쓰는데, 두 정의의 cascade가 전 요소에 섞여 적용됨. 의도된 두 배치인지·표시가 의도대로인지 화면 확인이 필요해 후보로 기록.
2. **ChatImageSheet `.img-sheet__archive-delete-selected` 3분할 규칙**(후보): 기본 블록(1001) + `flex:3`(1015) + 배경색(1021)이 흩어져 있으나 동일 선언 순서를 유지한 병합만 가능하고 기능 결함은 아님. 해당 파일은 미커밋 상태라 병합 이득이 낮아 보류.
3. **NotificationsView '처리할 일(운행 승인 · 제안 · 요청)로 이동' 문구**(불확실): 조사 '로'가 닫는 괄호 뒤에 붙은 비표준 표기. 전체 재문안은 문맥·내용량 판단이 필요해 후보로 기록(단독 오류 아님).
4. **기존 반복 후보(19:02·18:09 기록분 유지, 상태 재확인)**: '정산 대기중' 백엔드 캐노니컬 일괄 교정 · 승인 대기 상태 3중 반복+Primary 2개 · hero-badge/브랜드 위 글자색 전역 설계 · OrderCreateView 추가 데드 CSS(.create-search 등) · MyMarketView FAB 글리프 · RideHistoryView 집계 기준 · ChatThread 아이콘 버튼 aria-label · `.detail-driver-only` 다크 대비 · coin+"원" 이중 표기 · UiChip 기본 스타일 · 채팅 목록 탭 초기화 · '지난주' 칩 · 승인 요청 칩 색 · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 칩 색 · DashboardView 잔여 CSS(중복·데드) · 레벨 타이틀 · 홈 추천 조건% 미노출(의도) · auth CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · SupportView 문의 placeholder · MarketView 데드 CSS · 등록자 가입 직행 · `role === 'Driver'` 리터럴 · RegisterView 설명 문구 · AdminView.vue 잔여 전수 판독 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 997ms) — 수정 2개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 이번 수정은 전부 CSS 규칙 제거(문자열·심볼·import 영향 0건) → `npm run build`(템플릿·CSS 컴파일 포함) 통과로 검증 대체. 수정 후 그렙 재확인: `.dash-columns` 정의 1건·`v8-noti` 참조 0건, 제거된 블록 잔존 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분(2파일)과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
