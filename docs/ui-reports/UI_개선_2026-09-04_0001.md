# UI 개선 보고서 — 2026-09-04 00:01

## (a) 실행 시각
- 2026-09-04 00:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 심층 검토에서 빠진 화면**):
  - `views/orders/MyMarketView.vue`(전체 1094줄 — 내 마켓: 상태 탭(공개/진행중/정산/요청)·요청 받음/보냄·진행중 세분화(전체/예약/운행중)·일괄요청 그룹·휴지통(30분 자동 요청취소)·정산 일괄 CTA·검색 모달)
  - `views/ProfileView.vue`(전체 802줄 — 프로필 히어로·XP·요약/근무/활동 세그먼트 탭·실적 카드·차량·인증·바로가기·레벨·리뷰)
  - `views/community/CommunityPostView.vue`(전체 655줄 — 게시글 상세: 피드 카드·영상/숏츠·댓글·수정 모달)
  - `views/community/UserPageView.vue`(전체 691줄 — 공개 사용자 페이지: 히어로·평점·실적·탭(글/운행/리뷰))
  - `views/settings/SettingsVerificationView.vue`(전체 110줄 — 인증 상태·신청)
- 대조 파일: `components/layout/HeaderBar.vue`(`.hb-badge` L359-374 — 16px 원형 카운트 배지 10px/400 표준), `components/orders/OrderCard.vue`·`SetGroupCard.vue`(danger 배지 10px/400), `assets/base.css`(`.verify-badge`·`.verify-row__*` L705-768 — 공용 인증/상태 배지 정의 확인), `views/CommunityView.vue`·`HomeView.vue`·`MarketView.vue`(건수 칩/카운트 배지 규칙 대조)
- 전역 감사(grep): 10px 미만 폰트 전수(`font-size: 6/7/8/9px` — 0건), `min-width:16px`+`border-radius:999px` 원형 카운트 배지 전수(배지 스펙 대조), UI 용어 잔존("오더/주문번호" 0건, "드라이버"는 코드 식별자·AdminView "기사 관리" 등 올바른 한국어 문구만 — 신규 위반 없음)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_2303.md`·`2205.md`·`2104.md`·`2004.md` 등(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **MyMarketView 휴지통 건수 배지 font-weight 700 (신규 발견·적용)**: `.status-tabs__trash-count`(L841-854)는 요청 탭 우측 휴지통 아이콘 버튼 위 **`min-width:16px/height:16px/padding:0 4px`의 red(`--danger`) 원형 카운트 배지로, HeaderBar `.hb-badge`(L359-374)와 크기·모양·font-size(10px)가 완전히 동일한데 글자 두께만 700으로 달랐다**. `.hb-badge`는 09-02 0914 회차에서 배지 스펙대로 10px/400으로 정리된 표준이며, 배지 스펙(1px 6px·10px·400)과 형제 컴포넌트 일관성을 위해 400으로 맞췄다. (다른 danger 계열 원형 배지 — SetGroupCard `.set-card__new` 20px "N" 표시·OrderCard `.order-card__urgent` 10px/400 배지 — 는 각각 20px 원형 아이콘/이미 존재하는 400 배지라 이번 대상과 다름)
2. **MyMarketView 클린 (신규 전체 확인)**: EmptyState의 "+ 운행 등록"(유일 Primary)은 빈 목록에서만 노출되고 FAB는 `v-if="listTab !== '정산' && (orders.length || loading)"`로 빈 목록·정산 탭에서 숨겨져 **한 화면 Primary 1개 원칙 유지**. 상태 탭·하위 카테고리(받음/보냄, 전체/예약/운행중) 2단 구조는 이중 필터가 아닌 요청/진행중 탭의 방향·세분화 선택, `.claim-group__badge`(09-02 2301 적용)·`.order-card` 상태 배지·휴지통 카드 "요청취소" 오버라이드 전부 배지 스펙 준수. 한국어 문구·"운행" 용어 정상. 신규 위반 없음.
3. **ProfileView 클린 (신규 전체 확인)**: `.verify-badge`(차량 인증/면허 인증)는 base.css 공용 정의 사용(미정의 아님 — base.css L714-724 확인), XP 바 dark 쌍(11:03 적용) 유지, 실적 카드 틴트(rgba 0.12~0.15)는 장식용 미세 틴트로 대비 문제 없음, 세그먼트 탭·아바타는 기존 관용 분류(아래 e). 신규 위반 없음.
4. **CommunityPostView·UserPageView 클린 (신규 전체 확인)**: 카테고리 칩·VIP 배지(금색 그라디언트)·인증 배지(badge--vehicle/license/car 라이트+`html.dark` 쌍 완비)·상태 배지(`.order-row__status` brand-soft)·영상 오버레이 모두 스펙·토큰 준수, dark hover 쌍(01:01 적용) 유지. 좋아요/댓글 아이콘·아바타 brand-gradient 흰 이니셜은 기존 관용 분류(아래 e). 신규 위반 없음.
5. **SettingsVerificationView 클린 (신규 전체 확인)**: 신청 버튼 2개 모두 `ghost`(외곽선) — 채움 Primary 버튼 아님. 페이지 내 Primary 채움 0개로 UI 미니멀 원칙 준수(기존 "primary 2개" 후보는 ghost 전환으로 해소된 상태 재확인). 신규 위반 없음.
6. **전역 캐너리 통과**: 10px 미만 폰트 0건, "오더/주문번호" UI 노출 0건, 22:05 OrderCard·SetGroupCard `.status-badge--accepted/completed` 규칙 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/MyMarketView.vue` | `.status-tabs__trash-count` `font-weight: 700` → `400` + 주석 갱신("HeaderBar .hb-badge와 동일한 16px 원형 카운트 배지") — 휴지통 건수 배지를 앱 카운트 배지 표준(10px/400)과 통일 |

`<style scoped>` CSS 값·주석만 변경(템플릿 구조·script·import 무변경). 배경(`--danger`)·글자색(`#fff`, 위험색 흰 글자 관용)·크기(16px)·font-size(10px)·구조·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ProfileView naive 세그먼트 활성 탭(brand 채움+흰 글자)·아바타 brand-gradient 흰 이니셜·CommunityPostView/UserPageView 아바타·VIP 금색 배지**: naive/아바타/프리미엄 배지 관용 — 전역 "brand 위 글자색"·naive 테마 설계 후보 유지(기존 회차 반복 미적용).
- **SetGroupCard `.set-card__new`(20px 원형 "N" 11px/700)·CommunityView/HomeView 건수 칩(600/700)**: 원형 아이콘 배지·탭 칩 내부 숫자 관용으로 red 원형 카운트 배지(이번 적용 대상)와는 성격이 다름 — 유지(신규 재분류 기록).
- **ProfileView 실적 카드 틴트 하드코딩(`#2ea043`/`#ffa940` rgba)**: 알파 0.12~0.15 장식용 미세 틴트 — naive 그린·앰버 전역 교체(대형 후보)와 묶어 재평가(유지).
- **UserPageView `.badge--car` 라이트 연두(#f6ffed/#389e0d)·CommunityPostView `.feed-action--liked` `#e5484d`**: 토큰·형제 화면과 동일한 의미색/관용 — 하드코딩 토큰화 후보(대형)로 기록(유지).
- **MyMarketView FAB("＋" brand 채움 흰 글자)·OrderDetailView `.detail-hero` 흰 글자·라이트 naive primary 초록·SettingsVerificationView ghost 2개(의도적)·`.v8-noti` 외 데드/중복 CSS**: 기존 대형/설계 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 942ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: MyMarketView L850 `.status-tabs__trash-count` `font-weight: 400` 확인(기존 700 잔존 없음).
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값·주석뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
