# UI 개선 보고서 — 2026-09-03 22:05

## (a) 실행 시각
- 2026-09-03 22:05 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **부분 검토만 되던 핵심 화면 2건**): `views/HomeView.vue`(전체 1204줄 — 히어로/추천 탭(단일·왕복·셋트)/일괄요청 체인/빠른 메뉴), `views/MarketView.vue`(전체 1410줄 — 필터 칩 행·필터 모달·노선 검색 모달·연결 운행 섹션·전체 운행 그리드·빠른매칭 드로어)
- 컴포넌트(직접 전체 읽기 — **최근 보고서는 클래스 규칙 대조만 수행, 본문 전체 읽기 이력이 오래된 대상**): `components/orders/OrderCard.vue`(전체 — 운행/셋트 카드 공용 상태 배지), `components/orders/SetGroupCard.vue`(전체 — 동일 배지 규칙 공유)
- 대조 파일: `views/orders/OrderCreateView.vue`(내가 수행하는 활성 운행 카드 — 예약(accepted) 노출 확인), `views/orders/MyMarketView.vue`(등록 운행 카드 사용부), `views/orders/RideHistoryView.vue`(`statusTextColor` L265-268 — 상태 배지 글자색 표준), `utils/colors.js`(`statusColorVar`), `assets/base.css`(`--status-*` 라이트/다크 토큰)
- 전역 감사(grep): `status-badge--` 클래스 규칙 전수(2개 파일만 보유 — 다른 잔존 없음), 상태 배지 글자색 규칙 대조(OrderCard·SetGroupCard·RideHistoryView·ChatThread·AdminView 상태 목록 비교), `font-size: n.npx` 비정수 전수(11.5px·10.5px 계열 16곳 — 필터/탭 칩·EmptyState 힌트의 앱 전역 일관 패턴 확인)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_2104.md`·`2004.md`·`1907.md`·`1801.md`·`1701.md`·`1603.md`(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / 상태 배지 글자색 표준)

## (c) 발견한 개선 포인트
1. **OrderCard·SetGroupCard 상태 배지 — 예약(accepted)·완료(completed) 흰 글자 저대비 (신규 발견·적용)**: 두 카드의 배지 글자색 규칙이 `.status-badge--published/driving/trading/acceptance-pending`만 어두운 글자(`#101418`)로 예외 처리하는데, **`accepted`는 script에서 배경을 앰버(`--status-trading`, script 주석: "예약(가져오기 수락) 배지는 오렌지색")로 칠하면서 클래스는 `status-badge--accepted`라 어두운 글자 규칙에서 빠져** 라이트 `#ffa940`(대비 ≈1.9:1)·다크 `#c98f3e`(≈2.8:1) 위 흰 10px 글자가 남는 잔존이었다. `completed`도 초록 배경(라이트 `#18a058` ≈3.4:1·다크 `#27946a` ≈3.8:1 — 모두 4.5:1 미만)인데 규칙에 없음. **RideHistoryView 표준 `statusTextColor`는 completed를 어두운 글자로, trading 계열도 어두운 글자로 처리**하며, 같은 파일의 trading 셀렉터가 이미 앰버 배경 어두운 글자를 확립했으므로 클래스 목록 누락이 화면 간 불일치다. 노출 경로: OrderCreateView(예약·진행중 활성 운행)·MyMarketView(등록 운행 예약/완료) 등 OrderCard·SetGroupCard 사용처 전반.
2. **HomeView 클린 (신규 전체 확인)**: 히어로(내 다음 운행 없을 때 최우선 추천 폴백)·추천 탭(단일/왕복/셋트, 활성 탭만 노출)·EmptyState 3종 안내·일괄요청중 집계 칩·빠른 메뉴 — 화면당 Primary 액션 1개, 정보 순서(정보→행동), 한국어·"운행" 용어, 배지 스펙(1px 6px/10px/400), `#07120e` 표준 준수. `font-size: 11.5px` 탭 칩은 MarketView·ChatList·MyMarketView 등 앱 전역 필터/탭 칩의 일관 패턴으로 이상 아님. 히어로 "보상" 라벨은 퀘스트 성격 copy로 보임 — 후보로만 기록(아래 e-4).
3. **MarketView 클린 (신규 전체 확인)**: 서비스 유형 상단 칩 + 필터 모달(이중 필터 아님, 상단 칩은 모달 옵션과 동일)·필터 요약 칩 개별 해제·결과 건수·연결/전체 섹션 분리·Primary("적용"/"검색") 각 1개 — 구조·용어·배지 스펙 준수, 신규 위반 없음.
4. **기타 재확인 클린**: `status-badge--` 규칙은 OrderCard·SetGroupCard 2개 파일뿐(전역 다른 잔존 없음). ChatThread·AdminView·RideHistoryView의 상태 글자색 규칙(accepted는 인디고 배경이라 흰 글자 유지 정상)은 카드 배지의 앰버 변환과 무관 — 위 1건만 수정 대상.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/orders/OrderCard.vue` | 어두운 글자 규칙에 `.status-badge--accepted`·`.status-badge--completed` 추가 + 근거 주석("accepted는 script에서 앰버(--status-trading)로, completed는 초록 — 둘 다 밝아 어두운 글자") — 예약·완료 배지 흰 글자 저대비 해소 |
| `components/orders/SetGroupCard.vue` | 동일 적용 — `.status-badge--accepted`·`.status-badge--completed` + 동일 주석 |

`<style scoped>` CSS 셀렉터 목록 확장 + 주석 2건(템플릿·script·import 무변경). 값은 두 파일 기존 규칙·RideHistoryView `statusTextColor`(completed→`#101418`) 표준과 동일. 배경·패딩·font-size(10px)·구조·동작 무변경 — 라이트·다크 모두 어두운 글자로 통일(다크 앰버 `#c98f3e`·다크 초록 `#27946a`에서도 대비 4.5:1 이상 확보).

## (e) 적용하지 않은 항목과 이유 (후보)
- **HomeView 히어로 "보상" 라벨·코인 아이콘**: 금액/운행료 대신 "보상" 단어를 쓴 퀘스트 성격 copy — 제품 문구 결정 사항(신규 후보 기록).
- **`font-size: 11.5px`(필터/탭 칩)·`10.5px`(EmptyState 힌트) 비정수 계열 16곳**: 앱 전역 필터/탭 칩·힌트의 일관 패턴(19:07 EmptyState 판단 연장) — 정상 분류(미적용).
- **`status-badge--settled/cancelled/draft` 흰 글자**: 보라·위험(레드)·회색 배경 — 기존 관용(위험색 흰 글자·보라 대비 정상) 분류 유지(RideHistoryView 표준과 동일).
- **OrderDetailView `.detail-hero` 흰 글자·아바타/FAB·brand-gradient·ProfileView naive 세그먼트·라이트 naive primary 초록 불일치·HomeView 탭 카운트 vs 표시 상한·SettingsVerificationView primary 2개**: 기존 대형/설계 후보 유지(반복 미적용).
- **HomeView/MarketView 중복·데드 CSS(`.home-rec-list` 2회 정의 등)**: 렌더링 변화 없는 코드 정리로 UI 개선 아님 — 유지.

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 955ms`, 에러 0건(exit 0).
- 수정 후 grep 검증: OrderCard(L549-551)·SetGroupCard(L264-266) 모두 `.status-badge--accepted`·`.status-badge--completed` 포함 확인 — 2/2.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 셀렉터·주석뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
