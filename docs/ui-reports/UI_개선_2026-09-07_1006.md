# UI 개선 보고서 — 2026-09-07 10:06

## 실행 결과 요약
- **수정 0건(변경 없음)** — 직전 실행(59회차, 09-06 01:02) 이후 `frontend/src` 변경은 채팅 동기화 신규 기능 4개 파일뿐(09-07 09:54~09:55, mtime 스캔 — AdminView 01:02:38은 59회차 자체 적용분). 이 4개 파일의 UI 델타(채팅 목록 분류 칩·'모두 읽음' 툴바·유형 칩 라이트/다크 팔레트)를 전수 열람·대조한 결과 **수정할 명확·소형 결함은 발견되지 않음** — 신규 코드가 앱 표준(홈 칩 언어·알림센터 v8 상태 칩 팔레트·배지 스펙·다크 오버라이드·한국어/'운행' 용어)을 이미 따르고 있어 반복·임의 수정은 하지 않음. 직전 회차 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-07 10:06 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 09-06 01:02 이후 `frontend/src` 변경 4건 (모두 오늘 09:54~09:55, 채팅 동기화 기능의 전·후단 묶음)
  1. `api/chats.js` — `read-all`·`/sync`·요청 확정(`resolve`) API 추가
  2. `stores/chats.js` — `markAllRead`·증분 동기화(`reloadMessages` 리팩터)·`resolveRequest` 추가
  3. `views/ChatView.vue` — 목록용 `.page-shell` 조건부 적용·폴링 10s→3s·`markAllRead` 배선
  4. `components/chat/ChatList.vue` — 분류 칩(오늘/어제/지난주)+건수·'모두 읽음' 툴바·유형 칩 라이트/다크 팔레트·빈 기간 문구·배지 스펙 적용(+`BaseIcon` import)
- **직접 열람(전수)**: 위 4개 파일(전체), `components/chat/ChatThread.vue`(헤더/요청 카드/폴링 연동부), `components/chat/ChatRequestEvent.vue`(요청 카드 라벨·상태·수락/거절 CTA), `components/chat/ChatRequestSheet.vue`(요청 유형 라벨 대조), `assets/base.css`(토큰·공용 채팅/스켈레톤 규칙 대조 — `--card-gap/--card-pad/--card-radius/--chips-gap` 정의·`html.dark` 토큰), `utils/icons.js`(`check-done` 정의 확인)
- **대조·감사(grep)**: `--accent` 채움 아바타(관용 후보 분류 확인) · ChatList 칩 스타일 ↔ `HomeView .home-tab-chip`·`ReviewsView .reviews-tabs`·`NotificationsView .v8-priority-status`/`.v8-icon` 팔레트 동일성 · 유형 라벨(목록 칩↔시트↔이벤트 카드) 문구 정합 · OrderDetailChat의 sync API 사용(신규 미커밋 파일 — 09-03/09-04 전수 리뷰 대상, UI 변경 없음)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-06_0102.md`(59회차)·`0006.md`(58회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
신규 델타(채팅 목록 화면) 전수 점검 결과 — **수정할 명확·소형 결함 없음**. 확인된 정합 사항:
1. **분류 칩(오늘/어제/지난주)**: `role=tablist/aria-selected`·`type="button"`·건수 `<em>` — HomeView `.home-tab-chip`과 동일한 필·스펙(11.5px/600, 카운트 1px 5px/10px/600/line-height 16, 활성 brand 채움+`#07120e` — HomeView와 문자 그대로 동일한 디자인 언어). `overflow-x:auto` 스크롤바 숨김 등 모바일 처리 정상.
2. **'모두 읽음' 툴바**: 안 읽은 대화가 있을 때만 노출(`hasUnread`), ghost 필 버튼(brand 텍스트) — 화면 Primary 없음(목록 맥락) 원칙 위배 없음. 아이콘 `check-done`은 `utils/icons.js` 정의 확인(정상 렌더).
3. **유형 칩 팔레트**: 라이트 `#0e9d8d/#9a6a00/#c03a3f` + `html.dark` 밝은 톤(`var(--brand)`/`#ffd071`/`#ff8a8a`) — NotificationsView `.v8-priority-status--success/warn/danger`(L574-579)와 **텍스트 색이 정확히 일치**(주석 "알림센터 v8 상태 칩과 동일 팔레트" 실제 확인). 배경 틴트는 기존 대비/어두운 틸트 유지. 다크 그린만 `--brand` 토큰 사용(승인=브랜드 의미, 의도로 판단).
4. **배지 스펙**: `.chat-list__order-chip`·`.chat-list__type-chip` 모두 `1px 6px/10px/400`으로 변경 적용 확인(기존 11px/700 위반 정리 — 09-02 10:02 지적분의 최종 해소).
5. **구분 라벨/빈 상태**: 첫 그룹 앞 구분 라벨 생략(마켓·홈과 동일) — 탭별 단일 그룹이어도 자연스러움. 빈 문구 "이 기간의 대화가 없습니다." 한국어·카드형(`--surface/--card-radius`) 정상.
6. **라벨 문구 정합**: 목록 칩(승인 요청/시간 변경/경로 변경/요금 협의/취소 요청) ↔ ChatRequestSheet(L76-81) ↔ ChatRequestEvent(L43-49) — '요금 협의' 포함 서로 일치(요청 카드는 '~ 요청' 확장형, 칩은 축약형 — 의도된 계층).
7. **ChatView**: 목록에만 `.page-shell`(14px 좌우 여백) 부여 — 이전에는 채팅 목록이 다른 페이지와 달리 여백 없이 끝까지 붙던 것 개선(대화방은 자체 고정 레이아웃이라 제외). 스켈레톤·목록·빈 상태 간 세로 시작점 일치. 폴링 3s·SSE 연동은 기능 판단(UI 아님).
8. 기존 관용 분류 유지 확인: `.chat-list__avatar`·`.cb-avatar`(`--accent` 다크=틸 위 흰 이니셜)는 **아바타 관용 — 전역 "brand 위 글자색" 설계 후보**(59개 회차 반복 분류, 임의 수정 금지 대상).

## (d) 실제 적용한 수정
- **없음(변경 없음)** — 신규 코드가 위 표준을 준수해 반복·임의 수정을 하지 않음.

## (e) 적용하지 않은 항목과 이유 (후보)
- **채팅 목록 탭 선택이 대화방 진입 시 초기화**: ChatList는 `v-else-if="!store.activeId"`로 스레드 열 때 언마운트 → '지난주' 등 선택 탭이 매번 '오늘'로 리셋(활성 대화량 많은 기사는 매번 재선택 필요). `v-show` 전환 또는 `activeTab` 상향(부모/스토어)로 해소 가능하나 컴포저블/스토어 변경 수반 — 후보 기록(반복 미적용).
- **'지난주' 칩 라벨 vs 내부 그룹 라벨**: 2일 전~이번 주 대화도 '지난주' 칩에 담기며 내부 구분은 '이번 주'/'이전'으로 표기 — 칩 라벨과 그룹 라벨 의미 단차. "대화 숨김 방지" 의도(주석)라 라벨 결정 사항으로 후보만 기록.
- **승인 요청 칩 = 그린(민트)**: '미처리 승인 요청'이 v8 분류상 '확인 필요(옐로우)'이나 목록에서는 그린으로 표기 — 알림센터·액션센터와의 색 시맨틱 통일 관점 후보(09-06 00:06 ActionCenter 타입 칩 후보와 동일 부류, 화면 맥락 차이 가능성 — 디자인 결정, 반복 미적용).
- **HomeView와 ChatList 칩 CSS 사실상 중복**: `.home-tab-chip` ↔ `.chat-chip` 동일 스타일이 파일 간 중복 — 공용 칩 클래스/컴포넌트 추출은 복수 화면(Home·Market·Community·Chat) 영향으로 큰 변경 — 기존 '화면 내 CSS 중복' 분류에 통합해 후보 유지.
- **기존 반복 후보(59회차 기록분 유지)**: ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출 · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구 · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) — 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) — **통과**, 4362 modules, built in 969ms. 오늘 추가된 채팅 동기화 코드(신규 import 포함)의 최초 빌드 검증 — ChatView 청크(채팅 목록·툴바 CSS/JS 포함)·api/store 번들 정상 생성.
- import 누락 점검: 이번 실행 수정 0건. 대신 오늘 신규 코드의 import를 점검 — ChatList→`BaseIcon`(`check-done` 정의 확인)·`formatTime`·`chatTime`·`auth` 스토어, ChatView→`ChatList`/`ChatThread`/`EmptyState`, chats.js 신규 export(`apiMarkAllChatsRead`/`apiChatMessagesSync`/`apiResolveChatRequest`)와 stores/OrderDetailChat의 import 일치 확인 — 누락 0건.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
