# UI 개선 보고서 — 2026-09-09 16:02

## 실행 결과 요약
- **코드 수정 6건(5개 파일)** — 직전 실행(14:01, "변경 없음") 이후 소스 변경은 13:58~14:42 외부 작업이 커밋(`fd36d02`)으로 흡수되어 워킹트리 소스 변경 없음. 이번 실행은 검색 에이전트 전수 감사를 병행해 **신규 소형 개선 포인트 5종을 발굴**, 명확·저위험 항목 6건을 직접 수정하고 `npm run build` 통과를 검증했다. 적용 내역: ① 정산 '출금하기' CTA에 계좌 미등록 disabled 조건 누락 보완, ② 임박 배지 다크모드 글자 대비 수정(OrderCard·SetGroupCard), ③ 한국어 보조용언 띄어쓰기 통일 3곳(채팅 빈 대화·커뮤니티 글 작성 placeholder 2곳).

## (a) 실행 시각
- 2026-09-09 16:02 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_1401.md`(직전), 1301·1101 — 적용 내역·후보 전수 대조, 반복 적용 없음.
- **mtime 델타 스캔(14:01 이후)**: `frontend/src` 전체 — 소스 변경 0건(최신 수정 14:42 `App.vue`는 커밋 fd36d02에 포함, 워킹트리 클린). `git status`: 소스 미커밋 0건(docs 일부만 변경 중).
- **검색 에이전트 전수 감사**(`views/**/*.vue`·`components/**/*.vue`·`assets/base.css`, 백엔드 포맷터 `app/Support/Orders/OrderListRowBuilder.php` 교차): 템플릿 영어 노출·기술 식별자·10px 미만 폰트·뒤집힌 분기·다중 Primary → 신규 0건.
- **직접 판독(수정 전후 검증)**:
  - `views/SettlementView.vue`(60~160행) — 출금 신청 가드(77~90행)와 CTA disabled(153행) 대조.
  - `components/orders/OrderCard.vue`(215·236~275·543~620행) — 임박 배지·밝은 배경 배지 글자색 규칙(609~620행 주석).
  - `components/orders/SetGroupCard.vue`(88·252~290행) — 동일 규칙.
  - `components/chat/ChatList.vue`(206~220행), `views/ChatView.vue`(83행) — 같은 문구의 띄어쓰기 단차 확인.
  - `components/community/CommunityPostEditor.vue`(122행), `views/community/CommunityView.vue`(420행).
  - `components/orders/OrderCard.vue` 금액 표기·`components/ui/UiChip.vue`(16~27행) 사용처(NotificationsView 350 / QuickMatchPanel 556~591) — 후보 분류용.
  - `app/Support/Orders/OrderListRowBuilder.php:165~170` — amount 형식 `number_format().'원'` 확인.
  - 전역 그렙: `해보세요|해 보세요` 13건(단차 확인), `임박` 관련 배지 정의 4종, `style` 인라인 hex 색 0건.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 / 접근성 관례).

## (c) 발견한 개선 포인트
1. **정산 '출금하기' CTA — 계좌 미등록 시에도 활성화(명백한 누락)**: [SettlementView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SettlementView.vue#L153) `:disabled`에 `!summary.account`가 없어, 계좌 미등록 + 출금 가능 금액이 있으면 클릭 가능한 Primary가 노출되고 클릭 시 경고 토스트로만 응답. `requestPayout()`(78~81행)의 첫 가드와 동일 조건을 disabled에 누락(나머지 두 가드인 대기 출금·금액 없음은 이미 반영). 계좌 등록이 먼저라는 힌트(159행)도 이미 있음 → **적용**.
2. **임박 배지 다크모드 대비 부족(가독성)**: [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L543) `.order-card__urgent`·[SetGroupCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L253) `.set-card__urgent`는 `background: var(--danger)`(다크=밝은 빨강 `#d86262`) + 흰 글자 10px/400 → 다크에서 대비 약 3.6:1로 AA(4.5:1) 미달. 같은 파일에 이미 "밝은 배경 배지는 어두운 글자(#101418)" 접근성 규칙(609~620행)이 있고 밝은 계열 배지들만 적용돼 임박 배지가 빠짐 → **적용**(html.dark 전용 오버라이드).
3. **한국어 보조용언 띄어쓰기 단차 3곳**: 앱 전반 힌트는 "~해 보세요" 띄어쓰기 표준(ChatView 83행 "대화를 시작해 보세요" 등 다수)인데, [ChatList.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatList.vue#L214) "대화를 시작해보세요", [CommunityPostEditor.vue](file:///d:/wwwroot/nowhere/frontend/src/components/community/CommunityPostEditor.vue#L122)·[CommunityView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L420) "공유해보세요"만 붙여씀 → **적용**(앱 내 동일 문구·힌트 문체와 통일).
4. **금액 단위 이중 표기(후보로만 기록)**: `OrderCard`(236~239·270~275)·`SetGroupCard`·HomeView 히어로는 `₩` 각인된 coin 아이콘 + `order.amount` 문자열("85,000원")을 함께 노출 → 화폐 표기 중복. 다만 icon·원 텍스트 중 어느 쪽을 유지할지는 상품 시각 결정이라 적용 보류(아래 (e)-1).
5. **UiChip 배지 스펙 단차(후보로만 기록)**: [UiChip.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiChip.vue#L16) 기본이 `padding 4px 7px / 11px / 700`로 배지 스펙(1px 6px/10px/400)과 다르나, NotificationsView 유형 태그 외 QuickMatchPanel의 라벨+값 조합 칩(556~591행)이 같은 기본 스타일을 공유 → 전역 축소는 부작용 위험. 적용 보류(아래 (e)-2).

## (d) 실제 적용한 수정
1. `views/SettlementView.vue:153` — CTA disabled에 `!summary.account` 추가: `:disabled="submitting || !summary.account || !summary.pending_total || hasPendingPayout"` — requestPayout 가드 3종과 disabled 조건이 일치해 성공 불가 상태의 활성 버튼 제거.
2. `components/orders/OrderCard.vue` — `.order-card__priority` 앞에 `html.dark .order-card__urgent { color: #101418; }` 추가(주석 명시: 다크에서 danger 톤 다운 시 흰 글자 대비 부족 → 어두운 글자 전환).
3. `components/orders/SetGroupCard.vue` — 동일하게 `html.dark .set-card__urgent { color: #101418; }` 추가.
4. `components/chat/ChatList.vue:214` — "대화를 시작해보세요" → "대화를 시작해 보세요".
5. `components/community/CommunityPostEditor.vue:122` — "공유해보세요." → "공유해 보세요.".
6. `views/community/CommunityView.vue:420` — 동일 placeholder 교정.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **coin 아이콘 + "원" 이중 표기**(OrderCard·SetGroupCard·HomeView 히어로 3종): 금액 텍스트를 숫자로 줄이거나 coin 아이콘을 제거하는 두 방향이 모두 가능하고, 화면별 일괄 적용 범위(3파일)라 명확한 단일 방향으로 확정하기 어려움 → **상품 시각 결정으로 후보** (다음 실행 시 사용자 방향 확인 후 처리).
2. **UiChip 기본 스타일 배지화**: QuickMatchPanel 칩이 기본 스타일을 공유해 전역 변경 시 해당 화면 퇴행 위험 → NotificationsView의 소형 유형 태그만 정렬하려면 `compact` 변형 추가가 필요(소형 범위 초과) → 후보.
3. **OrderDetailView `.hero-badge--urgent`(#e5484d + 흰 글자)**: 히어로 그라디언트 배경 위 요소로 파일 내 다른 오버레이 배지(`--today/--tomorrow`)와 함께 설계된 구조라 카드 배지와 같은 일괄 규칙 적용이 부적절 → 후보(별도 판단).
4. **기존 반복 후보(14:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색 · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버" · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · 문의 placeholder 문구(SupportView) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market` · `role === 'Driver'` 리터럴 7곳 상수 치환 · RegisterView '등록자' 설명 문구 · AdminView.vue 잔여 구간 전수 판독 — 기존 분류 유지.
- 검토 중 채택 보류로 기록하지 않은 항목: 템플릿 영어 노출·기술 식별자·10px 미만 폰트·다중 Primary·뒤집힌 분기 — 신규 0건.

## (f) 빌드 결과
- **`npm run build` 통과** (✓ built in 955ms) — 수정 5개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 이번 수정은 모두 템플릿 문구·CSS 규칙·바인딩 속성 변경이라 import 추가·제거 없음(신규 심볼 참조 0건). `npm run build`는 템플릿·CSS 컴파일까지 수행하므로 통과로 검증 대체.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 수정 6건은 미커밋 상태로 워킹트리에 남김(사용자 커밋 대기).
