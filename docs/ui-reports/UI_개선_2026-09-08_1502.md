# UI 개선 보고서 — 2026-09-08 15:02

## 실행 결과 요약
- **수정 1건 적용** — 직전 실행(09-08 14:01) 보고서 작성 이후 `frontend/src` 신규 변경분 **1건**(ChatView.vue 14:03)이며 이는 14:01 보고서의 `ref` 미사용 import 제거분 자체임을 확인(현재 import 2건 모두 사용 확인) — 신규 코드 아님. 이번 실행 감사에서 기존 반복 후보였던 **App.vue 데드 CSS `.bottom-nav__item--button`(더보기 버튼 규칙)을 템플릿·스크립트 사용 0건 재확인 후 제거**했다. 그 외 신규 위반(기술 식별자·용어·소형 폰트·영문 라벨·배지 스펙) 0건.

## (a) 실행 시각
- 2026-09-08 15:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 14:01 이후 수정분)**: `frontend/src` 전체 재귀 — **1건**: `views/ChatView.vue`(14:03). 파일 판독 결과 14:01 보고서의 적용분(`ref` import 제거)이 반영된 상태로, 잔존 import 2건(onBeforeUnmount·onMounted) 사용처 확인 — 이미 검토·반영된 변경이라 신규 코드 아님.
- **직접 판독(이번 실행 적용 대상 검증)**:
  - `App.vue` 템플릿 하단 네비 영역(300~331) 및 `<style>` 블록 전체(341~374) — 하단 네비 '더보기' 항목이 `router-link`(323~330)로만 구성됨을 확인, `bottom-nav__item--button` 클래스 사용처 부재 확인
  - `components/chat/ChatList.vue`(45~46 타입 칩 분류, 168~283 칩/배지 CSS) — 칩·배지 스펙 준수 재확인
- **전역 규칙 감사(grep)**:
  - `App.vue` 내 `bottom-nav__item--button` / `app-content--full` / `nav-badge` 사용처: 데드 규칙 1건(bottom-nav__item--button, 제거 대상) 외 전부 사용 확인
  - 템플릿 기술 식별자(`오더|주문번호|주문 번호`): **0건** · `orderId` 템플릿 노출: **0건**(12건 매칭은 전부 코드 식별자 — props/API/함수 인자, 규칙상 유지)
  - `font-size: 7~9px`: **0건**
  - 템플릿 영문 라벨: `VIP`·`NoWhere`(브랜드/관용 표기)만 존재 — 위반 아님
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1401.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **App.vue 데드 CSS `.bottom-nav__item--button`(기존 반복 후보 해소 적용)**: 하단 네비 '더보기' 항목이 과거 `<button>`에서 `router-link`로 전환된 뒤 잔존한 규칙(주석 1줄 + 선언 6줄). 템플릿·스크립트에서 클래스 사용 **0건** 확인 — 순수 데드 CSS. 제거로도 렌더링·동작 변화 없음(같은 `<style>` 블록의 `main.app-content--full`·`.nav-badge .n-badge-sup`은 템플릿 사용 확인, 유지).
2. **ChatList.vue 타입 칩·칩 스펙 — 위반 없음(재확인)**: `chat-list__type-chip`/`chat-list__order-chip`(padding 1px 6px / font-size 10px / font-weight 400)이 배지 스펙에 정확히 부합. `chat-chip`(기간 탭)의 font-size 11.5px·font-weight 600은 인터랙티브 탭 요소로 상태 배지 컨벤션 대상 아님.
3. **신규 위반 — 0건**: 템플릿 기술 식별자·소형 폰트·영문 라벨 감사 모두 위반 없음. 14:01 보고서 이후 신규 코드 자체가 없어 신규 결함 발생 요인 없음.
4. 기존 반복 후보 상태 변화 없음(14:01 기록분 그대로).

## (d) 실제 적용한 수정
- **App.vue — 데드 CSS `.bottom-nav__item--button` 8줄 제거**(주석 "하단 네비 '더보기' 버튼 — 링크와 동일한 모양" 및 규칙 전체 삭제). 하단 네비는 전부 `router-link`로 동작하므로 스타일 손실 없음 — 순수 CSS 정리로 렌더링·동작 변화 없음. 로컬 전용 작업.
- **ChatView.vue**: 이번 실행 수정 없음(14:03 변경은 14:01 보고서 적용분, 상태만 재확인).

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(14:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView '(운행 번호·날짜 포함 시…)' — 사용자가 운행 번호를 알 수 없어 안내 실효성 문제) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 11곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지(렌더링 영향 없거나 대형·디자인·제품 결정 수반, 반복 미적용).
- **App.vue `.bottom-nav__item--button`**: 이번 실행에서 해소(데드 CSS 제거) — 후보 목록에서 삭제.
- 이번 실행 신규 감사 결과 적용할 신규 결함 없음(위 (c) 2~3 참조).

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, 996ms. App.vue 데드 CSS 제거 후 전역 청크(index) 정상 생성.
- import 누락 점검: 이번 실행은 `<style>` 블록 내 데드 규칙 삭제만(스크립트·템플릿 변경 없음) — 추가 import 대상 없음. `main.app-content--full`(App.vue:287) 등 잔존 규칙의 템플릿 사용 재확인 완료.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
