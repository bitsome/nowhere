# UI 개선 보고서 — 2026-09-10 11:00

## 실행 결과 요약
- **코드 수정 4건(2개 파일)** — 직전 실행(01:03) 이후 `frontend/src` 변경은 OrderCard.vue(01:37, 타 세션 찜·태그·별점 마무리) 1건뿐이며, 검토 시점 기준 9시간 이상 경과해 안정화된 것으로 판단했다.
- 적용: **brand 채움 위 흰 글자/아이콘 저대비 4곳을 앱 표준(#07120e)으로 통일** — 신규 찜 빈 화면 CTA 1곳(`OrderFavoritesView`) + 채팅 이미지 시트 선택·전송완료 표시 3곳(`ChatImageSheet`). 모두 `background: var(--brand)` + `color: #fff` 조합으로 라이트(#36adff)·다크(#63e2b7) 양쪽에서 대비가 약했고, 동일 파일에 이미 확립된 표준(주석 포함)이 존재해 최소 변경으로 정리했다.
- 전역 표준 스캔(용어·보조용언·소형 폰트·rel/alt·배지 규격) 신규 위반 0건 — 기존 클린 상태 유지.
- `npm run build` 통과. 로직·구조·신규 기능 변경 없음(전부 CSS 색상값).

## (a) 실행 시각
- 2026-09-10 11:00~11:05 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_0103.md`·`2026-09-10_0013.md` — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + `frontend/src` mtime 전수 검사 — 01:03 보고서 이후 변경은 `components/orders/OrderCard.vue`(01:37) 1건. 타 세션 편집 흔적은 있으나 9시간 이상 경과해 안정 상태로 판단, 이번 수정 대상에서도 찜 로직 본체는 제외.
- **직접 판독**: OrderCard.vue(전량 871행 — 찜 하트·태그 검색·별점·모달 CSS), OrderFavoritesView.vue(전량 239행 — 신규 찜 페이지, CSS 포함), ChatImageSheet.vue(보관함 선택 체크 922~943 / 썸네일 선택 체크 1133~1156 / 전송 완료 표시 1256~1267 / 전송 버튼 1230~1244), ScoreStars.vue(전량 111행), UiChip.vue·UiSection.vue(전량), SettlementView.vue(455~478 정산 상태 점), SettingsVerificationView.vue(320~390 배지·거절 사유 박스), QuickMatchPanel.vue(800~920 토글 스위치·원큐 설정), HomeView.vue diff(더보기·탭 전환·등록자 홈 플래시 방지), ReviewsView.vue diff, ActionCenterView/SettlementView/RideHistoryView/ChatRequestSheet/ChatThread/SetGroupCard/DashboardView diff.
- **전역 그렙**: `오더|주문번호`(0건) · `font-size: 6~9px`(0건) · 보조용언 붙여쓰기(0건 — '알려 드릴게요' 2건은 띄어쓰기 정상) · `target="_blank"` `rel="noopener"` 누락(0건, 8건 전수 확인) · `var(--danger)` 사용처 80건 의미 검토(레드=취소·거절·문제/삭제/신고 전용으로 정상) · `background: var(--brand)` + `color: #fff` 조합 전수(→ 4곳 발견·수정, 나머지는 `--accent` 아바타·FAB·그라디언트 CTA 등 기존 관용/전역 설계 후보 분류).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 배지·색상 표준, 메모리 규칙(한국어 UI / "운행" 용어 / 다크 대비 표준 `#07120e`).

## (c) 발견한 개선 포인트
1. **신규 찜 페이지 빈 화면 CTA 저대비(신규 발견)**: [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue#L228-L238) `.fav-empty__cta`가 `background: var(--brand); color: #fff`. 라이트 brand `#36adff`(흰 글자 ≈2.3:1)·다크 brand `#63e2b7`(≈1.5:1) 모두 대비가 약하다. 앱 표준은 "밝은 brand 채움 위 어두운 글자 `#07120e`"(HomeView·MarketView·SettlementView·MessageBubble 등 다수 적용). **적용.**
2. **채팅 이미지 시트 brand 채움 아이콘 저대비(기존 'ChatImageSheet 흰 아이콘' 후보)**: [ChatImageSheet.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatImageSheet.vue#L938-L943) `.img-sheet__archive-check.is-checked`(942행), [썸네일 선택](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatImageSheet.vue#L1150-L1155)(1154행), [전송 완료 표시](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatImageSheet.vue#L1256-L1267)(1266행)가 `background: var(--brand); color: #fff`. 같은 파일의 `.img-sheet__archive-add`(1020행)·`.img-sheet__send`(1237행)는 이미 `#07120e` 표준이 적용돼 있어 파일 내 규칙이 불일치했다. **적용(3건).**
3. **표준 스캔 신규 위반 0건**: 용어("오더"/주문번호)·보조용언 붙여쓰기·기술 식별자·소형 폰트(6~9px)·`rel="noopener"` 누락. 이번에 판독한 OrderCard(별점 14px·조건 버튼 aria-label/title 병기), ScoreStars(라이트 `#d3d3da`/다크 `#4f4f57` 회색 별, 금색 별 다크 톤다운), UiChip·UiSection, QuickMatchPanel 토글 스위치(`html.dark` 쌍), SettlementView 상태 점, SettingsVerificationView 배지·거절 사유 박스는 기존 표준 준수 확인.
4. **기존 반복 후보(01:03·00:13 기록분 유지, 상태 재확인)**: OrderFavoritesView eyebrow `--danger` 컬러 · QuickMatchPanel `.qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · coin 아이콘 + '원' 이중 표기 · '정산 대기중' 백엔드 캐노니컬 · UiChip 기본 스타일 · MyMarketView FAB '＋' 글리프 · 아바타(`--accent` 다크 틸) 흰 이니셜 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/OrderFavoritesView.vue` | `.fav-empty__cta` 글자색 `#fff` → `#07120e` + 표준 근거 주석 추가(228행). 빈 상태의 '마켓 보러 가기' CTA가 라이트·다크 모두에서 읽히도록 앱 표준에 맞춤. 배경·패딩·폰트·동작 무변경 |
| `components/chat/ChatImageSheet.vue` | `.img-sheet__archive-check.is-checked`(942행) 아이콘색 `#fff` → `#07120e`, `.img-sheet__thumb-check.is-checked`(1154행) 동일, `.img-sheet__thumb-done`(1266행) 동일 — 세 곳 모두 brand 채움 위 선택·완료 표시라 어두운 아이콘 표준 적용(주석 2건 병기). 구조·동작 무변경 |

- 공통: 색상값만 변경. 템플릿·스크립트·props·emit 무변경이라 런타임 로직 영향 0건.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **OrderCard.vue 찜·태그·별점 신규 영역 일체(타 세션 01:37 최종 편집)**: 최신 편집 파일이라 찜 하트(`aria-label`+`title` 병기)·태그 칩(마켓 검색 연동)·별점 모달의 접근성·표준 준수를 확인했고 명백한 결함이 없어 수정하지 않았다. `:title="'조건 0~5점 · 클릭해 근거 보기'"`(319행)는 정적 문자열을 동적 바인딩한 형태 + '0~5점' 표현이 다소 기술적이나, 문구·바인딩 취향 수준이라 후보로만 기록한다(직접 수정하지 않음).
2. **OrderFavoritesView eyebrow `--danger` 컬러(기존 후보, 파일 안정화됨)**: 파일은 안정 상태가 됐으나 eyebrow의 레드가 '레드=문제 전용' 원칙 위반인지, '하트(찜)' 색 통일 의도인지 단정 불가(OrderCard 하트·OrderDetailView 찜 영역도 `--danger` 계열). 이번에는 명백한 대비 결함만 수정하고 컬러 시맨틱 결정은 유보.
3. **아바타·FAB·그라디언트 CTA 흰 글자(`--accent`=다크 틸)**: 라이트 `--accent`는 어두운 검정(#1f1f1f)이라 정상이고 다크에서만 저대비 → `#07120e` 일괄 적용 시 라이트 대비가 역전된다. 전역 "brand 위 글자색/`--on-brand` 토큰" 설계 결정이 필요한 건으로 기존 분류 유지.
4. **기존 반복 후보(01:03·00:13 기록분 유지)**: QuickMatchPanel `.qmp-quick` 구조 충돌 · DashboardView `.settle-item` 상충 재사용 · coin+'원' 이중 표기 · '정산 대기중' 캐노니컬 · UiChip 기본 스타일 · MyMarketView FAB 글리프 · HomeView `.home-more`(12px/700) 신규 추가분의 공통 컴포넌트화 — 기존 분류 유지.
5. **HomeView '더보기' 버튼 3곳 중복 마크업**: 탭별로 동일한 `.home-more` 버튼이 반복되나 동시에 1개만 노출되어(탭 상호 배타) 사용자 혼란·Primary 충돌 없음. 컴포넌트화는 이득 대비 변경 범위가 커 보류.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 993ms, 4369 modules) — 수정 2개 파일 포함 전체 번들 오류·경고 없음.
- import 누락 점검: 수정이 전부 `<style>` 블록 내 색상값 변경(CSS 전용)이라 import·심볼 영향 0건 → 빌드 통과로 검증 대체. 수정 후 그렙 재확인: 4개 셀렉터 모두 `#07120e` 잔존·지속성 확인(OrderFavoritesView 235행, ChatImageSheet 942·1154·1266행).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
