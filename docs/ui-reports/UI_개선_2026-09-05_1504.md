# UI 개선 보고서 — 2026-09-05 15:04

## 실행 결과 요약
- **수정 1건** — 14:04(46회차) 이후 `frontend/src` 변경 6건(14:04~14:19: **차량·면허 증빙 심사(B-3) 기능 도입**: SettingsVerificationView 신규 + 설정 메뉴 진입점 + AdminView '증빙 심사' 탭 + verification API) 전수 열람. 신규 화면·탭은 한국어/"운행" 용어/상태 색 의미(대기=옐로우·승인=그린·거절=레드)/배지 체계를 잘 지켜 대체로 정합적. 유일한 명확한 소형 결함: **설정 메뉴의 '인증 상태' 카드가 역할 무관하게 노출 — 증빙 유형이 차량·면허(운전 자격)뿐인데 등록자/관리자에게도 보여 동일 화면의 기사 전용 '차량 설정' 카드(v-if="isDriver")와 불일치** → 기사 전용 게이팅으로 수정. 직전 회차(14:04) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 15:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 14:04 이후 `frontend/src` 변경 6건 — `views/orders/OrderDetailView.vue`(14:04:35, 46회차 수정 반영분) · `api/operations.js`(14:04:58) · `composables/useProfileSettings.js`(14:16:54, 주석만) · `api/verification.js`(14:17:02, 신규) · `views/settings/SettingsVerificationView.vue`(14:17:50, 신규) · `views/AdminView.vue`(14:19:00, 증빙 심사 탭 추가)
- **직접 열람**: 증빙 심사 신규분 전수 — SettingsVerificationView(전체, 템플릿+scoped CSS) · AdminView(증빙 심사 탭 템플릿 L1312-1384 + 로직 L824-889 + scoped CSS L2760-2910 + 처리 모달 L1797-1835 + 상단 탭 구조 L900-978) · SettingsView(메뉴 그리드·역할 게이팅 L41-77) · ProfileView(차량·인증 요약 카드 L162-188 — 관리 링크가 settings-verification로 정상 연결) · `api/verification.js`(전체) · `composables/useProfileSettings.js`(전체, 인증 신청 이관 주석 확인) · `api/operations.js`(전체) · `router/index.js`(settings-verification 라우트) · `assets/base.css`(색 변수: --status-completed #18a058/#27946a, --danger 등) · `utils/icons.js`(shield/image/check/refresh/inbox 등록 확인) · `app/Models/VerificationRequest.php`(typeOptions = 차량·면허만) · `app/Services/Verification/VerificationService.php`(역할 제한 없음 확인)
- **전역 감사(grep)**: `formatWon(...)원`·`원원` 단위 중복 0건 / font-size 10px 미만 0건 / 기술 식별자 노출은 AdminView 운영 개입 탭 2건(L1478·L1737 order_number)뿐 — 관리자 콘솔 설계상 노출로 기존 분류 유지 / 아이콘 'shield'·'image'·'check' 등록 확인(런타임 렌더 정상)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1404.md`(46회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **설정 메뉴 '인증 상태' 카드 역할 게이팅 누락(→수정)**: [SettingsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsView.vue#L63-L69)의 '인증 상태'(차량·면허 인증 신청) 카드가 모든 역할에 노출. 증빙 유형은 차량·면허(운전 자격)뿐이고, 같은 그리드의 '차량 설정' 카드가 `v-if="isDriver"`로 기사 전용인 패턴과 불일치. 등록자/관리자에게는 의미 없는 진입점이며 ProfileView의 차량·인증 요약 카드도 기사 전용(`v-if="isDriver"`)인 점과 어긋남. → 수정 대상.
2. **증빙 심사 화면 전체 정합성**: SettingsVerificationView는 상태 칩(인증 완료=민트·대기=옐로우·거절=레드·미인증=무채색) + 상태별 카드 본문(완료/대기-증빙 미리보기/거절-사유+재신청/미인증-신청 폼) + '정보→이유→행동' 순서·화면당 행동 1개로 정합. 대기 상태에선 신청 폼을 숨겨 중복 신청 방지, 폼 제출 시 이미지·미리보기·메모 초기화 후 재조회. 사진 피커는 선택 전후 전환(미리보기 + '사진 바꾸기') 정상. 거절 사유 박스(레드 틴트)는 문제 의미에 부합.
3. **AdminView 증빙 심사 탭**: [전체 상태/심사 대기/승인/거절] 상태 필터 + 이름 검색 + 대기 카드에 승인/거절 버튼(거절 시 사유 필수 — 신청자 전달 안내), 처리 후 상태 칩(대기=옐로우/승인=그린/거절=레드)과 처리자·처리 시각 표시로 일일 운영 체계(🔴>🟡>🟢)와 정합. 사용자 관리 탭의 기존 즉석 '승인/인증됨' 토글(증빙 미검토)과 신규 심사 탭이 공존하나, 관리자 직접 부여 오버라이드와 증빙 심사라는 역할 차이가 있어 유지 판단.
4. **이관 정합성**: useProfileSettings 주석으로 '차량·면허 인증 신청은 SettingsVerificationView 전담' 명시, SettingsProfileView에서 기존 인증 신청 UI 제거 확인, ProfileView 차량·인증 요약의 '관리' 링크가 settings-verification 라우트로 정상 연결 — 잔재·깨진 링크 없음.
5. **전역 캐너리 통과**: 단위 중복 0건 / 10px 미만 폰트 0건 / 신규 아이콘 전부 등록 확인(shield L141·image L155·check L119·refresh L122·inbox L176).

## (d) 실제 적용한 수정
- [SettingsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsView.vue#L63) — '인증 상태' 카드에 기사 전용 게이팅 추가:
  - 전: `<button type="button" class="settings-card" @click="router.push({ name: 'settings-verification' })">` (모든 역할 노출)
  - 후: `<button v-if="isDriver" ...>` (같은 그리드 '차량 설정' 카드의 `v-if="isDriver"` 패턴과 일치)
- 그 외 수정 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
- **SettingsVerificationView·AdminView 상태 칩 스타일 미세 차이**: `verify-chip`(2px 8px·700)와 `v-chip`(1px 8px·700)이 같은 의미색 체계인데 패딩이 다름 — 채움 상태 칩 계층이라 사용자 배지 스펙(1px 6px·400)과 별개로 보되, 두 파일 간 1px 차이는 대형 정리 시 통일 검토(후보).
- **신규 상태 칩의 백색 텍스트 대비(승인/완료=그린 #18a058 위 흰색 10px)**: naive-ui success 태그 팔레트와 동일한 관례적 조합이며 46회차까지 반복 미지적 — 전역 의미색 정책과 함께 검토(후보).
- **설정 '인증 상태' 카드 숨김으로 인한 등록자 접근 차단**: 차량·면허 외 다른 인증 유형(사업자 등)이 추가되면 게이팅 재검토 필요 — 현 스코프에선 운전 자격뿐이라 게이팅 타당(유지).
- **AdminView 사용자 탭 즉석 인증 토글 vs 증빙 심사 탭 중복**: 관리자 직접 부여(오버라이드)와 사진 심사(기록 기반) 역할 분리로 유지 — 프로세스 단일화는 운영 결정 필요(후보).
- **'인증 완료 후 마켓에서 배지 표시' 안내 문구**: 사용자 페이지·프로필에 배지가 노출되므로 정확한 범위 문구로 다듬는 것은 제품 결정(후보).
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, 홈 추천 카드 조건% 미노출, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) **통과** — built in 925ms. AdminView·SettingsVerificationView 청크 정상 생성.
- import 누락 점검: 수정이 SettingsView 템플릿의 `v-if` 1건 — `isDriver`는 동 파일 computed(L21)에 기정의되어 있어 신규 import 없음, 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
