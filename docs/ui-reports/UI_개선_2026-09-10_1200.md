# UI 개선 보고서 — 2026-09-10 12:00

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 직전 실행(11:00) 이후 커밋 상태가 바뀌었다(HEAD `c0c4a71` → `f240340`). 이번 시간대에는 **타 세션의 정산 수수료(fee_rate)·등록자 정산 화면 작업**(11:16~11:34, SettlementView·AdminView·MoreView·router·api/settlement.js·신규 RegistrantSettlementView)만 있었고, 11:00 보고서 적용분은 커밋 `f240340`에 포함됐다.
- 적용: **빠른매칭 패널 카드 하단 행동 버튼 `.qmp-card__view`의 라이트 모드 저대비 텍스트 수정** — `background: var(--brand)` + `color: #fff`(라이트 #36adff ≈2.3:1)였고, 같은 규칙에 `html.dark` 전용 예외(`#07120e`)만 있어 모드별로 규칙이 어긋난 상태였다. 앱 표준(양 모드 공통 `#07120e`)으로 통일하고 중복된 다크 예외를 제거했다.
- 전역 표준 스캔(용어·보조용언·소형 폰트·`rel="noopener"`·배지 규격) 신규 위반 0건 — 기존 클린 상태 유지.
- `npm run build` 통과. 로직·구조·신규 기능 변경 없음(전부 CSS 색상값).
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 12:02~12:10 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_1100.md`(적용 2파일·후보 분류 전수) — 반복 적용 없음 확인.
- **실행 전 상태 점검**: `git status`/`git log` — HEAD `f240340`(찜·태그 검색 + 9/9~9/10 UI 개선분 일괄 커밋). 워킹트리 미커밋분은 **타 세션의 정산 기능 작업**(`.env.example`, Settlement/Admin 컨트롤러·모델·서비스·마이그레이션 3건, `frontend/src/api/settlement.js`·`api/admin.js`·`router/index.js`·`views/SettlementView.vue`·`views/AdminView.vue`·`views/MoreView.vue`·신규 `views/RegistrantSettlementView.vue`) + `.trae/`.
- **mtime 점검**: `frontend/src` 최근 3시간 변경 9건 — RegistrantSettlementView(11:33, 신규)·router(11:34)·MoreView(11:34)·AdminView(11:33)·SettlementView(11:16)·api 2건(11:22·11:32) + 직전 적용분 2건(11:02·11:03, 커밋 완료). **타 세션 편집 진행 구간(11:16~11:34)은 읽기 전용으로만 검토하고 수정하지 않았다.**
- **직접 판독**: `views/RegistrantSettlementView.vue`(전량 287행 — 신규 등록자 정산·입금 화면: 헤더·스켈레톤·입금 대기 히어로·입금 계좌·정산 내역 칩), `components/orders/QuickMatchPanel.vue`(템플릿 600~615 카드 하단 행동 버튼 + scoped CSS 1015~1039, 파일 내 `--brand` 사용처 전수 834·843·890·950·988·1008·1026·1074), `views/OrderFavoritesView.vue`(전량 239행 — 찜 페이지: eyebrow·빈 상태 CTA·더보기·하트 콜백), `components/orders/OrderCard.vue`(찜 하트 CSS 473~496), `components/common/EmptyState.vue`(전량 — 아이콘 변형색·컴팩트 모드), `components/ui/UiListRow.vue`(전량), `assets/base.css`(토큰 L1~70 + white-on-fill 스팟 L165~184·515~527·580~596·762~791), `views/DashboardView.vue`(금액·단위 표기 L86~99·198~224·555~566·816~829), `views/SettlementView.vue`(정산 상태 칩·금액 CSS L320~575 — 타 세션 편집분, 읽기 전용), `views/SupportView.vue`(문의 상태 칩 L393~411).
- **전역 그렙**: `오더|주문번호`(0건) · `font-size: 6~9px`(0건) · 보조용언/맞춤법 붙여쓰기(2건 — "잘못되었거나"·"잘못되었습니다", 표준 표기로 정상) · `target="_blank"` 8건 전수 `rel="noopener"` 확인 · `padding: 1px Npx` 전수(60여 건 — 1px 5px/7px/8px 잔존분은 카운트 칩·채움 상태 칩 계층으로 기존 분류) · `color: #fff/#ffffff/white` 전수(80여 건 컨텍스트 — 아바타·FAB·토스트·danger 버튼·그라디언트 관용) · `<n-spin` 5건 · FAB 3곳 `aria-label` 확인.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 배지·색상 표준, 메모리 규칙(한국어 UI / "운행" 용어 / 다크 대비 표준 `#07120e`).

## (c) 발견한 개선 포인트
1. **빠른매칭 카드 버튼 라이트 모드 저대비(신규 발견)**: [QuickMatchPanel.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1016-L1034) `.qmp-card__view`("마켓에서 빠른매칭 보기")가 `background: var(--brand); color: #fff`. 라이트 brand `#36adff` 위 흰 글자는 ≈2.3:1로 읽기 어렵고, 같은 파일에 `html.dark .qmp-card__view { color: #07120e }` 예외만 있어 **모드별 규칙이 어긋난 미완성 상태**였다. 앱 표준("밝은 brand 채움 위 어두운 글자 `#07120e`", HomeView·MarketView·SettlementView·MessageBubble 등 다수)과 동일 목적 버튼(MarketView 필터 적용 버튼 1513~1524)이 이미 표준 적용돼 있어 최소 변경으로 정리했다. **적용.**
2. **등록자 정산 화면(신규 RegistrantSettlementView) 표준 준수 확인**: 배지 규격(`.settle-recent__status` = padding 1px 6px / font-size 10px / font-weight 400), "운행" 용어, 기술 식별자 미노출, 상태색(입금 대기=brand 틴트 / 입금 확인=`--status-settled`)이 [SettlementView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SettlementView.vue#L548-L559)의 기존 패턴과 동일 — **위반 없음, 수정 없음.**
3. **표준 스캔 신규 위반 0건**: 용어(오더/주문번호)·10px 미만 폰트·`rel="noopener"` 누락·FAB `aria-label` 누락·한국어 보조용언 붙여쓰기 모두 0건. EmptyState·UiListRow·base.css 공용 규칙, OrderCard 찜 하트(호버/활성 `--danger`), DashboardView 금액 카드, SupportView 문의 상태 칩은 기존 표준·기존 분류 준수 확인.
4. **기존 반복 후보(상태 재확인, 유지)**: FAB 3곳 흰 글리프(MarketView·CommunityView·MyMarketView) · OrderFavoritesView eyebrow `--danger` · `qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀(백엔드 동기 필요) · EmptyState 아이콘 변형색 하드코딩 · `--accent` FAB·아바타·그라디언트 CTA 흰 글자(전역 "brand 위 글자색" 설계 대기).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/orders/QuickMatchPanel.vue` | `.qmp-card__view`(1016행) 글자색 `#fff` → `#07120e` + 표준 근거 주석 추가, 중복·불일치였던 `html.dark .qmp-card__view { color: #07120e }` 예외 블록 제거(1032~1034). 두 모드 공통으로 어두운 글자가 적용돼 라이트·다크 모두에서 버튼 텍스트가 읽힌다. 배경·패딩·폰트·동작·템플릿 무변경 |

- 공통: 색상값만 변경. 템플릿·스크립트·props·emit 무변경이라 런타임 로직 영향 0건.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **타 세션 작업 구간(11:16~11:34) 일체 — SettlementView / AdminView / MoreView / router / api**: 정산 수수료(fee_rate)·등록자 정산 기능이 **진행 중인 편집 구간**이라 충돌 위험을 피해 읽기 전용으로만 검토했다. 판독한 범위(SettlementView 상태 칩·금액 CSS, RegistrantSettlementView 전량)에서는 표준 위반을 찾지 못했다.
2. **FAB 3곳(MarketView `.market-fab`·CommunityView `.community-fab`·MyMarketView `.my-market-fab`) `var(--brand)` + 흰 글리프**: 09-03~09-10 여러 회차에서 "아이콘 관용 + 전역 brand 위 글자색 설계 결정" 후보로 분류된 항목이다. 세 버튼 모두 `aria-label`은 갖추고 있고, 일괄 변경 시 라이트/다크 색 규칙을 전역으로 다시 설계해야 하므로 기존 분류 유지(반복 미적용).
3. **OrderFavoritesView eyebrow `--danger`(177~182행)**: 하트(찜) 색 관례가 `--danger`(OrderCard `.order-card__fav--on` 494~496행)라 페이지 헤더 레드가 '레드=문제 전용' 원칙 위반인지, 하트 색 연결 의도인지 단정 불가 — 컬러 시맨틱 결정 사안으로 유보(기존 후보 유지).
4. **`<n-spin :show="false">` 데드 래퍼(OrderFavoritesView 117행, MarketView 1200행)**: 항상 표시하지 않는 스피너 래퍼로 시각적 영향은 0(레이아웃 래퍼 역할만). 2개 화면이 같은 형태를 쓰고 있어 정리 필요성은 낮고, 제거 시 회귀 위험 대비 이득이 작아 후보로만 기록.
5. **DashboardView 금액 단위 표기 불일치**: 매출 카드(213행)는 `{{ … }}원`을 18px/700로 렌더해 단위가 숫자와 같은 굵기인 반면, 평점(220행)은 `.dash-card__unit`(11px/500/muted)을 쓰고 기사 요약 카드(96행)는 `<small>원</small>`을 쓴다. 다만 SettlementView 히어로 금액(`.settle-hero__amount`)도 단위를 같은 크기로 렌더하는 앱 관례이므로, 화면 간 타이포 계층 결정 사안으로 후보 기록(직접 수정하지 않음).
6. **SupportView `.support-ticket-item__status`(398~404행) `1px 8px / 10px / 700`**: 색 채움이 있는 상태 칩이라 "채움 상태 칩 계층(1px 8px·700)"으로 기존 분류된 그룹에 해당 — 사용자 정보 배지 스펙(1px 6px·400) 적용 대상이 아니므로 유지.
7. **기존 반복 후보(11:00·01:03·00:13 기록분 유지)**: `qmp-quick` 구조 충돌 · DashboardView `.settle-item` 상충 재사용 · coin+`원` 이중 표기 · '정산 대기중' 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · EmptyState variant 색 하드코딩 · UiChip 기본 스타일 · MyMarketView FAB '＋' 글리프 · HomeView `.home-more`(12px/700) 공통화 · `--accent` FAB·아바타·그라디언트 CTA 흰 글자(전역 `--on-brand` 토큰 설계) — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 1.31s, 4371 modules) — 수정 파일 포함 전체 번들 오류·경고 없음. MarketView·QuickMatchPanel 청크 정상 생성.
- import 누락 점검: 수정이 `<style>` 블록 내 색상값 변경 + 중복 다크 예외 제거(CSS 전용)라 import·심볼 영향 0건 → 빌드 통과로 검증 대체. 수정 후 그렙 재확인: `.qmp-card__view`가 `color: #07120e`(1034행 부근)로 잔존하고 `html.dark .qmp-card__view` 예외는 제거됨(지속성 확인).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
