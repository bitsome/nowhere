# UI 개선 보고서 — 2026-09-04 15:02

## 실행 결과 요약
- **수정 1건** — 전 앱에서 유일하게 라벨 텍스트(#checked/#unchecked)를 넣은 naive `n-switch`(AdminView 자동 운행 스위치)의 켜짐 레일 글자 저대비를 해소. 켜짐 레일을 `var(--brand)`(라이트 `#36adff`·다크 `#63e2b7` — 모두 밝음)로 채우면서 naive 기본 글자색(라이트 흰색·다크 밝은 회색)을 그대로 둔 지점으로, 그동안 전 화면에서 정리한 **"밝은 채움 위 흰/밝은 글자" 저대비 결함 계열의 신규 잔존**. 14:01(33회차) 이후 신규 소스 변경 없음(OrderDetailView 14:04 터치는 33회차 자체 수정).

## (a) 실행 시각
- 2026-09-04 15:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 전체 열람 후 여러 차례 개별 패치만 되던 관리 화면**):
  - `views/AdminView.vue`(전체 873줄 — 운영 관리: 사용자 관리(인증 승인/해제)·기사 관리(상태 전환 n-select)·자동 운행(설정 카드 + 자동 등록 이력/삭제) — script L1-243·템플릿 L245-531·CSS L533-873 전수)
- naive 스위치 테마 실측(전역 대조): `node_modules/naive-ui/es/switch/styles/light.mjs`(`textColor: 'white'`·`railColor: 'rgba(0,0,0,.14)'`), `dark.mjs`(`textColor: textColor2`·off 레일 `rgba(255,255,255,.20)`), `src/styles/index.cssr.mjs`(라벨 `.n-switch__checked`/`__unchecked`가 `color: var(--n-text-color)` — L57-67), `src/Switch.mjs`(활성 시 루트에 `n-switch--active` — L280, 텍스트 슬롯 렌더 — L324-330) — 이번 수정의 대비 근거
- 전역 스팟(직접 열람): `components/orders/QuickMatchPanel.vue`(자체 제작 토글 `.qmp-switch` — naive 아님, 노브 `#07120e` 12:07 적용 유지), `components/chat/MessageBubble.vue` L184(`background:var(--brand);color:#07120e` — brand 채움 위 어두운 글자 표준의 원천)
- 14:01 수정 유지 확인(grep): OrderDetailView `rideBarTextColor`(L115)·`:text-color="rideBarTextColor"`(L1217)·`.detail-actionbar__step--dark`(L1224·L2299) 존재 — 33회차 적용 유지
- 전역 감사(grep): `n-switch` + `#checked`/`rail-style` 사용처 전수(**AdminView L406-414 1곳뿐** — 텍스트 슬롯 스위치는 이곳이 유일), 10px 미만 폰트 전수(0건), 고정 `text-color="#ffffff"` 전수(0건), "오더/주문번호" .vue 노출 전수(0건), `background: var(--brand)` 채움 41건 컨텍스트(전부 기존 `#07120e`/`#101418` 표준 적용분 또는 FAB/아이콘 관용)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1401.md`(33회차 — OrderDetailView 하단 진행 바)·`1305.md`·`1201.md` 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 14:01 이후 첫 실행)
- 작업 트리 확인: 14:01 이후 `frontend/src` 소스 변경은 OrderDetailView.vue(14:04 — 33회차 자체 수정) 1건뿐 — **신규 코드 리뷰 대상 없음** → 이번 회차는 전체 열람 이력이 가장 오래된 AdminView를 재열람
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **AdminView 자동 운행 스위치 — 밝은 brand 레일 위 naive 기본 글자색 저대비 (신규 발견·적용)**: 자동 운행 설정 카드의 `n-switch`(L406-414)는 켜짐일 때 `:rail-style`으로 레일을 `var(--brand)`로 채우지만, 라벨("시작"/"중지")의 글자색은 naive 기본값 그대로다. naive 스위치 라벨(`.n-switch__checked`/`__unchecked`)은 `color: var(--n-text-color)`를 쓰고, 라이트 테마는 `textColor: 'white'`·다크 테마는 `textColor2`(밝은 회색)로 정의 — 라이트·다크 **두 모드 모두 밝은 레일(`#36adff`/`#63e2b7`) 위 밝은 글자 = 대비 약함**(≈2:1). 전 앱 grep 결과 라벨 텍스트 슬롯을 쓰는 `n-switch`는 이곳뿐이며, 14:01(33회차)·11:04(30회차) 등에서 정리한 "밝은 채움 위 글자색" 결함 계열의 **유일한 잔존**으로 판정. QuickMatchPanel의 토글은 naive가 아닌 자체 제작이라 별개이고, brand 채움 위 어두운 글자(`#07120e`)는 MessageBubble 등 앱 표준 원천이 확인됨.
2. **AdminView 나머지 클린 (신규 전체 확인)**: 사용자/기사 탭 카드·상태 태그(밝은 상태 6종 `#101418` — 17:01 적용 유지)·인증 상태 success n-button(naive 의미색 관용)·n-select 상태 전환·페이지네이션 "이전/다음"·자동 등록 이력 배지(1px 6px/10px/400, `autoStatusColor`/`autoStatusTextColor` 표준)·EmptyState 문구("사용자가 없습니다" 등)·한국어·"운행" 용어 — 전부 정상. Primary("설정 저장") 1개 + 위험 동작("전체 삭제" error) 구분 — 화면당 Primary 1개 원칙 준수. 신규 위반 없음.
3. **전역 캐너리 통과**: 텍스트 슬롯 스위치 잔존 AdminView 1곳뿐(이번 수정), 10px 미만 폰트 0건, 고정 `text-color="#ffffff"` 0건, "오더/주문번호" 0건, brand 채움 흰 글자 신규 0건. 14:01 OrderDetailView 수정 유지 확인.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/AdminView.vue` | CSS 추가 — `.auto-card :deep(.n-switch.n-switch--active .n-switch__checked/unchecked)` `color: #07120e`(켜짐: 레일이 밝은 brand → 두 모드 공통 어두운 글자) |
| `views/AdminView.vue` | CSS 추가 — `.auto-card :deep(.n-switch__checked/unchecked)` `color: #07120e`(라이트 기본: 꺼짐 연한 레일(`rgba(0,0,0,.14)`) 위 naive 흰 글자 가독성 해소) + `html.dark` 동일 선택자 `color: initial`(다크 꺼짐: 짙은 레일 위 naive 기본 밝은 글자 유지) — 상태·모드별 최종 규칙 3종, 근거 주석 포함 |

`<style scoped>` CSS 3규칙·주석만 추가(템플릿 구조·script·import 무변경). specificity 설계로 다크에서 active 규칙(0,4,0)이 reset 규칙(0,3,1)보다 우선해 켜짐은 항상 `#07120e`, 꺼짐은 라이트 `#07120e`/다크 naive 유지 — 네 모드×상태 조합 모두 대비 확보. 구조·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **AdminView `page-head__desc` "기사 차량·면허 인증을 승인하거나 해제합니다."**: 화면이 사용자/기사/자동 운행까지 다루는 점을 고려하면 설명이 좁으나, 관리 화면 문구 방향(제품 문구 vs 현황형)은 제품 결정 사항 — 후보 기록(기존 회차 반복 미적용).
- **인증/상태 success n-button·n-tag(brand 대신 naive 기본색)**: naive 컴포넌트 테마 전역 설계 후보(라이트 primary 초록 등 기존 분류와 동일 계열) — 단독 파일 수정 시 다른 naive 요소와 어긋남 우려(기존 반복 후보 유지).
- **자동 등록 이력 개별 삭제 버튼(trash 아이콘) 등 아이콘/관용 지점·AdminView 그 외 데드 CSS 없음 확인**: 아이콘 글리프 관용 — 유지.
- **HomeView 탭 카운트 불일치·OrderDetailView `.detail-hero` 흰 글자·naive 라이트 primary 초록·`.v8-noti` 데드 CSS·RegisterView CSS 복제 등 기존 후보**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 920ms`, 4347 modules transformed, 에러 0건(exit 0). AdminView CSS 청크 반영(5.18 kB) 확인.
- 수정 후 grep 검증: AdminView에 `.n-switch--active` deep 규칙·`html.dark` reset 규칙·근거 주석 존재 확인 — 3/3.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 규칙·주석 추가뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음, 빌드 성공으로 재확인).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
