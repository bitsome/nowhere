# UI 개선 보고서 — 2026-09-09 19:02

## 실행 결과 요약
- **코드 수정 7건(4개 파일)** — 직전 실행(18:09) 이후 `frontend/src` 신규 변경은 HomeView.vue(18:11, 역할별 홈 플래시 가드 — 외부 작업)뿐이었고, UI 보고서 적용 내역과 겹치지 않는 신규 항목만 직접 수정했다. 검색 에이전트 2개를 병행해 최근 보고서에서 정밀 판독이 빠졌던 파일(OrderDetailView·OrderCreateView·OrderDetailChat·ChatThread·ChatRequestEvent·MoreView·MyMarketView·RideHistoryView·CommunityPostView 등)을 전수 감사했다.
- 신규 적용 3유형: ① 사용자 노출 문구 '대기중' 붙여쓰기 2건(의존명사 '중' 띄어쓰기 표준 위반 — '요청 대기 중'·'수락 대기 중') ② OrderDetailView 미사용 데드 CSS 3블록 제거(.hero-badge--today/--tomorrow·.detail-text--urgent) ③ OrderCreateView 동일 선택자 중복 정의 2건 병합·제거(.create-tags·.schedule-list — 표시 변화 없는 중복).
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 19:02~19:13 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_1809.md`·1702·1602·1401 — 적용 내역·후보 전수 대조, 반복 적용 없음. 18:09 이후 변경분은 git 델타·mtime 기준 HomeView.vue(18:11, 역할 분기 플래시 가드 — 외부 작업, 방향 타당성만 확인 후 보존)뿐임을 확인.
- **병행 작업 주의**: 실행 중 QuickMatchPanel.vue가 19:02:40에 타 세션에 의해 갱신되는 것을 관찰(git diff HEAD +50행 증가). 충돌 방지를 위해 해당 파일은 검토·수정에서 제외.
- **검색 에이전트 전수 감사 2건**(직접 Read 전수 + 프로젝트 표준 교차 확인):
  - (1) `OrderDetailView.vue`(2,545행 전량)·`components/orders/OrderDetailChat.vue`·`components/chat/ChatThread.vue`·`ChatRequestEvent.vue`·`MoreView.vue`·`settings/SettingsProfileView.vue`
  - (2) `orders/OrderCreateView.vue`·`orders/MyMarketView.vue`·`orders/RideHistoryView.vue`·`settings/SettingsAppearanceView.vue`·`SettingsView.vue`·`community/CommunityPostView.vue`
- **수정 전후 직접 판독**: OrderDetailView.vue(426·2082~2110·2257~2269행), OrderCreateView.vue(900~905·955~966·1022~1028·1089~1094행), useOrderDetail.js(206~214·668~671행), OrderCard.vue(101행).
- **전역 그렙**: `대기중|수락대기|요청 대기` 전수 — 잔존 '정산 대기중'은 백엔드 캐노니컬 문자열(아래 (e)-1). 보조용언 붙여쓰기·조사 오류·'오더' 용어·기술 식별자·7~9px 폰트 — 신규 0건(직전 보고서 상태 유지).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피 표준(보조용언 띄어쓰기·의존명사 '중'), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙).

## (c) 발견한 개선 포인트
1. **사용자 노출 '대기중' 붙여쓰기 2건(명백한 표준 위반)**: [OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L426) 히어로 배지 '요청 대기중', [useOrderDetail.js](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderDetail.js#L670) 하단 액션 버튼 라벨 '수락대기중'. 앱 전반은 의존명사 '중'을 띄어 쓰는 표준('승인 대기 중'·'운행 중'·'처리 중' 다수, 같은 파일 508·510행 '대기 중')이라 붙여쓰기가 국소 아웃라이어 → **적용**(관련 주석 4건 동기화 포함).
2. **OrderDetailView 미사용 데드 CSS 3블록(명백한 잔재)**: `.hero-badge--today`·`.hero-badge--tomorrow`(2104~2110행)는 템플릿 배지가 --waiting/--priority/--urgent뿐이고, `.detail-text--urgent`(2267~2269행)는 파일 내 사용처 0건(그렙 확인) → **적용**(제거).
3. **OrderCreateView 동일 선택자 중복 정의 2건(명백한 CSS 결함)**: `.schedule-list`(901·1090행, gap 18px→10px로 후자가 덮어씀), `.create-tags`(960·1023행, 중복 블록이 열 방향 레이아웃과 충돌) — 템플릿 사용처 1곳씩. 후자 블록만 유지·병합하면 표시 변화 없음 → **적용**.
4. 신규 위반(기술 식별자·용어·소형 폰트·배지 스펙) 0건 — 직전 보고서 상태 유지.

## (d) 실제 적용한 수정
1. `views/orders/OrderDetailView.vue:426` — '요청 대기중' → '요청 대기 중'(히어로 배지).
2. `composables/useOrderDetail.js:670` — 액션 버튼 라벨 '수락대기중' → '수락 대기 중'.
3. `composables/useOrderDetail.js:373·668` — 동일 표기 주석 2건 띄어쓰기 동기화('요청 대기 중 배지'·'수락 대기 중').
4. `views/orders/OrderDetailView.vue` — 데드 CSS 제거: `.hero-badge--today`·`.hero-badge--tomorrow`·`.detail-text--urgent`(3블록).
5. `views/orders/OrderCreateView.vue` — `.schedule-list` 상단 중복 정의(901~905행) 제거(하단 10px 정의 유지, 표시 불변).
6. `views/orders/OrderCreateView.vue` — `.create-tags` 상단 중복 정의(960~966행) 제거 + 하단 정의(1023행)에 `margin-bottom: 14px` 병합(기존 표시 보존).
7. `components/orders/OrderCard.vue:101`·`composables/useOrderDetail.js:208` — '수락대기' 개념 주석 2건 → '수락 대기' 띄어쓰기 동기화(코드 참조 정확성).

## (e) 적용하지 않은 항목과 이유 (후보)
1. **'정산 대기중' 잔존 5건(후보로 전환)**: OrderDetailView에서 '대기중' 표준화를 하다 발견. 이 문자열은 **백엔드 캐노니컬 라벨**(`app/Services/Driver/DriverService.php:159`·`app/Services/Order/OrderListService.php:65`, `tests/Feature/Api/OrderApiTest.php:1633`이 `statusLabel === '정산 대기중'`을 단언)이고, 프론트 `DashboardView.vue:245`·`useOrderDetail.js:214`도 사용. 프론트만 고치면 API 라벨과 화면 간 단차가 생겨 **백엔드+테스트 일괄 교정으로만 의미 있음** → 백엔드 범위 포함이라 UI 보고서 단독 실행에서는 후보로만 기록(다음 전면 정비에서 처리).
2. **OrderDetailView 승인 대기 상태의 상태 문구 3중 반복 + Primary 2개 동시 노출**(신규, 구조적): 히어로 배지 '요청 대기 중' + 승인 대기 카드('등록자 승인 대기 중') + 하단 액션 '수락 대기 중' 버튼이 같은 상태를 중복 안내하고, 카드 CTA('채팅으로 승인 요청')와 하단 indicator 버튼이 동시 노출됨 → 노출 조건 조정은 상태 흐름 로직 변경이라 후보로 기록.
3. **히어로 배지/그라디언트 대비**(기존 후보 갱신): `.hero-badge--waiting`(brand 채움 + 흰 글자) 및 히어로 전체의 흰 글자 — 파일 내부엔 이미 '#07120e' 규칙(1541~1543행)이 있어 기준은 명확하나, 히어로 오버레이 배지·그라디언트는 전면 재설계가 필요한 'brand/accent 위 글자색 전역 설계' 후보의 일부 → 기존 분류 유지.
4. **OrderCreateView 추가 데드 CSS**(후보): `.create-search`·`.create-sort` 계열(651~692행)·`.filter-body`·`.filter-label`·`.filter-amount-*`(969~1002행)은 템플릿 사용처 0건(단 `.filter-footer`는 템플릿 모달에서 재사용 중 → 유지). 다중 블록 제거라 대규모 데드 CSS 정리 후보로 기록.
5. **기타 신규 후보**: ① MyMarketView FAB 흰 글리프(다크 대비, 같은 파일 정산 CTA는 #07120e — 'brand 위 글자색' 후보에 포함) ② MyMarketView 정산 탭 빈 상태 문구·CTA 문맥(18:09 기록분 유지) ③ RideHistoryView 총 건수(`pagination.total`) vs 금액 합계(단건 기준) 집계 기준 불일치 가능성 ④ '수락' vs '승인' 용어 단차 ⑤ ChatThread 운행 요청·첨부·보내기 아이콘 버튼 aria-label 부재 ⑥ OrderDetailView `.detail-driver-only` 다크 보더 대응 누락 — 디자인/제품 판단 여지로 후보.
6. **기존 반복 후보(18:09 기록분 유지, 상태 재확인)**: coin 아이콘+"원" 이중 표기 · UiChip 기본 스타일 배지화 · hero-badge--urgent · 채팅 목록 탭 선택 초기화 · '지난주' 칩 단차 · 승인 요청 칩 색 · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 칩 색 · DashboardView 데드 CSS · 레벨 타이틀 "드라이버" · 홈 추천 조건% 미노출(의도된 결정) · auth CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · 문의 placeholder(SupportView) · MarketView 데드 CSS · 등록자 가입 직행 · `role === 'Driver'` 리터럴 상수 치환 · RegisterView 설명 문구 · AdminView.vue 잔여 구간 판독 — 기존 분류 유지.
- 참고: QuickMatchPanel.vue의 19:02 외부 변경분(+50행)과 HomeView.vue 18:11 변경분은 타 세션 작업으로 UI 보고서 범위에서 제외(미커밋 상태 보존).

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 972ms) — 수정 4개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 이번 수정은 전부 문자열 리터럴·CSS 규칙 제거/병합·주석이라 import 추가·제거·신규 심볼 참조 0건. `npm run build`(템플릿·CSS 컴파일 포함) 통과로 검증 대체. 수정 후 그렙 재확인: '요청 대기중/수락대기중/hero-badge--today/--tomorrow/detail-text--urgent' 잔존 0건, `.create-tags`·`.schedule-list` 정의 각 1건 유지.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분(4파일)과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
