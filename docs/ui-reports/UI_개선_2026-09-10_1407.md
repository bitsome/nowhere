# UI 개선 보고서 — 2026-09-10 14:07

## 실행 결과 요약
- **코드 수정 5건(5개 파일)** — ① 등록자 홈 Primary CTA의 **다크 모드 글자 대비 결함**(`--accent` 채움 위 흰 글자) 수정 ②③ 사용자 화면 상태 칩 2곳의 **배지 규격 위반**(padding 2px 8px·1px 8px / font-weight 700)을 프로젝트 배지 컨벤션(1px 6px / 10px / 400)으로 통일 ④⑤⑥ 아코디언(펼침) 토글 버튼 5곳에 **`aria-expanded` 누락** 보완(같은 앱의 RideHistoryView·OrderCreateView는 이미 보유한 자체 규약).
- 직전 실행(13:01) 이후 `frontend/src` 신규 편집은 0건(최신 mtime = 13:04:51, 직전 보고서 적용분) — 안정 상태에서 검토.
- 전역 표준 스캔(용어·10px 미만 폰트·하드코딩 상태색·`service_date`·금액 표기·`title`/`aria-label`) 신규 위반 0건. 신규 발견 위반은 위 3계열뿐.
- `npm run build` 통과(✓ built in 1.07s, 4371 modules). `<script>`·import·로직 변경 0건(템플릿 속성·스코프 CSS만).
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 14:07~14:15 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_1301.md`(적용 4건) · `2026-09-10_1200.md`(`.qmp-card__view` 라이트 저대비 → `#07120e` 규격 통일) — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`/`git log`(HEAD `f240340`, 전부 미커밋) + `frontend/src` mtime 상위 25건 — 13:04 이후 신규 편집 0건.
- **전량 판독**: `views/settings/SettingsVerificationView.vue`(456행 — 역할별 인증 항목·상태 판정·신청 폼·상태 칩 CSS), `views/SupportView.vue`(456행 — 공지/FAQ 아코디언·1:1 문의·내 문의 상태 칩), `views/home/CustomerHome.vue`(150행 — 등록자 홈 히어로 CTA·빠른 메뉴), `views/settings/SettingsView.vue`(166행)·`SettingsProfileView.vue`(101행)·`SettingsVehiclesView.vue`(203행)·`SettingsNotificationsView.vue`(106행)·`SettingsAppearanceView.vue`(111행), `components/common/VerifiedBadge.vue`(28행)·`SegmentedGroup.vue`(64행)·`LevelBadge.vue`(225행)·`EmptyState.vue`(122행), `components/ui/UiChip.vue`(93행)·`UiListRow.vue`(110행), `utils/colors.js`(98행 — 중앙 팔레트).
- **부분 판독**: `components/orders/OrderCard.vue`(긴급도 색 738~830행 + `html.dark` 개별 예외 확인), `components/layout/HeaderBar.vue`(script 전량 + 템플릿 172~268행 — `PAGE_LABEL`·`showBack`·우측 액션), `views/NotificationsView.vue`(script 20~40행 + 템플릿 280~371행 — 우선순위 카드·필터·날짜 그룹 토글), `views/orders/ActionCenterView.vue`(288~420행 — 가져오기/요금 제안 아코디언), `views/orders/OrderDetailView.vue`(히어로 배지·신고 버튼 CSS 2115~2225행), `views/AdminView.vue`(수수료율 모달 2418~2451행·수금/출금 탭 1739~1810행·등록자 행 1495~1520행), `views/SettlementView.vue`·`RegistrantSettlementView.vue`(상태 칩 CSS), `views/ReviewsView.vue`(탭 활성·아바타 CSS 255~285 / 412~445행), `assets/base.css`(토큰 1~120행 · `.card`/`.btn` 계열 465~534행 · `.badge`·`verify-*` 553~792행), `router/index.js`(라우트 이름 전수).
- **전역 그렙**: `오더|주문번호|order_no`(0건) · `[가-힣](해주세요|해보세요|해드립니다|…)`(0건 — 보조용언 띄어쓰기 준수) · `font-size: [5-9]px`(0건) · `background: var(--accent)`(4건 전수 확인) · `#18a058|#e5484d|#ffa940|#2e…)`(43건 — 대부분 `var(--status-*)`/토큰 정의/`html.dark` 개별 예외 동반) · `aria-expanded`(2건 → 보완 후 7건) · `expanded|collapsed|--open`(토글 42행 전수) · `router.back()`/`*-back`(8건) · `toLocaleString('ko-KR')`(19건) · `service_date`(49건 — 표기 일관) · `→|▸|▾`(UI 노출 2건) · `^\.(v-chip|…__status|…__vip|…__priority)`(배지 정의 8곳 전수).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 배지·색상 표준(1px 6px / 10px / 400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출 / 다크 모드 대비).

## (c) 발견한 개선 포인트
1. **등록자 홈 Primary CTA의 다크 모드 저대비(신규 발견)**: [CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L88-L104) `.customer-hero__cta`는 `background: var(--accent)` + `color: #ffffff`다. 라이트(`--accent`=#1f1f1f)는 정상이나 **다크에서 `--accent`가 밝은 틸(#63e2b7)로 바뀌어 흰 글자 대비가 ≈1.6:1**로 떨어진다(AA 4.5:1 미달). 앱은 같은 문제를 이미 국소 예외로 해결하고 있다 — [ReviewsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ReviewsView.vue#L264-L273)(탭 활성)과 [HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1079-L1082)(히어로 CTA)가 `html.dark { color: #07120e }`를 적용 중이다. 등록자 홈 CTA만 누락돼 있어 **같은 규약을 적용**했다. **적용.**
2. **인증 상태 칩의 배지 규격 위반(신규 발견)**: [SettingsVerificationView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsVerificationView.vue#L316-L327) `.verify-chip`이 `padding: 2px 8px` / `font-weight: 700`이었다. 그런데 **동일한 상태값(완료·미인증)을 보여주는 회원정보 화면은 [base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L750-L766) `.verify-row__done`/`__pending`(1px 6px / 10px / 400)** 을 쓴다. 프로젝트 배지 컨벤션(1px 6px / 10px / 굵기 제거)에 맞춰 통일했다. **적용.**
3. **고객지원 문의 상태 칩의 동일 규격 위반(신규 발견)**: [SupportView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SupportView.vue#L398-L412) `.support-ticket-item__status`가 `padding: 1px 8px` / `font-weight: 700`. 같은 성격의 상태 칩·배지 대부분(정산 내역·카드 상태·홈 태그·더보기 VIP)이 1px 6px / 400을 쓰므로 2번과 함께 통일했다. **적용.**
4. **펼침(아코디언) 토글의 `aria-expanded` 누락 5곳(신규 발견)**: 같은 앱의 접기/펼치기 헤더는 [RideHistoryView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/RideHistoryView.vue#L459-L464)·[OrderCreateView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue#L220-L226)에 `:aria-expanded`가 이미 붙어 있으나, 공지/FAQ 아코디언(2)·처리할 일 아코디언 카드(2)·알림 날짜 그룹(1)은 상태가 스크린리더에 전달되지 않았다. **적용(5건).**
5. **표준 스캔 신규 위반 0건**: "오더"/주문번호 노출 0건, 10px 미만 폰트 0건, 보조용언 붙여쓰기 0건, 아이콘 전용 버튼 `aria-label` 누락 0건(직전 회차 보완분 유지), 금액 중복 단위(`formatWon(...)원`) 0건. 이번에 새로 확인한 `SettingsView`/`SettingsProfileView`/`SettingsVehiclesView`/`SettingsNotificationsView`/`SettingsAppearanceView`/`VerifiedBadge`/`SegmentedGroup`/`LevelBadge`/`UiChip`/`UiListRow`/`EmptyState`는 배지·국제화·"운행" 용어·다크 대응 모두 기존 표준 준수(수정 없음). `OrderCard`의 하드코딩 긴급도 색 4종은 `html.dark` 개별 예외를 동반하고 있어 정상.
6. **기존 반복 후보(상태 재확인, 유지)**: FAB 흰 글리프 3곳 · `--accent` 아바타 흰 글자(base.css `.chat-list__avatar`·ReviewsView `.review-item__avatar`) · EmptyState 아이콘 변형색 하드코딩 · `qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · MyMarketView FAB '＋' 글리프 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · OrderFavoritesView eyebrow `--danger` · coin+`원` 이중 표기 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/home/CustomerHome.vue` | `.customer-hero__cta`에 다크 전용 예외 추가 — `html.dark .customer-hero__cta { color: #07120e }`(+사유 주석). 라이트는 기존 흰 글자 유지(어두운 채움). 등록자 홈 Primary CTA의 다크 대비 1.6:1 → 10:1 수준 |
| `views/settings/SettingsVerificationView.vue` | `.verify-chip` `padding: 2px 8px` → `1px 6px`, `font-weight: 700` → `400`(주석에 배지 컨벤션 근거 명시). 함께 `--none` 변형의 중복 `font-weight: 400` 선언 제거 |
| `views/SupportView.vue` | `.support-ticket-item__status` `padding: 1px 8px` → `1px 6px`, `font-weight: 700` → `400` — 문의 상태 칩을 배지 규격으로 통일 |
| `views/SupportView.vue` | 공지 아코디언 머리(135행)·FAQ 아코디언 머리(162행) 버튼에 `:aria-expanded="openedPostId === post.id"` 추가 |
| `views/orders/ActionCenterView.vue` | 가져오기 신청 아코디언 머리(297행)·요금 제안 아코디언 머리(387행)에 `:aria-expanded` 추가 |
| `views/NotificationsView.vue` | 날짜 그룹 토글(330행)에 `:aria-expanded="isExpanded(group.label)"` 추가 |

- 공통: `<script>`·props·emit·import·DOM 구조 무변경(템플릿 속성 1개 추가 + 스코프 CSS 값만 변경) → 런타임 로직·데이터 흐름 영향 0건. CSS는 스코프드라 타 화면 파급 없음. 동일 파일 복수 수정은 순차 편집(원복 방지) 후 `git diff`로 재확인.
- 수정 후 확인: `aria-expanded` 5건(Support 135·162, ActionCenter 297·387, Notifications 330) 잔존 / `.verify-chip` 1px 6px·400 / CustomerHome `html.dark` 규칙 1건 — 지속성 확인.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **관리자 화면 상태 칩 4곳(AdminView `.report-card__status` 1px 8px·700 / `.op-order__status` 2px 8px·700 / `.v-chip` 1px 8px·700 / `.support-ticket-chip` 1px 8px·700)**: 이번 회차에 통일한 2곳과 동일한 형식 위반이지만, 관리자 화면은 정보 밀도가 높은 운영 도구로 굵은 상태 칩이 의도된 구분일 수 있고 역대 회차에서 "관리자 화면 배지 1px Npx 잔여분"으로 분류돼 왔다. 기사 앱 화면 우선 원칙에 따라 이번에도 후보로만 기록.
2. **`assets/base.css`의 레거시 `.card`·`.btn`·`.btn--primary`·`.btn--ghost`·`.btn--danger`·`.btn--block`(465~534행)**: `class="btn…"`/`class="card"` 사용처가 0건인 죽은 CSS임을 이번에 확정했으나, 제거는 UI 개선이 아닌 정리 작업이고 동적 클래스 결합 가능성을 완전히 배제할 수 없어 후보 유지.
3. **HeaderBar 라우트 표 정리**: `showBack` 제외 목록의 `'my-orders'`와 `PAGE_LABEL`의 `'match'`는 현재 라우터에 없는 이름(데드)이고, `actions`·`settlement`·`registrant-settlement`·`support`·`settings*`·`community-post`는 헤더 라벨 미등록(각 화면이 자체 page-head/제목 보유). 노출 결함은 없고 라우터-헤더 계약 정리 건이라 후보로 기록.
4. **하위 화면 뒤로가기 중복(헤더 화살표 + 페이지 내 '설정/뒤로' 버튼)**: SupportView·설정 하위 5화면·CommunityPostView에서 같은 목적의 뒤로가기 컨트롤이 2개 보인다. 제거 방향(헤더만/페이지 버튼만)은 화면 구조 결정이 필요해 후보로만 기록.
5. **앰버 팔레트 3종 혼재(`#f0a800` 하드코딩 2곳 vs `--warn`/`--status-trading` #ffa940 vs `--status-acceptance-pending` #c28a2e)**: 동일 "대기" 의미가 파일마다 다른 값을 쓴다. 토큰 신설이 필요한 팔레트 설계 건이라 후보.
6. **`--accent` 채움 아바타의 흰 글자(base.css `.chat-list__avatar`, ReviewsView `.review-item__avatar`)**: 1번과 같은 원인이지만 아바타·그라디언트 계열 전체에 해당해 일괄 수정 시 라이트 대비가 역전될 수 있다. 전역 `--on-brand`/`--on-accent` 토큰 설계가 선행돼야 하므로 이번에는 CTA 1곳만 국소 예외로 처리(기존 분류 유지).
7. **알림 화면 텍스트 글리프**: "처리할 일(…) 화면으로 이동 →"(295행)와 날짜 그룹 `▾/▸`(333행)가 다른 화면의 `BaseIcon`(arrow-forward/chevron-down) 관례와 다르다. 마크업·회전 스타일 변경이 따르는 시각 변경이라 후보.
8. **말줄임표 표기 혼재**: `'접수 중...'` 등 ASCII 마침표 3개 사용처가 남아 있다. `…` 통일은 전역 타이포 결정이라 부분 수정 시 혼재가 심해져 후보 유지.
9. **SettingsVehiclesView 차량 목록 로딩 중 빈 카드**(`v-else-if="!vehicleLoading"`로 로딩 동안 아무것도 렌더되지 않음) — 같은 설정 그룹의 SettingsVerificationView는 `.sk-card` 스켈레톤을 보유. 스켈레톤 추가는 화면 요소 신설이라 후보.
10. **모달 푸터 문구 혼재('닫기' vs '취소')**: ActionCenterView 거절 모달·OrderDetailView 취소/거절/완료 모달은 '닫기', 대부분의 중립 모달은 '취소'. 파괴적 동작과의 혼동을 피하려는 의도적 구분 가능성이 있어 후보.
11. **AdminView 수금/출금 카드 금액 인라인 포맷(1758·1762·1763·1799행)**: 같은 파일의 `formatWon` 헬퍼가 있는데도 `Number(...).toLocaleString('ko-KR')` + `원`을 반복한다(출력 동일). 표시 결과가 같아 UI 개선이 아닌 정리 작업으로 후보.
12. **기존 반복 후보(13:01·12:00 기록분 유지)**: FAB 3곳(`var(--brand)`+흰 글리프) · `HeaderBar html.dark .hb-btn` 중복 예외 · 보조용언/말줄임표 전역 · HomeView `.home-more` 공통화 · SettlementView 계좌 마크업 중복 · `new Date(created_at_iso)` 방어 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 1.07s`, 4371 modules, 오류·경고 0건) — 최종 수정(SupportView 상태 칩) 반영 후 재실행 결과. 수정 5개 파일 청크(`CustomerHome`·`SettingsVerificationView`·`SupportView`·`ActionCenterView`·`NotificationsView`) 정상 생성.
- import 누락 점검: 변경이 모두 `<template>` 속성 및 스코프 `<style>` 값이라 import·심볼·전역 CSS 영향 0건 → 빌드 통과로 검증 대체. `git diff --stat`으로 변경 범위가 5개 파일(+17/−11, 직전 회차 적용분 포함)임을 확인.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
