# UI 개선 보고서 — 2026-09-10 01:03

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 직전 실행(00:13) 이후 `frontend/src`에 타 세션 변경 0건(전 파일 mtime 00:13 이전). 반복 적용 항목 없음.
- 적용 1건: **NotificationsView '처리할 일(…)로 이동' 조사 어색 문구 재문안** — 여러 시간 반복 후보로 남아 있던 명확한 문법·문구 개선 항목을 마침내 수정(닫는 괄호 뒤 '로' → '… 화면으로 이동').
- 전역 표준 스캔(용어·보조용언·소형 폰트·rel/alt·문구 문법) 신규 위반 0건 — 기존 클린 상태 유지.
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-10 01:03~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_0013.md`·`2026-09-09_2302.md` — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + 전 파일 mtime 검사 — 00:13 보고서 이후 `frontend/src` 산하 변경 **0건**. 모든 후보 상태는 직전 보고서와 동일.
- **직접 판독**: NotificationsView.vue(212~306 지금 처리할 알림 카드·하단 이동 버튼 문구), ChatRequestEvent.vue(전량 231행 — 요청 카드 상태별 액션), UiListRow.vue(전량 110행), MessageBubble.vue(전량 211행), LoginView.vue(전량 215행).
- **전역 그렙**: `오더|주문번호`(0건) · `font-size: 6~9px`(0건) · 보조용언 붙여쓰기(해주세요/해드려요/드릴게요 등 0건 — '알려 드릴게요' 2건은 띄어쓰기 정상) · `target="_blank"` `rel="noopener"` 누락(0건, 8건 전수 확인) · 닫는 괄호 뒤 조사 접속 문구 패턴(사용자 노출 1건 → 이번 수정 대상, 나머지는 주석·백엔드 라벨 처리 문자열).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피·배지 표준, 메모리 규칙(한국어 UI / "운행" 용어).

## (c) 발견한 개선 포인트
1. **NotificationsView '처리할 일(운승 승인 · 제안 · 요청)로 이동' 조사 어색(기존 반복 후보 → 이번에 수정)**: [NotificationsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L295) — '지금 처리할 알림' 카드 하단 이동 버튼. 문구 구조가 "처리할 일(설명)로"로 닫는 괄호 뒤에 조사 '로'가 붙어 표기·읽기가 어색했다. **적용.**
2. **표준 스캔 신규 위반 0건**: 용어("오더"/주문번호)·보조용언 붙여쓰기·기술 식별자·소형 폰트(6~9px)·`rel="noopener"` 누락 모두 클린. 금회 판독한 ChatRequestEvent·UiListRow·MessageBubble·LoginView는 요청 카드 액션(수락/거절 Primary-보조 배치, 처리 중 disabled), 배지 규격(padding 1px 6px/10px/400), 상태색 사용(수락 민트·거절 레드), 다크 대비('밝은 brand 채움 위 #07120e 어두운 글자' 표준) 모두 기존 표준 준수.
3. **기존 반복 후보(00:13·23:02 기록분 유지, 상태 재확인)**: OrderFavoritesView eyebrow `--danger` 컬러(빨간 하트 통일 의도와 공존, 타 세션 파일) · QuickMatchPanel `.qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · coin 아이콘 + '원' 이중 표기 · '정산 대기중' 백엔드 캐노니컬 · ChatImageSheet 3분할 규칙 · UiChip 기본 스타일 · MyMarketView FAB '＋' 글리프 — 기존 분류 유지.

## (d) 실제 적용한 수정
1. `views/NotificationsView.vue` 295행 — `처리할 일(운행 승인 · 제안 · 요청)로 이동 →` → `처리할 일(운행 승인 · 제안 · 요청) 화면으로 이동 →`. 괄호 안 설명 내용(처리 항목 종류)은 유지한 채 조사 어색함만 제거하는 최소 재문안. 템플릿 텍스트 변경으로 로직·심볼 영향 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **NotificationsView 동일 카드 상단 헤더 '지금 처리할 알림' + 하단 '처리할 일(…) 화면으로 이동' 버튼 문구 중복성**: '지금 처리할 알림' 카드 내 각 항목은 알림 상세로 이동하고, 하단 버튼만 액션 센터로 가는 구조라 용도가 달라 병합은 오히려 혼란 — 문구만 이번에 다듬고 구조 변경은 보류.
2. **OrderFavoritesView eyebrow `--danger` 컬러**: 의미 오용(레드=문제 전용) 가능성과 '빨간 하트 통일' 의도 공존으로 단정 불가. 타 세션 신규 기능 파일 — 안정화 후 재검토 권장.
3. **기존 반복 후보(00:13·23:02 기록분 유지)**: QuickMatchPanel `.qmp-quick` 구조 충돌 · DashboardView `.settle-item` 상충 재사용 · coin+'원' 이중 표기 · '정산 대기중' 캐노니컬 · ChatImageSheet 3분할 · UiChip 기본 스타일 · MyMarketView FAB 글리프 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 998ms, 4369 modules) — 수정 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 수정이 템플릿 한글 문구 변경(템플릿 컴파일 대상, import·심볼 영향 0건) → `npm run build` 통과로 검증 대체. 수정 후 그렙 재확인: 295행 신규 문구 잔존 확인.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
