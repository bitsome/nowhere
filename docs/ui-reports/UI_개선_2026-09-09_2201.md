# UI 개선 보고서 — 2026-09-09 22:01

## 실행 결과 요약
- **코드 수정 2건(2개 파일)** — 직전 실행(21:02) 이후 `frontend/src`에 타 세션 변경이 있었음(21:08~21:21 OrderFavoritesView·HeaderBar·MoreView·useOrderDetail.js·OrderCard·OrderDetailView 등 활발 편집 — 찜 기능 구현으로 추정). 직전 보고서 적용 내역·후보와 겹치지 않는 신규·명확 항목만 직접 수정했다. 타 세션 편집 중인 파일(OrderDetailView·OrderCard·MarketView·HomeView·QuickMatchPanel 등)은 수정에서 제외해 병행 충돌을 방지했다.
- 신규 적용 2유형: ① ChatThread 아이콘 전용 버튼 3건에 `aria-label` 누락 보완(코드베이스 표준 규약 — 동일 채팅 기능 내 ChatImageSheet·HeaderBar·ImageGallery 등 40건 보유) ② OrderCreateView 데드 CSS 제거(과거 검색·정렬·필터 잔재 12개 블록, 사용 중인 `.filter-footer`는 유지).
- 용어("오더"/주문번호)·보조용언 붙여쓰기·배지 규격 위반은 신규 0건 — 직전 보고서 상태 유지.
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 22:01~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_2102.md`·2001 — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + mtime 검사 — 21:02 보고서 이후 OrderDetailView.vue(21:21)·OrderCard.vue(21:21)·useOrderDetail.js(21:13)·MoreView.vue(21:10)·router/index.js(21:10)·OrderFavoritesView.vue(신규, 21:09)·HeaderBar.vue(21:09)·MarketView.vue(21:08) 타 세션 갱신 확인 → **활발 편집 중인 파일로 분류해 수정 제외**.
- **직접 판독**: ChatThread.vue(163~274행), OrderCreateView.vue(585~647 템플릿 말미·649~1010 CSS 전 구간, 데드 후보 검증), NotificationsView.vue(270~315), HomeView.vue(705~730), OrderCard.vue(288~335), QuickMatchPanel.vue(890~1040 scoped/전역 CSS·542/626 템플릿).
- **전역 그렙**: `오더|주문번호|order_id`(사용자 노출 0건 — 전부 코드 파라미터) · `[가-힣](해주세요|드릴게요|드려요|봐주세요|...)` 보조용언 붙여쓰기(0건) · `aria-label` 규약 현황(40건) · `coin|원` 이중 표기 사용처 · OrderCreateView 데드 후보 클래스 전수.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피·배지 표준, 메모리 규칙(한국어 UI / "운행" 용어).

## (c) 발견한 개선 포인트
1. **ChatThread 아이콘 전용 버튼 `aria-label` 누락(기존 반복 후보, 명백한 접근성 결함)**: [ChatThread.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatThread.vue) 하단 입력 영역의 '운행 요청'(239행)·'이미지 첨부'(250행)·'보내기'(269행) 3개 아이콘 버튼이 `title` 속성만 보유. `title`은 스크린리더에 비표준적이고 마우스 호버 전용 안내라 키보드/보조기기 사용자 접근성 결함. 동일 채팅 기능 내 ChatImageSheet·HeaderBar·ImageGallery·MarketView 등은 이미 `aria-label`을 표준으로 사용 중(전역 40건) → 같은 기능 파일에만 누락된 아웃라이어 → **적용**.
2. **OrderCreateView 데드 CSS(명백한 잔재, 기존 반복 후보 확정)**: [OrderCreateView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue) 과거 '내 운행 검색'·'빠른 정렬 칩'·'필터 모달' 잔재로 `.create-search`, `.create-sort` 계열 7블록, `.filter-body`·`.filter-label`·`.filter-amount-row`(+`.n-input`)·`.filter-amount-sep` 5블록이 템플릿·스크립트 사용처 0건(파일 전수 그렙 확인). `.filter-footer`만 템플릿 저장 모달 푸터(636행)에서 실제 사용 중 → 사용 블록 제외하고 잔재만 제거, 주석도 실제 용도('모달 푸터')로 정정 → **적용**.
3. **`.qmp-quick` scoped/전역 충돌(21:02 후보 재확인, 보류 유지)**: QuickMatchPanel이 scoped(907행, 패널 내 원큐 바)와 전역(1044행, 텔레포트 모달)에 같은 클래스를 정의해 전역 `display:flex; flex-direction:column; gap:18px`가 패널 내 원큐 바(542행)에도 적용되는 구조. 파일이 타 세션 작업 중(20:34 미커밋)이고 실제 렌더 간격 확인이 필요해 보류(기존 21:02 기록 유지).
4. **DashboardView `.settle-item` 상충 재사용·coin+"원" 이중 표기·NotificationsView '처리할 일(…)로 이동' 문구·UiChip 기본 스타일 등 기존 후보(보류 유지)**: 표시 결과 판단·재문안·파일 안정화 필요로 분류 유지. 신규 표준 위반(용어·보조용언·기술 식별자)은 0건.

## (d) 실제 적용한 수정
1. `components/chat/ChatThread.vue` — 아이콘 전용 버튼 3건에 `aria-label` 추가: '운행 요청'(240행)·'이미지 첨부'(252행)·'보내기'(272행). `title`과 동일 텍스트로 병기해 마우스 툴팁·스크린리더 양쪽 커버. 다른 변경 없음.
2. `views/orders/OrderCreateView.vue` — 데드 CSS 제거: ① 스타일 블록 선두의 '내 운행 검색'(`.create-search`)·'빠른 정렬 칩'(`.create-sort`·`::-webkit-scrollbar`·`__btn`·hover 미디어·`--active`) 8블록 삭제 ② '필터 모달' 섹션에서 미사용 `.filter-body`·`.filter-label`·`.filter-amount-row`·`.filter-amount-row .n-input`·`.filter-amount-sep` 5블록 삭제. 사용 중인 `.filter-footer`는 유지하고 주석을 '모달 푸터 — 템플릿 저장 등에서 공용'으로 정정.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **QuickMatchPanel `.qmp-quick` scoped/전역 클래스 충돌(구조적)**: 파일이 타 세션 미커밋 작업 중(20:34)이고, 원큐 바(패널)와 모달이 현재 어떻게 렌더되는지 표시 확인이 필요해 후보 유지. 최소 수정안은 모달 루트 클래스 분리 + 전역 선택자 교체 2곳이며 파일 안정화 후 재검토 가능.
2. **DashboardView `.settle-item` 클래스 상충 재사용·ChatImageSheet 3분할 규칙**: 의도된 재사용/부분 규칙으로 판단, 병합·분리 이득 낮아 보류(기존 기록 유지).
3. **coin 아이콘 + '원' 이중 표기(HomeView·OrderCard·SetGroupCard·SettlementView)**: coin 아이콘 자체에 '₩' 각인(SVG)이 있고 옆 금액에 '원'이 붙는 구조지만, 금색 동전+원 표기는 의도된 표현으로 보여 표시 판단 보류(기존 기록 유지).
4. **NotificationsView '처리할 일(운행 승인 · 제안 · 요청)로 이동' 문구(295행)**: 닫는 괄호 뒤 조사 '로'가 비표준적이나 단독 오류가 아니고 문맥상 재문안 판단이 필요해 후보 유지.
5. **타 세션 활발 편집 파일(OrderDetailView·OrderCard·HomeView·MarketView·HeaderBar·OrderFavoritesView·MoreView·useOrderDetail.js·router/index.js)**: 21:08~21:21 연속 편집 관찰로 병행 충돌 위험이 커 이번 실행 수정 대상에서 제외. 작업 안정화 후 다음 실행에서 신규 검토 가능.
6. **기존 반복 후보(21:02·20:01 기록분 유지, 상태 재확인)**: '정산 대기중' 백엔드 캐노니컬 · 승인 대기 3중 반복+Primary 2개 · hero/브랜드 위 글자색 전역 설계 · ChatImageSheet 3분할 규칙 · UiChip 기본 스타일 · HomeView·ChatList 칩 CSS 중복 · MyMarketView FAB 글리프 · RideHistoryView 집계 기준 · '처리할 일(… )로 이동' 문구 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 941ms, 4367 modules) — 수정 2개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: ① ChatThread 수정은 템플릿 속성 추가 3건(HTML 속성, import·심볼 영향 0건) ② OrderCreateView 수정은 CSS 규칙 삭제(문자열·심볼·import 영향 0건) → `npm run build`(템플릿·CSS 컴파일 포함) 통과로 검증 대체.
- 수정 후 그렙 재확인: ChatThread `aria-label` 3건 잔존(240·252·272행), OrderCreateView에서 제거한 클래스(`create-search`·`create-sort`·`filter-body`·`filter-label`·`filter-amount-*`) 잔존 0건, `.filter-footer` 템플릿(636)·CSS(912) 1:1 유지.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분(2파일)과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
