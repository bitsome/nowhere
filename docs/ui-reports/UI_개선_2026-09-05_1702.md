# UI 개선 보고서 — 2026-09-05 17:02

## 실행 결과 요약
- **변경 없음** — 16:03(48회차) 이후 `frontend/src` 변경 1건(16:06:15 `views/SupportView.vue` — 48회차 수정 자체의 저장 반영분)뿐이고, 그 상태를 재확인한 결과 48회차 '공지 배지 제거' 수정이 정상 반영·잔재 없음(support-card__badge·kind_label 0건). 신규 기능 코드가 없어 전수 열람 대상이 없으므로, 기존 화면에 대한 신선한 시점 점검(전역 감사 + 대표 화면 직접 열람)을 수행했으나 **명확한 소형 결함을 발견하지 못해 수정 없음**. 직전 회차(16:03) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 17:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 16:03 이후 `frontend/src` 변경 1건 — `views/SupportView.vue`(16:06:15, 48회차 수정 반영분) → **재확인**: 공지 카드 = 제목·날짜·셰브론만 유지, `.support-card__badge` CSS·템플릿 span 제거 상태 정상(grep `support-card__badge|kind_label` 0건). FAQ 카드(Q. 프리픽스)·문의 카드(상태 칩) 구조와도 정합.
- **직접 열람(신선 시점 점검)**: SupportView(전체 — 배지 제거 반영 확인) · NotificationsView(분류 로직 KIND/classify/TAG_VARIANT/STATUS_TONE L41-133 + v8 CSS L410-610 — 우선 알림·상태 배지 색 체계) · SettlementView(정산 상태 칩 CSS L500-563) · OrderCard(추적·상태 배지 로직 L1-120) · MyMarketView(FAB 노출 조건·빈 상태 CTA L540-700)
- **전역 감사(grep)**: `원원` 단위 중복 0건 / font-size 8·9px 0건 / 기술 식별자 노출 — AdminView 운영 개입 2건(L1771·L2030 order_number, 관리자 콘솔 설계상 유지)·ActionCenterView·HomeView는 코드 식별자(orderId 파라미터)만으로 UI 노출 0건 / 배지·칩 스타일 — 사용자 화면(1px 6px·10px·400) 및 채움 상태 칩(1px 8px·700, 기존 관례) 체계 일관
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1603.md`(48회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
- **신규 없음**: 이번 시간대 신규 기능 코드 0건 + 기존 화면 신선 점검에서도 명확한 소형 결함 미발견.
- 점검 결과 정합 확인 사항:
  1. **알림센터 색·분류 체계 정합**: NotificationsView의 classify → KIND(icon/cls/tag)·TAG_VARIANT(요청·확인·요금=옐로우, 취소·신고=레드, 완료·운행=민트, 채팅=블루, 항공=퍼플, 시스템=그레이) 및 STATUS_TONE(상태 배지) 매핑이 알림센터 색상 규칙과 일치. 취소/거절/신고는 타이틀 우선, 항공·요금·정산은 본문 포괄 등 규칙 순서도 문제 없음. 우선 처리(priority)는 읽지 않은 request 그룹만 — '처리 필요 > 확인 필요' 우선순위 원칙 부합.
  2. **정산 칩·금액 색 정합**: settle-recent 상태 칩(pending=브랜드 틴트, paid=정산 색 틴트)은 사용자 배지 스펙(1px 6px·10px·400) 준수. 상태 수가 2개(정산 예정/완료)뿐인 점을 고려하면 문제·보류=레드 구간은 현재 발생하지 않는 상태 — 색 추가 불필요(색은 의미 있을 때만, 유지).
  3. **내 마켓 Primary 액션 중복 없음**: 운행 등록 FAB는 정산 탭·빈 목록에서 숨김(v-if), 빈 상태에서는 EmptyState 내 '+ 운행 등록' CTA만 노출 — '한 화면 Primary 버튼 하나' 원칙 부합. 검색 모달의 Primary(검색)는 오버레이 내부라 화면과 분리됨(유지).
  4. **SupportView 배지 제거 잔재 0건**: 48회차 수정(공지 카드 레드 배지 제거)이 16:06 저장분에 온전히 반영 — 추가 정리 불필요.

## (d) 실제 적용한 수정
- 없음 (변경 없음).

## (e) 적용하지 않은 항목과 이유 (후보)
- **신규 문의 상태 칩 answered(민트+백색 10px 텍스트) 대비·v-chip/verify-chip 패딩 미세 차이(1px/2px·8px)**: 46~48회차 반복 후보 — 채움 상태 칩 계층은 사용자 배지 스펙과 별개, 민트+백색은 AdminView 기존 관례와 일관 → 전역 상태 칩 정책으로 통합 검토(후보 유지).
- **일일 운영 대시보드 '문의 접수(open 티켓)' 카운트 미포함**: 백엔드 daily API 확장 필요(기능 변경) → 후보 유지.
- **1:1 문의 placeholder '(운행 번호·날짜 포함 시 확인이 빠릅니다)' 문구**: '운행 번호'가 사용자 UI에 노출되지 않는 점 고려, '날짜·노선' 중심 문구 조정 검토(복사 정책 결정 — 후보 유지).
- **홈 추천 카드 조건%(match_score) 미노출**: 근거 체크리스트는 노출 중이나 % 숫자는 미표시 — 노출 여부는 제품 결정 사항으로 반복 후보 유지.
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 없음에도 현재 작업 트리 정합성 확인차 `npm run build`(frontend, vite v8.2.1) 실행 — **통과**, 4362 modules, built in 919ms. SupportView 청크 정상 생성.
- import 누락 점검: 코드 수정 없음 → 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
