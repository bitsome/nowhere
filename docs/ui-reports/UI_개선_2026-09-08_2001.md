# UI 개선 보고서 — 2026-09-08 20:01

## 실행 결과 요약
- **수정 2건 적용** — 직전 실행(09-08 19:05) 보고서 이후 `frontend/src` 변경분 **0건**(mtime 델타 스캔 결과)이므로 이번 실행은 전역 재감사 차원에서 **named import 사용 횟수 일괄 스캔**을 실시, 데드 import **2건**(icons.js `ChatbubblesOutline` / SettingsVerificationView `roleLabel`)을 식별·제거했다. 그 외 신규 위반(기술 식별자·용어·소형 폰트·배지 스펙) 0건.

## (a) 실행 시각
- 2026-09-08 20:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 19:05 이후 수정분)**: `frontend/src` 전체 재귀 — **0건** (19:05 보고서 적용분 이후 신규 코드 변경 없음)
- **직접 판독(이번 실행 신규 감사 — named import 전수 스캔·검증)**:
  - `utils/icons.js`(전체 199줄) — 이름→아이콘 매핑 파일. `@vicons/ionicons5`에서 67개 import → `ICONS` 맵과 대조해 **`ChatbubblesOutline` 1건 매칭 없는 데드 import 확인**(아래 (c)-1). `CoinIcon`(직접 그린 svg)·`iconOf` 폴백 구조는 정상.
  - `components/common/BaseIcon.vue`(전체 21줄) — `iconOf(name)` → `ICONS[name] ?? ICONS.help` 폴백으로 해석됨을 재확인. 맵에 없는 이름은 폴백이므로, 맵 미등록 아이콘의 import 유무는 렌더링에 영향 없음.
  - `views/settings/SettingsVerificationView.vue`(전체 452줄) — 신규 인증 화면. `import { ROLE_CUSTOMER, roleLabel } from '../../data/roles'`에서 **`roleLabel`만 전체 사용 0건**(isCustomer는 ROLE_CUSTOMER만 참조, 18행) — 데드 import 확인(아래 (c)-2). 나머지: `.verify-chip--done/pending/rejected/none`이 v8 의미색(틸/앰버/레드/그레이) 준수, 화면당 Primary('신청하기') 1개, 용어·기술 식별자 위반 없음.
  - `views/AdminView.vue`(script 1~120행 import 블록 + 템플릿 1204~1363행 운영 지표/사용자 관리 구간) — 18:17 대규모 변경 후 **이번 실행에서 후속 점검**. 운영 지표 탭이 🔴 즉시 처리 / 🟡 확인 필요 / 🟢 진행 중 / 정산·가입 현황 그룹(운영 문서 색 체계와 일치), 사용자 카드(이름+역할 n-tag+제재 상태 op-badge+역할별 인증 버튼) 구조·용어 정상. import 블록의 api 함수·상수(ROLE_*·roleLabel·statusColorVar)·컴포넌트(EmptyState·BaseIcon) 전부 사용처 확인. TODO/FIXME/console.log 0건.
  - `views/SettlementView.vue`·`views/community/CommunityView.vue`(grep 스팟) — 색상·font-weight 사용처 확인(기존 후보 분야, 아래 (e)).
- **전역 규칙 감사(grep)**:
  - 템플릿 기술 식별자(`주문번호|주문 번호|오더번호|운행 번호`): **1건** — SupportView.vue 문의 placeholder(기존 후보 유지) · `font-size: 7~9px`: **0건** · 템플릿 '오더' 노출: **0건**
  - **named import 사용 횟수 전수 스캔**(전 파일 전체 \b토큰 카운트, `.test.js` 제외): 사용 1회 이하(import에만 존재) 식별 → **2건**(icons.js·SettingsVerificationView) → 이번 수정으로 해소, 재스캔 결과 **0건**
  - 배지 스펙 감사(알림 화면 중심): `.v8-priority-status`·`.v8-day__count` 등 padding 1px 6px / font-size 10px / font-weight 400 표준 준수 재확인
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1905.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **icons.js 데드 import `ChatbubblesOutline`(이번 실행 적용)**: 22행에서 import됐으나 `ICONS` 맵(96~190행) 어디에도 등록돼 있지 않고, 전체 코드베이스에서 이 이름 사용처가 import 행 1곳뿐(대소문자 무시 grep `chatbubbles` 1건 = 22행). BaseIcon은 `iconOf` 폴백으로 미등록 이름을 처리하므로 **제거해도 렌더링·동작 변화 없음** — 번들 내 미사용 아이콘 코드 감소.
2. **SettingsVerificationView 데드 import `roleLabel`(이번 실행 적용)**: 8행에서 `ROLE_CUSTOMER`와 함께 import됐으나 `roleLabel`은 스크립트·템플릿 전체에 사용 0건(18행 `isCustomer`는 ROLE_CUSTOMER만 비교). 역할 라벨 표기가 없는 화면이라 제거 — 동작 변화 없음. (16:12 신규 파일 작성 시 습관적 import 잔재)
3. **신규 위반 — 0건(재확인)**: 기술 식별자·용어·소형 폰트·배지 스펙 전역 감사 모두 위반 없음. 19:05 이후 신규 코드 자체가 없어 신규 결함 발생 요인 없음.
4. **AdminView 18:17 변경분 — 후속 점검 결과 위반 없음**: 운영 지표 탭 그룹핑(즉시/확인/진행/정산)과 사용자 관리 카드(역할 n-tag·moderation op-badge·역할별 인증 버튼)가 운영 문서 의미색·역할 구분에 부합. import 전수 사용 확인. 대규모 파일이지만 이번 구간 판독으로 신규 UI 결함 미발견.
5. 기존 반복 후보 상태 변화 없음(19:05 기록분 그대로).

## (d) 실제 적용한 수정
- **`utils/icons.js`** — 22행 `ChatbubblesOutline,` import 제거. (ICONS 미등록·전체 미사용 — `iconOf` 폴백에 영향 없음)
- **`views/settings/SettingsVerificationView.vue`** — 8행 import를 `import { ROLE_CUSTOMER, roleLabel } from '../../data/roles';` → `import { ROLE_CUSTOMER } from '../../data/roles';`로 정리. (잔존 ROLE_CUSTOMER는 18행 isCustomer에서 사용 확인)
- 두 건 모두 데드 import 제거로 렌더링·동작 변화 없음 — 로컬 전용 작업.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(19:05 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 8곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지.
- **AdminView.vue 18:17 대규모 변경(후보 이월)**: 운영 지표·사용자 관리 구간(1204~1363행)은 점검 완료. 잔여 탭(운영 개입·신고·출금·고객지원, 1790~2484행) 전수 판독은 화면 규모상 다음 실행 점검 후보로 유지.
- 이번 실행은 위 (c)와 같이 데드 import 2건만 적용 — 신규 미적용 항목 없음.

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, `✓ built in 1.42s`. SettingsVerificationView·index 등 관련 청크 정상 생성.
- import 누락 점검: 제거한 2개 식별자(`ChatbubblesOutline`·`roleLabel`)의 잔존 사용 0건을 파일 판독(icons.js ICONS 맵 전체·SettingsVerificationView 전체)으로 사전 확인했고, 제거 후 **named import 전수 재스캔 결과 사용 1회 이하 0건** — 누락 없음. 잔존 import(ROLE_CUSTOMER 18행 등) 사용처 확인 완료. `npm run check:refs` 영향 없음(템플릿 참조 코드 미변경).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
