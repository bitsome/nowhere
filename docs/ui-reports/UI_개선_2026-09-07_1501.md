# UI 개선 보고서 — 2026-09-07 15:01

## 실행 결과 요약
- **수정 1건** — 직전 실행(64회차, 09-07 14:01) 보고서 작성 이후 14:16~14:17에 auth 영역 5개 파일(App.vue·LoginView·router/index.js·ForgotPasswordView·api/auth.js)이 외부 작업으로 변경됨. 변경분을 전수 검토해 **인증 화면 전체 뷰포트 처리(`app-content--full`)가 login에만 적용된 누락**을 발견, App.vue에 register·password-reset을 추가하는 소형 수정 1건 적용. 전역 위반 재스캔(기술 식별자·'오더/드라이버' 잔존 라벨·7~9px 소형 폰트)에서는 신규 명확 결함 없음.

## (a) 실행 시각
- 2026-09-07 15:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(14:01 이후 수정분)**: `frontend/src/App.vue`(14:17:34) · `views/auth/LoginView.vue`(14:17:27) · `router/index.js`(14:17:10) · `views/auth/ForgotPasswordView.vue`(14:16:47) · `api/auth.js`(14:16:02) — 5개 파일이 14:16~14:17(약 90초 간격, 일괄 작업 형태)에 변경됨. 로그인 화면 전환·비밀번호 찾기 2단계(이메일→인증코드+새 비밀번호) 개편으로 추정. 이 파일들을 전수 읽기 검토.
- 동일 auth 패턴 비교를 위해 `views/auth/RegisterView.vue`도 함께 검토(델타는 아니지만 login과 스타일·구조 공유).
- **대조·감사(grep)**: `frontend/src` 전역 위반 재스캔 —
  - 기술 식별자 UI 노출: `주문번호|오더 번호|오더번호|주문 번호` **0건**
  - `오더` 한글 노출 **0건**(용어 규칙 준수)
  - `드라이버` 노출: 템플릿 직접 문구 0건 — 잔존은 `data/levels.js` 레벨 타이틀(기존 반복 후보)과 코드·HTML 주석뿐
  - `font-size: 7~9px` **0건**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-07_1401.md`(64회차)·`1301.md`(63회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지), `assets/base.css`(.app-content 레이아웃)

## (c) 발견한 개선 포인트
1. **[적용] 인증 화면 `app-content--full` 누락** — App.vue에서 HeaderBar 숨김 조건은 `['login','register','password-reset']` 3개 화면 모두에 적용되는데, base.css `.app-content` 기본 패딩(상단 18px + 하단 `calc(88px + env(safe-area-inset-bottom))` — 고정 헤더·하단 네비용)을 제거하는 `app-content--full` 클래스는 `login`에만 걸려 있음. 그 결과:
   - 비밀번호 찾기(reset-wrap, `min-height: 100dvh`, 스크롤 잠금 없음)는 기본 패딩만큼 아래로 밀리고 하단에 약 106px의 빈 배경 여백이 생겨 불필요한 스크롤이 발생.
   - 회원가입(login-wrap, `height: 100dvh` + body 스크롤 잠금)은 카드가 상단 18px 만큼 아래로 치우쳐 중앙 정렬이 어긋남.
   - login 화면은 이미 전체 뷰포트 처리(패딩 0)가 되어 있어 3개 auth 화면의 레이아웃 처리 방식이 불일치 — 14:16 auth 개편 과정에서 누락된 것으로 판단.
2. **[확인] auth 화면 내부는 규칙 준수** — LoginView·ForgotPasswordView·RegisterView 모두 공용 UiCard 표면 사용, 단계별 Primary 버튼 1개(v-if/v-else로 동시 노출 없음), 한국어 문구·"운행" 용어 사용, 기술 식별자 없음, 인증 코드 입력은 `maxlength="6"`·`inputmode="numeric"` 등으로 미니멀 원칙에 부합.
3. **[후보 유지] LoginView·RegisterView 동일 `.login-*` scoped CSS 복제**(login-wrap/card/head/mark/title/desc/footer…) — 기존 반복 후보(공통 추출은 3개 화면 동시 조정 + 방금 외부 수정된 파일이라 충돌 위험, 대형으로 분류).

## (d) 실제 적용한 수정
- **`frontend/src/App.vue`** — `<main class="app-content">`의 `app-content--full` 바인딩 조건에 register·password-reset 추가:
  - 기존: `'app-content--full': isChatThread || route.name === 'login'`
  - 변경: `'app-content--full': isChatThread || ['login', 'register', 'password-reset'].includes(route.name)`
  - 효과: 회원가입·비밀번호 찾기도 로그인과 동일하게 전체 뷰포트(패딩 0)로 렌더링 — 비밀번호 찾기의 하단 불필요 여백/스크롤 제거, 회원가입 카드 중앙 정렬 복원. 템플릿 클래스 바인딩만 수정해 import·스크립트 변경 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).

## (e) 적용하지 않은 항목과 이유 (후보)
- **LoginView·RegisterView auth CSS 중복**: 공통 `.login-*` 스타일을 3개 auth 화면에서 공유하는 추출 작업은 대상 파일 다수가 이번 실행 직전(14:16~14:17) 외부에서 수정된 상태라 충돌 위험 + 3개 화면 동시 조정(대형) — 후보 유지.
- **기존 반복 후보(64회차 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(코드 주석상 의도된 결정) · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이(채움 상태 칩 계층 — 사용자 배지 스펙과 별개) · 문의 placeholder 문구 · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · App.vue 데드 CSS `.bottom-nav__item--button` · ChatView.vue 미사용 `ref` import — 기존 분류 유지(렌더링 영향 없거나 대형·디자인 결정 수반, 반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) — **통과**, `✓ built in 1.18s`, 에러 0건. 14:16 auth 개편분 포함 전체 4364 modules 변환, LoginView·ForgotPasswordView·RegisterView 청크 정상 생성.
- import 누락 점검: 수정은 App.vue 템플릿 클래스 바인딩 1건뿐 — import/식별자 변경 없음, 점검 이상 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
