# UI 개선 보고서 — 2026-09-09 14:01

## 실행 결과 요약
- **변경 없음(본 실행 코드 수정 0건)** — 직전 실행(13:01) 보고서 이후 `frontend/src`에서 **외부/사용자 변경 2건** 발생(13:58, 미커밋): ① `App.vue` `preloadTabViews()` core 프리로드 4건을 즉시 평가 `import(...)` → 지연 호출 `() => import(...)`로 통일(rest 목록과 패턴 일치), ② `views/HomeView.vue` `ROLE_DRIVER` import 누락 복구(18행). 이 2건은 코드 위생·런타임 오류 수정 성격의 변경으로 **새 UI 결함·규칙 위반을 만들지 않았음을 확인**했고, 현재 워킹트리 전체가 `npm run build` 통과함을 검증했다. 전역 규칙 감사(기술 식별자·용어·소형 폰트)는 직전 보고서와 동일 위반 0건. 본 실행에서 적용할 명확·소형 UI 개선이 없어 코드 수정 없이 마무리.

## (a) 실행 시각
- 2026-09-09 14:01 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(13:01 이후 수정분)**: `frontend/src` 전체 재귀 — **2건** (신규 외부 변경):
  - `App.vue`(2026-09-09 13:58) — `preloadTabViews()` core 배열(139~144행) `import('./views/…')` → `() => import('./views/…')` 지연 통일. `preload(loader)` 시그니처와 rest 목록(146~152행)이 이미 지연 방식이었으므로 이번 변경으로 core·rest 패턴 일치. 중복 프리로드·동작 변화 없음(초기 동시 다운로드 저감 의도 유지).
  - `views/HomeView.vue`(2026-09-09 13:58) — 18행 `import { ROLE_DRIVER } from '../data/roles';` 추가. 28행 `isDriver = computed(() => auth.user?.role === ROLE_DRIVER)`가 템플릿 598행 `<CustomerHome v-if="!isDriver" />` 등에 사용되므로, import 누락이던 직전 상태는 Driver 첫 렌더 시 ReferenceError 위험이 있었음 → 복구로 해소.
- **직접 판독(git diff HEAD 대조 + 그렙 스팟)**:
  - `App.vue`(1~318행 script 일부·template 일부) — 변경 구간 137~175행 전후 및 네비/프로바이더 구조, 243행 기사 분기. 변경으로 인한 UI 영향 없음 확인.
  - `views/HomeView.vue` import 블록(15~19행)·`isDriver` 사용처(28·558·571·598행) — import 추가로 인한 중복·충돌 없음.
- **전역 규칙 감사(grep, `frontend/src`)**:
  - `ROLE_DRIVER` 사용·import 전수 대조: 20건 매치 전부 import 존재 확인(누락 0건). 단독 사용처 HomeView는 이번 변경으로 복구 완료.
  - 잔여 `'Driver'` 리터럴(`role === 'Driver'` 등): **7곳**(App.vue 243 / QuickMatchPanel 30 / MarketView 30 / MoreView 27 / ProfileView 24·43 / SettingsVehiclesView 28) — 기존 후보(아래 (e)), 상수 치환 진행 중인 외부 작업과 겹칠 수 있어 본 실행에서는 미적용.
  - 템플릿 기술 식별자(`주문번호|주문 번호|오더번호|오더 번호`): **0건** · `운행 번호|운행번호`: **1건**(SupportView.vue 189행, 기존 후보 유지) · `font-size: 7~9px`: **0건**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-09_1301.md` — 적용 내역·후보 전수 대조, 반복 적용 없음.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **외부 변경 2건 — UI 개선 포인트 아님(코드 위생)**: App.vue 지연 프리로드 통일은 화면 변화가 없는 로딩 전략 정리, HomeView import 복구는 런타임 오류 수정. 둘 다 적용 방향이 올바르며 신규 UI 결함 유발 요소 없음(빌드 검증 완료).
2. **신규 위반 — 0건**: 델타 2건 모두 UI 무영향 코드 변경이라 신규 규칙 위반(기술 식별자 노출·용어·소형 폰트·배지 스펙) 없음. 전역 감사 결과 직전 보고서와 동일.
3. **SupportView 문의 placeholder(기존 후보 유지)**: 189행 `"(운행 번호·날짜 포함 시 확인이 빠릅니다)"` — "운행 번호"는 프로젝트 규정 용어이고 분쟁·조회 가속 안내라 노출 제거 대상 아님. 유지.

## (d) 실제 적용한 수정
- **없음 (변경 없음)** — 본 실행에서는 코드 수정 0건. 13:58 발생한 외부 변경 2건(App.vue·HomeView.vue)은 다른 세션/사용자 작업으로 보이며 미커밋 상태로 워킹트리에 존재 → 방향이 타당함을 확인만 하고 별도 수정 없이 보존.

## (e) 적용하지 않은 항목과 이유 (후보)
- **외부 진행 중 작업과 겹침 우려**: 잔여 `role === 'Driver'` 리터럴 7곳 → `ROLE_DRIVER` 상수 치환(전역 일괄, 소형 범위 초과 + HomeView에 방금 상수화가 진행된 점으로 보아 외부 세션이 순차 적용 중일 가능성 → 충돌 방지를 위해 미적용, 기존 후보 갱신).
- **기존 반복 후보(13:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView, 위 (c)-3) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · RegisterView '등록자' 설명 문구(제품 문구 결정) · AdminView.vue 잔여 구간 전수 판독 — 기존 분류 유지.
- 이번 실행은 신규 미적용 항목 없음 — 적용할 명확·소형 UI 개선이 없어 "변경 없음" 처리.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, 4365 modules, ✓ built in 9.95s) — 본 실행 코드 수정은 없었으나, 13:58 외부 변경(지연 프리로드·import 복구)이 포함된 현재 워킹트리 상태를 검증하기 위해 실행. 빌드 오류 없음.
- import 누락 점검: HomeView `ROLE_DRIVER`는 빌드로 잡히지 않는 런타임 ReferenceError 유형이라 그렙 전수 대조로 확인(전 사용처 import 존재). 그 외 신규 import·데드 import 변경 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 미커밋 변경 2건(`git status: M App.vue, M HomeView.vue`)은 사용자 커밋 대기 상태로 그대로 둠.
