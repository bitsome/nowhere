# UI 개선 보고서 — 2026-09-05 01:01

## 실행 결과 요약
- **수정 1건** — 00:02(42회차) 이후 `frontend/src` 변경 6건(실측 mtime: CommunityView 00:17 · MarketView 00:23 · icons.js 00:33 · OrderCreateView/useOrderSchedule/RideHistoryView 00:43)을 전수 재열람·캐너리 감사. 운행 기록 요약 히어로에서 내부 상태값 `정산완료`가 "총 정산완료 건수"로 그대로 노출되는 작은 결함을 발견해 사용자 표기(`정산`)로 교정. 나머지 변경분은 기존 표준 준수로 수정 없음.

## (a) 실행 시각
- 2026-09-05 01:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔(`Get-ChildItem -Recurse`): 00:02 이후 `frontend/src` 변경 6건 — `views/community/CommunityView.vue`(00:17) · `views/MarketView.vue`(00:23) · `utils/icons.js`(00:33) · `views/orders/OrderCreateView.vue`·`views/orders/RideHistoryView.vue`·`composables/useOrderSchedule.js`(00:43)
- **직접 열람**: `views/orders/RideHistoryView.vue`(전체 — script·템플릿·scoped CSS), `views/orders/OrderCreateView.vue`(목록/캘린더 템플릿 L102-257 + 등록/수정 폼 L259-646), `composables/useOrderSchedule.js`(전체), `views/orders/MyMarketView.vue`(STATUS_TABS 대조 L20-25), `views/community/CommunityView.vue`(카테고리 칩 색상 규칙 L807-841), `utils/communityCategories.js`(전체)
- **전역 감사(grep)**: `font-size` 10px 미만 0건 / "주문번호|order_number|오더번호" UI 노출 0건 / `정산완료` 사용처 전수(상태값·amountLabel 맵·STATUS_TABS value — 노출 결함은 RideHistoryView 히어로 1곳뿐)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_0002.md`(42회차 — 변경 없음)·`09-04_2301.md`·`2201.md` 등 — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **운행 기록 히어로에 내부 탭 값 노출 (신규 발견)**: `RideHistoryView.vue` 요약 히어로 건수 칸이 `<span>총 {{ listTab === '전체' ? '운행' : listTab }} 건수</span>`로 탭 value를 그대로 사용 — `정산` 탭 선택 시 정산 탭 value인 `정산완료`가 화면에 "총 정산완료 건수"로 표시됨. 탭 라벨(정산)과 화면 문구(정산완료) 불일치 + 내부 상태값 노출(사용자 표기 원칙 위반). 완료/취소/전체는 라벨=value라 영향 없음. 동일 패턴은 useOrderSchedule·MyMarketView에는 없음(라벨=value) — 이 파일이 유일.
2. **00:43 변경분(OrderCreateView·useOrderSchedule·RideHistoryView) 클린**: 상태 탭(진행중/예약/운행중 라벨=value)·일별/월별 그룹(지난/날짜 미정 분류)·캘린더(today `#07120e`)·빈 상태 문구·저장 CTA Primary 1개 분기·배지 스펙 — 신규 위반 없음. (useOrderSchedule `initCollapsedOnce`에 `groupUnit==='year'` 분기가 있으나 해당 화면 GROUP_UNITS는 day/month뿐 — 시각 영향 없는 사소한 잔여 로직, 변경 안 함)
3. **00:23·00:17 변경분(MarketView·CommunityView) 클린**: MarketView 필터/차량 2단/30초 폴링 문구 등 캐너리(용어·폰트·배지) 위반 없음. CommunityView 카테고리 칩 6색 변형(airport/route/region/car/money/shop)은 09-02 21:01 배지 스펙 적용분 유지. `--shop` 칩 틸 틴트 ↔ 인디고 글자 어색함은 09-04 12:01 관찰된 기존 분류(아래 e-2) — 신규 아님.
4. **전역 캐너리 통과**: 10px 미만 폰트 0건, 기술 식별자 노출 0건.

## (d) 실제 적용한 수정
- **`views/orders/RideHistoryView.vue` (1건)**: 요약 히어로 건수 라벨 교정
  - `countLabel` computed 추가 — 탭 value→사용자 표기 맵(전체→운행 / 완료→완료 / 정산완료→정산 / 취소→취소, 기본 '운행')을 `amountLabel`(기존) 옆에 정의
  - 템플릿: `총 {{ listTab === '전체' ? '운행' : listTab }} 건수` → `총 {{ countLabel }} 건수`
  - 효과: `정산` 탭에서 "총 정산완료 건수" → "총 정산 건수"로 표시(클릭한 탭 라벨과 일치), 나머지 탭 문구 무변경. `amountLabel`·전체 구조·동작 무변경, 신규 import 없음(`computed` 기존 사용)

## (e) 적용하지 않은 항목과 이유 (후보)
- **CommunityView `.feed-card__cat--shop` 틸 틴트(`#63e2b7`) ↔ 글자 인디고(`var(--status-accepted)`)**: 09-04 12:01 회차부터 "시스템 차원의 커뮤니티 카테고리 색 설계" 후보로 분류된 기존 항목 — 카테고리 6색 체계의 통일된 색 방향(어느 초록 계열로 통일할지)이 정해지면 일괄 적용 대상. 단독 톤 보정은 체계 결정의 선행으로 판단, 후보 유지.
- **useOrderSchedule `initCollapsedOnce`의 `year` 분기**: 해당 화면 단위 전환에 year 없음(dead 분기) — 시각 영향 없는 코드 정리로 리팩터링 후보 유지(미적용).
- **기존 반복 후보(HomeView 탭 카운트 불일치·OrderCreateView 중복 CSS/들여쓰기·RideHistoryView/OrderCreateView 상태 탭 CSS 중복·커뮤니티 카테고리 색 체계·DashboardView 하드코딩 틴트·`/v8-noti` 데드 CSS·naive 전역 테마 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 후 `npm run build`(frontend, 로컬) 실행 — **성공** (exit 0, `✓ built in 962ms`, 4347 modules). RideHistoryView 모듈 정상 번들 확인.
- import 누락 점검: 수정은 `computed`만 사용(기존 import) — 신규 import 불필요, 누락 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
