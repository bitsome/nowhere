# UI 개선 보고서 — 2026-09-03 15:01

## (a) 실행 시각
- 2026-09-03 15:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 읽기): `views/community/UserPageView.vue`(전체 — 최근 미검토 화면), `views/NotFoundView.vue`(전체), `views/settings/SettingsAppearanceView.vue`·`SettingsVehiclesView.vue`·`SettingsVerificationView.vue`(전체), `views/orders/OrderDetailView.vue`(하단 원터치 액션 바·완료 폴백 라벨 및 모달/축하 문구 전역 대조), `views/DashboardView.vue`(오늘/내일 카드·요약 상단 영역), `views/ProfileView.vue`(세그먼트 활성 탭·아바타 영역 재확인)
- 컴포넌트: `components/layout/HeaderBar.vue`(전체 — 최근 미검토 화면), `components/ui/UiCard.vue`(전체), `components/common/ConfirmDialog.vue`(전체), `components/orders/OrderCardSkeleton.vue`(전체)
- 전역 감사(grep): "오더" UI 노출 전수(0건), "주문/주문번호/ORDER-/No.#" 노출(0건 — 매칭은 NotificationsView 색상값뿐), brand/accent/gradient 배경 + 흰 글자 40곳 재분류(전원 기후보 관용 패턴), semantic(`--status-*`/`--badge-*`) 배경 + 흰 글자(0건), 999px 칩 중 font-weight 600/700 잔존 분류(버튼·필터 칩으로 기후보 범주), **"운행완료" vs "운행 완료" 띄어쓰기 전수(불일치 1곳 발견)**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1405.md`·`1301.md`·`1207.md`(후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first), `assets/base.css`(토큰 대조)

## (c) 발견한 개선 포인트
1. **하단 액션 바 완료 폴백 라벨 띄어쓰기 불일치 (신규 발견·적용)**: OrderDetailView 하단 원터치 진행 바(`.detail-actionbar`)에서 진행 단계가 남아있지 않을 때 보여주는 폴백 버튼 라벨이 `운행완료`(붙여쓰기). 같은 파일의 운행 완료 확인/축하 모달 문구(L152·1053·1067·1093)와 앱 전역(OrderDetailView 9곳·`useOrderDetail.js` 2곳·`ReviewsView.vue`·`data/levels.js` 등 총 13곳)은 전부 `운행 완료`(띄어쓰기)로 일관 — 사용자 노출 라벨 1건만 표준에서 벗어남. "운행중·정산완료·운행시작" 등은 상태/탭 키워드로 붙여쓰기가 관행이므로 이번 대상은 폴백 라벨만 해당.
2. **최근 미검토 화면 전체 클린 (신규 확인)**: HeaderBar(알림 배지 `--badge-red`+흰 글자는 의미색 관용)·UserPageView(아바타 그라디언트는 관용)·NotFoundView·SettingsAppearanceView·SettingsVehiclesView·SettingsVerificationView·UiCard·ConfirmDialog·OrderCardSkeleton 모두 배지 스펙(1px 6px/10px/400)·한국어·용어("운행")·placeholder·다크 대응 준수 — 신규 위반 없음.
3. **brand/accent/gradient + 흰 글자 잔존 40곳 = 전원 기후보 관용 패턴**: 원형 아바타 이니셜(UserPageView·CommunityView·CommunityPostView·ProfileView 등), FAB '＋'(CommunityView L1091)·MyMarketView L731, brand-gradient 아이콘 박스(SettingsView·SetGroupCard), Login/Register 그라디언트 CTA, ProfileView 세그먼트 활성 탭(L490)·ReviewsView 활성 탭·ChatList/MessageBubble 아바타(다크 --accent 틸) — 13:01~14:05 후보 목록과 동일, 신규 발견 없음.
4. **기타 재확인 클린**: UI 노출 "오더"/"드라이버"/주문번호 0건(코드 주석·`data/levels.js` 동기 데이터만), semantic 상태색 위 흰 글자 신규 0건, SettingsVerificationView primary ghost 2건은 기후보.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/OrderDetailView.vue` | 하단 원터치 진행 바 완료 폴백 라벨 `<template v-else>운행완료</template>` → `운행 완료`(L1214) + 동일 라벨을 인용한 코드 주석(L51) 동기화 — 앱 전역 "운행 완료" 띄어쓰기 표준과 일치 |

템플릿 문자열 1건 + 주석 1건 교체(script·`<style scoped>`·import 무변경). 의미·레이아웃·상호작용 무변경 — 한글 띄어쓰기 표준화만 적용.

## (e) 적용하지 않은 항목과 이유 (후보)
- **avatar/FAB/brand-gradient 아이콘 박스/Login·Register 그라디언트 CTA 흰 글자**: 관용 패턴 — 기존 "brand 위 글자색" 전역 결정 사항(12:07~13:01 후보 유지).
- **ProfileView naive 세그먼트 활성 탭·ReviewsView 활성 탭/아바타(--accent 다크=틸 위 흰 글자)·ChatList/MessageBubble 아바타**: naive 테마/아바타 관용 — 기존 후보 유지.
- **ChatImageSheet 흰 아이콘 11곳·ChatThread 상태 알약 흰 글자**: 이미지 오버레이/상태색별 대비 검토 필요 — 기존 후보 유지(14:05).
- **라이트 naive-ui primary 초록(#18a058)·다크 primary 흰 글자 전역 불일치**: 전역 naive 테마 오버라이드 필요 — 대형 후보(12:07~13:01 유지).
- **SettingsVerificationView primary(ghost) 2개·DashboardView naive 그린/앰버 하드코딩·HomeView 탭 카운트 불일치·`.login-submit` CTA 크기(11px/600)·data/levels.js "드라이버"**: 기존 후보 유지(반복 미적용).
- **"운행중·정산완료·운행시작" 등 붙여쓰기 키워드**: 상태값/탭 value 관행 — 미적용(정상 분류).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 906ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: OrderDetailView 내 `운행 완료` 11곳 일관(L1214 폴백 라벨 포함), `운행완료`(붙여쓰기) 0건 — 1/1 적용 확인.
- import 누락 점검: 이번 변경은 템플릿 문자열 1건 + 코드 주석 1건뿐이며 script 구조·`<script setup>` import·CSS 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
