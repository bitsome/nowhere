# UI 개선 보고서 — 2026-09-06 00:06

## 실행 결과 요약
- **수정 4건** — 09-05 23:02(56회차) 이후 `frontend/src` 변경 0건(mtime 스캔·git 상태 확인, 마지막 변경은 56회차 자체 수정 SettingsView 23:03)이라 신규 기능 코드가 없어 전수 열람 대상이 없음. 09-05 중 전체 재열람이 밀려 있던 **정산(SettlementView)·대시보드(DashboardView)·처리할 일(ActionCenterView 잔여 구간)** 을 신선 시점 점검하고, 앱 표준 '밝은 상태색 채움 위 어두운 글자(#101418)' 규칙을 대조하는 전역 감사를 수행. 그 결과 **`--status-completed`(초록 #18a058/#27946a) 채움 위 `#ffffff` 흰 글자를 쓰는 상태 칩 4곳**(문의 답변 상태 칩 · 증빙 심사 승인 칩)이 같은 화면의 앰버 칩(#f0a800+#101418) 및 OrderCard 상태 배지 표준(주석: "completed는 초록 — 둘 다 밝아 어두운 글자")과 비대칭인 소형 결함을 발견해 `#101418`로 통일 수정. 직전 회차(23:02) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-06 00:06 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 09-05 23:02 이후 `frontend/src` 변경 0건 (`Get-ChildItem LastWriteTime > 23:02` 결과 없음 — 마지막 수정은 56회차 자체 적용분 SettingsView.vue 23:03:45뿐)
- **직접 열람(신선 시점 점검)**: `views/SettlementView.vue`(전체 563줄 — 출금 가능 히어로·출금 계좌·출금 내역·최근 정산 + scoped CSS) · `views/DashboardView.vue`(전체 835줄 — 오늘/내일 운행·요약·정산 현황·차트·상태 분포 + scoped CSS) · `views/orders/ActionCenterView.vue`(전체 935줄 — 승인 요청·요금 제안·채팅 요청 아코디언 카드 + scoped CSS, 50회차에서 본 L500-930 외 L1-500 템플릿/script 포함)
- **대조·감사(grep)**: `background: var(--status-*)` 채움 9건 전수 대조(텍스트 색 규칙) · `#fff|#ffffff|var(--bg)` 텍스트 75건 문맥 확인(이미지 갤러리 오버레이·아바타·danger 채움은 정상) · `background: var(--brand)/var(--accent)` 56건(기존 'brand/accent 위 글자색 전역 설계' 후보 범주) · `font-size: 7/8/9px` 0건 / `원원`·`formatWon(...)원` 단위 중복 0건(DashboardView·UserPageView 7건은 지역 formatWon 단위 미포함 정상) / `주문번호|order_no|order_number` — AdminView 운영 개입 2건(L1771·L2030)뿐 / UI "드라이버" — `data/levels.js` 레벨 타이틀(기존 반복 후보)뿐, 나머지는 코드 주석
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_2302.md`(56회차)·`2204.md`(55회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
- **신규 소형 결함 1종(4건, 수정)**: '답변 완료/승인' 초록 칩(`--status-completed`) 위 흰 글자 저대비.
  - 앱 표준 확정 근거: OrderCard 상태 배지 CSS 주석 — "밝은 배경 배지 — 흰 글자 대비가 약하므로 라이트·다크 모두 어두운 글자(접근성)"로 `published/driving/trading/acceptance-pending/accepted/completed` 채움 위 텍스트가 `#101418`(L609-620). RideHistoryView도 동일 규칙(상태 채움 위 다크 글자 전환). 09-04 10:01·11:04·14:01 수정과 같은 결함 계열.
  - 실제 토큰 대비: 라이트 `--status-completed #18a058` 위 흰 글자 ≈ 3.1:1(AA 4.5 미달) vs `#101418` ≈ 5.7:1 · 다크 `#27946a` 위 `#101418` ≈ 4.8:1 — 다크 글자가 라이트·다크 공통으로 안전.
  - 반면 같은 파일들의 '대기' 앰버 칩은 전부 `#f0a800` + `#101418`, '거절'은 danger + 흰 글자 — 초록 칩만 형제 칩과 비대칭.
- 점검 결과 정합 확인 사항(수정 외):
  1. **SettlementView 정합**: 출금 가능 히어로 CTA(채움 위 `#07120e`)·계좌 저장 버튼(라이트 var(--text)+var(--bg) / 다크 brand+#07120e)·상태 dot(대기=brand 틴트/지급 완료=--status-settled/거절=danger)·지급·정산 상태 칩(1px 6px·10px·400) — 배지 스펙·의미색 준수. '출금하기' Primary 1개 + 안내 문구 순서(정보→행동) 정합.
  2. **DashboardView 정합**: 오늘(민트 틴트)/내일(앰버 틴트) 카드는 투명 틴트 위 기본 텍스트라 대비 정상, 상태색 채움·흰 글자 사례 없음(초록 채움 3곳은 차트 막대·상태 dot·진행 바로 텍스트 미포함). 정산 처리 버튼(등록자만)·`#101418`/`#07120e` 채움 텍스트 규칙 준수.
  3. **ActionCenterView 정합**: 카드 건수·승인 요청 인디케이터·타입 칩(1px 6px·10px·400), 거절(레드 틴트)/승인(brand 채움+#07120e)/채팅(ghost) 버튼 — 배지·의미색 준수. 거절+승인 2버튼 구성은 '문제 운행만 수동 처리' 문맥상 대응 행동 쌍으로 유지 판정(기존 관례).
  4. **ActionCenterView 채팅 요청 타입 칩 색(모두 brand 틴트, 취소만 레드)**: ChatList/ChatRequestEvent의 시간·경로·요금 변경 앰버 분류와 다르지만, 이곳은 '지금 처리할 요청' 집합 안내 맥락으로 틸 통일이 의도일 수 있어 **색 결정 사항으로 후보만 기록**(아래 e).
  5. **전역 캐너리 통과**: 단위 중복·10px 미만 폰트·기술 식별자(관리자 콘솔 제외)·UI "드라이버"(레벨 타이틀 제외) 모두 0건.

## (d) 실제 적용한 수정
상태색 채움 위 흰 글자 → 어두운 글자(`#101418`) 통일(4건, 모두 scoped CSS 값만 변경):
1. [SupportView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SupportView.vue#L409-L412) — 내 문의 목록 '답변 완료' 칩 `.support-ticket-item__status--answered`
2. [SettingsVerificationView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsVerificationView.vue#L300-L303) — 차량·면허 인증 '승인 완료' 칩 `.verify-chip--done`
3. [AdminView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/AdminView.vue#L3177-L3180) — 증빙 심사 '승인' 칩 `.v-chip--approved`
4. [AdminView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/AdminView.vue#L3343-L3346) — 고객지원 관리 '답변 완료' 칩 `.support-ticket-chip--answered`

```css
/* Before — 흰 글자: --status-completed(#18a058/#27946a) 위 대비 부족 */
background: var(--status-completed);
color: #ffffff;

/* After — 앱 표준(OrderCard .status-badge--completed 등)과 동일한 다크 텍스트 */
background: var(--status-completed);
color: #101418;
```
거절 칩(danger + 흰 글자)은 의도대로 유지(수정 대상 아님).

## (e) 적용하지 않은 항목과 이유 (후보)
- **ActionCenterView 채팅 요청 타입 칩 색(시간·경로·요금 변경을 ChatList의 앰버 대신 틸로 통일)**: 같은 유형 시맨틱이 채팅(앰버)과 액션센터(틸)에서 다르게 표기 — 색 의미 통일 관점의 후보이나, '지금 처리할 요청' 집합 안내라는 화면 맥락 차이의 의도일 수 있어 디자인 결정으로 후보 기록(반복 미적용).
- **DashboardView scoped CSS 중복·데드 규칙**: `.settle-item`·`.settle-item__label`·`.settle-item__value` 이중 정의(정산 현황 컬럼 vs 건당 평균 행의 서로 다른 레이아웃 의도가 동일 클래스에 충돌, 후행 정의 우선으로 두 사용처 모두 컬럼 스택+테두리 박스로 수렴해 렌더는 일관) + `.dash-columns` 중복 정의 + `.settle-bar`/`.settle-bar__fill` 미사용(데드). 렌더링 영향 없는 CSS 정리·의도 확정 필요로 후보 기록(기존 '화면 내 CSS 중복' 분류에 통합).
- **기존 반복 후보(레벨 타이틀 "드라이버" data/levels.js↔LevelTable.php, 홈 추천 조건% 미노출, LoginView·RegisterView auth CSS 중복, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마, v-chip/verify-chip 패딩 차이, 문의 placeholder 문구, MarketView 데드 CSS, brand/accent 위 글자색 전역 설계 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) — **통과**, 4362 modules, built in 999ms. 수정 화면 청크 정상 생성(AdminView·SupportView·SettingsVerificationView CSS/JS 포함).
- import 누락 점검: 수정 4건 모두 scoped CSS 색상 값만 변경 — script·템플릿·import 변경 없음, 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
