# UI 개선 보고서 — 2026-09-10 18:01

## 실행 결과 요약
- **코드 수정 2건(2개 파일)** — 이번 회차는 `frontend/src` 의 **아이콘 계약(정의 키 ↔ 사용 키)** 을 처음으로 전수 대조해 두 건의 실제 결함을 찾아 교정했다.
  1. [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L221-L229) 바로가기 "대시보드"가 `name="grid"`를 쓰는데, `grid`는 [MoreView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MoreView.vue#L137)의 **"운영 관리"(관리자)** 전용 글리프라 화면 간 의미가 충돌한다. [icons.js](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L106)에 대시보드용으로 정의된 `dashboard: SpeedometerOutline` 키가 **미사용 상태**여서 `dashboard`로 교체.
  2. [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L273) `startRemainIcon`의 폴백이 `?? 'time'`인데 **`time`은 ICONS에 없는 키**다 → `iconOf()`가 `help`(정보 ⓘ)로 폴백해 "출발까지 남은 시간" 자리에 무관한 아이콘이 뜨는 잠재 결함. 같은 시계 글리프인 `history`(TimeOutline)로 교정.
- 실행 전 상태 점검: HEAD `f240340`, `frontend/src` 최신 mtime = 16:02:33(`SharedOrderView.vue`, 16:02 회차 적용분) → **17:01 회차 이후 신규 편집 0건**(안정 상태). 즉 신규 산출물이 없어 이번 회차는 기존 코드 계약 감사로 방향을 전환했다.
- 전역 표준 스캔 신규 위반 0건: "오더"/주문번호 0건, 10px 미만 폰트 0건, 보조용언 붙여쓰기 0건, **미정의 아이콘 키 0건**.
- `npm run build` 통과(`✓ built in 1.02s`). import·DOM·레이아웃 변경 0건(아이콘 키 문자열 2곳).
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 18:01~18:14 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_1701.md`(변경 없음 + Blade 공유 페이지 검토) · `2026-09-10_1602.md`(적용 1건: 공유 화면 Primary CTA 다크 예외) · `2026-09-10_1501.md`(적용 1건: 랜딩 CTA 다크 예외) — 적용 내역·미적용 후보 전수 대조, **반복 적용 없음**(이번 2건은 역대 보고서에 등장하지 않는 신규 항목).
- **실행 전 상태 점검**: `Get-Date`(18:01) + `git log --oneline -3`(HEAD `f240340`) + `git status --short`(미커밋 백엔드·프론트 혼재, `??` 신규 파일 목록) + `frontend/src` mtime 상위 25건 → 16:02:33 이후 신규 편집 0건.
- **아이콘 전량 대조(이번 회차 핵심)**: [icons.js](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js)(전량 198행 — ICONS 이름→컴포넌트 맵 75개 키, `CoinIcon` 자체 SVG, `iconOf()` 미정의 시 `help` 폴백), [BaseIcon.vue](file:///d:/wwwroot/nowhere/frontend/src/components/common/BaseIcon.vue)(`iconOf(props.name)` 경유 확인), [UiListRow.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiListRow.vue#L28-L33)(`icon` prop → `BaseIcon :name` 전달 경로 확인).
- **사용처 스캔(정의↔사용 대조)**: `frontend/src` 전체 `name="…"` / `icon="…"` 286행 추출 → ICONS 키 집합과 비교. 실제 문자열 아이콘 이름 중 **미정의 키 0건**(동적 바인딩 `:name="startRemainIcon"`·슬롯 `name="icon"`은 비대상으로 분류).
- **결함 구간 판독**: [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L205-L264)(바로가기 카드 5행 — 내 마켓/대시보드/사용자 커뮤니티/내가 올린 글/설정), [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L235-L273)(`startRemain` 텍스트·긴급도 레벨 산출 + `START_REMAIN_ICONS` 매핑 + 폴백) · [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L605-L640)(어필 태그 칩 — 3px 9px/10px/400, 탭 영역 확보용 의도값으로 기존 분류 유지), [MoreView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MoreView.vue#L95-L161)(내 운행/커뮤니티/운영/설정/고객지원/계정 메뉴 + script 1~65행 `isCustomer` 정의 확인), [MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L955-L986)(필터 바 내 마켓·검색·찜 아이콘 버튼, `aria-label` 부여 확인).
- **전역 그렙**: `오더|주문번호|주문 번호|오더번호|운행번호`(0건) · `font-size: [0-9]px`(0건 — 10px 미만 없음) · `[가-힣](해주세요|해보세요|해드려요|드려요)`(0건) · `'time'`(1건 — OrderCard 폴백, 이번 교정 대상) · `dashboard`(8행 전수 — 라우트명·컴포저블·HeaderBar 라벨 외에 **아이콘 키 사용 0건** 확인).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·정보 우선순위), 프로젝트 배지·색상 표준(1px 6px / 10px / 400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출).

## (c) 발견한 개선 포인트
1. **ProfileView "대시보드" 아이콘 의미 충돌 + 미사용 전용 키(신규 발견·적용)**: [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L223) 바로가기의 "대시보드"가 `grid`(4분할 격자)를 쓴다. 그런데 같은 `grid` 글리프는 [MoreView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MoreView.vue#L137)에서 **관리자 "운영 관리"** 를 나타내므로, 두 화면을 오가는 사용자에게 동일 글리프가 다른 목적지를 가리킨다. 반면 [icons.js](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L106)에는 "더보기/빠른 메뉴" 그룹에 `dashboard: SpeedometerOutline`(계기판)가 **정의만 되고 사용처 0건**이다 — 바로 ProfileView 바로가기 목록과 같은 성격의 메뉴용으로 만들어진 키다. `dashboard`로 교체하면 ① 의미 충돌 해소 ② 기존에 정의된 키의 사문화 해소가 동시에 된다.
2. **OrderCard 출발 임박 아이콘 폴백이 미정의 키(신규 발견·적용)**: [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L265-L273)는 긴급도별 아이콘을 `green:'moon' / orange:'alarm' / yellow:'car' / teal:'speedometer'`로 매핑하고 폴백을 `?? 'time'`으로 둔다. 그런데 ICONS에 `time` 키는 없고 시계 글리프는 `history`(TimeOutline)뿐이다 → `iconOf()`가 [help 폴백](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L196-L198)을 타 **"출발 N시간 전" 옆에 정보(ⓘ) 아이콘**이 렌더된다. 현재는 네 레벨이 모두 매핑돼 도달 불가지만, 폴백의 의도(시계)가 깨져 있어 레벨이 추가되는 순간 조용히 잘못된 아이콘이 노출되는 구조다.
3. **아이콘 계약 자체는 대체로 정합**: 사용처 286행 중 실제 문자열 아이콘 이름의 미정의 키는 0건(오타·데드 키 없음). `:name="startRemainIcon"`(동적)·`<slot name="icon">`(슬롯) 같은 비아이콘 `name`은 정상 분류. ProfileView 바로가기 5행은 모두 `aria-label` 없는 버튼이지만 **텍스트 라벨(strong+small)을 포함**하므로 접근성 결함 아님.
4. **MoreView 아이콘 중복은 등록자 역할에서만 발생(후보 유지)**: [MoreView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MoreView.vue#L120-L123)에서 "정산·입금"(`v-if="isCustomer"`)과 "처리할 일"(전 역할)이 둘 다 `cash`. 기사에게는 "정산"(`coin`)과 겹치지 않으므로 문제는 등록자 화면 한정이다. 다만 대체 아이콘(`bell`/`check-done`/`inbox`) 선택이 화면 의미 판단이라 17:01 회차와 동일하게 후보로 유지.
5. **"내 마켓" 아이콘이 화면마다 다름(신규 관찰·후보)**: 같은 목적지(`my-market`)를 [MoreView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MoreView.vue#L122)는 `my-market`(CarOutline, 자동차), [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L212)·[MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L964)는 `cart`(쇼핑 카트)로 표기한다. "등록한 운행 관리"라는 의미에는 자동차가 맞지만 2:1로 `cart`가 다수여서 **어느 쪽을 표준으로 삼을지 결정이 필요** → 후보.
6. **기존 반복 후보(상태 재확인, 유지)**: FAB 3곳 흰 글리프 · `--accent` 아바타 흰 글자 · EmptyState 아이콘 변형색 하드코딩 · `qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · OrderFavoritesView eyebrow `--danger` · coin+`원` 이중 표기 · 관리자 화면 상태 칩 규격 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · HeaderBar 라우트 표 데드 엔트리 · 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종 혼재 · 말줄임표/모달 푸터 문구 혼재 · `new Date(created_at_iso)` 방어 · SettingsVehiclesView 로딩 스켈레톤 부재 · HomeView 탭 카운트 불일치 · `role === 'Driver'` 리터럴 상수 치환 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/ProfileView.vue` | 바로가기 "대시보드" 아이콘 `name="grid"` → `name="dashboard"`(계기판). `grid`는 MoreView 관리자 "운영 관리" 글리프라 화면 간 의미 충돌 → ICONS에 정의만 돼 있던 대시보드 전용 키로 정리. DOM·텍스트·동작 무변경(22px 크기 유지) |
| `components/orders/OrderCard.vue` | `startRemainIcon` 폴백 `?? 'time'` → `?? 'history'`. `time`은 ICONS 미정의 키라 `help`(정보)로 폴백되던 잠재 결함 — 동일 시계 글리프(TimeOutline)로 교정해 "출발까지 남은 시간" 의미와 일치 |

- 공통: `<script>` 문장 1곳 + `<template>` 속성 1곳, **import·컴포넌트 등록·props·emit·DOM 구조·CSS 무변경**. 두 파일 모두 단일 편집(순차 실행)이라 원복 위험 없음 — 수정 후 그렙으로 지속성 확인(아래).
- 수정 후 확인: `?? 'history'` 1건([OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L273)), `BaseIcon name="dashboard"` 1건([ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L223)) 잔존. `git diff --stat` 결과 `OrderCard.vue` 1± / `ProfileView.vue` 1± = **정확히 2행 변경**.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **MoreView "정산·입금"/"처리할 일" 아이콘 중복(`cash`)**: 등록자 역할에서만 동시 노출되는 실제 중복이지만, "처리할 일"을 `bell`(알림과 혼동)·`check-done`·`inbox`(=`list`, "내 운행"과 중복) 중 무엇으로 바꿀지, 혹은 "정산·입금"을 `wallet`으로 돌릴지가 **화면 의미 판단**이고 `aria-label`·문구·동작에는 결함이 없다 → 후보 유지(17:01 기록과 동일).
2. **"내 마켓" 아이콘 표기 불일치(`my-market` vs `cart`)**: 다수결(cart 2곳)과 의미 적합성(CarOutline)이 충돌한다. 표준을 정하는 순간 3개 화면 중 1~2곳이 바뀌므로 **전역 아이콘 사전 결정 사안** → 후보.
3. **FAB 3곳(`var(--brand)` + 흰 글리프)**: 아이콘 관용 + 전역 brand 위 글자색 설계 결정 건으로 09-03~09-10 여러 회차에서 반복 분류된 항목 → 기존 분류 유지(반복 미적용).
4. **관리자 화면 상태 칩 잔여분(AdminView 6곳 — 1px 8px·2px 8px / 700)**: 정보 밀도가 높은 운영 도구로 굵은 칩이 의도된 구분일 수 있어 기사 앱 화면 우선 원칙에 따라 후보 유지.
5. **`assets/base.css` 레거시 `.card`·`.btn` 계열(465~534행)**: 사용처 0건으로 확정된 죽은 CSS. UI 개선이 아닌 정리 작업이고 동적 클래스 결합 가능성을 배제할 수 없어 후보 유지.
6. **기존 반복 후보(17:01·16:02·15:01 기록분 유지)**: 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종(`#f0a800` vs `--warn` #ffa940 vs `--status-acceptance-pending` #c28a2e) · 말줄임표 ASCII/`…` 혼재 · 모달 푸터 '닫기'/'취소' 혼재 · SettingsVehiclesView 로딩 중 빈 카드(스켈레톤 부재) · AdminView 금액 인라인 포맷 · HomeView `.home-more` 공통화 · SettlementView 계좌 마크업 중복 · `new Date(created_at_iso)` 방어 · 공유 크롤러 Blade 페이지 링크 색(`#36adff` on `#f4f4f4`, 대비 ≈2.2:1 — Blade 공개 페이지 링크 색 규약 미정) — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite, `✓ built in 1.02s`, 오류·경고 0건) — 수정 반영 후 실행. `ProfileView`·`OrderCard` 청크 정상 재생성(OrderCard·ProfileView 포함 번들 목록 확인).
- import 누락 점검: 변경이 ① 아이콘 키 문자열 ② computed 폴백 문자열뿐이라 import·심볼 영향 0건. 참조한 `dashboard` 키는 [icons.js](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L106)에 실제 정의(`SpeedometerOutline`), `history` 키는 [동일 파일](file:///d:/wwwroot/nowhere/frontend/src/utils/icons.js#L108)에 정의(`TimeOutline`)로 존재 확인 → 빌드 통과로 검증 대체.
- 아이콘 계약 재검증: 사용처 전수 대조 결과 **미정의 아이콘 키 0건**(동적 바인딩·슬롯 제외).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
