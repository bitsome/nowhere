# UI 개선 보고서 — 2026-09-05 12:02

## 실행 결과 요약
- **수정 1건** — 11:00(44회차) 이후 `frontend/src` 변경 14건(11:17~11:54: 채팅 요청 카드 수락/거절 · 정산·출금 신규 화면 · 관리자 자동운행·출금 처리 · 홈 추천 근거 카드 활성화) 전수 열람. 신규 화면·변경분은 대체로 가이드(한국어/"운행" 용어/배지 스펙/색 의미론) 준수. 유일한 명확한 소형 결함: SettlementView 출금 신청 성공 토스트의 단위 중복("원원")을 수정. 직전 회차(11:00) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 12:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 11:00 이후 `frontend/src` 변경 14건 — `api/chats.js`(11:17) · `stores/chats.js`(11:17) · `components/chat/ChatRequestEvent.vue`(11:18) · `views/orders/ActionCenterView.vue`(11:19) · `composables/useOrderDetail.js`(11:24) · `views/orders/OrderDetailView.vue`(11:25) · `api/admin.js`(11:41) · `api/settlement.js`(11:41) · `views/SettlementView.vue`(11:42) · `router/index.js`(11:42) · `views/MoreView.vue`(11:42) · `views/AdminView.vue`(11:43) · `views/HomeView.vue`(11:53) · `components/orders/OrderCard.vue`(11:54)
- **직접 열람**: 위 중 UI 노출 파일 전수(템플릿+scoped CSS) — SettlementView(신규, 전체) · ChatRequestEvent(신규, 전체) · AdminView(신규, 템플릿 전체) · ActionCenterView(템플릿+CSS) · OrderDetailView(템플릿 전체) · HomeView(추천 탭/히어로 템플릿) · OrderCard(전체) · MoreView(템플릿) · stores/chats.js(전체) · router/index.js(전체)
- **전역 감사(grep)**: `font-size` 10px 미만 0건 / "주문번호|order_number|오더번호" 노출 0건 / 배지 스펙(padding 1px 6px + font-size 10px + font-weight≠400) 위반 0건 / `원원`·`formatWon(...)원` 단위 중복은 SettlementView L95 1건 발견(→수정), DashboardView의 `formatWon(..)원`은 지역 formatWon이 단위 미포함이라 정상 확인
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1100.md`(44회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·추천 카드 규격), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **SettlementView 출금 성공 토스트 단위 중복**: 지역 `formatWon`이 이미 "원"을 붙이는데 메시지에 `원 지급 대기`를 덧붙여 "45,000원원 지급 대기"로 출력되는 결함(L95). → 수정 대상.
2. **홈 추천 카드에 근거 체크리스트 도입(설계 방향성 정합)**: OrderCard에 `showMatchReasons` prop(L37-40)·`matchReasons`(L49) 추가, HomeView가 단일/왕복 추천 카드에 `:show-match-reasons` 전달(L696·L705), 카드 하단에 ✓ 근거 목록 렌더(L232-237, "퍼센트 없이 이유 문구만" 주석). PRODUCT_VISION 추천 카드 규격의 근거 체크리스트 부분이 구현되어 직전 회차(11:00 c-3)의 "match_reasons 프론트 사용처 0건" 관찰이 해소됨. 조건%는 여전히 미노출(아래 e-1).
3. **채팅 요청 카드 확정 흐름 일관성**: ChatRequestEvent는 받은 미처리 시간/경로/요금/취소 요청은 카드에서 수락·거절(수락=민트 계열, 거절=레드), '승인 요청'은 채팅 카드가 안내·기록 역할만 하고 Action Center 이동 버튼 제공 — 역할(운행 업무센터) 분리에 정합. 라벨·카드 타이틀·결과 문구("수락되었습니다/거절되었습니다")·상태 배지 스펙 모두 준수.
4. **신규 화면 클린**: SettlementView(출금 가능/이번 달 정산 요약 + 출금하기 Primary 1개 + 계좌/출금/정산 내역 — 금액 히어로 최상단 배치, 상태 배지 스펙 준수) · AdminView(관리자 전용 naive-ui 콘솔 — 사용자/기사/자동 운행/출금 처리 탭, "운행" 용어, 대기 건수·상태 표기 정상) · MoreView(정산 메뉴 기사 전용 추가 L121) — 신규 UI 위반 없음.
5. **전역 캐너리 통과**: 10px 미만 폰트 0건, 기술 식별자 노출 0건, 배지 스펙 위반 0건.

## (d) 실제 적용한 수정
- [SettlementView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SettlementView.vue#L95) — 출금 신청 성공 메시지의 중복 단위 제거:
  - 전: `출금 신청 완료 — ${formatWon(...)}원 지급 대기` → "45,000원원 지급 대기" (단위 중복)
  - 후: `출금 신청 완료 — ${formatWon(...)} 지급 대기` → "45,000원 지급 대기"
- 그 외 수정 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
- **홈 추천 카드 조건%(match_score) 미노출**: 근거 체크리스트만 노출하는 것이 OrderCard 주석의 명시적 설계 결정이며, % 추가는 제품 결정(색·위치) 필요 — 적용 대상 아님(기존 후보 유지).
- **SettlementView 출금 가능 금액 색상(--text, 무채색)**: 정산 화면 금액 강조(옐로우 계열) 여부는 전역 의미색 체계와 함께 제품 결정 필요 — 후보.
- **OrderCard `<article>` 들여쓰기 불일치(L172~178)**: 렌더링엔 무영향 서식 문제 — 대형 리팩터링 시 정리 대상(후보).
- **ActionCenterView "승인 요청" 섹션 헤더·카드 인디케이터·본문 문구 중복성**: 각각 섹션명/카테고리/상태 설명 역할이 달라 유지.
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, HomeView 탭 카운트, OrderCreateView CSS 중복·들여쓰기, 커뮤니티 카테고리 색, naive 전역 테마, 의미색 하드코딩 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) **통과** — 4352 modules, 1.04s. SettlementView 청크 정상 생성.
- import 누락 점검: 수정이 문자열 리터럴 1건에 국한되어 신규 import 없음 — 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
