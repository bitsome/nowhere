# UI 개선 보고서 — 2026-09-02 20:01

## (a) 실행 시각
- 2026-09-02 20:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰: `views/ReviewsView.vue`, `views/ProfileView.vue`, `views/community/UserPageView.vue`, `views/community/CommunityView.vue`, `views/community/CommunityPostView.vue`, `views/auth/LoginView.vue`, `views/auth/RegisterView.vue`, `views/AdminView.vue`, `views/DashboardView.vue`, `views/HomeView.vue`(일부), `views/NotificationsView.vue`(일부)
- 컴포넌트: `components/common/LevelBadge.vue`, `components/orders/OrderCard.vue`, `components/orders/SetGroupCard.vue`, `components/chat/ChatList.vue`, `components/chat/ChatRequestSheet.vue`(일부)
- CSS/기타: `assets/base.css`(토큰 정의), `utils/formatTime.js`, `utils/colors.js`, `data/levels.js`, `utils/chatTime.js`(대조)
- 백엔드 직렬화 대조(프론트 로컬 작업 범위 밖, 표시 형식 확인용): `app/Http/Controllers/Api/ReviewController.php`, `app/Services/Community/UserProfileService.php`, `app/Http/Controllers/Api/AdminController.php`, `app/Http/Controllers/Api/NotificationController.php`
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_0914.md`, `UI_개선_2026-09-02_1002.md`, `UI_개선_2026-09-02_1603.md`, `UI_개선_2026-09-02_1801.md`, `UI_개선_2026-09-02_1901.md`
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "기사" 용어 / 배지 스펙 1px 6px·10px·400)

## (c) 발견한 개선 포인트
1. **리뷰 작성 시각이 영문 상대시간으로 노출 (신규 발견)**: 서버는 리뷰 `created_at`을 `diffForHumans()`(영문, 예: "5 minutes ago")로 반환하는데, ReviewsView(받은/쓴 리뷰 2곳)·ProfileView(활동 탭)·UserPageView(사용자 리뷰 탭)가 이를 그대로 렌더 — 한글 UI에 영문 노출. 같은 서버 형식을 쓰는 알림센터(ChatList `last_message_at`, NotificationsView `created_at`)는 이미 `formatTime()`으로 변환해 사용 중이라 리뷰 화면만 일관성에서 벗어남.
2. **그 외 재확인 결과 클린**: UI 노출 "드라이버"(다이얼로그·메시지·안내) 0건(비노출 주석·백엔드 동기 데이터 `levels.js`만 잔존), 낡은 fallback hex(`var(--x, #…)`) 0건, 영문 placeholder 0건, UI 노출 "오더"/"유저"/"히스토리" 0건, `order_number` 0건 — 19:01 감사 결과 유지.
3. **AdminView·chat-order 등 `created_at`은 절대형(`Y-m-d`/`Y-m-d H:i`) 또는 `formatTime`/`getChatTimestamp` 처리 확인**: 관리자 화면 절대 시각·채팅 변환은 현행 유지가 적절.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/ReviewsView.vue` | 받은 리뷰·내가 쓴 리뷰 타임스탬프 `{{ review.created_at }}` → `{{ formatTime(review.created_at) }}` 2곳 + `../utils/formatTime` import 추가 — 영문 상대시간("5 minutes ago")을 한국어("5분 전")로 변환 |
| `views/ProfileView.vue` | 활동 탭 받은 리뷰 타임스탬프 동일 변환 1곳 + `../utils/formatTime` import 추가 |
| `views/community/UserPageView.vue` | 사용자 리뷰 타임스탬프 동일 변환 1곳 + `../../utils/formatTime` import 추가 (기존 로컬 `timeAgo`는 ISO용 게시글 시간과 분리 유지) |

변환 헬퍼는 알림센터가 동일 백엔드 형식에 이미 쓰는 `utils/formatTime.js`를 재사용 — 형식 불일치 없음, 미매칭 문자열은 원문 그대로 반환하므로 절대형 변경 대비 안전.

## (e) 적용하지 않은 항목과 이유 (후보)
- **data/levels.js 레벨 타이틀 "신입/일반/…/마스터 드라이버"**: 백엔드 `LevelTable.php`와 동기화된 값 — 교차 변경 필요(기존 보고서 후보 유지).
- **코드 주석 내 "드라이버"(api/auth.js·MoreView·HomeView·useOrderDetail 등)**: UI·에러 메시지 노출이 아니며 기존 방침대로 비노출 주석은 유지.
- **리뷰 별점 하드코딩 `#ffa940`(ReviewsView `.review-item__stars`·ProfileView/UserPageView `n-rate color`)**: `--warn`과 라이트 값은 동일(#ffa940)하나 다크 오버라이드는 `--warn`이 #c98f3e로 톤다운되어 토큰화 시 다크 별점 색이 어두워짐 — 색상 체계 판단 필요(기존 보고서 후보와 동일 계열).
- **ReviewsView `.reviews-tabs__count`(1px 7px/11px/700)**: 활성 탭 위 흰색(불투명 25%) 카운트로, 배지 스펙(10px/400) 적용 시 활성 탭 대비가 약해져 보임 — 타 탭 카운트(NotificationsView·ActionCenterView)와 동일 적용 여부는 탭 액티브 상태 디자인과 함께 재검토 대상.
- **formatTime.js "1 year ago" 등 연 단위 미매핑**: 알림센터와 동일 한계(동작 일관)이며 기존 util 확장은 별도 판단 필요.
- **OrderDetailView 의미색 하드코딩·confetti 색 배열·다크 모드 brand 위 흰 글자(--on-brand)·ChatList 타입 칩·EmptyState 5색·OrderCard 긴급도 틸**: 전역 설계/디자인 결정 사항(기존 보고서 후보 유지, 반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 937ms`, 4343 modules transformed, 에러 0건 (`formatTime` 청크가 신규 번들에 포함됨).
- 수정 후 grep 검증: `{{ review.created_at }}`(리뷰 타임스탬프 원문 렌더) 0건.
- import 누락 점검: 3개 뷰 모두 `utils/formatTime.js` import를 추가했고 템플릿·스크립트 다른 참조는 그대로 — 빌드 통과로 import/사용부 정합 확인. **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
