# UI 개선 보고서 — 2026-09-02 21:01

## (a) 실행 시각
- 2026-09-02 21:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰: `views/community/CommunityView.vue`, `views/community/CommunityPostView.vue`, `views/community/UserPageView.vue`, `views/ChatView.vue`, `views/NotFoundView.vue`, `views/ProfileView.vue`(일부), `views/orders/OrderCreateView.vue`(템플릿 영역), `views/orders/OrderDetailView.vue`(모달 영역), `views/DashboardView.vue`(일부), `views/HomeView.vue`(일부)
- 컴포넌트: `components/orders/QuickMatchPanel.vue`(스위치·칩), `components/chat/ChatList.vue`·`ChatThread.vue`·`ChatRequestSheet.vue`·`ChatImageSheet.vue`, `components/ui/UiChip.vue`·`UiCard.vue`, `components/common/VerifiedBadge.vue`·`EmptyState.vue`·`LevelBadge.vue`(색 대조)
- CSS/기타: `assets/base.css`(토큰 대조), `utils/formatTime.js`·`utils/chatTime.js`(대조), `data/levels.js`(대조)
- 백엔드 대조(프론트 로컬 작업 범위 밖, 노출 형식 확인용): `diffForHumans` 반환 8곳 — `ChatService.php`(chat/대화), `UserProfileService.php`(사용자 리뷰), `NotificationController.php`(알림), `OrderTemplateController.php`(템플릿), `ReviewController.php`(리뷰)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_0914.md`, `_1002.md`, `_1603.md`, `_1801.md`, `_1901.md`, `_2001.md`
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "오더"→"운행" / 배지 스펙 1px 6px·10px·400)

## (c) 발견한 개선 포인트
1. **배지 스펙 위반 잔존 — 커뮤니티 "인기 글" 카드 (신규 발견)**: `.popular-card__badge`("인기" 표식)와 `.popular-card__cat`(인기 글 카테고리)이 `1px 7px / 11px / 700` — 배지 스펙(1px 6px/10px/400)에서 벗어남. 인기 글 목록은 커뮤니티 첫 화면 노출 요소라 이전 회차 배지 정리에서 누락돼 있던 부분.
2. **배지 스펙 위반 잔존 — 커뮤니티 피드 카테고리 칩**: `.feed-card__cat`(피드 카드의 카테고리 라벨)이 `1px 8px / 11px / 700` — 정보성 라벨 배지로 스펙 위반(색상 변형 6종은 그대로 유지).
3. **VIP 표기 화면 간 불일치 (신규 발견)**: 동일한 VIP 필 배지가 화면마다 제각각 —
   - MoreView `.more-hero__vip`·UserPageView `.badge--vip`(base) : **1px 6px / 10px / 400 (스펙 준수)**
   - 커뮤니티 피드·인기 글 `.feed-badge--vip`(CommunityView·CommunityPostView) : **2px 6px / 11px / 700** — 커뮤니티에서만 굵고 크게 보이는 불일치.
4. **기타 재확인 결과 클린**: 낡은 fallback hex(`var(--x, #…)`) 0건, `--text-secondary`/`--card-bg` 0건, `order_number` 0건, `review.created_at` 원문 렌더 0건(20:01 수정 유지), 영문 placeholder 0건(1곳 한글), UI 노출 "오더"/"히스토리"/"유저"/"드라이버" 0건(주석·백엔드 동기 데이터만), 혼합 외래어 표기 0건.
5. **diffForHumans 형식 노출 전수 확인**: OrderTemplateController의 템플릿 `updated_at`(diffForHumans)은 OrderCreateView 템플릿 목록에서 **미표시**(이름·노선만)라 영문 상대시간 노출 없음. 채팅·알림·리뷰는 기존 `formatTime`/`getChatTimestamp` 처리 유지, 관리자 화면 절대 시각은 현행 유지가 적절(20:01 판단과 동일).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/community/CommunityView.vue` | `.popular-card__badge`·`.popular-card__cat` `1px 7px/11px/700` → `1px 6px/10px/400` (인기 글 배지·카테고리) |
| `views/community/CommunityView.vue` | `.feed-card__cat` `1px 8px/11px/700` → `1px 6px/10px/400` (피드 카테고리 라벨, 6색 변형 유지) |
| `views/community/CommunityView.vue` | `.feed-badge` base 11px → 10px, `.feed-badge--vip` `2px 6px/11px/700` → `1px 6px/10px/400` (VIP 배지 — MoreView·UserPageView와 동일 스펙 통일) |
| `views/community/CommunityPostView.vue` | `.feed-card__cat` `1px 8px/11px/700` → `1px 6px/10px/400` |
| `views/community/CommunityPostView.vue` | `.feed-badge` base 11px → 10px, `.feed-badge--vip` `2px 6px/11px/700` → `1px 6px/10px/400` |

모두 `<style scoped>` 내 순수 CSS 속성 변경으로, 색상·구조·동작 변경 없음(인기 글 배지의 `--danger` 의미색·카테고리 6색 유지). 커뮤니티 목록(CommunityView)과 상세(CommunityPostView)가 동일 카테고리 칩·VIP 배지를 공유하므로 두 파일 모두 정리.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ReviewsView·CommunityView 탭/카운트 계열 `1px 7px`**: 활성 탭 위 흰색 오버레이 카운트로 10px/400 적용 시 대비가 약해짐 — 탭 액티브 디자인과 함께 재검토(20:01 후보 유지).
- **피드 액션 버튼(`.feed-action` 2px 6px/11px)·탭·정렬 칩(6px 12px/11px/600)·섹션 eyebrow(11px 800)**: 클릭형 버튼·라벨·강조 타이틀로 배지 스펙 대상 아님(16:03 판단 유지).
- **`#e5484d` 등 의미색 하드코딩(피드 좋아요 활성·OrderDetailView·confetti·OrderCard 긴급도 등)**: 토큰화 시 다크 모드 값(톤다운)으로 바뀌며 라이트/다크 양쪽 설계 검증 필요 — 색상 체계 결정 후 일괄 적용(기존 보고서 후보 유지).
- **`#f0a500`(MarketView 즐겨찾기 인라인)·ProfileView 스탯 아이콘 그린/앰버·QuickMatchPanel 스위치 그레이·UserPageView 검증 배지 색**: 토큰과 미세 값 차이/다크 대비 연동 — 색상 체계 결정 사항(후보).
- **다크 모드 brand 위 흰 글자(`--on-brand`)·ChatThread 상태 칩 흰 글자·EmptyState 5색·커뮤니티 카테고리 6색 grayscale 통일**: 전역 설계/디자인 결정 사항(기존 보고서 후보 유지, 반복 미적용).
- **OrderTemplateController `updated_at`(diffForHumans)은 프론트 미표시**라 수정 불필요 확인(서버 코드 변경 아님).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 881ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: 커뮤니티 2파일에서 `.feed-card__cat`·`.feed-badge--vip`·`.popular-card__*` 모두 `1px 6px/10px/400`로 통일, `padding: 1px 7px` 잔존 0건.
- import 누락 점검: 이번 변경은 순수 `<style scoped>` CSS 속성 5건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
