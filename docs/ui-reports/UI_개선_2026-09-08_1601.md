# UI 개선 보고서 — 2026-09-08 16:01

## 실행 결과 요약
- **수정 3건 적용** — 직전 실행(09-08 15:02) 보고서 작성 이후 `frontend/src` 신규 변경분 **1건**(App.vue 15:02:43)이며 이는 15:02 보고서의 데드 CSS 제거분 자체임을 확인 — 신규 코드 아님. 이번 실행은 **미사용 import 전역 감사(신규 차원)** 를 실시해 데드 import **3건**(OrderCreateView.vue `computed` / BaseIcon.vue `NIcon` / LevelBadge.vue `NButton·NModal`)을 사용처 0건 재확인 후 제거했다. 그 외 신규 위반(기술 식별자·용어·소형 폰트·배지 스펙) 0건.

## (a) 실행 시각
- 2026-09-08 16:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 15:02 이후 수정분)**: `frontend/src` 전체 재귀 — **1건**: `App.vue`(15:02:43). 파일 판독 결과 15:02 보고서의 적용분(데드 CSS `.bottom-nav__item--button` 제거)이 반영된 상태 — 이미 검토·반영된 변경이라 신규 코드 아님.
- **직접 판독(이번 실행 신규 감사 — 미사용 import 전역 스캔)**:
  - `views/orders/OrderCreateView.vue`(1~100 script) — `computed` import 후 스크립트·템플릿 사용 0건 확정
  - `components/common/BaseIcon.vue`(전체) — `NIcon` import 후 `<n-icon>`는 템플릿 kebab 사용, `NIcon` PascalCase 사용 0건
  - `components/common/LevelBadge.vue`(전체) — `NButton·NModal` import 후 `<n-modal>`/`<n-button>`는 템플릿 kebab 사용, PascalCase 사용 0건
  - 오탐 해소: `views/orders/OrderDetailView.vue`(6·21행) — `useMessage as useNaiveMessage` 별칭 import로 실제 21행에서 호출됨(정상 사용, 유지)
- **전역 규칙 감사(grep)**:
  - `import {…} from 'vue'` 전체 26개 파일 · `import X from` 컴포넌트 default import · `import {…} from 'naive-ui'` 등 **모든 named import의 사용 횟수 스캔**(전체 파일 \b토큰 카운트) — 사용 1회 이하(import만 존재) 파일 식별
  - naive-ui `<n-*>` kebab 태그 사용 파일 39건 중 **naive-ui import가 전혀 없는 파일**(DashboardView·LoginView·RegisterView·NotFoundView·ChatList·ConfirmDialog 등) 존재 확인 → NaiveUiResolver(unplugin-vue-components, vite.config.js 43~46) auto-import 의존이 전역에서 정상 동작함을 입증
  - `check:refs`(`npm run check:refs`) 실행 — 템플릿 참조 미정의 **0건**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1502.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **OrderCreateView.vue 데드 import `computed`(이번 실행 적용)**: `import { computed, onMounted, ref, watch } from 'vue'`에서 `computed`가 스크립트·템플릿 전체에 **0회** 사용(모든 계산 상태는 useOrderSchedule/useOrderForm/useOrderSet 컴포저블에서 공급). import 줄 제외 \bcomputed\b 발생 1건뿐이라 순수 데드 import — 제거로도 동작 변화 없음.
2. **BaseIcon.vue 데드 import `NIcon`(이번 실행 적용)**: `import { NIcon } from 'naive-ui'`이나 템플릿은 `<n-icon>`(kebab)만 사용 — naive-ui 컴포넌트는 vite의 NaiveUiResolver가 템플릿 단위로 auto-import하므로(이미 naive-ui import 없는 파일 다수에서 kebab `<n-*>`가 정상 빌드됨을 확인) PascalCase 명시 import는 무효. 제거 후에도 `<n-icon>`은 빌드 시 resolver가 동일하게 주입 — 렌더링 변화 없음.
3. **LevelBadge.vue 데드 import `NButton·NModal`(이번 실행 적용)**: BaseIcon과 동일 패턴. 템플릿의 `<n-modal>`(34행)·`<n-button>`(79행)은 kebab auto-import 대상으로 명시 import 불필요 — 제거해도 resolver가 동일 처리.
4. **신규 위반 — 0건(재확인)**: 기술 식별자·용어·소형 폰트·배지 스펙 등 반복 감사 차원 및 `check:refs`에서 위반 없음. 15:02 이후 신규 코드 자체가 없어 신규 결함 발생 요인 없음.
5. 기존 반복 후보 상태 변화 없음(15:02 기록분 그대로).

## (d) 실제 적용한 수정
- **OrderCreateView.vue — 미사용 `computed` import 제거**: `import { computed, onMounted, ref, watch } from 'vue'` → `import { onMounted, ref, watch } from 'vue'`. 잔존 import(onMounted 92행 / ref 22·49·50·51행 / watch 25·39행) 모두 사용 확인.
- **BaseIcon.vue — 미사용 `NIcon` import 제거**: `import { NIcon } from 'naive-ui'` 삭제. `<n-icon>`는 NaiveUiResolver auto-import로 동일 처리.
- **LevelBadge.vue — 미사용 `NButton·NModal` import 제거**: `import { NButton, NModal } from 'naive-ui'` 삭제. `<n-modal>`/`<n-button>`는 auto-import로 동일 처리.
- 세 건 모두 데드 import 제거로 렌더링·동작 변화 없음 — 로컬 전용 작업.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(15:02 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView '(운행 번호·날짜 포함 시…)' — 사용자가 운행 번호를 알 수 없어 안내 실효성 문제) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 11곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지(렌더링 영향 없거나 대형·디자인·제품 결정 수반, 반복 미적용).
- **OrderDetailView.vue `useNaiveMessage`**: 별칭 import(`useMessage as useNaiveMessage`)로 실제 21행에서 호출 — 데드 아님(오탐 해소, 유지).
- 이번 실행 신규 감사(미사용 import)에서 위 3건 외 적용할 신규 결함 없음(위 (c) 4 참조).

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, 1.21s. import 제거 후 OrderCreateView·LevelBadge·index 청크 정상 생성(n-icon·n-modal·n-button은 NaiveUiResolver가 auto-import하여 번들 포함 확인).
- import 누락 점검: 제거한 4개 식별자(computed·NIcon·NButton·NModal)의 잔존 템플릿/스크립트 사용 0건을 파일 판독으로 사전 확인했고, `check:refs` 재실행 결과 **미정의 템플릿 참조 0건** — 누락 없음. 잔존 vue import(onMounted·ref·watch) 및 naive 사용(useMessage 등)의 사용처 확인 완료.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
