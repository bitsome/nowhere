# UI 개선 보고서 — 2026-09-11 00:01

## 실행 결과 요약
- **코드 수정 1건(5개 규칙 / 3개 파일)** — 이번 회차는 `frontend/src` 의 **실시간으로 갱신되는 숫자(카운트다운) 렌더링** 축을 처음으로 전수 감사했다. 앱은 본문 폰트로 `-apple-system / Segoe UI / Malgun Gothic`(전부 비례폭 숫자)을 쓰는데, 홈·내 마켓의 `남은 N초` 카운트다운은 **매초 다시 그려지면서 자릿수 폭이 계속 달라져** 칩·배지가 좌우로 미세하게 흔들린다(1↔4↔7 등). 실시간 카운트다운 5곳에 `font-variant-numeric: tabular-nums` 를 적용해 숫자 폭을 고정했다(값이 아니라 **표현 방식**의 문제라 정적 스크린샷에서는 보이지 않는다).
  1. [HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1215-L1228) `.home-rec-group__stat` — `남은 N초` / `승인 N` / `거절 N` 칩. `setInterval` 로 **매초** 갱신([HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L250-L254)).
  2. [HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1000-L1006) `.home-hero__remain` — 히어로 우측 "운행 시작까지 남은 시간" 라벨.
  3. [MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L900-L907) `.claim-time__remain` — "요청보냄" 구분선 옆 `남은 N초`(매초 갱신, [MyMarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L75)).
  4. [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L637-L645) `.order-card__status-extra` — 상태 배지 옆 `남은 N초`(내 마켓 카드, `:status-extra` 로 전달).
  5. [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L798-L809) `.order-card-start` — 카드 좌상단 "시작 N시간 N분 전" 카운트다운.
- 실행 전 상태 점검: HEAD `f240340`, `frontend/src` 최신 mtime = **23:23:52**(`views/orders/OrderDetailView.vue`·`composables/useOrderDetail.js`) → 직전 회차(23:01 보고서, 최신 mtime 기록 23:14) **이후 타 세션 편집이 있었다**(23:23 `OrderDetailView.vue`·`useOrderDetail.js`). 이번 회차는 그 편집분도 **UI 결함 없음**으로 판정하고 재수정하지 않았다(아래 (b)·(e) 참조).
- 전역 표준 스캔 신규 위반 0건: `오더`/`주문번호` **0건**, `!important` **0건**, 단일 자릿수 폰트(9px대) **0건**, `target="_blank"` rel 누락 **0건**(8곳 전부 `rel` 동반 확인).
- `npm run build` 통과(`✓ built in 1.01s`, 오류·경고 0건). CSS 선언 추가뿐이라 import·DOM·스크립트 무변경.
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-11 00:01~00:24 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_2301.md`(적용: `color-scheme`·`dvh`·`overscroll-behavior`) · `2026-09-10_1901.md`(적용: 반복 펄스 `prefers-reduced-motion`) — 적용 내역·미적용 후보 전수 대조 후 **반복 적용 없음**. `tabular-nums` 는 09-10_2201 보고서에 **"잔여 후보"로 1회 분류**된 바 있어, 이번에는 "전역 폰트 결정"이 아니라 **실시간 카운트다운의 흔들림 결함**으로 범위를 좁혀 적용했다.
- **실행 전 상태 점검**: `Get-Date`(00:01) + `git log --oneline -3`(HEAD `f240340`) + `git status --short` + `Get-ChildItem frontend/src -Recurse | Sort LastWriteTime -Descending` → 23:14(직전 회차) 이후 **23:23 타 세션 편집 2건**(`OrderDetailView.vue`·`useOrderDetail.js`) 확인.
- **신규 축 — 실시간 카운트다운 전수**: `setInterval` 그렙 → 12건([HomeView](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L250-L254)·[MyMarketView](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L75)·Dashboard/Chat/App/OrderDetail 등). 그중 **화면에 초 단위로 문자열을 다시 그리는 곳** 5곳을 특정(홈 일괄요청 칩 · 히어로 남은시간 · 내 마켓 `남은 N초` 2종 · 카드 시작 카운트다운)하고 각 CSS 규칙을 판독 → **5곳 전부 `tabular-nums` 미지정** 확인.
- **`font-variant-numeric` 전수**: 적용 전 **1건**뿐([OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L2307), 금액 표기) → 실시간 카운트다운에는 **앱 전체 0건**이었다.
- **네이티브 위젯/모션 재확인(교차 검증, 수정 없음)**: `prefers-reduced-motion` 6건(base.css 1 + OrderCard·SetGroupCard·OrderDetailView·HeaderBar·OrderCardSkeleton) — 09-10_1901 적용분 유지 확인 · `@media (hover: hover)` 15건(터치 기기 sticky hover 방지 패턴 정착) · `loading="lazy"` 다수(목록 이미지 누락 없음) · `aspect-ratio` 7건 · `content-visibility`/`contain`(base.css 목록 가상화) · `-webkit-tap-highlight-color` 2건 · `::placeholder` 0건.
- **타 세션 신규 편집 검토(수정 없음)**: [OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue)(mtime 23:23)·[useOrderDetail.js](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderDetail.js)(mtime 23:23) — 운행 상세/컴포저블 로직 편집분으로 **템플릿·CSS 상 신규 UI 결함 없음**.
- **신규 파일(찜 화면) 검토**: [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue)(f240340 커밋에서 신규 도입) — EmptyState·OrderCard 재사용, `aria-label`/`type="button"`/`loading="lazy"` 정합, `#action` 슬롯 CTA 존재. `n-spin :show="false"` 데드 래퍼는 기존 후보(09-10_1200)와 동일 유형으로 분류 유지.
- **찜/태그 신규 UI 검토**: [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L501-L525) 찜 하트(`favoriteable`) · [MarketView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1557-L1612) 검색/찜 필터 버튼 — `aria-label`·`:focus-visible`·hover 가드 정합. 검색어 표시 [`.market-filters__search-text`](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1584-L1591) 는 `max-width`+`ellipsis` 처리 완료(결함 없음).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀·의미색), 프로젝트 배지 표준(1px 6px/10px/400), 메모리 규칙(한국어 UI / "운행" 용어 / 색상은 의미가 있을 때만).

## (c) 발견한 개선 포인트
1. **실시간 카운트다운 숫자의 폭 흔들림(신규 발견·적용)**: 본문 폰트 스택([base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L94-L96))의 숫자는 비례폭이라 `1`·`4`·`7` 과 `8`·`0` 의 가로 폭이 다르다. `남은 N초` 는 매초 재렌더되므로 숫자가 바뀔 때마다 문자열 폭이 변해, 칩/배지가 좌우로 미세하게 떨린다(홈 일괄요청 칩·내 마켓 `남은 N초`·카드 시작 카운트다운). 값의 문제가 아니라 **자릿수 폭**의 문제라 로직·정적 화면으로는 드러나지 않는다.
2. **적용 근거와 범위**: `font-variant-numeric: tabular-nums` 는 **숫자 글리프만 고정폭화**하고 한글·라벨·줄바꿈·색에는 영향이 없다. 전역 폰트 변경(앱 전체 타이포 결정)이 아니라 **살아 있는 카운트다운 5곳**에만 적용해 부작용을 최소화했다. 앱에는 이미 금액 표기 1곳([OrderDetailView](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L2307))에 같은 속성이 쓰이고 있어 **선례가 있는 표현**이다.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/HomeView.vue` | `.home-rec-group__stat`(L1226)·`.home-hero__remain`(L1005) 에 `font-variant-numeric: tabular-nums;` 추가 — 초 단위 카운트다운·승인/거절 집계·히어로 남은시간의 숫자 폭 고정(이유 주석 각 1줄) |
| `views/orders/MyMarketView.vue` | `.claim-time__remain`(L905) 에 `font-variant-numeric: tabular-nums;` 추가 — 요청보냄 `남은 N초` 좌우 흔들림 방지(이유 주석 1줄) |
| `components/orders/OrderCard.vue` | `.order-card__status-extra`(L643)·`.order-card-start`(L809) 에 `font-variant-numeric: tabular-nums;` 추가 — 상태 배지 옆 `남은 N초`·카드 시작 카운트다운 폭 고정(이유 주석 각 1줄) |

- 공통: **CSS 선언 추가만** 수행(`<script>`·템플릿·컴포넌트 등록·props·emit·import·텍스트·DOM 구조 무변경). 숫자 글리프 폭만 바뀌므로 레이아웃·여백·색·모션 변화 없음.
- 파일당 편집은 **순차** 로 진행(동일 파일 병렬 편집 시 선행 수정이 원복되는 기존 교훈 준수). 서로 다른 파일은 병렬 처리 후, 그렙으로 **5개 규칙 전부 잔존 확인**([HomeView L1005](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1005)·[L1226](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1226) · [MyMarketView L905](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L905) · [OrderCard L643](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L643)·[L809](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L809)).
- 적용 후 재확인: `frontend/src` 의 `font-variant-numeric` **1 → 6건**(신규 5 + 기존 금액 1).

## (e) 적용하지 않은 항목과 이유 (후보)
1. **`.order-card__fav` 찜 하트 터치 영역(신규 관찰·후보)**: [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L501-L516) 의 찜 버튼은 **24×18px** 로, WCAG 2.5.8(AA)의 최소 타깃 24×24 에 높이가 미달한다(태그 칩과 18px 위선 정렬을 맞춘 결과). 키우려면 찜 바 행 높이·태그 칩 정렬이 함께 바뀌는 **레이아웃 변경**이라 후보로 기록. 터치 오작동 보고가 있으면 `::before` 로 히트 영역만 확장하는 방식 권장.
2. **`OrderFavoritesView` 아이브로우 색 `--danger`(신규 관찰·후보)**: [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue#L177-L182) 의 "찜한 운행" 라벨이 danger(레드)다. 메모리 색 규약(레드=취소/거절/문제)과 다른 아이브로우 관례(`.page-head__eyebrow` 뮤트 · `.home-hero__eyebrow` 브랜드)를 보면 의미색 오용 소지가 있으나, **찜=빨간 하트 정체성과의 연결 의도**일 수 있어 제품 결정 사안 → 후보.
3. **금액·시간 정렬용 `tabular-nums` 확대(신규 관찰·후보)**: 카드·정산 목록의 금액/시각에도 적용하면 자릿수 정렬이 깔끔해지지만, 대상이 수십 곳이라 이번 "결함 교정" 범위를 넘는 **전역 타이포 결정** → 후보.
4. **스피너 모션 감소 미적용(`cb-spin`·`ig-spin`·`img-sheet-spin`·`img-sheet-bar-slide`)**: 09-10_1901 회차에서 **"진행 상태를 전달하는 정보성 모션 → 유지"** 로 이미 분류 → 기존 분류 유지.
5. **`n-spin :show="false"` 데드 래퍼(OrderFavoritesView 117행 등)**: 시각 영향 0(레이아웃 래퍼 역할). 제거 시 회귀 위험 대비 이득이 작아 09-10_1200 분류 유지.
6. **소수점 폰트(11.5px/10.5px 등)·`:focus-visible`/`outline: none`·`transition: all`·base.css 레거시 `.btn`/`.card`**: 각각 전역 디자인 결정 또는 순수 정리 작업으로 09-03~09-10 여러 회차에서 분류된 항목 → 기존 분류 유지(반복 미적용).
7. **기존 반복 후보(상태 재확인, 유지)**: 아바타(`--accent`) 흰 이니셜(09-07 "임의 수정 금지" 유지) · FAB 3곳 흰 글리프 · 하단 네비·ChatList `n-badge` 색/크기 통일 · `semantic.badgeRed` 사문화 · 커뮤니티 피드 카드 키보드 접근 · 알림 화면 카운트 배지 radius 혼재 · 카피 문체 혼재 · UiChip 하드코딩 6색 팔레트 · 커뮤니티 카테고리 색 체계 · "내 마켓" 아이콘 표기 불일치 · MoreView 정산/처리할 일 아이콘 중복 · DashboardView `.settle-item` 상충 재사용 · `data/levels.js` "드라이버" 타이틀 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · coin+`원` 이중 표기 · HeaderBar 라우트 표 데드 엔트리 · 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종 · SettingsVehiclesView 로딩 스켈레톤 부재 · `border-radius: 14px` 하드코딩 · `@media` 브레이크포인트 토큰화 · 알림 본문 `word-break: break-all` · 테마 `prefers-color-scheme` 연동 · `index.html` 메타 문체 혼재 · 탭 ARIA 완성 · 관리자 모달 `60vh`/`50vh` · MarketView `n-drawer` 80vh · PWA 매니페스트 용어("드라이버 오더 마켓") · `theme-color` 테마 연동 · `aria-live` 도입 · `user-scalable=no` — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 1.01s`, 오류·경고 0건) — 수정 반영 후 실행. 청크는 종전과 동일하게 생성(`HomeView` 16.67 kB · `OrderDetailView` 56.42 kB · `index` 272.66 kB 등).
- import 누락 점검: 변경이 **CSS 선언 5개(주석 5줄) 추가뿐**이라 import·심볼·컴포넌트 등록 영향 **0건**. `font-variant-numeric` 은 표준 CSS 속성으로 외부 식별자를 참조하지 않는다.
- 빌드 산출물 검증: `dist/assets` 에서 신규 선언 반영 확인 — `HomeView-DwGr-v2w.css`·`MyMarketView-DREgaPVu.css`·`OrderCard-B_7-rw85.css`·`OrderDetailView-C6HBwv-j.css` 4개 청크에 `font-variant-numeric:tabular-nums` 포함.
- 런타임 위험 점검: ① `tabular-nums` 는 **숫자 글리프 폭만** 고정하고 한글·라벨·색·줄바꿈·`white-space: nowrap` 동작은 그대로다. ② 미지원 브라우저에서는 무시되어 종전과 동일하게 렌더된다. ③ 5곳 모두 `white-space: nowrap` 이 이미 걸린 단일 행 요소라 줄바꿈·오버플로 시나리오 변화 0. 레이아웃·여백·모션 변화 0.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
