# UI 개선 보고서 — 2026-09-05 23:02

## 실행 결과 요약
- **수정 1건** — 22:04(54회차) 이후 `frontend/src` 변경 0건(mtime 스캔)으로 신규 기능 코드가 없어 전수 열람 대상이 없음. 오늘 회차 중 전면 열람 이력이 없던 **설정 화면 그룹(SettingsView·SettingsVehiclesView·SettingsProfileView·SettingsNotificationsView·SettingsAppearanceView) 전체**를 신선 시점 점검. 유일한 명확한 소형 결함: **설정 진입 화면(SettingsView)의 `.settings-block`이 하드코딩 `16px`을 쓰는 반면, 같은 설정 그룹의 나머지 4개 화면은 공용 토큰 `var(--card-radius)`(14px)를 사용** → 토큰 통일로 수정. '검증' 용어는 공용 VerifiedBadge와 일치 확인 후 유지 판정. 직전 회차(22:04) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 23:02 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 22:04 이후 `frontend/src` 변경 0건 (`Get-ChildItem LastWriteTime > 22:04` 결과 없음, git status는 기존 파일 재구성 이력성 변경뿐으로 신규 작업분 없음)
- **직접 열람(신선 시점 점검 — 설정 화면 그룹 전수)**: `views/settings/SettingsView.vue`(전체 — 설정 카드 그리드 진입점·기사 전용 게이팅(차량/인증 상태)·로그아웃) · `views/settings/SettingsVehiclesView.vue`(전체 — 내 차량 목록·'기본/검증' 태그·차량 등록/수정 모달) · `views/settings/SettingsProfileView.vue`(전체 — 이름·연락처 수정 폼) · `views/settings/SettingsNotificationsView.vue`(전체 — 브라우저 알림 토글) · `views/settings/SettingsAppearanceView.vue`(전체 — 화면 모드·앱 설치)
- **용어 교차 확인**: `components/common/VerifiedBadge.vue`(전체 — 차량 검증 배지 표기) · `views/ProfileView.vue`·`views/settings/SettingsVerificationView.vue`·`views/AdminView.vue`(사용자 인증(차량/면허) 용어 — grep) — SettingsVehiclesView '검증' 태그와의 개념·표기 비교
- **전역 감사(grep)**: `font-size: 7/8/9px` 0건 / `원원`·`formatWon(...)원` 직접 병기 — DashboardView·UserPageView 7건은 지역 `formatWon`(L28/L48, 단위 미포함)이라 정상 / `주문번호|order_no|order_number` 노출 — AdminView 운영 개입 2건(L1771·L2030, 관리자 콘솔 설계상 유지)뿐
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_2204.md`(54회차)·`2102.md`(53회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
- **신규 소형 결함 1건(수정)**: SettingsView `.settings-block` `border-radius: 16px` 하드코딩 — 동 그룹 4개 화면(차량/프로필/알림/화면)은 전부 `var(--card-radius)`(base.css 14px) 사용. 동일 컴포넌트 계층(n-card·n-alert 래퍼)인데 진입 화면만 다른 값으로 미세하게 어긋남 → 토큰 통일 대상.
- 점검 결과 정합 확인 사항:
  1. **'검증' 용어 정합(유지 판정)**: SettingsVehiclesView 차량 태그 '검증'(`vehicle.is_verified`)은 공용 VerifiedBadge(차량 단위 검증, title '검증 완료된 차량')·ActionCenterView/OrderDetailView 배지와 동일 표기·개념 — 사용자 단위 '차량·면허 인증'(ProfileView·SettingsVerificationView·AdminView 증빙 심사)과는 별개 레이어로 충돌 없음. 수정 대상 아님.
  2. **설정 화면 구조·Primary 정합**: 각 화면별 카드 1장 + 행동 버튼 1종(차량 등록 ghost / 저장 primary / 알림·화면 토글), 뒤로가기(설정) 통일 — 미니멀 원칙 준수. 차량 등록/수정 모달 footer 취소+저장(primary 1개), 필수값(name)만 required — '정보 → 행동' 순서 정합.
  3. **배지·태그 체계**: '기본'(info=청색 계열, 정보성)·'검증'(success=민트 계열, 정상) n-tag는 naive 태그 계층(콘텐츠 칩)으로 기존 'naive 전역 테마' 후보 범주 내 — 신규 불일치 없음.
  4. **설정 카드 그리드**: 아이콘(brand 그라디언트 + 흰 아이콘 — 아이콘 관용)·이름·설명 구조, 기사 전용 항목(차량 설정/인증 상태) `isDriver` 게이팅 유지(15:04 적용분 반영 확인) — 역할별 메뉴 분리 정합. 로그아웃은 화면 유일한 error(레드) 톤 액션.
  5. **전역 캐너리 통과**: 단위 중복·10px 미만 폰트·기술 식별자(관리자 콘솔 제외) 모두 0건.

## (d) 실제 적용한 수정
- [SettingsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/settings/SettingsView.vue#L99-L102) — `.settings-block` border-radius 하드코딩 제거:
```css
.settings-block {
    margin-bottom: 14px;
    border-radius: var(--card-radius);   /* 16px → 토큰 통일 */
}
```

## (e) 적용하지 않은 항목과 이유 (후보)
- **설정 화면 그룹의 `.settings-page`·`.settings-block`·`.settings-back` CSS 블록 4파일 중복**: LoginView·RegisterView auth CSS 중복과 같은 '화면 간 CSS 중복' 범주 — 렌더링 영향 없는 코드 정리로 UI 개선에 해당하지 않아 미적용(기존 분류에 통합 기록).
- **'기본'/'검증' n-tag 스타일(naive 기본 라운드·컬러)**: naive-ui 컴포넌트 계층으로 기존 'naive 전역 테마' 후보 범주 — 전역 결정 필요(반복 미적용).
- **기존 반복 후보(레벨 타이틀 "드라이버" data/levels.js↔LevelTable.php, 홈 추천 조건% 미노출, LoginView·RegisterView auth CSS 중복, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩, naive 전역 테마, v-chip/verify-chip 패딩 차이, 문의 placeholder 문구, MarketView 데드 CSS 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) — **통과**, 4362 modules, built in 942ms. SettingsView 청크(CSS `SettingsView-DpK5rokJ.css`·JS `SettingsView-cDaFWeGX.js`) 정상 생성.
- import 누락 점검: 수정이 SettingsView scoped CSS 값 1건에 국한 — script·템플릿·import 변경 없음, 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
