# UI 개선 보고서 — 2026-09-03 21:04

## (a) 실행 시각
- 2026-09-03 21:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 전체 읽기 이력이 가장 오래된 화면**): `views/DashboardView.vue`(전체 834줄 — 기사 오늘 요약/오늘·내일 운행 위젯·미니리스트·요약 카드·정산 현황·바 차트·상태 분포)
- 뷰(부분 집중): `views/orders/RideHistoryView.vue`(캘린더 `.cal__day--today` L789-815), `views/orders/OrderCreateView.vue`(동일 캘린더 `.cal__day--today` L1236-1261 — **신규 적용 대상**)
- 전역 감사(grep): views/components의 `color:#fff` 잔존 60건 위치 파악 후 brand 채움 텍스트 잔존 재분류, `html.dark .cal__day--today` 중복·잔존 전수, 캘린더 today 셀 두 파일 동일성 대조
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_2004.md`·`1907.md`(e-2 항목 — "오늘" 셀 라이트 흰 글자 유지 판단은 *두 캘린더 일관성*이 이유 → 이번 회차 두 파일 동일 적용으로 일관성 보존) 및 최근 회차들(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`, 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **캘린더 "오늘" 셀 라이트 모드 흰 글자 저대비 (신규 발견·적용)**: 내 마켓 일정(OrderCreateView)과 운행 기록(RideHistoryView) 캘린더의 `.cal__day--today`가 `background: var(--brand)`(라이트 `#36adff`·다크 `#63e2b7` — 모두 밝은 색) 위에 `color: #ffffff`를 사용 — 라이트 대비 ≈1.9:1로 실패. **다크 모드는 12:07 회차에서 `html.dark` 오버라이드로 `#07120e`를 적용해 해결됐지만 라이트 모드 흰 글자는 그대로 남은 잔존** — 20:04/19:07 회차가 "두 캘린더 일관성"을 이유로 유지했으나, 이번 회차는 **두 파일에 동일 적용해 일관성을 깨지 않고** 코드베이스 커스텀 brand 채움 표준(`#07120e`, 14:05~20:04에 걸쳐 정리된 표준)과 맞춤.
2. **"오늘" 셀 운행 표시 점 `.cal__day--today .cal__dot` 흰색 저대비 (신규 발견·적용)**: 4px 원형 도트가 동일하게 `background: #ffffff` — 밝은 brand 위에서 거의 보이지 않는 상태. 글자색을 `#07120e`로 바꾸는 것과 함께 도트도 동일색으로 통일.
3. **DashboardView 클린 (신규 전체 확인)**: 토큰 사용·한국어 문구("오늘의 운행"/"건당 평균 매출")·empty/skeleton 처리 정상, `.mini-list__empty`·`.status-list__bar` dark 쌍 존재, 라이트 전용 틴트 잔존 없음. `rgba(24,160,88)`(오늘)·`rgba(255,169,64)`(내일) 카드 틴트는 naive 기본 초록/앰버의 의미색 하드코딩이나 장식용 미세 틴트(알파 0.02~0.1)라 실제 대비 문제 없음 — 수정 불필요. 잔존 흰 글자 33곳은 재분류 결과 위험색·상태색 배지/이미지 오버레이/아이콘 관용으로 기존 회차 분류와 동일(신규 brand 채움 텍스트 잔존 없음).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/RideHistoryView.vue` | `.cal__day--today` 글자색 `color: #ffffff` → `#07120e` + 근거 주석("brand는 라이트·다크 모두 밝아 흰 글자 대비 약함 — 앱 표준 #07120e를 두 모드 공통 사용") + 중복 `html.dark .cal__day--today` 오버라이드 블록 제거(기본값과 동일해진 데드 규칙) |
| `views/orders/OrderCreateView.vue` | 동일 적용 — `.cal__day--today` 글자색 `#07120e` + 근거 주석 + `html.dark` 오버라이드 제거 |
| `views/orders/RideHistoryView.vue` | `.cal__day--today .cal__dot` `background: #ffffff` → `#07120e` — 밝은 brand 위 운행 표시 점 가시성 확보 |
| `views/orders/OrderCreateView.vue` | 동일 적용 — `.cal__day--today .cal__dot` `#07120e` |

`<style scoped>` CSS 값·주석만 변경(템플릿 구조·script·import 무변경). 두 파일의 캘린더 today 셀이 동일하게 정리되어 **화면 간 일관성 유지 + 라이트/다크 모드 내부 일관성 확보**. 라이트 `#36adff` 위 `#07120e` 대비 ≈7.7:1. 배경·border·font-weight(800)·패딩·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **DashboardView 중복 CSS(`.dash-columns` 2회·`.settle-item` 중복 정의)와 데드 CSS(`.settle-bar`/`.settle-bar__fill` — 템플릿 사용 0건)**: 렌더링 변화 없는 코드 정리로 UI 개선 아님 — 유지(신규 기록).
- **DashboardView today/tomorrow 카드 `rgba(24,160,88)`·`rgba(255,169,64)` 하드코딩**: 알파 0.02~0.1 장식용 미세 틴트라 대비 문제 없고 naive 기본색 전역 교체(대형 후보)와 묶어 재평가(유지).
- **OrderDetailView `.detail-hero` 흰 글자·아바타/`--accent` 채움(다크 틸+흰 글자)·HomeView 탭 카운트 불일치·SettingsVerificationView primary 2개·naive primary 초록/`n-badge`**: 기존 대형/설계 후보 유지(반복 미적용).
- **캘린더 `.cal__day--today` 흰 글자 유지(2004 회차 판단)**: "두 캘린더 일관성"이 유일 이유였고 이번 회차는 두 파일 동일 수정이라 반박됨 — 기존 판단 대신 잔존 저대비 해소로 전환(신규 적용, 위 d-1).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 955ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: 두 파일 모두 `html.dark .cal__day--today` 블록 0건(제거 확인), `.cal__day--today`·`.cal__day--today .cal__dot`가 `#07120e` 기본값으로 정리 — 2/2 파일. 템플릿 클래스 바인딩(L418/L187) 무변경.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값·주석·데드 규칙 제거뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
