# UI 개선 보고서 — 2026-09-04 12:01

## 실행 결과 요약
- **변경 없음** — 최근 전체 읽기 이력이 없던 컴포넌트 8종 + 인증 화면 2종 + 커뮤니티 상세 전체 읽기, 전역 캐너리 검증 결과 신규 적용 대상 미발견. 11:04(29회차) 이후 신규 소스 변경 없음(ProfileView가 마지막).

## (a) 실행 시각
- 2026-09-04 12:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 컴포넌트(직접 전체 읽기 — **홈/마켓·공용에서 쓰이는 핵심 카드/프리미티브 8종**):
  - `components/orders/OrderCard.vue`(전체 490줄 — 상태 배지·공개=금액 전환·시작 전 남은 시간(초록→주황→노랑→틸 긴급도)·승인 시간·태그/차량/인원 행)
  - `components/orders/SetGroupCard.vue`(전체 316줄 — 셋트 카드: 셋트명+아바타·일정 N개·신규 'N' 원형·임박 배지·공개=금액 전환·경유 일정 행·항공편)
  - `components/common/EmptyState.vue`(전체 103줄 — 공용 빈 상태: 아이콘 6색 variant·타이틀/힌트/액션 슬롯)
  - `components/ui/UiListRow.vue`·`UiSection.vue`·`UiChip.vue`·`UiCard.vue`(공용 행/섹션/칩/카드 프리미티브 — 화면 간 시각 일관성 원천)
- 뷰(직접 전체 읽기 — **최근 전체 읽기 이력이 없던 화면 4종**):
  - `views/community/CommunityPostView.vue`(전체 575줄 — 커뮤니티 상세: 헤더·작성자+카테고리 칩·본문·사진·영상/숏츠·좋아요/댓글·댓글 입력·수정 모달)
  - `views/auth/LoginView.vue`·`views/auth/RegisterView.vue`(로그인/가입 — 브랜드 마크·naive 폼·CTA)
  - `views/settings/SettingsProfileView.vue`·`SettingsVerificationView.vue`(프로필 수정·인증 상태)
- 전역 대조(직접 열람): `views/community/CommunityView.vue`(피드 카테고리 칩 L802-818·작성 모달 카테고리 선택 L1122-1141 — 상세 화면과 규칙 일치 대조), `assets/base.css`(색상 토큰 — `--danger` 라이트 `#b42318`·다크 `#d86262`, `--status-*` L20-28·L51-59 — 배지 대비 근거)
- 전역 감사(grep): 10px 미만 폰트 전수(`font-size: 6/7/8/9px` — 0건), 영문 placeholder/label 잔존 전수(0건), `n-button type="primary"` 사용처 31건 컨텍스트 열람(모달/폼 푸터·상태별 v-if CTA — 화면당 Primary 1개 원칙 준수 여부)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1104.md`(29회차 — ProfileView naive 세그먼트 적용)·`1001.md`(28회차 — ReviewsView)·`0903.md`·`0001.md`·`2026-09-03_2303.md`·`2205.md` 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 11:04 이후 첫 실행)
- 작업 트리 확인: 11:04 이후 `frontend/src` 변경은 `ProfileView.vue`(29회차 수정) 1건뿐 — **신규 코드 리뷰 대상 없음**
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), `docs/daily-review/2026-09-04.md`(12:01 — 지시 없음), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **OrderCard 클린 (신규 전체 확인)**: 상태 배지는 script `statusColorVar` 참조 + 공용 `.status-badge` 규칙(1px 6px/10px/400), 밝은 배경 배지 5종(published·driving·trading·acceptance-pending·accepted·completed) `#101418` 어두운 글자 규칙 보유 — 22:05 적용 유지. 공개 상태는 배지 대신 금액 노출(배지 정보 가치 낮음 — UI 미니멀 원칙 부합). 임박/긴급 배지(1px 6px/10px/400)는 `--danger`·`--status-settled` 채움 — 순서·라벨 "운행" 용어 정상. 신규 위반 없음.
2. **SetGroupCard 클린 (신규 전체 확인)**: 상태 배지 규칙이 OrderCard와 동일 골격(밝은 배지 어두운 글자), 공개=금액 전환·차량/인원 사이드라인 muted 11px·경유 일정(시간/항공편/서비스 라벨/노선) 정보 순서 명확, 임박 배지(1px 6px/10px/400 `--danger`). 신규 'N' 원형(20px/11px/700)은 빨강 원형 표시기 관용(기존 분류 유지). 신규 위반 없음.
3. **CommunityPostView 클린 (신규 전체 확인)**: 카테고리 칩은 CommunityView와 동일 정의(1px 6px·radius 6px·10px/400 — 목록→상세 전환 시 시각 일치), 카테고리 색 7종 동일 규칙, VIP 배지 금색 그라디언트(1px 6px/10px/400), 댓글/좋아요 액션 muted, "댓글 달기..." 플레이스홀더 양 화면 일치, 한국어·"운행" 용어 정상. 신규 위반 없음.
4. **LoginView·RegisterView 클린 (신규 전체 확인)**: Primary CTA 1개 + 보조(회원가입/로그인 링크), 뷰포트 잠금·스크롤 처리, 브랜드 마크(brand-gradient+흰 'N')는 로고 관용, naive 폼 라벨·에러 문구 한국어. 신규 위반 없음. (두 화면이 동일 `<style>`을 복제 보유 — 기존 "RegisterView CSS 복제" 후보 유지.)
5. **SettingsProfileView·SettingsVerificationView 클린 (신규 전체 확인)**: 프로필 수정 — Primary("저장") 1개. 인증 상태 — 완료/미인증 배지(base.css 공용, 1px 6px/10px/400), "신청" ghost primary 2개는 기존 반복 후보(방향 확인 필요, 이번에도 유지). 한국어 정상. 신규 위반 없음.
6. **공용 프리미티브·전역 캐너리 통과**: EmptyState 아이콘 6색 variant는 의미색 관용(기존 분류), UiChip variant 6종 라이트/다크 쌍 완비, UiListRow/UiSection/UiCard 토큰 사용 정상. 10px 미만 폰트 0건, 영문 placeholder 0건, Primary 배치 31건 전수 결과 모달 푸터·상태별 단일 CTA 패턴(OrderDetailView 등 상태 분기 v-if — 화면 동시 노출 아님). `.feed-card__cat--shop`(틸 틴트+인디고 글자)은 CommunityView·CommunityPostView **양 화면 동일 규칙** — 카테고리 색 설계의 일관된 구현(기존 "커뮤니티 카테고리 색" 후보 분류 유지).

## (d) 실제 적용한 수정
- **없음** (변경 없음 — 전체 읽기 12종 모두 신규 위반 미발견, 기존 적용 내역 반복 없음)

## (e) 적용하지 않은 항목과 이유 (후보)
- **`.feed-card__cat--shop` 카테고리 칩 틸 틴트(#63e2b7) ↔ 글자 인디고(var(--status-accepted)) 어색**: CommunityView·CommunityPostView **두 파일이 동일 규칙**이라 시스템 차원의 카테고리 색 설계 사항 — 단독 수정 시 목록/상세 어긋남 우려(기존 "커뮤니티 카테고리 색" 후보 유지, 신규 관찰 기록).
- **SettingsVerificationView "신청" ghost primary 2개(차량/면허)**: 화면 동시 2개 — 방향(보조 버튼 전환/그룹 병합) 확인 필요(기존 반복 후보 유지).
- **SetGroupCard `.set-card__new` 빨강 원형 'N'(20px/11px/700)·Login/Register 브랜드 마크(brand-gradient+흰 글자)**: 원형 표시기·로고 관용(기존 분류 유지).
- **OrderCard 시작 전 남은 시간 4색(#18a058/#ffa940/#f0a800/#13c2c2)·"3시간 미만=틸"**: 긴급도 의미색 설계(레드 전환 등) 판단 필요 — 기존 반복 후보 유지.
- **EmptyState 아이콘 variant 5색 하드코딩·UiChip variant 색·VIP 금색 그라디언트**: 의미색·프리미엄 배지 관용 — 기존 분류 유지.
- **base.css `.btn--primary`(legacy, 템플릿 사용 0건 확인)·`.notify-banner`(--notify+흰 글자 — 다크 #3ca97e 대비)**: 전역 공용 정의 설계 후보 — 기존 회차 반복 미적용 유지.
- **LoginView·RegisterView `<style>` 전면 복제·OrderDetailView/OrderCreateView/MyMarketView/DashboardView/AdminView 등 대형 화면 잔존 구간·HomeView 탭 카운트 불일치**: 기존 대형/코드 정리 후보 유지(반복 미적용).

## (f) 빌드 결과
- 이번 회차는 코드 수정 없음(변경 없음) → `npm run build` 미실행(수정 시에만 빌드 검증 방침 유지).
- 캐너리 grep 검증으로 회귀 없음 확인: 10px 미만 폰트 0건, 영문 placeholder/label 0건, 29회차 ProfileView 적용 유지(11:04 이후 소스 변경은 해당 1건뿐) — import 누락 여부 점검 대상 변경 없음(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
