# UI 개선 보고서 — 2026-09-04 14:01

## 실행 결과 요약
- **수정 1건** — 최근까지 부분 열람만 반복되던 핵심 화면 2종(홈 추천 전체 / 운행 상세 전체 템플릿·script·핵심 CSS)을 전체 검토해, 운행 상세 **하단 원터치 진행 바**의 고정 흰 글자가 밝은 단계색 위에서 저대비인 지점을 신규 발견·적용(앱 표준 상태색 채움 글자 규칙 `#101418`). 13:05(31회차) 이후 신규 소스 변경 없음.

## (a) 실행 시각
- 2026-09-04 14:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 전체 읽기 이력이 없던 핵심 화면 2종**):
  - `views/HomeView.vue`(전체 1203줄 — 홈 추천: 히어로(내 다음 운행/최우수 추천)·단일/왕복/셋트 탭·일괄요청 폴링·빠른 메뉴 — 스크립트 L1-532·템플릿 L534-745·CSS L747-1203 전수)
  - `views/orders/OrderDetailView.vue`(전체 2401줄 — 운행 상세: 히어로·진행 스테퍼·운행 정보/셋트 그룹/일정/상태 관리·신청자 승인 거절·요금 제안·모달 6종·하단 원터치 진행 바 — 스크립트 L1-366·템플릿 L368-1259·핵심 CSS(히어로 배지·스테퍼·신청/제안·액션바) 전수, 나머지 CSS는 토큰·배지 규칙 grep으로 커버)
- 전역 대조(직접 열람): `views/orders/RideHistoryView.vue`(`statusTextColor` L265-268 — 상태 배지 글자색 표준·밝은 상태 목록), `composables/useOrderDetail.js`(`resolveCssVarColor` L679-687·`statusButtonColor` L689-693 — naive 버튼 색상 전달 방식), `utils/colors.js`(`statusColorVar` — 상태→CSS var 맵), `assets/base.css`(`--status-*` 라이트/다크 토큰 실측)
- 전역 감사(grep): 채팅/공용 컴포넌트·기타 뷰의 고정 `text-color="#ffffff"` + 상태색/브랜드 채움 쌍 전수(신규 잔존 0건), OrderDetailView 배지 규칙(`.hero-badge`·`.detail-claim-title__count`·`.ride-steps__time`·`.detail-actionbar__step`) 스펙 확인, 10px 미만 폰트 0건
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1305.md`(31회차 — 변경 없음)·`1201.md`·`1104.md` 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 13:05 이후 첫 실행)
- 작업 트리 확인: 13:05 이후 `frontend/src` 소스 변경 0건 — **신규 코드 리뷰 대상 없음**(HomeView·OrderDetailView는 기존 대형 후보로 남아 있던 미전체검토 화면)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / 상태색 채움 위 글자 표준 `#101418`)

## (c) 발견한 개선 포인트
1. **OrderDetailView 하단 원터치 진행 바 — 밝은 단계색 위 흰 글자 저대비 (신규 발견·적용)**: `.detail-actionbar__primary` 버튼이 `:color="resolveCssVarColor((rideNextStep ?? …).color)"`로 **단계 상태색**을 배경에 채우면서 텍스트는 고정 `text-color="#ffffff"`였다. 단계 색 6종 중 4종(`ride_start`→published, `pickup_arrived`→driving, `passenger_arrived`→trading, `arrived`/완료 폴백→completed)이 **라이트(`#36adff`·`#13c2c2`·`#ffa940`·`#18a058`)·다크(`#63e2b7`·`#1c9f9f`·`#c98f3e`·`#27946a`) 모두 밝거나 중간색**이라 흰 글자 대비가 약함(라이트 published ≈2.4:1·다크 published ≈1.6:1). 이 화면은 운행 상세의 **핵심 행동 버튼**(기사 운행중 원터치 진행)이라 노출 빈도와 크기가 크다. ChatThread·AdminView 등에서 정리된 "상태색 채움은 밝은 상태 어두운 글자(`#101418`)" 표준(RideHistoryView `statusTextColor` L265-268)이 **적용되지 않은 잔존 지점** — 신규로 판정해 같은 규칙 적용.
2. **HomeView 클린 (신규 전체 확인)**: 히어로(오늘의 운행/오늘 받을 추천 — CTA 1개, 다크 CTA `#07120e` 표준)·추천 탭 칩(활성 `brand-soft`+brand, 활성 건수 칩 `#07120e` L1201)·왕복 그룹 랭크/집계 칩(`1px 6px/10px/400`)·일괄요청 버튼(`#07120e` — 12:07 적용 유지)·태그 칩·빠른 메뉴 — 전부 배지 스펙·"운행" 용어·한국어 정상. 신규 위반 없음. (탭별 표시 개수 상한 `single 5·set 3` vs 탭 칩 카운트 전체 수 — 기존 "탭 카운트 불일치" 후보를 실측 재확인, 하단 후보 항목 참조)
3. **OrderDetailView 나머지 클린 (신규 전체 확인)**: 히어로 배지(`.hero-badge` — 1px 6px/10px/400 스펙 준수, 흰 글자는 히어로 단위 설계로 기존 분류 유지)·진행 스테퍼(단계 점 색·`ride-steps__time` 10px — 18:01 적용 유지)·승인 대기 카드(Primary 1개+secondary 1개)·신청자 카드(거절/승인 — 등록자 역할 처리, "가져오기 신청" 건수 칩 `#07120e` 19:07 적용 유지)·요금 제안/리뷰/취소/완료 모달(모달 푸터 Primary 1개 패턴)·EmptyState·"운행" 용어·한국어 정상. 모달 푸터 `운행 취소`(type=error) 등 위험 동작 색 구분도 정상. 신규 위반 없음.
4. **전역 캐너리 통과**: 고정 흰 글자 + 상태색/브랜드 채움 버튼 잔존은 이번 수정분 제외 0건(의도적 흰 글자 지점은 짙은 채움 위뿐 — 히어로 배지·아바타·VIP 배지 등 기존 관용 분류), 10px 미만 폰트 0건, "오더/주문번호" 노출 0건, 영문 노출은 브랜드명뿐.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/OrderDetailView.vue` | script — `rideBarDarkText` computed 추가: 다음 단계 색 CSS var의 상태명이 앱 표준 밝은 목록(`published/driving/trading/completed`)이면 어두운 글자 판정(완료 폴백 `var(--status-completed)` 포함) + `rideBarTextColor` computed(`#101418`/`#ffffff`) |
| `views/orders/OrderDetailView.vue` | 템플릿 L1217 — 하단 바 버튼 고정 `text-color="#ffffff"` → `:text-color="rideBarTextColor"` |
| `views/orders/OrderDetailView.vue` | 템플릿 L1220-1223 — 진행도 배지 `N/6`에 `detail-actionbar__step--dark` 조건 클래스 바인딩 |
| `views/orders/OrderDetailView.vue` | CSS — `.detail-actionbar__step--dark` 추가: 어두운 글자일 때 배지 틴트를 `rgba(255,255,255,.22)` → `rgba(16,20,24,.16)`로 전환해 글자 대비 유지 |

`<script setup>`·템플릿·`<style scoped>` 모두 동일 표준(`#101418` — RideHistoryView·ChatThread·AdminView와 같은 상태 목록·같은 값)만 적용, 신규 import 없음(`computed`는 기존 import). 짙은 단계색(승인=인디고·이동=퍼플)은 흰 글자 유지 — 라이트/다크 의미·구조·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **HomeView 탭 카운트 vs 표시 개수 불일치(실측 재확인)**: 단일 탭 칩 카운트는 전체 수(`singleRecs.length`)인데 목록은 상위 5개, 셋트도 카운트 전체 수 vs 상위 3개 표시. 왕복은 3개 한도로 일치. "선택지 축소" 의도로 보이나 수치와 표시 어긋남 — 카운트를 표시 수에 맞출지 "더 보기" 경로를 둘지 제품 방향 확인 필요(기존 후보 유지, 신규 관찰 기록).
- **OrderDetailView `.detail-hero` 흰 글자·히어로 배지(`--brand` 채움 `#ffffff`·퍼플/레드)**: 브랜드 그라디언트 히어로 위 흰 텍스트 설계 — 기존 대형/설계 후보 유지(히어로 단위 일관 변경 필요, 반복 미적용).
- **OrderDetailView 하단 바 `:loading` 스피너 naive 기본색**: 어두운 글자 단계에서 스피너 색상은 naive 기본 렌더 — 로딩 순간 노출이라 대비 영향 미미, 별도 오버라이드는 과설계로 판단(유지).
- **OrderDetailView·HomeView 그 외 미검토/데드 CSS 조각**(OrderDetailView `L2100`대 미열람 잔여 규칙 일부·HomeView 중복 없음 확인): 비노출 정리 — 기존 데드 CSS 후보 분류 유지(UI 개선 아님).
- **HomeView 히어로/OrderDetailView 아바타·FAB·gradient 채움**: 아이콘·아바타 관용 — 기존 분류 유지.
- **SettingsVerificationView primary 2개·naive 라이트 primary 초록·`data/levels.js` "드라이버" 등**: 기존 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 927ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: `rideBarTextColor`(script L115)·`:text-color="rideBarTextColor"`(템플릿 L1217)·`.detail-actionbar__step--dark`(템플릿 L1224·CSS L2299) 존재, 고정 `text-color="#ffffff"` 제거 확인 — 5/5.
- import 누락 점검: 이번 변경은 기존 import(`computed`)만 사용하는 computed 2개 추가 + 템플릿 바인딩 2곳 + scoped CSS 1건 — 신규 import 추가·제거 없음, 빌드 통과로 런타임 참조 검증 완료. **import 누락 없음.**
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
