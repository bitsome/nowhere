# UI 개선 보고서 — 2026-09-10 16:02

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 이번 회차의 신규 편집 대상은 **운행 공유 공개 화면**([SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue), `/share/order/:token`, 15:35 신규 생성·미커밋)이다. 이 화면의 Primary CTA가 `background: var(--accent)` + `color: #ffffff`를 쓰는데, 다크 모드에서 `--accent`가 밝은 틸(#63e2b7)로 바뀌어 **흰 글자 대비가 ≈1.6:1**로 떨어진다(AA 4.5:1 미달). 같은 앱이 이미 쓰는 국소 예외 규약(`html.dark { color: #07120e }`)을 그대로 적용했다.
- 직전 실행(15:01) 이후 `frontend/src` 신규 편집은 **6건**이다 — `SharedOrderView.vue`(15:35:20, 신규)·`useOrderShare.js`(15:36:28, 신규)·`App.vue`(15:35:45)·`router/index.js`(15:35:33)·`api/orders.js`(15:35:03)·`views/orders/OrderDetailView.vue`(15:36:55). 앞의 두 건은 역대 보고서에서 검토된 적이 없는 신규 산출물이라 이번 회차의 검토 초점으로 삼았다.
- 전역 표준 스캔(용어·10px 미만 폰트·보조용언 띄어쓰기·배지 규격·`--accent` 채움) 신규 위반 0건. 신규 발견 위반은 위 1계열뿐.
- `npm run build` 통과(✓ built in 1.02s). `<script>`·import·로직 변경 0건(스코프 CSS 1블록 추가).
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 16:02~16:10 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_1501.md`(적용 1건: 랜딩 Primary CTA 다크 예외) · `2026-09-10_1407.md`(적용 5건: 등록자 홈 CTA 다크 예외·상태 칩 2곳 규격·`aria-expanded` 5곳) — 적용 내역·미적용 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git log --oneline -3`(HEAD `f240340`, 전부 미커밋) + `frontend/src` mtime 상위 25건 → 15:02(LandingView, 직전 회차 적용분) 이후 편집은 위 6건, 그 외 0건.
- **신규 전량 판독**: `views/SharedOrderView.vue`(395행 — 상단 내비·로딩 스켈레톤·404 상태·조건 칩·공유 CTA·푸터 + 전량 스코프 CSS), `composables/useOrderShare.js`(93행 — 공유 시트 → 클립보드 → textarea 폴백 → 링크 직접 노출 4단계 폴백).
- **대조 판독(diff)**: `views/orders/OrderDetailView.vue`(16+/2− — `useOrderShare` 도입부·등록자 공유 버튼(922~932행)·리뷰 수 표기 `(n)` → `(n리뷰)` 2곳) · `App.vue`(`HeaderBar`·`app-content--full` 제외 목록에 `'welcome'`·`'shared-order'` 추가 diff) · `router/index.js`(`welcome`·`shared-order`·`registrant-settlement` 라우트 + `requiresAuth` 미인증 시 `login` → `welcome` 리다이렉트 가드 diff) · `api/orders.js`(`apiShareOrder`·`apiPublicOrder` 추가) · `frontend/index.html`(OG/Twitter 카드 메타 + 타이틀 교체 diff).
- **규약 대조**: `views/LandingView.vue`(408~413행 — 15:01 적용분 `html.dark .lp-btn--primary, html.dark .lp-card__action { color: #07120e }`), `assets/base.css`(토큰 1~70행 — `--accent` 라이트 #1f1f1f / 다크 #63e2b7, `--brand` 라이트 #36adff / 다크 #63e2b7, `--brand-gradient`).
- **전역 그렙**: `오더|주문번호|order_no`(0건) · `font-size: [5-9]px`(0건 — 10px 미만 폰트 없음) · `보조용언 붙여쓰기`(0건) · `background: var(--accent)`(8곳 전수 — base.css `.chat-list__avatar`·`.btn--primary`, ReviewsView 활성 탭·아바타, CustomerHome CTA, LandingView 2곳, SharedOrderView 1곳) · `padding: (1px 8px|2px 8px|2px 10px|3px 9px|1px 10px)`(8건/3파일 — AdminView 6·LandingView 1·OrderCard 1, 전부 기존 분류) · `eyebrow`(LandingView·SharedOrderView·HomeView·CustomerHome 4곳 — 모두 11px/`--brand`, 앱 공통 규약 확인) · `isRegistrant|공유|sharing`(OrderDetailView 내 공유 액션 중복 0건 확인).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·Primary 버튼 하나), 프로젝트 배지·색상 표준(1px 6px / 10px / 400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출 / 다크 모드 대비).

## (c) 발견한 개선 포인트
1. **공유 공개 화면 Primary CTA의 다크 모드 저대비(신규 발견)**: [SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue#L337-L343) `.sp-btn--primary`("운행 확인하고 신청하기"·"기사로 가입하고 신청하기"·404 화면 "마켓으로 가기")가 `background: var(--accent)` + `color: #ffffff`다. 라이트(`--accent`=#1f1f1f)는 정상이나 **다크에서 `--accent`가 #63e2b7로 바뀌어 흰 글자 대비 ≈1.6:1**로 떨어진다. 이 화면은 **로그인 없이 카카오 오픈채팅·카페 링크로 유입되는 첫 접점**이라 대비 결함 노출 가치가 크고, 앱은 같은 문제를 국소 예외로 이미 해결하고 있다 — [LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L408-L413)(15:01 적용)·[home/CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L88-L104)(14:07 적용)·[HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1079-L1082)·[ReviewsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ReviewsView.vue#L264-L273). **같은 규약을 적용.**
2. **공유 화면 배선·접근 경로는 결함 없음**: `shared-order` 라우트는 `requiresAuth` 없이 열려 있고([router/index.js](file:///d:/wwwroot/nowhere/frontend/src/router/index.js#L40-L42)), `HeaderBar`·`app-content--full`·하단 내비 모두 공개 화면으로 분기돼 크롬 구성이 정상([App.vue](file:///d:/wwwroot/nowhere/frontend/src/App.vue#L274-L299)). 토큰이 없는 링크는 404 상태(운행을 찾을 수 없습니다)로 안내하고 대체 CTA를 제공한다. 수정 없음.
3. **공유 흐름 폴백 체인 이상 없음**: [useOrderShare.js](file:///d:/wwwroot/nowhere/frontend/src/composables/useOrderShare.js#L25-L48)가 공유 시트(AbortError는 실패로 처리하지 않음) → clipboard → textarea+`execCommand` → 링크 직접 노출 순으로 폴백한다. 비HTTPS 환경에서도 링크 전달이 보장된다. `useDialog` 사용 화면은 App.vue에 `n-dialog-provider`가 이미 포함돼 있어 선행 조건 충족. 수정 없음.
4. **표준 스캔 신규 위반 0건**: "오더"/주문번호 노출 0건, 10px 미만 폰트 0건, 보조용언 붙여쓰기 0건. 신규 화면의 조건 칩(`.sp-conditions__item` 5px 11px / 12px)은 상태 배지가 아닌 조건 태그로 기존 규격(OrderCard 조건 칩과 동일 계열)을 따른다. `.sp-eyebrow`(11px/700/`--brand`)는 앱 공통 eyebrow 규약과 일치. `index.html`의 OG 메타·타이틀도 "운행" 용어만 사용하고 기술 식별자를 노출하지 않는다.
5. **기존 반복 후보(상태 재확인, 유지)**: FAB 흰 글리프 3곳 · `--accent` 아바타 흰 글자 · EmptyState 아이콘 변형색 하드코딩 · `qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · MyMarketView FAB '＋' 글리프 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · OrderFavoritesView eyebrow `--danger` · coin+`원` 이중 표기 · 관리자 화면 상태 칩 규격 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · HeaderBar 라우트 표 데드 엔트리 · 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종 혼재 · 말줄임표/모달 푸터 문구 혼재 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/SharedOrderView.vue` | `.sp-btn--primary:hover` 뒤에 다크 전용 예외 블록 1개 추가 — `html.dark .sp-btn--primary { color: #07120e }`(+사유 주석). 라이트는 기존 흰 글자 유지(어두운 채움), 다크만 어두운 글자로 전환. 공유 화면 Primary CTA(정상·404 상태 공통)의 다크 대비 1.6:1 → 10:1 수준 |

- 공통: `<script>`·props·emit·import·DOM 구조 무변경(스코프 CSS 규칙 1블록 추가, 6행). 선택자 특이도(`html.dark .sp-btn--primary` = 0,2,1)가 기존 단일 클래스 규칙보다 높아 **CSS 선언 순서와 무관하게 적용**되며, 스코프드라 타 화면 파급 0건. 단일 파일·단일 편집이라 원복 위험 없음(그렙으로 지속성 확인).
- 수정 후 확인: `html.dark .sp-btn--primary` 1건 잔존([SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue#L351-L353)), `color: #ffffff`는 `.sp-mark`(로고 그라디언트)·`.sp-btn--primary` 라이트 기본값 2건으로 유지 — 의도한 상태. 신규 청크 `SharedOrderView-0JobN9-_.css` / `SharedOrderView-Y0D-YVr4.js` 생성 확인.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **공유 화면 `.sp-mark`(로고 마크)의 `--brand-gradient` + 흰 글자**: 다크에서 그라디언트가 틸(#63e2b7→#3ca97e)로 바뀌어 "N" 글자 대비가 낮다. 1번과 원인이 같지만, 15:01·13:01 보고서에서 "마크·그라디언트 계열"로 이미 후보 분류된 동일 계열이며, 아바타·그라디언트 채움 전체를 건드리면 라이트 대비가 역전될 수 있어 전역 `--on-accent` 토큰 설계가 선행돼야 한다. 이번에도 CTA 1곳만 국소 예외로 처리.
2. **`.sp-eyebrow`의 11px/700/`--brand`**: 라이트 배경에서 `--brand`(#36adff) 대비가 ≈2.2:1로 낮다. 다만 LandingView·HomeView·CustomerHome 4곳이 모두 같은 값을 쓰는 **앱 공통 eyebrow 규약**이라 개별 수정은 일관성을 깨뜨린다. 전역 타이포·색 결정 건이라 후보.
3. **공유 화면 404 상태의 자체 마크업**: 공통 [EmptyState.vue](file:///d:/wwwroot/nowhere/frontend/src/components/common/EmptyState.vue)를 쓰지 않고 `.sp-empty`를 직접 정의한다(공개 화면용 확대 타이포·중앙 정렬 목적). 컴포넌트 통합은 화면 구조 결정이 필요해 후보.
4. **`.sp-btn--primary`의 `box-shadow: 0 8px 22px rgba(0, 0, 0, 0.14)`**: 다크 배경에서는 그림자가 거의 보이지 않는다. 노출 결함이 아니라 시각 미세 조정이며, 랜딩·등록자 홈 CTA와 동일 값이라 일괄 판단이 필요해 후보.
5. **`OrderDetailView` 공유 버튼의 노출 조건(`status === 'published'`)**: 등록자가 `published`일 때만 공유 버튼이 보인다. 거래중(`trading`)에도 재공유가 필요할 수 있으나 백엔드 발급 정책과 맞물린 기능 판단이라 후보.
6. **관리자 화면 상태 칩 6곳(AdminView 1px 8px·2px 8px / 700)**: 14:07·15:01과 동일하게 관리자 화면은 정보 밀도가 높은 운영 도구로 굵은 칩이 의도된 구분일 수 있어 기사 앱 화면 우선 원칙에 따라 후보 유지.
7. **`assets/base.css`의 레거시 `.card`·`.btn` 계열(465~534행)**: 사용처 0건으로 확정된 죽은 CSS. 제거는 UI 개선이 아닌 정리 작업이고 동적 클래스 결합 가능성을 배제할 수 없어 후보 유지.
8. **HeaderBar 라우트 표 정리**: 신규 `welcome`·`shared-order`는 제외 목록에 정상 반영됐으나, `showBack` 제외 목록의 `'my-orders'`·`PAGE_LABEL`의 `'match'`는 라우터에 없는 데드 이름이고 `registrant-settlement`도 헤더 라벨 미등록(화면 자체 page-head 보유). 노출 결함이 없어 후보.
9. **기존 반복 후보(15:01·14:07 기록분 유지)**: 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종(`#f0a800` vs `--warn` #ffa940 vs `--status-acceptance-pending` #c28a2e) · 말줄임표 ASCII/`…` 혼재 · SettingsVehiclesView 로딩 중 빈 카드(스켈레톤 부재) · 모달 푸터 '닫기'/'취소' 혼재 · AdminView 금액 인라인 포맷 · FAB 3곳 흰 글리프 · HomeView `.home-more` 공통화 · SettlementView 계좌 마크업 중복 · `new Date(created_at_iso)` 방어 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 1.02s`, 오류·경고 0건) — 수정 반영 후 실행. 신규 청크 `SharedOrderView-0JobN9-_.css` / `SharedOrderView-Y0D-YVr4.js` 정상 생성(`dist/assets` 확인).
- import 누락 점검: 변경이 스코프 `<style>` 규칙 1블록뿐이라 import·심볼·전역 CSS 영향 0건 → 빌드 통과로 검증 대체. 검토 대상 신규 파일(SharedOrderView·useOrderShare)의 import 경로(`../api/orders`·`../stores/auth`·`../composables/useOrderShare`·`naive-ui`)는 모두 파일 존재·기존 사용처 확인으로 정합.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
