# UI 개선 보고서 — 2026-09-04 11:04

## 실행 결과 요약
- **수정 1건** — ProfileView naive 세그먼트 활성 탭의 brand 채움 위 흰 글자 저대비를 앱 표준 `#07120e`로 해소(라이트·다크 공통). 장기 후보였던 "naive 세그먼트 활성 탭"의 유일 잔존 지점으로, 같은 결함 계열(밝은 채움 위 흰 글자)을 앱 전역에서 정리한 표준을 이번에 적용.

## (a) 실행 시각
- 2026-09-04 11:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **최근 보고서에서 부분 언급만 반복되던 화면 3건**):
  - `views/ProfileView.vue`(전체 802줄 — 프로필 히어로·XP 바·요약/근무/활동 세그먼트 탭·실적 3카드·차량·인증 요약·바로가기·리뷰/레벨 카드)
  - `views/NotificationsView.vue`(전체 831줄 — 알림센터: 읽지 않음 배지·지금 처리할 알림·필터 탭·날짜 그룹 펼침·요금 제안 인라인)
  - `views/community/UserPageView.vue`(전체 691줄 — 공개 프로필: 히어로·평점·실적·올린 글/등록한 운행/리뷰 탭)
- 전역 스팟(직접 열람): `assets/base.css`(색상 토큰 L1-70 — `--brand` 라이트 `#36adff`/다크 `#63e2b7`, `--accent` 라이트 `#1f1f1f`/다크 `#63e2b7` — 이번 수정의 대비 근거), `views/DashboardView.vue`(막대 차트·상태 분포 L643·L712), `views/community/CommunityView.vue`(탭 카운트·아바타·FAB), `views/community/CommunityPostView.vue`(아바타·영상 배지), `views/ReviewsView.vue`(활성 탭 다크 쌍 대조 — 10:01 적용 유지 확인)
- 전역 감사(grep): `background: var(--brand)/var(--accent)/var(--brand-gradient)` 채움 사용처 전수(views 35건 — 막대/차트·아바타·FAB·아이콘·탭), views `color: #fff(fff)` 100건 컨텍스트 열람, `.n-tabs-tab--active` 오버라이드 전수(ProfileView 1곳만 유일)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1001.md`·`0903.md`·`0104.md`·`0001.md` 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 10:01 이후 첫 실행)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 / 밝은 brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **ProfileView naive 세그먼트 활성 탭 글자 저대비 (신규 적용)**: `.profile-tabs :deep(.n-tabs-nav--segment-type .n-tabs-tab.n-tabs-tab--active)`(L489-494)가 **`background: var(--brand)` + 흰 글자**인데 `--brand`는 라이트 `#36adff`·다크 `#63e2b7` **두 모드 모두 밝은 색** → 흰 글자 대비 약함(≈2:1). 지금까지 "naive 테마 전역 설계 후보"로만 분류되던 지점이지만, views 전수 결과 해당 오버라이드는 ProfileView 한 곳뿐(전역 naive 테마 영향 없음)이고 **같은 파일의 scoped CSS가 배경·글자색을 모두 명시**하므로 로컬 수정으로 완전 해소 가능. 10:01에 ReviewsView 활성 탭(채움 위 글자색)을 `#07120e` 표준으로 정리한 것과 동일한 결함 계열의 유일한 잔존.
2. **NotificationsView 클린 (신규 전체 확인)**: v8 상황별 색 체계(요청/확인=옐로우·취소=레드·완료=민트·채팅=파랑·항공=퍼플) 라이트/다크 쌍 완비, 우선 처리 아이콘은 밝은 채움 위 어두운 글자(`#07120e`·`#171208`·`#180707`) 표준, 상태 배지·건수 필 `1px 6px/10px/400` 스펙 준수. 지금 처리할 알림 카드 Primary("처리할 일에서 처리") 1개 원칙 준수. "처리할 일로 이동"·"알림센터" 한국어·"운행" 용어 정상. 신규 위반 없음.
3. **UserPageView 클린 (신규 전체 확인)**: 활성 탭은 **surface pill + `--accent` 글자**(grayscale-first) 패턴 — 라이트 `#1f1f1f`·다크 `#63e2b7` 모두 대비 정상(brand 채움+흰 글자 아님). 인증 배지(차량/면허/등록 차량)·상태 배지 배지 스펙 준수, 다크 쌍 완비. "올린 글/등록한 운행/받은 리뷰" 한국어·"운행" 용어 정상. 신규 위반 없음.
4. **DashboardView·Community 계열 스팟 통과**: DashboardView 막대 차트/상태 분포 채움은 라벨이 막대 밖(muted)이라 채움 위 글자 아님. CommunityView 탭 건수 배지 active는 brand 채움 + `#07120e`(기존 표준 적용 유지). 잔존 흰 글자는 아바타 이니셜(gradient)·VIP 금색 배지(프리미엄)·영상 재생 오버레이(흑배경)·FAB '＋'(아이콘)·설정 아이콘 박스(아이콘)·MoreView 히어로 아이콘(아이콘) — 전부 기존 관용 분류.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/ProfileView.vue` | `.profile-tabs :deep(.n-tabs-nav--segment-type .n-tabs-tab.n-tabs-tab--active)` 글자색 `color: #ffffff` → `#07120e` + 근거 주석("brand는 라이트(#36adff)·다크(#63e2b7) 모두 밝아 흰 글자 대비가 약함 — 앱 표준 #07120e를 두 모드 공통 사용") |

`<style scoped>` CSS 값 1곳 + 주석만 변경(템플릿 구조·script·import 무변경). 라이트/다크 모두 활성 탭 텍스트 대비 개선, 구조·동작 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ProfileView·UserPageView·CommunityView·CommunityPostView·MoreView·SettingsView 아바타/아이콘 박스(brand-gradient+흰 글자)·MarketView·CommunityView FAB·ReviewsView 아바타**: 아바타·아이콘 글리프 관용 — 기존 회차 반복 분류 유지(전역 아바타 글자색 설계 후보).
- **VIP 금색 그라디언트 배지(`#f7b731/#f2994a`+흰 글자 — MoreView·UserPageView·CommunityView·CommunityPostView)**: 프리미엄 배지 관용 — 기존 분류 유지.
- **UserPageView `.user-section__title` 등 미사용 CSS 조각·NotificationsView `.v8-noti` 데드 CSS**: 비노출 정리 — UI 개선 아님(기존 데드 CSS 후보 분류 유지).
- **ProfileView brand-gradient 채움 위 흰 글자 요소(아바타·바로가기 아이콘·level-bar 채움 등)**: 채움 자체에 글자가 얹히지 않는 아이콘/진행바 요소 — 유지.
- **DashboardView·AdminView 등 관리자 화면 나머지 미검토 구간·OrderDetailView/OrderCreateView 등 대형 화면 반복 후보**: 기존 대형 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 10.10s`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: ProfileView `.profile-tabs ... n-tabs-tab--active` `color: #07120e` 적용·근거 주석 추가 확인, views 내 brand 채움+흰 글자 텍스트 잔존 0건(아바타/아이콘/FAB/VIP 배지 관용만).
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값·주석 변경뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
