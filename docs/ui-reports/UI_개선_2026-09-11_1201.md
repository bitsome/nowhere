# UI 개선 보고서 — 2026-09-11 12:01

## 실행 결과 요약
- **코드 수정 1건(2개 파일)** — 이번 회차는 `frontend/src` 의 **`position: fixed` 플로팅 요소 배치를 처음으로 전수 대조**했다. 전역으로 렌더링되는 "맨 위로" 버튼([ScrollTopButton.vue](file:///d:/wwwroot/nowhere/frontend/src/components/common/ScrollTopButton.vue))과 **페이지 자체 FAB(빠른매칭·글쓰기·운행 등록)가 같은 좌표(우하단)를 쓰고 있어**, 스크롤을 내리면 두 원형 버튼이 겹치고 z-index 가 높은 FAB 가 "맨 위로" 버튼을 덮어 **보이지도 눌리지도 않는 죽은 컨트롤**이 되는 결함을 찾았다.
  1. **좌표 완전 중첩(마켓)**: [.market-fab](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1462-L1479) 은 `right:16px · bottom:calc(76px + safe-area) · 54×54 · z-index:90`, [.scroll-top-btn](file:///d:/wwwroot/nowhere/frontend/src/components/common/ScrollTopButton.vue#L53-L69) 은 `right:16px · bottom:calc(84px + safe-area) · 42×42 · z-index:50` → 맨 위로 버튼 사각형(x 16~58, y 84~126)이 **FAB 사각형(x 16~70, y 76~130) 안에 완전히 포함**되고 FAB 불투명 배경이 위를 덮는다.
  2. **부분 중첩(커뮤니티·내 마켓)**: [.community-fab](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L1105-L1122) · [.my-market-fab](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L722-L738) 는 `right:max(18px, …) · bottom:calc(84px + safe-area) · 54×54 · z-index:60` → 좌측 2px 슬리버를 제외한 **맨 위로 버튼 대부분이 가려진다**.
  → 기능을 삭제하지 않고(비전 원칙 8) **페이지 FAB 이 있는 화면에서만 맨 위로 버튼을 FAB 위로 올리는 `--raised` 변형**을 추가해 두 컨트롤을 모두 살렸다(App.vue 가 라우트로 판정, 버튼은 표시만 담당).
- 실행 전 상태 점검: HEAD `f240340`. 직전 보고서(10:06) 이후 타 세션 편집 **7건** 확인 — `ProfileView.vue`(11:05), `OrderFieldsForm.vue`·`orderCreate.js`·`orders.js`·`useOrderForm.js`·`orderCreate.test.js`·`OrderCreateView.vue`(11:53~12:00, 등록 폼 공용화). **전부 UI 결함 없음**으로 판정(아래 (b)·(e)).
- 전역 표준 스캔 신규 위반 0건: `오더`/`주문번호` **UI 노출 0건**(18건 전량 코드 주석), `드라이버` UI 노출은 `data/levels.js` 레벨 타이틀 6건뿐(기존 후보), 한국어 보조용언 띄어쓰기 **신규 위반 0건**, `!important` **0건**, `transition: all` **1건**(기존 후보).
- `npm run build` 통과(`✓ 4378 modules transformed`, `✓ built in 1.03s`, 오류·경고 0건), `vitest run` **14 files / 77 tests 통과**, `check:refs`(템플릿 참조) **OK**.
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-11 12:01~12:1x (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-11_1006.md`(적용: QuickMatchPanel scoped↔전역 클래스 분리 + 토글 `role="switch"`) · `2026-09-11_0101.md`(적용: 알림 `priorityCls` 에 `report: 'danger'`) · `2026-09-11_0001.md`(적용: 카운트다운 `tabular-nums`) — 적용 내역·미적용 후보 전수 대조 후 **반복 적용 없음**. 역대 보고서 그렙(`scroll-top`·`맨 위로`·`market-fab`·`community-fab`·`my-market-fab`·`겹`) 결과, FAB 관련 기록은 **09-04 09:03(스팟 열람)·09-10 12:00/17:01/22:01(색상 후보)** 뿐이고 **"전역 플로팅 유틸 버튼 ↔ 페이지 FAB 자리 충돌" 축은 등장하지 않는 신규 감사 축**임을 확인.
- **실행 전 상태 점검**: `Get-Date`(12:01) + `git log --oneline -3`(HEAD `f240340`) + `git status --short` + `Get-ChildItem frontend/src -Recurse | Sort LastWriteTime -Descending` → 10:06(직전 회차) 이후 타 세션 편집 7건 확인.
- **신규 축 1 — `position: fixed` 플로팅 요소 좌표·z-index 전수 대조(신규 감사)**: `position: fixed` 9개 블록 판독 — [base.css `.bottom-nav`](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L140-L152)(z-index 100) · [base.css `.notify-banner`](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css#L232-L254)(z-index 30) · [ScrollTopButton](file:///d:/wwwroot/nowhere/frontend/src/components/common/ScrollTopButton.vue#L53-L69)(z 50) · [MarketView `.market-fab`](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1462-L1479)(z 90) · [CommunityView `.community-fab`](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L1105-L1122)(z 60) · [MyMarketView `.my-market-fab`](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L722-L738)(z 60) · [OrderDetailView `.detail-actionbar`](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L2403-L2417)(z 100) · ImageGallery(z 9999) · ChatThread(z 9). → FAB 3곳과 맨 위로 버튼만 **동일 좌표**였다(나머지는 하단 바·상단 배너·전체 화면으로 충돌 없음).
- **신규 축 2 — 페이지 FAB 렌더 조건 대조(신규 감사)**: [MarketView FAB](file:///d:/wwwroot/nowhere/frontend/src/views/MarketView.vue#L1276-L1286)(`v-if="isDriver"`) · [CommunityView FAB](file:///d:/wwwroot/nowhere/frontend/src/views/community/CommunityView.vue#L385-L393)(항상) · [MyMarketView FAB](file:///d:/wwwroot/nowhere/frontend/src/views/orders/MyMarketView.vue#L696-L706)(`listTab !== '정산' && (orders.length || loading)`) → 세 화면 모두 스크롤 320px 초과 상황이 흔해 실제 노출된다.
- **신규 축 3 — 한국어 보조용언 띄어쓰기 재검증**: `해주|해보|되어있|되있|드려요|보세요|하여|해두|안되` 전수 그렙(13줄) → 전부 정상 표기(`추천해 드려요`·`보여줘요`(합성어 `보여주다` 결합)·`등록해 두면`·`확인해 새 비밀번호`). **신규 위반 0건**(09-09 17:02 전수 교정 유지).
- **타 세션 신규 편집 검토(수정 없음)**: [OrderFieldsForm.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderFieldsForm.vue)(11:53 신설 — 직접 입력 폼과 AI 구조화 결과가 공유하는 필드 컴포넌트. 라벨 한국어·`inputmode:'tel'` 을 `input-props` 로 내부 `<input>` 에 전달·배지/색 규격 위반 없음·기술 식별자 노출 없음 → 결함 없음) · [OrderCreateView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue)(공용 컴포넌트 2곳 적용 L318·L347, 상태 탭·캘린더 등 기존 스타일 유지 → 결함 없음) · [ProfileView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue)(요약 탭 실적 카드 3종·바로가기·근무 상태 — `n-switch` 시맨틱 내장, 하드코딩 배경은 라이트/다크 쌍 또는 아이콘 채움 → 결함 없음).
- **전역 표준 스캔(수정 없음)**: `오더`/`주문번호`/`드라이버` 18건 전량 판독 → **UI 노출은 [data/levels.js](file:///d:/wwwroot/nowhere/frontend/src/data/levels.js#L15-L21) 레벨 타이틀("신입 드라이버" 등 6건)뿐**이고 나머지는 코드 주석. `transition: all` 1건([QuickMatchPanel L1094](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1094), 기존 후보) · `!important` 0건.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀·의미색·기능 삭제 금지), 프로젝트 배지 표준(1px 6px/10px/400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출).

## (c) 발견한 개선 포인트
1. **전역 "맨 위로" 버튼이 페이지 FAB 에 가려 죽은 컨트롤이 됨(신규 발견·적용)**: [ScrollTopButton](file:///d:/wwwroot/nowhere/frontend/src/components/common/ScrollTopButton.vue) 은 [App.vue](file:///d:/wwwroot/nowhere/frontend/src/App.vue#L312) 에서 **모든 화면에 전역 렌더링**되고 `window.scrollY > 320` 이면 우하단에 나타난다. 그런데 마켓·커뮤니티·내 마켓은 각자 **같은 우하단 자리(우 16~18px, 하단 76~84px)** 에 지름 54px FAB 을 띄운다. 좌표상 FAB 사각형이 맨 위로 버튼(42px)을 **완전히 또는 대부분 포함**하고 z-index 도 FAB(60·90) > 맨 위로(50)라, 세 화면에서 스크롤을 내리면 "맨 위로" 버튼이 **시각적으로 덮여 눌리지 않는다**. 컨트롤이 사라진 것이 아니라 **눌리지 않는 채로 남는** 형태라 사용자에겐 "버튼이 안 먹는다"로 보인다.
2. **표준 스캔 결과 신규 위반 없음(기록)**: `오더`/`주문번호` UI 노출 0건, 한국어 보조용언 띄어쓰기 위반 0건, `!important` 0건 — 직전 회차 교정분이 유지되고 있다.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `App.vue` | ① 페이지가 자체 FAB 을 띄우는 화면을 판정하는 `hasPageFab` computed 신설(L39-40) — `['market', 'community', 'my-market'].includes(route.name)`(기존 `moreActive`·`tabActive` 와 동일한 라우트명 목록 관례). ② 템플릿에서 `<ScrollTopButton :raised="hasPageFab" />` 로 전달(L312) |
| `components/common/ScrollTopButton.vue` | ③ `raised` prop 추가(Boolean, 기본 false, L5-11) — 라우트 지식은 App.vue 가, 표시는 버튼이 담당. ④ 루트 버튼에 `:class="{ 'scroll-top-btn--raised': raised }"` 바인딩(L43). ⑤ `.scroll-top-btn--raised { bottom: calc(146px + env(safe-area-inset-bottom)); }` 신설(L71-74) |

- **값 근거**: FAB 은 `bottom 84px + safe-area` 에서 지름 54 → 상단이 **138px**. 맨 위로 버튼을 146px 로 올리면 **8px 여유**를 두고 FAB 위에 놓인다(마켓은 FAB 하단이 76px 라 여유 16px).
- **원칙 부합**: FAB 을 숨기거나 맨 위로 버튼을 삭제하지 않고 **위치만 올려 두 컨트롤을 모두 유지**한다(비전 원칙 8 "기능을 삭제하지 않고 노출량을 줄인다"). 세로로 쌓인 원형 버튼 2개(주요 행동 FAB 아래·유틸 버튼 위)는 기존 관용 패턴과 일치한다.
- 공통: 신규 **prop·computed·클래스**만 추가했고 `<script>` 로직·표시 텍스트·DOM 구조·기존 클래스는 무변경. 파일당 편집은 **순차**로 진행(동일 파일 병렬 편집 시 선행 수정이 원복되는 기존 교훈 준수) 후 그렙으로 **잔존 확인** — `hasPageFab` 2곳(App.vue L40·L312), `--raised` 3곳(prop L7·바인딩 L43·CSS L72) 총 5곳 반영.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **FAB 미노출 상태까지 반영한 정확한 분기(신규 관찰·후보)**: `market` 은 비기사(`v-if="isDriver"`), `my-market` 은 정산 탭·빈 목록일 때 FAB 이 없다. 라우트 기준 판정이라 그때도 맨 위로 버튼이 8px 높게 뜨지만 **시각 차이가 미미**하고, 정확히 하려면 화면 → App 으로 FAB 노출 상태를 공유(스토어 플래그)해야 해 범위가 커진다 → 후보.
2. **`data/levels.js` 레벨 타이틀 "드라이버" 6건(신규 재확인·후보)**: 실제 사용자 노출 문자열([ProfileView 활동 탭](file:///d:/wwwroot/nowhere/frontend/src/views/ProfileView.vue#L327) `{{ level.title }}`)이지만 "기사" 로 바꾸는 것은 전역 네이밍 결정(기존 후보 유지).
3. **`OrderFieldsForm` 도입에 따른 필드 정의 중복(신규 관찰·후보)**: 공용 컴포넌트가 생겼지만 [셋트 일정 그리드](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue#L473-L517)는 여전히 자체 필드를 반복 정의하고 그리드 최소폭도 다르다(`minmax(240px)` vs `minmax(200px)`). 셋트 폼을 공용 컴포넌트로 흡수하는 리팩터링은 범위 초과 → 후보.
4. **기존 반복 후보(상태 재확인, 유지)**: FAB 3곳 `var(--brand)` + 흰 글리프 · `--accent` 아바타 흰 이니셜 · `:focus-visible` 미정의/`outline: none` · UiChip 하드코딩 팔레트 · 관리자 상태 칩 굵기 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · PWA 매니페스트 용어 · `aria-live` 도입 · `user-scalable=no` · `transition: all`([QuickMatchPanel L1094](file:///d:/wwwroot/nowhere/frontend/src/components/orders/QuickMatchPanel.vue#L1094)) · 소수점 폰트(11.5px·10.5px) · `border-radius: 14px` 하드코딩 · `@media` 브레이크포인트 토큰화 · 알림 본문 `word-break: break-all` · `formatWon` 명칭 혼재(XP 점수에 재사용) · coin+`원` 이중 표기 · MoreView 아이콘 중복 · SettingsVehiclesView 로딩 스켈레톤 부재 · 관리자 모달 `60vh`/`50vh` · MarketView `n-drawer` 80vh · href 없는 `<a>` 행 · `role="button"` 카드 Space 키 미지원 · iOS 입력 확대(16px) 미대응 — 각각 전역 디자인 결정 또는 구조 변경으로 09-03~09-11 여러 회차에서 분류된 항목 → 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ 4378 modules transformed`, `✓ built in 1.03s`, 오류·경고 0건). 청크는 종전과 동일하게 생성(`MarketView` 42.03 kB · `CommunityView` 13.83 kB · `MyMarketView` 12.51 kB · `index` 272.85 kB 등).
- **`vitest run` 통과** — `Test Files 14 passed (14)`, `Tests 77 passed (77)`. **`npm run check:refs` OK**("모든 템플릿 참조가 정의되어 있습니다").
- **빌드 산출물 검증**(`dist/assets/index-BjY_t_uj.css`): `.scroll-top-btn--raised` 규칙이 포함됨을 확인(scoped 규칙으로 컴파일, 기존 `.scroll-top-btn` 규칙 뒤에 위치 → 동일 명시도에서 후위 규칙이 적용).
- import 누락 점검: 변경이 **신규 prop·computed·클래스**뿐이라 import·심볼·컴포넌트 등록 영향 **0건**. `defineProps` 로 선언했으므로 Vue 가 `raised` 를 DOM 속성으로 남기지 않는다.
- 런타임 위험 점검: ① FAB 이 없는 화면(대부분)은 클래스 미부여로 **종전과 완전히 동일**(bottom 84px). ② FAB 화면에서만 62px 상향되어 **겹침이 소멸**하고 두 버튼이 세로로 분리된다. ③ `bottom` 만 바꾸므로 크기·색·모션·z-index 변화 0. ④ `hasPageFab` 은 라우트명 비교만 하므로 부작용 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
