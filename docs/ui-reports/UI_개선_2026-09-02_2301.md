# UI 개선 보고서 — 2026-09-02 23:01

## (a) 실행 시각
- 2026-09-02 23:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰/컴포넌트(직접 읽기): `views/orders/MyMarketView.vue`(보낸 요청/일괄요청 그룹), `components/ui/UiChip.vue` + 사용부 `views/NotificationsView.vue`·`components/orders/QuickMatchPanel.vue`, `components/chat/ChatThread.vue`(상태 배지·양 모드 대비), `views/auth/RegisterView.vue`·`LoginView.vue`(대조), `views/settings/*.vue`(5파일, 이중 표제 여부), `views/HomeView.vue`(스크립트·추천 섹션 대조), `views/community/CommunityView.vue`·`CommunityPostView.vue`(피드 액션), `views/ReviewsView.vue`(탭 카운트), `assets/base.css`(알림·검증·채팅 공용 영역)
- 전역 감사(grep): fallback hex(`var(--x, #…)`), `font-size 11/12px·font-weight 700`, `padding 1px 7/8px·2px 6~9px`, `border-radius 999px` 전수, `color:#fff`/`#f*` 밝은색 전수, 배지-유사 패딩, UI 노출 용어(드라이버/히스토리/주문번호/오더/유저/Admin/ON), 영문 placeholder, `v-if="false"`·`v-show="false"`·TODO/FIXME, 미정의 토큰(`--card-bg`·`--text-secondary`)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_2205.md` 및 그 이전 보고서 8건(적용 내역 전수 확인)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색)

## (c) 발견한 개선 포인트
1. **배지 스펙 위반 잔존 — 내 마켓 "일괄요청" 라벨 (신규 발견)**: MyMarketView의 보낸 요청 그룹 헤더(`claim-group__head`)에 붙는 정적 정보 라벨 `.claim-group__badge`("일괄요청")가 `2px 8px / 10px / 700` — 배지 스펙(1px 6px/10px/400)에서 벗어남. 09:14 회차의 홈/알림/처리할 일 배지 정리와 달리 "요청보냄" 영역(그룹화 시점에만 노출)이라 그동안 grep `1px 7/8px` 유형에 안 걸리며 누락돼 있던 잔존 건. font-size는 이미 10px라 스펙과 일치, padding·weight만 위반.
2. **공용 UiChip 재검토**: 콘텐츠 칩(`<b>라벨</b>값`, padding 4px 7px / 11px / 700, border+bg 2층). QuickMatchPanel에서는 클릭 가능 버튼(`qmp-chip-btn`)에 감싸여 사용되고, NotificationsView에서는 종류 태그로 사용 — 클릭형/전용 컴포넌트에 해당해 배지 스펙 대상이 아니라고 판단(16:03 판단과 일치). 09:14 보고서의 "다음 회차 variant별 재검토" 항목을 재확인한 결과, variant 색 6종은 라이트+`html.dark` 쌍이 모두 갖춰져 있어 추가 수정 불필요.
3. **양 모드 대비 재확인 결과 클린**: ChatThread `.chat-order-card__status`는 라이트 `#fff` + 다크 `#101418` 오버라이드(색상 배경이 양 모드에서 달라지는 RideHistory 패턴과 동일)로 처리돼 있고, `html.dark .chat-type-sep` 등도 변수 기반. base.css `.alert--error`의 라이트 하드코딩(#fdeceb/#f5c0bc)은 다크 오버라이드가 존재해 대비 결함 없음.
4. **기타 재확인 결과 클린**: 낡은 fallback hex 0건, 미정의 토큰(`--card-bg`·`--text-secondary`) 0건, UI 노출 "드라이버"/"히스토리"/"오더"/"유저"/"Admin"/"ON" 0건(주석·백엔드 동기 데이터만), UI 노출 `order_number`·"주문번호" 0건, `v-if="false"`/TODO/FIXME 0건, 설정 화면 이중 표제 0건(SettingsProfileView 해소 유지), 영문 placeholder는 모두 형식 예시(KE101/KLOOK/URL)라 한글화 대상 아님.
5. **HomeView 추천 섹션**: 연결고리/단일/셋트 3섹션 구조·추천 카드 조건%(match_score)·근거 체크리스트·일괄요청 카운트다운 상태가 시안(v8)과 PRODUCT_VISION에 부합 — 구조 변경 불필요(후보로만).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/MyMarketView.vue` | `.claim-group__badge`("일괄요청" 라벨) `padding: 2px 8px` → `1px 6px`, `font-weight: 700` → `400` — 배지 스펙(1px 6px/10px/400) 준수. 배경(`--brand-soft`)·색상(`--brand`)·font-size(10px)·구조·동작 변경 없음. |

순수 `<style scoped>` CSS 속성 변경 1건 — 의미·레이아웃·상호작용 무변경.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ReviewsView·CommunityView 탭/카운트 `1px 7px`**: 활성 탭 위 흰 오버레이 카운트로 10px/400 적용 시 대비 약화 — 탭 액티브 디자인과 함께 재검토(기존 후보 유지).
- **MyMarketView `.claim-time__text`·`__remain`(10px/700)**: 남은 시간 카운트다운 강조(빨간색 위험 시각)로 시간 정보 강조 — 배지 대상 아님(유지).
- **공용 `UiChip.vue`(11px/700, padding 4px 7px)**: 클릭형 칩 버튼·전용 컴포넌트로 배지 스펙 대상 아님. 전역 시각 변화가 큰 구조 변경 가능성은 후보로 유지.
- **data/levels.js 레벨 타이틀 "드라이버"(신입~마스터)**: 백엔드 `LevelTable.php`와 동기화된 값 — 교차 변경 필요(기존 후보 유지).
- **`.verify-row__done`·`__badge`·VerifiedBadge `#2ea043`/`#1f9d63` vs `--status-completed`(#18a058)**: 토큰화 시 라이트 색 미세 변화 수반 — 색상 체계 결정 후 일괄 적용(기존 후보 유지).
- **ChatImageSheet 전송 버튼·아바타·OrderDetailView·OrderCard 등 `color:#fff` on brand/상태색 계열**: 전역 `--on-brand` 설계 의존 — 기존 후보 유지(반복 미적용).
- **OrderDetailView 의미색 하드코딩(`#ffb020`·`#f0a020`·`#ffa940`)·confetti 색 배열**: 토큰화 시 다크 값과 달라 색 변화 수반 — 색상 체계 판단 필요(기존 후보 유지).
- **AdminView `.auto-history__status` 라이트 흰 글자**: statusTextColor 헬퍼 유형 적용이 필요한 색상 체계 결정(기존 후보 유지).
- **SettingsVerificationView "신청" primary 2개·HomeView 히어로 CTA 크기 등**: 주동작 강조 개선이지만 단순 타입 변경으로는 시각 균형 깨짐 — 별도 설계(기존 후보 유지).
- **EmptyState 아이콘 5색·커뮤니티 카테고리 6색 grayscale 통일**: 전역 디자인 결정(기존 후보 유지).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 886ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `padding: 2px 8px`(MyMarketView) 잔존 없음, `.claim-group__badge`가 `1px 6px`/`font-weight: 400`로 변경 확인. 배지-유사 padding 잔존은 탭 카운트·액션 버튼·상태 칩(후보)뿐.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 속성 1건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
