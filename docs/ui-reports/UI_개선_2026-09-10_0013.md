# UI 개선 보고서 — 2026-09-10 00:13

## 실행 결과 요약
- **직접 코드 수정 0건 (변경 없음)** — 직전 실행(23:02) 이후 `frontend/src`의 변경은 타 세션(찜 기능 마무리)이 편집한 OrderCard.vue·MarketView.vue(23:30, 이번 검토 시점 기준 40분 전)뿐. 해당 파일·동반 신규 파일(OrderFavoritesView·ScoreStars·OrderDetailView 찜 영역)은 활발 편집분으로 분류해 직접 수정에서 제외하고 후보로만 기록했다.
- 직전 보고서 적용 내역(aria-label 8건, OrderCreateView 데드 CSS 등)은 전수 재확인으로 잔존·지속성 검증 완료. 반복 적용 항목 없음.
- 전역 표준 스캔(용어·보조용언·배지·소형 폰트·aria-label·다크 미대응 하드컬러) 신규 위반 0건 — 전 카테고리 클린 상태 유지.
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-10 00:13~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_2302.md`·2201 — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + mtime 검사 — 23:02 보고서 이후 OrderCard.vue(23:30)·MarketView.vue(23:30) 타 세션 갱신 확인 → **활발 편집 중인 파일로 분류해 수정 제외**. 그 외 frontend/src 파일은 23:03~23:04(직전 실행 자체 변경분) 이후 갱신 없음.
- **직접 판독**: OrderCard.vue(찜 하트·조건 별점 바, 330~412·473~497), MarketView.vue(찜 버튼·필터 모달 940~1160·찜 CSS), OrderFavoritesView.vue(전량 239행 — 신규 찜 페이지), ScoreStars.vue(전량 111행 — 신규 별점 컴포넌트), NotificationsView.vue(185~510 우선 처리 카드·칩), OrderDetailView.vue(찜 하트 460~480), UserPageView.vue(badge 485~549), UiChip.vue·UiCard.vue(전량), ForgotPasswordView.vue(전량), EmptyState.vue(전량), ChatList.vue(라벨), HeaderBar.vue(타이틀·버튼), AdminView.vue(1598).
- **전역 그렙**: `오더|주문번호`(0건) · 보조용언 붙여쓰기(0건) · `font-size < 10px`(0건) · 배지 규격 위반(padding 1px 6px / 10px / weight 400 유지, 위반 0건) · 아이콘 전용 버튼 `aria-label` 누락(0건 — 신규 찜 버튼들도 `aria-label`+`title` 병기 보유) · `<img>` alt 누락(0건) · `target="_blank"` 링크 `rel="noopener"` 누락(0건) · 다크 미대응 하드코딩 배경(`#e#…`/`#f#…`) 잔존(0건 — 전부 `html.dark` 재정의 또는 색상 의미 용도) · 중복 id(0건).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피·배지 표준, 메모리 규칙(한국어 UI / "운행" 용어).

## (c) 발견한 개선 포인트
1. **OrderFavoritesView(신규 찜 페이지) eyebrow 시맨틱 컬러 후보(신규 관찰, 미적용)**: [OrderFavoritesView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/OrderFavoritesView.vue#L177-L182) `.fav-head__eyebrow`가 `font-weight: 700`에 `color: var(--danger)`(빨강)를 사용. 알림/UI 색상 원칙상 레드는 '취소·거절·문제' 전용인데, '찜한 운행' 페이지 상단 라벨로 쓰면 의미 오용 소지. 다만 하트(찜) 강조가 OrderCard·OrderDetailView에서도 `--danger` 계열(빨간 하트)로 통일되어 있어 **의도된 통일 표현일 가능성**도 있어 보류 — 타 세션 작업 파일이기도 해 직접 수정하지 않음.
2. **OrderCard·MarketView 찜 추가분 표준 준수(확인 결과 정상)**: 하트 토글(찜 해제/찜하기 `aria-label`+`title` 병기), 조건 보기 버튼, 마켓 필터 우측 아이콘 버튼들(내 운행/검색/찜/필터 초기화) 모두 접근성 라벨·다크 변수·배지 규격 클린. 직접 수정할 결함 없음.
3. **기존 반복 후보(23:02·22:01·21:02 기록분 유지, 상태 재확인)**: NotificationsView '처리할 일(운행 승인 · 제안 · 요청)로 이동' 문구 · QuickMatchPanel `.qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · coin 아이콘 + '원' 이중 표기 · '정산 대기중' 백엔드 캐노니컬 · ChatImageSheet 3분할 규칙 · UiChip 기본 스타일 · MyMarketView FAB '＋' 글리프 · HomeView·ChatList 칩 CSS 중복 — 기존 분류 유지.
4. **신규 표준 위반 0건**: 용어("오더"/주문번호)·보조용언 붙여쓰기·기술 식별자·소형 폰트·aria-label 누락·alt 누락·rel 누락·다크 결함 전부 클린. 작업 트리 전반(찜 기능 포함)이 기존 UI 표준을 유지한 상태로 확인.

## (d) 실제 적용한 수정
- **없음 (변경 없음).** 코드 수정 0건. 이번 실행에서 발견된 신규 관찰(OrderFavoritesView eyebrow 컬러)은 타 세션 활발 편집 파일 내 항목이라 직접 수정하지 않고 (e) 후보로만 남긴다.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **타 세션 활발 편집 파일 일체(OrderCard.vue·MarketView.vue·OrderDetailView.vue·OrderFavoritesView.vue·ScoreStars.vue 등 찜 기능 묶음)**: OrderCard·MarketView가 23:30(검토 시점 40분 전)에 갱신되어 아직 진행 중일 가능성이 높아 병행 충돌 방지를 위해 수정 제외. 찜 하트·별점 컴포넌트의 접근성·배지·다크 준수는 확인했고 명백한 결함이 없어 수정 동기가 낮음.
2. **OrderFavoritesView eyebrow `--danger` 컬러(신규 관찰)**: 의미 오용 가능성과 '빨간 하트 통일 표현' 의도 가능성이 공존해 단정 불가. 파일이 타 세션 신규 기능(찜) 분이고 HeaderBar 타이틀 '찜한 운행'과의 문구 중복도 함께 검토가 필요해, 파일 안정화 후 재검토 권장.
3. **NotificationsView '처리할 일(운행 승인 · 제안 · 요청)로 이동' 문구(295행)**: 닫는 괄호 뒤 조사 '로' 비표준 이슈는 문맥상 재문안(괄호 내용 정리 또는 '…에서 이동') 판단이 필요해 후보 유지.
4. **기존 반복 후보(23:02·22:01·21:02 기록분 유지)**: QuickMatchPanel `.qmp-quick` 구조 충돌 · '정산 대기중' 백엔드 캐노니컬 · 승인 대기 3중 반복+Primary 2개 · DashboardView `.settle-item` 상충 · coin+'원' 이중 표기 · ChatImageSheet 3분할 · UiChip 기본 스타일 · MyMarketView FAB 글리프 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 956ms) — 찜 기능(타 세션) 포함 전체 번들 오류 없음.
- 코드 수정 0건이므로 import 누락·신규 심볼 검증 대상 없음. 수정 전/후 비교 불필요.
- 적용분 없음(직접 수정 0건). 기존 미커밋분(찜 기능 포함)은 워킹트리에 남김(사용자 커밋 대기).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
