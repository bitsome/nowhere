# UI 개선 보고서 — 2026-09-05 14:04

## 실행 결과 요약
- **수정 1건** — 12:02(45회차) 이후 `frontend/src` 변경 8건(12:04 정산 토스트 단위 수정 반영, 13:03~13:21 **신고/분쟁 기능 도입**: ReportDialog 신규 + 운행 상세·채팅·사용자 페이지 신고 진입점 + 관리자 신고 처리 탭 + 알림 신고 분류) 전수 열람. 기능 전체는 한국어/"운행" 용어/색 의미론(문제=레드)/상태 배지 체계를 잘 지켜 대체로 정합적. 유일한 명확한 소형 결함: **OrderDetailView 운행 상세 히어로의 신고 텍스트 버튼이 백색 텍스트 영역에서 `--text-muted`(#6a6a6a)를 써 라이트 모드 대비가 거의 없는 문제**를 수정. 직전 회차(12:02) 적용분·후보 반복 적용 없음.

## (a) 실행 시각
- 2026-09-05 14:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔: 12:02 이후 `frontend/src` 변경 8건 — `views/SettlementView.vue`(12:04) · `api/reports.js`(13:03) · `components/reports/ReportDialog.vue`(13:16) · `views/orders/OrderDetailView.vue`(13:18) · `components/chat/ChatThread.vue`(13:18) · `views/community/UserPageView.vue`(13:19) · `views/AdminView.vue`(13:21) · `views/NotificationsView.vue`(13:21)
- **직접 열람**: 신고 기능 신규분 전수 — ReportDialog(전체, 템플릿+CSS) · OrderDetailView(히어로·신고 진입점·다이얼로그 위치+CSS) · ChatThread(신고 버튼 위치+CSS) · UserPageView(신고 버튼·CSS·로직) · AdminView(신고 처리 탭 전체+CSS+단계 진행 모달) · NotificationsView(신고 분류 규칙·우선순위 영역) · SettlementView(L95 단위 수정 확인) · `assets/base.css`(색 변수 대비 확인) · `utils/icons.js`('warning' 아이콘 존재 확인)
- **전역 감사(grep)**: `formatWon(...)원`·`원원` 단위 중복 0건 / font-size 10px 미만 0건 / "오더|주문번호|order_no" 노출 0건 / 신고 버튼 아이콘 `warning`은 `utils/icons.js` L172에 등록 확인(런타임 렌더 정상)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_1202.md`(45회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **운행 상세 히어로 '신고' 버튼 대비 결함(→수정)**: `.detail-hero`는 백색 텍스트 영역(`color:#fff`, 라이트=파랑·다크=틸 그라데이션)인데, 13:18에 추가된 신고 버튼이 `color: var(--text-muted)`(#6a6a6a — 페이지 배경용) + `opacity:.85`를 사용. 히어로 내 다른 요소(eyebrow·meta)는 흰색+opacity 체계인데 신고만 무채색 회색이라 **라이트 모드에서 그라데이션(특히 보라 끝단 #722ed1 계열) 위 대비가 약 1.4~2.2:1로 사실상 안 보임**. 호버의 `var(--danger)`(#b42318)도 파랑 배경 위에서 저대비. → 수정 대상.
2. **신고 기능 전체 정합성**: 진입점 3곳(운행 상세 히어로 텍스트·채팅 상단 우측·사용자 페이지 우측 하단) 모두 아이콘+텍스트 소형 버튼, 호버 시 레드 계열로 '문제' 의미 부여 — 일관. 본인/관리자/내가 등록한 운행은 신고 불가로 제외 로직(`canReport*`) 정상. ReportDialog는 유형 칩(활성=레드) + 사유 textarea + `신고 접수`(레드, 접수 버튼 하나) + 안내문 구조로 '정보→이유→행동' 순서·Primary 1개 원칙 준수. 카테고리/옵션은 백엔드 단일 소스(GET /reports/options)로 중복 없음.
3. **관리자 신고 처리 탭**: [접수 대기 기본 필터 + 총 N건] + 단계 진행 모달(다음 단계 select + 처리 메모, '처리·완료' 시 메모가 신고자에게 전달 안내). 상태 색 접수=레드(즉시 처리)/확인·조사=옐로우(확인)/처리·완료=민트·그린(정상) — 일일 운영 우선순위(🔴>🟡>🟢) 체계와 정합. 카드 정보(유형·상태·대상·이유·신고자·처리 메모·처리자) 구성 충분.
4. **알림 신고 분류**: 제목/내용에 '신고' 포함 시 레드(문제)로 분류, 태그 '신고' danger 칩, 요청 그룹(처리 필요) 소속 — 알림 색상 체계(레드=문제) 준수.
5. **전역 캐너리 통과**: 단위 중복 0건 / 10px 미만 폰트 0건 / 기술 식별자 노출 0건 / icon 'warning' 등록 확인.

## (d) 실제 적용한 수정
- [OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L2185-L2205) — 히어로 내 신고 버튼 대비 수정(CSS만):
  - 전: `color: var(--text-muted)` + `opacity:.85`, 호버 `color: var(--danger)` → 라이트 모드 히어로(파랑·보라) 위 저대비
  - 후: 기본 `color: rgba(255,255,255,.88)`(히어로 백색 텍스트 체계와 통일), 호버 `background:#e5484d`(히어로 배지 `--urgent`와 동일 톤)+`color:#fff` → 문제 의미(레드) 유지 + 대비 확보. `padding: 2px 6px; border-radius: 999px`로 기존 히어로 배지/다른 신고 버튼과 동일한 필 형태 통일. 호버 배경으로 인한 레이아웃 변이 방지용 기본 상태에도 동일 패딩 부여.

## (e) 적용하지 않은 항목과 이유 (후보)
- **신고 버튼 3곳 스타일 세부 차이**: 채팅·사용자 페이지는 무채색 텍스트(흰 배경 위) + 호버 레드 텍스트/틴트, 운행 상세는 백색 텍스트 + 호버 레드 필 — 배경 톤이 달라 별도 처리가 타당, 전역 통일은 불필요(유지).
- **AdminView 신고 상태 칩 `font-weight:700`·패딩 `1px 8px`**: 상태 채움이 있는 라벨로 사용자 배지 스펙(1px 6px·400)과 다른 계층 — 관리자 콘솔의 다른 상태 칩(1px 6px)과 미세하게 다르나 의미색 구분 목적상 유지(대형 정리 시 검토).
- **신고 처리 메모 placeholder·안내문 다소 장문**: 운영 안내 필요성에 따른 것 — 유지.
- **기존 반복 후보(LoginView·RegisterView auth CSS 중복, 홈 추천 카드 조건% 미노출, OrderCard 들여쓰기, OrderCreateView CSS 중복, 커뮤니티 카테고리 색, 의미색 하드코딩 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build`(frontend, vite v8.2.1) **통과** — 4355 modules, 1.08s. OrderDetailView 청크 정상 생성.
- import 누락 점검: 수정이 OrderDetailView scoped CSS 블록 1건에 국한 — 신규 import 없음, 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
