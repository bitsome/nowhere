# UI 개선 보고서 — 2026-09-03 19:07

## (a) 실행 시각
- 2026-09-03 19:07 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 컴포넌트(직접 전체 읽기 — **보고서 기록상 전체 읽기 이력이 없는 신규 대상 다수**): `components/common/ImageGallery.vue`(전체 화면 갤러리 — 포인터·핀치·줌 로직 + 오버레이 UI), `components/common/VerifiedBadge.vue`(검증 칩), `components/common/ConfirmDialog.vue`(공용 confirm), `components/common/ScrollTopButton.vue`, `components/common/SegmentedGroup.vue`, `components/common/EmptyState.vue`, `components/orders/OrderCardSkeleton.vue`, `components/layout/ChatListener.vue`·`NotificationListener.vue`(로직 전용, UI 없음)
- 뷰: `views/ChatView.vue`(전체 — 대화 목록/스켈레톤/EmptyState), `views/DashboardView.vue`(버튼·title 패턴 grep), `views/AdminView.vue`(상태 태그 글자색·n-tag 사용처 대조 — 17:01 수정 잔여 확인)
- 뷰(부분 대조): `views/orders/OrderDetailView.vue`(히어로 그라디언트·claim 카운트 칩·채팅 unread 닷·완료 축하 요소 등 흰 글자 잔존 위치 일괄 열람)
- 전역 감사(grep): `background: var(--brand)`·`var(--accent)` 채움 + `color:#fff` 잔존 전수, 미정의 CSS 변수 사용 전수(`--text-secondary`·`--card-bg` 등 0건), UI 용어 잔존("운행완료/드라이버/주문번호/오더" — 주석·동기 데이터만), 10px 미만 폰트 잔존(0건 — 18:01 수정 후 유지), 아바타(`--accent`) 다크 모드 오버라이드 유무
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1801.md`·`1701.md`·`1603.md`·`1501.md`·`1405.md` 등(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`, 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **가져오기 신청 건수 칩 brand 채움 위 흰 글자 잔존 (신규 발견·적용)**: 운행 상세 "가져오기 신청 N" 헤더의 `.detail-claim-title__count`(L1399-1412)가 `background: var(--brand)`(라이트 `#36adff`·다크 `#63e2b7` — 모두 밝은 색) 위에 `color: #fff`를 사용 — 대비 ≈1.6~1.9:1. 14:05 회차에서 같은 유형의 건수 칩 4종(ChatList `.chat-chip--active .chat-chip__count`·CommunityView `.community-tabs__count`·MarketView `.market-filters__badge`·NotificationsView 등)을 `#07120e`로 정리했는데, OrderDetailView의 이 칩만 정리 대상에서 빠진 잔존이다. 등록자(진행자 승인/거절) 판단 화면에서 반복 노출되는 숫자라 가독성 문제로 판정.
2. **신규 전체 읽기 컴포넌트 클린 확인**: ImageGallery(오버레이 흰 글자는 전부 흑배경 0.94 위 — 대비 정상, `.ig-gallery__index`는 11px/600 카운터로 배지 스펙 대상 아님), VerifiedBadge(배지 스펙 정확 — 1px 6px/10px/400; `#1f9d63` 단일색은 기존 칩 패턴과 동일 범위), ConfirmDialog(메시지 11px·취소+confirm 구성 정상), ScrollTopButton(aria-label·토큰 사용), EmptyState(문구·아이콘 틴트 정상, 다크 오버라이드 존재), SegmentedGroup·OrderCardSkeleton·ChatListener/NotificationListener(로직 전용) — 수정 불필요.
3. **OrderDetailView 히어로 흰 글자 재확인**: `.detail-hero` 그라디언트(브랜드→페이드) 위 `#ffffff` 텍스트 — 라이트에서 시작부가 밝은 파랑이라 대비가 약하나, 히어로(브랜드 배너) 특성·상태 태그 등 주변 요소와의 시각 균형상 단순 글자색 교체가 아닌 설계 판단이라 미적용(아래 e-4).
4. **아바타·`--accent` 기반 요소(다크 틸 위 흰 글자) 잔존**: ChatList/MessageBubble/ReviewsView 아바타·ReviewsView 활성 탭(`background: var(--accent)` — 라이트 `#1f1f1f`+흰 글자는 정상이나 다크 `#63e2b7`+흰 글자 저대비), ProfileView naive 세그먼트 활성 탭(`var(--brand)`+`#fff`), base.css `.btn--primary`(실사용처 0건 — 데드 CSS) — 전부 기존 회차가 "아바타 관용 패턴 / 전역 글자색 재설계 대상"으로 분류해 유지한 항목(반복 미적용).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/OrderDetailView.vue` | `.detail-claim-title__count` 글자색 `color: #fff` → `#07120e` + 근거 주석("brand 채움은 모두 밝은 색 — 14:05에 정리된 형제 건수 칩과 동일하게 어두운 글자") — "가져오기 신청 N" 건수 칩 저대비 해소. 배경·패딩·font-size(10px)·weight(400)·구조·동작 무변경 |

`<style scoped>` CSS 값 1건 + 주석 1건(템플릿·script·import 무변경). 라이트(`#36adff`)·다크(`#63e2b7`) 모두 `#07120e`가 코드베이스 커스텀 brand 채움 표준(HomeView·ActionCenter·MarketView 등)과 일치.

## (e) 적용하지 않은 항목과 이유 (후보)
- **OrderDetailView `.detail-hero` 흰 글자**: 브랜드 그라디언트 배너의 시각 정체성(흰 상태 태그·금액·글자)이 얽혀 단순 글자색 교체는 균형 깨짐 — 별도 설계 후보(신규 기록).
- **아바타·`--accent` 채움(채팅/리뷰 아바타, ReviewsView 활성 탭, ProfileView naive 세그먼트 활성 탭)**: `--accent`는 라이트에서 어두운 색(#1f1f1f)이라 라이트는 정상, 다크 틸에서만 문제 — `#07120e` 일괄 적용 불가(라이트 대비 역전)라 다크 전용 오버라이드가 필요하고 아바타는 관용 패턴 — 기존 "전역 brand 위 글자색 재설계" 후보 유지(반복 미적용).
- **base.css `.btn--primary`(다크 틸+흰 글자)**: 실사용처 0건인 데드 유틸 — 변경 시 영향 없으나 정리 자체가 UI 개선이 아님(유지).
- **ChatImageSheet 흰 아이콘·FAB(마켓/커뮤니티/내 마켓 '＋')·Login/Register 그라디언트 CTA·OrderDetail 채팅 unread 닷 `#ff4d4f` 하드코딩·`data/levels.js` "드라이버" 타이틀(백엔드 동기)**: 기존 후보 유지.
- **EmptyState 컴팩트 힌트 `font-size: 10.5px`**: 비정수 크기지만 의도적 축소로 보이며 가독성·일관성 영향 미미 — 미적용.
- **base.css·ChatView 등 나머지 10px+700 잔존**: 카운트 칩·강조 라벨로 배지 스펙 대상 아님(기존 분류 유지).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 950ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: `background: var(--brand)` + `color:#fff` 쌍은 FAB(관용)·ProfileView naive 세그먼트(후보)·아바타(후보)만 잔존 — 건수 칩류 brand 채움 흰 글자는 OrderDetailView 포함 전면 정리 확인.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값 1건 + 주석뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
