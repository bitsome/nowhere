# UI 개선 보고서 — 2026-09-03 13:01

## (a) 실행 시각
- 2026-09-03 13:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 읽기): `views/orders/OrderCreateView.vue`(내 운행 스케줄·캘린더 전체 — 최근 심층 리뷰에서 빠진 파일), `views/ReviewsView.vue`(전체 — 최근 탭 카운트 후보만 언급), `views/MoreView.vue`(전체), `views/orders/RideHistoryView.vue`(동일 캘린더 대조)
- 컴포넌트: `components/chat/ChatList.vue`(전체 — 최근 미검토), `components/chat/ChatRequestSheet.vue`(요청 바텀시트 전송 CTA), `components/chat/ChatRequestEvent.vue`(12:07 적용 상태 대조), `components/chat/ChatImageSheet.vue`(보관 체크 요소 성격 분류)
- CSS: `assets/base.css`(토큰 대조 — 라이트 `--brand:#36adff`/`--accent:#1f1f1f`, 다크 `--brand:#63e2b7`/`--accent:#63e2b7` 확인)
- 전역 감사(grep): `background: var(--brand)` + `color:#fff` 쌍 전수(10개 파일), `cal__day--today` 전수, `color:#fff/#ffffff` 60건 분류(brand 배경 텍스트 vs 이미지/아이콘/의미색), `html.dark .cal__day--today` 보유 파일 대조
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1207.md`·`1103.md`·`0101.md` 및 09-02 보고서 `0914.md`·`1002.md`(캘린더·채팅 시트 이력 grep 확인 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **OrderCreateView 캘린더 "오늘" 셀 다크 오버라이드 누락 (신규 적용)**: 내 운행 캘린더 `.cal__day--today`가 다크 모드에서 brand 틸(#63e2b7) 배경 위 흰 글자로 남음. 같은 코드를 공유하는 운행 기록 캘린더(RideHistoryView L789-797)에는 `html.dark .cal__day--today { color: #07120e; }`가 이미 있는데(09-02_0914 회차 적용) OrderCreateView 사본에만 없어 **화면 간 불일치** 상태. 09-02_1002 보고서에서 후보로 기록됐으나 RideHistoryView가 선 적용됨에 따라 이번 회차에서 동일 규칙으로 잔존 건 해소.
2. **ChatRequestSheet "요청 보내기" CTA 흰 글자 저대비 잔존 (신규 적용)**: 바텀시트 전체 폭 전송 버튼 `.rq-form__send`가 `background: var(--brand); color:#fff` — 라이트 파랑·다크 틸 모두 밝은 배경이라 흰 글자(≈1.9:1) 저대비. 12:07 회차에서 같은 요청 시트 계열의 승인 카드 `.rq-card__btn--primary`(ChatRequestEvent)만 `#07120e`로 변경되고 이 버튼은 누락됨 — 동일 표준 적용.
3. **활성 탭 카운트 칩 white-on-brand (신규 후보, 3곳 묶음)**: 작은 개수 칩이 활성 시 brand 채움 + 흰 글자 — CommunityView `.community-tabs__item--active .community-tabs__count`(L596-598)·MarketView `.market-filters__badge`(L1219-1224)·ChatList `.chat-chip--active .chat-chip__count`(L227). 10px 한 자리 수로 대비가 약하나(라이트·다크 모두), 수치 칩의 일관된 처리 방침(어두운 글자 vs 브랜드 딥 톤) 결정이 필요해 후보로 기록.
4. **ProfileView naive 세그먼트 탭 활성 white-on-brand (신규 후보)**: `.profile-tabs :deep(.n-tabs-tab--active)`(L489-494)가 brand 채움 + 흰 글자 — naive-ui 세그먼트 탭으로 전역 naive 테마 오버라이드(12:07 대형 후보)와 함께 검토 필요.
5. **ReviewsView·ChatList 아바타/활성 탭 accent 위 흰 글자(다크만 약함)**: `--accent`가 라이트=검정(#1f1f1f, 흰 글자 정상)·다크=틸(#63e2b7, 흰 글자 ≈1.9:1). 아바타 이니셜(`.chat-list__avatar`·`.review-item__avatar`)과 ReviewsView 활성 탭(`.reviews-tabs__item--active`) — 아바타 관용 패턴·탭 디자인 재검토 대상(기존 후보 유지).
6. **기타 재확인 결과 클린**: 배지 스펙(1px 6px/10px/400) 위반 0건(잔존 `1px 5px`·`1px 7px`는 카운트 칩·탭 카운트로 기존 분류), UI 노출 "드라이버"/"오더" 0건(MoreView `roleLabel`은 'Driver'→'기사' 매핑으로 정상 노출), MoreView VIP 배지·FAB('+' 54px)·아바타 그라디언트 흰 글자는 기존 "brand 위 글자색" 전역 후보 범주 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/OrderCreateView.vue` | `.cal__day--today` 뒤에 `html.dark .cal__day--today { color: #07120e; }` 추가 — 운행 기록 캘린더(RideHistoryView L795-797)와 동일 규칙·동일 값. 라이트 값(#ffffff)·구조·동작 무변경 |
| `components/chat/ChatRequestSheet.vue` | `.rq-form__send` 글자색 `#fff` → `#07120e` + 근거 주석 추가(라이트·다크 brand가 모두 밝아 어두운 글자 — ChatRequestEvent와 동일). 배경·패딩·구조·동작 무변경 |

전부 `<style scoped>` CSS 변경(템플릿·script·import 무변경) — 의미·레이아웃·상호작용 무변경. 값은 같은 코드베이스 커스텀 brand 요소의 기존 표준(`#07120e`) 및 RideHistoryView 동일 캘린더 스타일을 따름.

## (e) 적용하지 않은 항목과 이유 (후보)
- **`.cal__day--today` 라이트 모드 흰 글자**: RideHistoryView도 라이트는 흰 글자 유지 중이라 두 캘린더(내 운행/운행 기록)를 동일하게 두기 위해 라이트는 변경하지 않음. 전역 "brand 위 글자색" 설계 결정 후 일괄 재평가(기존 후보 계열 유지).
- **활성 탭 카운트 칩 white-on-brand 3곳**(CommunityView·MarketView·ChatList): 작은 수치 칩의 일관 처리 방침 결정 필요 — 신규 후보로 기록.
- **ProfileView naive 세그먼트 탭 active white-on-brand**: naive-ui 기본 컴포넌트 영역으로 라이트 naive primary 초록 문제(12:07 대형 후보)와 함께 전역 테마 오버라이드에서 처리 — 신규 후보.
- **아바타·ReviewsView 활성 탭·`.btn--primary`·FAB·VIP/그라디언트 배지 흰 글자**: 관용 패턴/부분 대비 확보/전역 결정 사항(기존 후보 유지).
- **DashboardView 오늘/내일 카드 하드코딩 naive 그린·앰버**(`rgba(24,160,88,…)`·`rgba(255,169,64,…)`): 라이트 naive primary 불일치(12:07 대형 후보)와 같은 근원 — 후보 유지.
- **라이트 naive-ui primary 초록(#18a058)·다크 primary 흰 글자·`.login-submit` 크기·HomeView 탭 카운트 불일치·SettingsVerificationView primary 2개·data/levels.js "드라이버"**: 기존 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 9.59s`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `html.dark .cal__day--today`가 OrderCreateView(L1242)·RideHistoryView(L795) 각 1건(2/2), ChatRequestSheet `.rq-form__send` `color: #07120e`(L347) 존재 — 2/2.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 2건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
