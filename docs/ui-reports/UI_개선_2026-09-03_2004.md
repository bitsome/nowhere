# UI 개선 보고서 — 2026-09-03 20:04

## (a) 실행 시각
- 2026-09-03 20:04 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 컴포넌트(직접 전체 읽기 — **최근 회차에서 세부 CTA가 누락돼 있던 신규 심층 대상**): `components/chat/ChatImageSheet.vue`(전체 1304줄 — 채팅 이미지 첨부 바텀시트: 새 사진 선택/보관함/썸네일 그리드/전송 흐름), `components/orders/QuickMatchPanel.vue`(전체 936줄 — 빠른매칭 패널·조건 모달)
- 대조 파일: `components/chat/ChatRequestSheet.vue`(`.rq-form__send` 13:01 적용 값 `#07120e` — 동일 계열 전체 폭 전송 CTA 표준 확인), `assets/base.css`(`--brand` 라이트 `#36adff`·다크 `#63e2b7`·`.btn--danger` 흰 글자 관용 확인)
- 전역 감사(grep): `background: var(--brand)` 규칙 + 인접 `color` 전수(views/components), components 내 `color:#fff/#07120e` 잔존 재분류(위험색·상태색·이미지 오버레이·아이콘 vs brand 채움 텍스트)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1907.md`(brand 채움 흰 글자 잔존 0건 판정이 채팅 시트 컴포넌트를 빠뜨린 부분 재확인)·`1801.md`·`1701.md`·`1603.md`·`1501.md`·`1405.md`·`1301.md`(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand 채움 위 글자색 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **이미지 첨부 바텀시트 전송 CTA `.img-sheet__send` 흰 글자 저대비 (신규 발견·적용)**: "전송 / 전송 N장"(11px/700 텍스트+아이콘) 전체 폭 CTA가 `background: var(--brand); color:#fff` — 라이트 파랑(#36adff)·다크 틸(#63e2b7) 모두 밝은 배경이라 흰 글자(≈1.9:1) 저대비. 같은 바텀시트 계열의 ChatRequestSheet `.rq-form__send`("요청 보내기" CTA)는 13:01 회차에서 `#07120e`로 정리됐는데, 이 파일의 전송 버튼은 누락된 잔존이다. 채팅 이미지 전송의 주동작이라 반복 노출되는 만큼 동일 표준 적용이 일관된 소규모 수정으로 판정.
2. **보관함 선택 추가 CTA `.img-sheet__archive-add` 흰 글자 저대비 (신규 발견·적용)**: "선택한 N장 추가"(11px/700 텍스트+아이콘, 행 flex 7/10)가 동일하게 `background: var(--brand); color:#fff` — 같은 행의 `.img-sheet__archive-delete-selected`(위험색)와 짝을 이루는 주동작 버튼으로 위 1건과 같은 잔존 범주.
3. **`.img-sheet__send.is-cancel`(전송 중 취소) 글자색 분기 필요 (신규 발견·적용)**: 1번의 공통 `color`를 `#07120e`로 바꾸면 취소 상태(위험색 배경)에서 글자가 어두워져 역저대비가 생기므로, `.is-cancel`에 흰 글자(`#ffffff`)를 명시해 분기 — `.btn--danger`·HeaderBar 배지 등 위험색 위 흰 글자 관용과 일치.
4. **QuickMatchPanel 클린 (신규 전체 확인)**: "켜짐" 상태 배지(padding 1px 6px/10px/400)·차량 인승 배지·조건 요약 칩 배지 모두 스펙 준수, `qmp-switch` 하드코딩 색(#d3d5da/#3a4146/#8b9490)에 라이트/다크 쌍·다크 노브 `#07120e`(12:07 적용) 존재, EmptyState 한국어 문구, 한국어 UI·"운행" 용어 준수 — 수정 불필요.
5. **ChatImageSheet 나머지 흰 요소 재분류 (기존 후보 유지)**: 체크 원형(`.img-sheet__thumb-check.is-checked`·`.img-sheet__archive-check.is-checked`)·완료 원형(`.img-sheet__thumb-done`)은 11~12px **아이콘 전용**이며 사진 위 오버레이 체크 관용 — 기존 "흰 아이콘 관용" 분류 유지. 삭제/미리보기/순서 버튼은 흑배경 `rgba(0,0,0,.65)` 위 흰 아이콘(대비 정상). 위험색 버튼(`.archive-delete-selected`·`.send.is-cancel`) 흰 글자는 의미색 관용.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `components/chat/ChatImageSheet.vue` | `.img-sheet__archive-add` 글자색 `color: #fff` → `#07120e` + 근거 주석("brand는 라이트·다크 모두 밝아 흰 글자 대비 약함 — ChatRequestSheet .rq-form__send와 동일하게 어두운 글자") — "선택한 N장 추가" CTA 저대비 해소 |
| `components/chat/ChatImageSheet.vue` | `.img-sheet__send` 글자색 `color: #fff` → `#07120e` + 근거 주석(동일 표준) — "전송/전송 N장" CTA 저대비 해소 |
| `components/chat/ChatImageSheet.vue` | `.img-sheet__send.is-cancel`에 `color: #ffffff` 명시(기존 배경 `var(--danger)`만 있던 곳) + 주석("위험색 배경 위 흰 글자는 앱 관용 .btn--danger와 동일") — 취소 상태 글자색 역저대비 방지 |

`<style scoped>` CSS 값 3곳 + 주석 3건(템플릿·script·import 무변경). 의미·패딩·레이아웃·동작 무변경 — 글자색만 ChatRequestSheet·HomeView claim 등 코드베이스 커스텀 brand CTA 표준(`#07120e`)과 일치. 아이콘(BaseIcon)은 `color`를 상속하므로 전송/추가 버튼의 아이콘도 함께 정리됨.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ChatImageSheet 체크/완료 원형 아이콘 white-on-brand**(`.thumb-check.is-checked`·`.archive-check.is-checked`·`.thumb-done`): 텍스트가 아닌 11~12px 아이콘 전용 + 사진 위 오버레이 체크 관용 — 기존 "흰 아이콘" 분류 유지(반복 미적용).
- **RideHistoryView `.cal__day--today` 라이트 모드 흰 글자**: 다크는 `#07120e` 오버라이드 존재하나 라이트는 흰 글자 유지 — 내 운행/운행 기록 두 캘린더 일관성 위해 변경 안 함(기존 13:01 후보 계열 유지).
- **ProfileView naive 세그먼트 활성 탭·FAB('＋')·아바타(--accent 다크=틸) 흰 글자**: naive/관용 패턴 — 전역 "brand 위 글자색" 설계 결정 후 일괄 재평가(기존 후보 유지).
- **라이트 naive primary 초록(#18a058)·App.vue 하단 네비 `n-badge` naive 기본색**: 전역 naive 테마 오버라이드 필요 — 대형 후보(기존 유지).
- **HomeView 탭 카운트 불일치·`.login-submit` CTA 크기·SettingsVerificationView primary 2개·data/levels.js "드라이버"·OrderDetailView `.detail-hero` 흰 글자**: 기존 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite v8.2.1) **성공** — `✓ built in 938ms`, 4347 modules transformed, 에러 0건(exit 0).
- 수정 후 grep 검증: `.img-sheet__archive-add`(L1017-1019)·`.img-sheet__send`(L1232-1236) `color: #07120e` 존재, `.img-sheet__send.is-cancel` `color: #ffffff`(L1240-1242) 존재 — 3/3. components 내 brand 채움 **텍스트** CTA 흰 글자 잔존 0건 확인(잔존 흰 글자는 위험색/상태색 배지·이미지 오버레이 아이콘뿐).
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값·주석뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**(런타임 영향 없음).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
