# UI 개선 보고서 — 2026-09-08 14:01

## 실행 결과 요약
- **수정 1건 적용** — 직전 실행(09-08 13:01) 보고서 작성 이후 `frontend/src` 신규 변경분 **0건**(HomeView.vue 13:03 수정은 13:01 보고서의 스켈레톤 CSS 정리분 — 상태 확인 완료). 이번 실행 신규 감사 4종(공용 UI 컴포넌트 일관성 · 폼 내부 버튼 type · 잔여 TODO/임시 마커 · 알림 색상 의미체계)에서 위반 0건이나, 기존 반복 후보였던 **ChatView.vue 미사용 `ref` import를 실제 사용 여부 재확인 후 제거**(12:48 리팩토링 이후 해소되지 않고 잔존 확인).

## (a) 실행 시각
- 2026-09-08 14:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 13:01 이후 수정분)**: `frontend/src` 전체 재귀 — **1건**: `views/HomeView.vue`(13:03). 13:01 보고서의 적용분(전역 `.sk-*`와 중복되는 scoped 스켈레톤 CSS 38줄 제거) 자체로, 잔존 scoped 규칙이 `.sk-line--title`/`.sk-line--cta`(852~853)뿐임을 재확인 — 이미 검토·반영된 상태라 신규 코드 아님.
- **직접 판독(이번 실행 신규 감사 대상)**:
  - 공용 UI 컴포넌트: `components/common/VerifiedBadge.vue` · `LevelBadge.vue` · `SegmentedGroup.vue` · `EmptyState.vue` · `components/ui/UiChip.vue` · `components/layout/HeaderBar.vue`
  - 폼 컨텍스트: `views/SupportView.vue`(173~195) · `views/settings/SettingsVerificationView.vue`(180~215) · `components/chat/ChatThread.vue`(232~270) · `components/orders/OrderDetailChat.vue`(215~233)
  - 알림 색상 의미체계: `views/NotificationsView.vue`(40~132)
  - 후보 해소 확인: `views/ChatView.vue`(전체)
- **전역 스캔(grep)**: `<form>` 포함 vue 파일 4건 · `<button>` type 속성 유무 · `TODO|FIXME|HACK|임시|일단|추후` 마커
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1301.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색·알림센터 색상 규칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **ChatView.vue 미사용 `ref` import(후보 해소 적용)**: `<script setup>`에서 `ref`를 import하나 템플릿·스크립트의 `ref(`·`ref =`·`:ref` 사용 **0건** — 12:48 대화 목록 스켈레톤 리팩토링에서도 쓰이지 않아 그대로 잔존한 데드 import. 제거로도 동작 변화 없음.
2. **공용 UI 컴포넌트 일관성 — 이상 없음**: VerifiedBadge는 배지 스펙(padding 1px 6px / 10px / 400) 정확 준수. UiChip 7개 variant(초록·노랑·빨강·파랑·보라·틸·기본)는 light/dark 모두 정의되고 의미색 체계와 부합. SegmentedGroup 크기 프리셋(11px) 정상. LevelBadge의 `font-weight: 800`은 게이미피케이션 배지(레벨 숫자)로 상태 배지 컨벤션 대상 아님.
3. **폼 내부 버튼 `type` 감사 — 위반 0건**: `<form>`은 SupportView(1:1 문의)·SettingsVerificationView(재신청)·ChatThread·OrderDetailChat(컴포저) 4곳뿐이며, 전부 `@submit.prevent` + 제출 버튼 1개(`type="submit"`/`attr-type="submit"`) 구조. 그 외 폼 내 버튼(첨부·이미지·운행 요청 등)은 모두 `type="button"`이라 **의도치 않은 폼 제출(메시지/문의 오전송) 위험 없음**.
4. **잔여 마커 스캔 — 0건**: TODO/FIXME/HACK/임시/일단/추후 텍스트가 vue/css/js에 없음 — 임시 UI 상태(placeholder·라벨 등) 방치 없음.
5. **알림센터 색상 의미체계 — 비전 부합**: NotificationsView의 `TAG_VARIANT`/`classify`가 요청·요금·확인=옐로우, 취소·거절·신고=레드, 완료·정산·승인=틸, 채팅=블루, 항공=퍼플, 시스템=기본(그레이)으로 분류 — docs/PRODUCT_VISION 알림센터 색상 규칙과 일치. 그룹/우선순위(처리 필요=요청형) 분리도 정상.
6. 기존 반복 후보 상태 변화 없음(13:01 기록분 그대로).

## (d) 실제 적용한 수정
- **ChatView.vue — 미사용 import 제거**: `import { onBeforeUnmount, onMounted, ref } from 'vue';` → `import { onBeforeUnmount, onMounted } from 'vue';`(`ref` 사용 0건 재확인). 잔존 import 2개(onBeforeUnmount 57행·onMounted 25행) 모두 사용 확인 — 렌더링·동작 변화 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(13:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView '(운행 번호·날짜 포함 시…)' — 사용자가 운행 번호를 알 수 없어 안내 실효성 문제) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계(아바타 포함) · App.vue 데드 CSS `.bottom-nav__item--button` · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 11곳 상수 치환(전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) — 기존 분류 유지(렌더링 영향 없거나 대형·디자인·제품 결정 수반, 반복 미적용).
- 이번 실행 신규 감사 결과 적용할 신규 결함 없음(위 (c) 2~5 참조).

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, 4365 modules transformed, 975ms. import 제거 후 ChatView 청크 정상 생성.
- import 누락 점검: `ref` 제거 후 남은 vue import(onBeforeUnmount·onMounted)의 사용처 2건 확인 완료 — 누락 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
