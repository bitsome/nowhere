# UI 개선 보고서 — 2026-09-02 18:01

## (a) 실행 시각
- 2026-09-02 18:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰/컴포넌트: `views/orders/OrderDetailView.vue`(승인 대기 카드·신청자 카드·제안 카드·지도 섹션), `views/ProfileView.vue`(역할 태그), `views/MoreView.vue`(역할 라벨), `views/community/UserPageView.vue`(roleLabel 참조)
- 컴포저블/데이터: `composables/useProfileSettings.js`(역할 라벨 맵), `composables/useOrderDetail.js`(참조)
- 참조(백엔드): `app/Services/Community/CommunityPostService.php`(roleLabel), `app/Support/Leveling/LevelTable.php`(레벨 타이틀)
- CSS/기타: `assets/base.css`(토큰 정의 대조), 전체 src `var()` 참조 감사, 전체 src 이모지/외래어 UI 노출 감사
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-02_0914.md`, `UI_개선_2026-09-02_1002.md`, `UI_개선_2026-09-02_1603.md`
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "드라이버"→"기사" 용어 / 토큰 기반 색상)

## (c) 발견한 개선 포인트
1. **역할 라벨 화면 간 불일치**: `useProfileSettings.js`의 역할 맵이 `Driver: '드라이버'`인데, 동일 역할을 MoreView(`Driver: '기사'`)는 "기사"로 표시 — 프로필(ProfileView 헤더 역할 태그)과 더보기(MoreView 헤더)가 같은 역할을 다르게 노출. UI 용어 규칙("기사")상 "드라이버"는 불일치.
2. **지도 열기 링크 영어 잔존**: OrderDetailView 지도 섹션의 앱 열기 링크 3개 중 "네이버 지도"/"카카오 지도"만 한글이고 Google만 "Google"로 노출 — 한글 UI 불일치.
3. **낡은 fallback hex 잔존(OrderDetailView 13곳)**: 승인 대기 카드·신청자 카드·제안 카드에 `var(--text-muted, #8a8a8e)`, `var(--text, #1a1a1e)`, `var(--brand, #4f6ef2)`, `var(--text-muted, #999)` 등 과거 팔레트 hex가 fallback으로 이중 관리 — 토큰은 항상 정의되어 fallback은 실제 미적용이지만(렌더링 영향 없음) 값 이원화로 유지보수·추후 토큰 개편 시 오해 소지.
4. **미정의 CSS 변수 없음 확인**: 전 파일 `var(--x)` 참조 vs 정의 감사 결과 미정의 토큰 0건(`confetti-color`/`cx`/`cy`는 JS 변수·SVG 속성 오탐).
5. **잔존 UI 문구 점검 완료**: "유저"/"오더"/"히스토리"(UI 노출)/"Enter"/"ON" 등은 이전 회차에서 해소된 상태 확인. UI 노출 이모지 없음(★/♡/✓/●/○ 등은 기능 기호로 유지 대상).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `composables/useProfileSettings.js` | 역할 맵 `Driver: '드라이버'` → `'기사'` — ProfileView 역할 태그를 MoreView와 동일하게 "기사"로 통일 |
| `views/orders/OrderDetailView.vue` | 지도 링크 "Google" → "구글 지도" (네이버/카카오 지도 병기와 일관성) |
| `views/orders/OrderDetailView.vue` | 낡은 fallback hex 정리 13곳 — `var(--text-muted, #8a8a8e)`→`var(--text-muted)`, `var(--text, #1a1a1e)`→`var(--text)`, `var(--brand, #4f6ef2)`→`var(--brand)`, `var(--text-muted, #999)`→`var(--text-muted)` (`.detail-pending-card__*`, `.detail-driver-only`, `.detail-claim-*`, `.offer-item__vehicle`). 렌더링 변화 없음(토큰 항상 정의됨). 0914 보고서의 같은 파일 낡은 brand fallback 제거에 이은 후속 정리 |

## (e) 적용하지 않은 항목과 이유 (후보)
- **백엔드 roleLabel `'Driver' => '드라이버'`(CommunityPostService.php, UserPageView 노출)**: 서버 코드 변경이며 배포 전 로컬 반영 불가 — 프론트 로컬 작업 범위 밖.
- **레벨 타이틀 "신입/일반/인기/스타/슈퍼/마스터 드라이버"(data/levels.js ↔ LevelTable.php)**: 백엔드 레벨 테이블과 동기화된 값 — 교차 변경 필요(기존 보고서 후보 유지).
- **OrderDetailView 의미색 하드코딩**: 승인 대기 타이머 `#ffb020`/rgba(255,176,32,.12), 평점 `#f0a020`/rgba(240,160,32,.1), 완료 축하 그라디언트 `#52c41a`→`#73d13d`, hero 배지 `#722ed1`/`#e5484d` — 토큰(--status-*/--warn) 라이트 값과 부분 일치하나 다크 오버라이드 값과 달라 토큰화 시 색 변화 수반. 색상 체계 판단 필요(기존 보고서 후보 유지).
- **confetti 색상 배열 JS 상수 토큰화**: JS 배열→CSS var 연동 구조 변경 필요(후보).
- **다크 모드 brand 위 흰 글자(`--on-brand`)·ChatList 타입 칩·EmptyState 5색·OrderCard 긴급도 틸**: 전역 설계/디자인 결정 사항 — 기존 보고서 후보 유지(반복 미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 834ms`, 4343 modules transformed, 에러 0건.
- 수정 후 grep 검증: `var(--[a-z-]+,\s*#`(fallback hex) 0건, `Driver: '드라이버'` 0건, `>Google<` 0건.
- import 누락 점검: 이번 변경은 문구 라벨 2건 + CSS fallback 제거(순수 CSS)뿐이며 템플릿 구조·import 변경 없음. `useProfileSettings`의 `roleLabel` 내보내기·ProfileView 사용부는 그대로 유지 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
