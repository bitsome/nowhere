# UI 개선 보고서 — 2026-09-09 23:02

## 실행 결과 요약
- **코드 수정 8건(6개 파일)** — 직전 실행(22:01) 이후 `frontend/src`에 타 세션 변경이 있었음(22:56~23:00 OrderCard·ScoreStars(신규)·OrderDetailView 활발 편집 — 찜 기능 마무리로 추정). 직전 보고서 적용 내역·후보와 겹치지 않는 신규·명확 항목만 직접 수정했다.
- 신규 적용 1유형: **아이콘 전용 버튼의 접근성 이름 누락 일괄 보완** — 캘린더 월 이동(이전 달/다음 달) 4건, 새로고침 2건, 시트 뒤로/닫기 2건, 자동 등록 삭제 1건에 `aria-label` 추가(전역 `title` 전수 스캔 결과 마지막 잔여분 — 코드베이스 표준 규약으로 ChatThread·MarketView·MyMarketView 등은 이미 `aria-label`+`title` 병기 보유).
- 용어("오더"/주문번호)·보조용언 붙여쓰기·배지 규격 위반은 신규 0건 — 직전 보고서 상태 유지. `font-size < 10px` 사용처도 0건.
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 23:02~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_2201.md`·2102 — 적용 내역·반복 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + mtime 검사 — 22:01 보고서 이후 OrderCard.vue(23:00)·OrderDetailView.vue(22:58)·ScoreStars.vue(신규, 22:56) 타 세션 갱신 확인 → **활발 편집 중인 파일로 분류해 수정 제외**. OrderCreateView(22:04)·ChatThread(22:03)는 직전 실행(22:01) 세션 자체 변경분으로 안정화 확인.
- **직접 판독**: MyMarketView.vue(전량 920행 — 탭/요청 카운트다운/휴지통/정산 구조 점검), RideHistoryView.vue(390~420 캘린더 헤더), OrderCreateView.vue(155~180 캘린더 헤더·155~175), ChatRequestSheet.vue(160~200 시트 헤더), ActionCenterView.vue(255~270 헤더), SettlementView.vue(113~125 헤더), AdminView.vue(1592~1605 자동 등록 이력).
- **전역 그렙**: `오더|주문번호`(사용자 노출 0건) · 보조용언 붙여쓰기(0건) · `font-size: 7~9px`(0건) · `<button>` 내 `BaseIcon` 단독 + `aria-label`/`title` 유무 전수(multiline) · `title=` 버튼의 `aria-label` 동반 여부 전수 · 저알파 `rgba(0,0,0,…)` 테두리·배경 37건 위치 확인.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피·배지 표준, 메모리 규칙(한국어 UI / "운행" 용어).

## (c) 발견한 개선 포인트
1. **아이콘 전용 버튼 `aria-label` 누락(명백한 접근성 결함, 신규 스캔 확정)**: 캘린더 보기의 월 이동 버튼 2벌([OrderCreateView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderCreateView.vue#L167-L169)·[RideHistoryView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/RideHistoryView.vue#L405-L407))은 `aria-label`도 `title`도 없는 순수 아이콘 버튼. 새로고침 2건([SettlementView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SettlementView.vue#L120)·[ActionCenterView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/ActionCenterView.vue#L263))과 AdminView 자동 등록 삭제([AdminView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/AdminView.vue#L1595-L1603))는 `title`만 보유, [ChatRequestSheet.vue](file:///d:/wwwroot/nowhere/frontend/src/components/chat/ChatRequestSheet.vue#L170-L180) 뒤로·닫기 버튼은 둘 다 없음. `title`은 스크린리더에 비표준적이어서 동일 기능 파일들(ChatThread·ChatImageSheet·MarketView·MyMarketView 등 전역 표준)과 달리 접근성 이름이 빠진 아웃라이어 — **적용**.
2. **MyMarketView FAB 글리프(기존 후보 재확인, 보류 유지)**: `＋` 전각 문자 사용(699행) — 커뮤니티 글쓰기 FAB와 동일 패턴이라 프로젝트 일관성상 의도된 표현으로 보여 보류.
3. **`aria-label` 누락 이외 신규 표준 위반 0건**: 용어·보조용언·기술 식별자·소형 폰트·저알파 다크 결함 모두 클린. 저알파 rgba 37건은 box-shadow/hover 배경 등 다크에서도 무해한 용도(테두리 아님)로 판정, OrderDetailView(.detail-driver-only) 같은 다크 결함 패턴 잔존 0건.
4. **기존 반복 후보(22:01·21:02 기록분 유지, 상태 재확인)**: QuickMatchPanel `.qmp-quick` scoped/전역 충돌 · NotificationsView '처리할 일(… )로 이동' 문구 · coin+"원" 이중 표기 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · ChatImageSheet 3분할 규칙 · UiChip 기본 스타일 등 — 기존 분류 유지.

## (d) 실제 적용한 수정
1. `views/orders/OrderCreateView.vue` — 캘린더 월 이동 아이콘 버튼 2건에 `aria-label` 추가: 이전 달(167행)·다음 달(169행). `calTitle`(YYYY년 M월)이 중앙에 표시되므로 이동 대상(달)을 명시한 라벨로 보완.
2. `views/orders/RideHistoryView.vue` — 동일 캘린더 패턴 2건에 `aria-label="이전 달"/"다음 달"` 추가(405·407행).
3. `components/chat/ChatRequestSheet.vue` — 시트 헤더 뒤로(174행, `aria-label="뒤로가기"`)·닫기(180행, `aria-label="닫기"`) 추가. 형제 시트 ChatImageSheet `img-sheet__close`("닫기")·CommunityPostView `post-head__back`("뒤로가기") 규약과 일치.
4. `views/SettlementView.vue` — `settle-head__refresh`에 `aria-label="새로고침"` 추가(120행). 기존 `title` 유지(마우스 툴팁).
5. `views/orders/ActionCenterView.vue` — `actions-head__refresh`에 `aria-label="새로고침"` 추가(263행). 기존 `title` 유지.
6. `views/AdminView.vue` — 자동 등록 이력 삭제 버튼 `auto-history__del`에 `aria-label="삭제"` 추가(1598행). 기존 `title` 유지.

- 공통: 모두 템플릿 속성 추가만 수행. 동일 파일 복수 수정은 단일 연속 블록(Search/Replace 1회)으로 처리해 중간 원복 위험 제거. 다른 변경 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **타 세션 활발 편집 파일(OrderCard.vue·OrderDetailView.vue·ScoreStars.vue)**: 22:56~23:00 연속 편집 관찰(찜 기능 마무리 추정)로 병행 충돌 위험이 커 이번 실행 수정 대상에서 제외. 해당 파일의 아이콘 버튼(`order-card__fav`·`detail-hero__fav`·`chat-sheet__close`)은 이미 `aria-label` 보유로 결함도 없음.
2. **QuickMatchPanel `.qmp-quick` scoped/전역 클래스 충돌(구조적, 기존 기록 유지)**: 파일이 타 세션 작업 중이고 실제 렌더 간격 확인이 필요해 보류. 최소 수정안은 모달 루트 클래스 분리 + 전역 선택자 교체이며 파일 안정화 후 재검토 가능.
3. **NotificationsView '처리할 일(운행 승인 · 제안 · 요청)로 이동' 문구(295행)**: 닫는 괄호 뒤 조사 '로' 비표준 문제는 문맥상 재문안(예: 괄호 내용 제거 또는 '…에서 이동') 판단이 필요해 후보 유지.
4. **MyMarketView FAB 전각 '＋' 글리프**: 커뮤니티 FAB와 동일 글리프·동일 CSS로 의도된 일관성으로 판단, 보류.
5. **coin 아이콘 + '원' 이중 표기(HomeView·OrderCard·SetGroupCard·SettlementView)**: 금색 동전 아이콘(SVG '₩' 각인) + 옆 '원' 표기는 의도된 표현으로 보여 표시 판단 보류(기존 기록 유지).
6. **기존 반복 후보(22:01·21:02·20:01 기록분 유지)**: '정산 대기중' 백엔드 캐노니컬 · 승인 대기 3중 반복+Primary 2개 · hero/브랜드 위 글자색 전역 설계 · ChatImageSheet 3분할 규칙 · UiChip 기본 스타일 · HomeView·ChatList 칩 CSS 중복 · DashboardView `.settle-item` 상충 재사용 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 937ms, 4369 modules) — 수정 6개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 전 수정이 템플릿 `aria-label` 속성 추가(HTML 속성, import·심볼 영향 0건) → `npm run build`(템플릿 컴파일 포함) 통과로 검증 대체.
- 수정 후 그렙 재확인: 적용 8건 전부 잔존(OrderCreateView 167·169 / RideHistoryView 405·407 / ChatRequestSheet 174·180 / SettlementView 120 / ActionCenterView 263 / AdminView 1598). `title=` 보유 버튼 중 `aria-label` 미보유 잔존 0건(전수 재스캔).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분(6파일)과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
