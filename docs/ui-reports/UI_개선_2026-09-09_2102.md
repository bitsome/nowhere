# UI 개선 보고서 — 2026-09-09 21:02

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 직전 실행(20:01) 이후 `frontend/src`에 타 세션 변경이 있었음(HomeView 20:20, MarketView·QuickMatchPanel 20:34, OrderDetailView 21:05~21:06 실시간 편집 관찰). 직전 보고서 적용 내역·후보와 겹치지 않는 신규·명확 항목만 직접 수정했다.
- 신규 적용 1유형: ① OrderDetailView `.detail-driver-only` 다크 모드 테두리 결함 — 경계선이 `rgba(0,0,0,0.12)` 하드코딩이라 다크에서 사실상 보이지 않음 → 테마 토큰 `var(--border)`로 교체.
- 근거: 같은 파일의 형제 요소 `.detail-map__placeholder`(2336행)는 이미 `dashed var(--border)` + `html.dark` 배경 보정(2341행)을 갖춘 표준이고, `.detail-claim-item__vehicle`(1637·1642행)도 `html.dark` 보정이 있어 `.detail-driver-only`만 다크 대응이 빠진 아웃라이어임을 확인.
- CSS 선택자 중복·데드 CSS 전수 스캔(node 스크립트) 결과, 추가 적용 가능한 신규 결함은 발견하지 못함(후보 상당수는 동적 `:class` 문자열 바인딩에 의한 스캐너 오탐으로 판별).
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 21:02~ (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_2001.md`·1902 — 적용 내역·후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status`(HEAD c0c4a71, 전부 미커밋) + mtime 검사 — 20:01 보고서 이후 HomeView.vue(20:20)·MarketView.vue(20:34)·QuickMatchPanel.vue(20:34) 타 세션 갱신 확인. **OrderDetailView.vue는 실행 중(21:05~21:06)에도 외부 세션이 편집 중임을 관찰**(판독 시 1500행이던 블록이 1531행으로 이동) → 병행 충돌 방지 위해 해당 파일은 단일 소규모 수정 후 추가 편집 중단.
- **CSS 동일 선택자 중복 전수 스캔(node, `frontend/src` 전체 .vue)**: ChatImageSheet(`.img-sheet__archive-add`·`--delete-selected`), QuickMatchPanel(`.qmp-quick`), DashboardView(`.settle-item` 계열), HomeView(`.home-hero`·`.home-tabs`) — 각각 직접 판독해 판정(아래 (c)).
- **데드 CSS 휴리스틱 스캔(node)**: 후보 다수 산출됐으나 대표 오탐 검증(ChatRequestEvent `rq-card__label--approval` 등 → `:class="\`rq-card__label--${type}\`"` 동적 바인딩 사용처 확인)으로 스캐너 신뢰성 부족 판명 → 개별 후보만 정밀 판독.
- **직접 판독**: OrderDetailView.vue(976·1490~1540·1605~1645·2310~2345행), QuickMatchPanel.vue(542·626·900~1180행), ChatImageSheet.vue(995~1027행), HomeView.vue(906~975·1270~1345행), ChatRequestEvent.vue(전량 232행), SegmentedGroup.vue(전량), UiChip.vue(전량), NotificationsView.vue(280~310·580~700행), ChatThread.vue(232~272행), LevelBadge.vue·VerifiedBadge.vue(전량).
- **전역 그렙**: `오더/주문번호/order_id`(사용자 노출 0건 — 식별자는 전부 코드 파라미터) · `rgba(0,0,0,` 저알파 테두리·배경 전수 · `1px dashed` 용례.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피·배지 표준, 메모리 규칙(한국어 UI / "운행" 용어).

## (c) 발견한 개선 포인트
1. **OrderDetailView `.detail-driver-only` 다크 테두리 결함(명백)**: [OrderDetailView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/orders/OrderDetailView.vue#L1531-L1538) 경계선이 `1px dashed rgba(0,0,0,0.12)` — 다크 모드에서 배경(color-mix로 이미 테마 대응) 위에 검정 12%는 거의 보이지 않음. 같은 파일 형제 요소는 `dashed var(--border)` + `html.dark` 보정으로 일관 처리 중(2336·2341행). `html.dark` 규칙 부재 확인 → **적용**.
2. **QuickMatchPanel `.qmp-quick` scoped/전역 클래스 충돌(신규, 구조적)**: scoped 스타일(907행, 패널 '원큐 설정' 바용)과 비-scoped 전역 스타일(1044행, 텔레포트 조건 모달용)이 같은 클래스명 `.qmp-quick`을 공유. 전역 규칙(`display:flex; flex-direction:column; gap:18px`)은 모달뿐 아니라 패널 내 원큐 바(542행)에도 적용되어 의도 간격보다 벌어질 가능성 있음. 파일이 타 세션 작업 중(20:34 갱신·미커밋)이고 표시 확인이 필요해 수정하지 않고 후보로 기록(최소 수정안: 모달 루트만 `qmp-modal` 등 별도 클래스로 분리).
3. **동일 선택자 복수 정의(검토 후 보류)**: ChatImageSheet `.img-sheet__archive-add`(1000·1014·1017행)·`--delete-selected`(1001·1015·1021행)는 3분할 부분 규칙(공통→비율→색상)으로 기능상 정상 분리(20:01 보고서 기록과 동일 패턴, add에 확장됐을 뿐). DashboardView `.settle-item` 계열(210/363·220/369·226/374행)은 의도된 재사용(20:01 기록 유지). HomeView `.home-hero`(914 order vs 963 본체)·`.home-tabs`(917 order vs 전역 1287)는 목적이 다른 보완 규칙 — 결함 아님.
4. 신규 표준 위반(기술 식별자 노출·용어·보조용언·소형 폰트) 0건 — 직전 보고서 상태 유지. '정산 대기중'은 백엔드 캐노니컬로 유지.
5. **병행 편집 관찰(위험성 기록)**: OrderDetailView.vue가 본 실행 도중 36행가량 외부 세션에 삽입됨(수정 블록 1500→1536행 이동). 적용분은 최종 그렙(1531·1538행)으로 잔존 확인했으나, 해당 세션의 후속 저장이 덮어쓸 수 있어 다음 실행에서 재확인 필요.

## (d) 실제 적용한 수정
1. `views/orders/OrderDetailView.vue` — `.detail-driver-only`(관람자 안내) `border: 1px dashed rgba(0, 0, 0, 0.12)` → `border: 1px dashed var(--border)`(1538행). 라이트 모드 표시 근사(불변), 다크 모드 테두리 가시성 복구. 다른 변경 없음.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **QuickMatchPanel `.qmp-quick` scoped/전역 충돌(신규)**: 파일이 타 세션 20:34 미커밋 작업 중이고 실제 렌더 간격 확인이 필요해 후보로 기록. 최소 수정안은 모달 루트 클래스명 분리 1곳 + 전역 선택자 1곳 교체뿐이라 다음 실행에서 파일 안정화 후 재검토 가능.
2. **기존 반복 후보(20:01·19:02·18:09 기록분 유지, 상태 재확인)**: DashboardView `.settle-item` 상충 재사용 · ChatImageSheet 3분할 규칙 · '정산 대기중' 백엔드 캐노니컬 일괄 교정 · 승인 대기 상태 3중 반복+Primary 2개 · hero/브랜드 위 글자색 전역 설계(MyMarketView FAB 포함) · OrderCreateView 데드 CSS(.create-search·.filter-*) · NotificationsView '처리할 일(… )로 이동' 문구 · ChatThread 아이콘 버튼 aria-label(현재는 title만 보유) · coin+"원" 이중 표기 · UiChip 기본 스타일 · HomeView·ChatList 칩 CSS 중복 · 의존명사·보조용언 등 UX 카피(신규 0건) — 기존 분류 유지.
- 참고: HomeView·MarketView·QuickMatchPanel·OrderDetailView의 타 세션 변경분(미커밋)은 작업 중으로 보존, UI 보고서 범위에서 제외.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 1.01s) — 수정 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 이번 수정은 CSS 선언값 교체 1건(문자열·심볼·import 영향 0건) → `npm run build`(템플릿·CSS 컴파일 포함) 통과로 검증 대체. 수정 후 그렙 재확인: `rgba(0, 0, 0, 0.12)` dashed 테두리 잔존 0건, `dashed var(--border)` 토큰 사용 2건(1538·2336행)으로 통일.
- 임시 스캔 스크립트(`_cssdup_scan.cjs`·`_cssdead_scan.cjs`)는 사용 후 삭제 완료.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분(1파일)과 기존 미커밋분은 워킹트리에 남김(사용자 커밋 대기).
