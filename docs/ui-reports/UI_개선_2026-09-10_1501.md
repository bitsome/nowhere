# UI 개선 보고서 — 2026-09-10 15:01

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 이번 회차의 신규 편집 대상은 **비로그인 공개 랜딩 화면**([LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue), `/welcome`, 14:56 생성·미커밋)이다. 이 화면의 Primary CTA 2곳이 `background: var(--accent)` + `color: #ffffff`를 쓰는데, 다크 모드에서 `--accent`가 밝은 틸(#63e2b7)로 바뀌어 **흰 글자 대비가 ≈1.6:1**로 떨어진다(AA 4.5:1 미달). 같은 앱이 이미 쓰는 국소 예외 규약(`html.dark { color: #07120e }`)을 그대로 적용했다.
- 직전 실행(14:07) 이후 `frontend/src` 신규 편집은 **3건**뿐이다 — `App.vue`·`router/index.js`(14:56, `/welcome` 라우트 배선) + 신규 파일 `LandingView.vue`(14:56). 세 건 모두 역대 보고서에서 검토된 적이 없는 신규 산출물이라 이번 회차의 검토 초점으로 삼았다.
- 전역 표준 스캔(용어·10px 미만 폰트·보조용언 띄어쓰기·배지 규격·`--accent` 채움·아이콘 버튼 `aria-label`) 신규 위반 0건. 신규 발견 위반은 위 1계열뿐.
- `npm run build` 통과(✓ built in 1.04s, 4373 modules). `<script>`·import·로직 변경 0건(스코프 CSS 1블록 추가).
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-10 15:01~15:12 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-10_1407.md`(적용 5건: 등록자 홈 CTA 다크 예외·상태 칩 2곳 규격·`aria-expanded` 5곳) · `2026-09-10_1301.md`(적용 4건: 문의 placeholder·리뷰 단위 3곳) — 적용 내역·미적용 후보 전수 대조, 반복 적용 없음.
- **실행 전 상태 점검**: `git status --short`/`git log --oneline -3`(HEAD `f240340`, 전부 미커밋) + `frontend/src` mtime 상위 25건 → 14:07 이후 편집은 `App.vue`(14:56:39)·`router/index.js`(14:56:25)·`LandingView.vue`(14:56:03) 3건, 그 외 0건(안정 상태).
- **신규 전량 판독**: `views/LandingView.vue`(537행 — 히어로·추천 카드 미리보기·흐름 5단계·왜 다른가·등록자 섹션·푸터 + 전량 스코프 CSS), `router/index.js`(`welcome` 라우트 신설 + `requiresAuth` 미인증 시 `login` → `welcome` 리다이렉트 가드 diff), `App.vue`(`HeaderBar`/`app-content--full` 제외 목록에 `welcome` 추가 diff + 하단 내비 노출 조건 확인), `assets/base.css`(토큰 1~70행 — `--accent` 라이트 #1f1f1f / 다크 #63e2b7, `--brand-gradient` 다크 틸 대조).
- **대조 판독**: `views/ReviewsView.vue`(264~273행 — `--accent` 활성 탭의 `html.dark` 예외 규약) · `views/home/CustomerHome.vue`(88~104행 — 14:07 적용분) · `views/HomeView.vue`(1067~1082행 — 히어로 CTA `html.dark .home-hero__cta { background: var(--brand); color: #07120e }`), `stores/theme.js`(라이트=기본, 다크=localStorage 옵트인 확인), `components/orders/OrderCard.vue`(140~160·318~325행 — 앱의 조건 점수는 `ScoreStars` 별점 + 근거 모달, "조건 96%" 배지 미사용 확인).
- **전역 그렙**: `오더|주문번호|order_no`(0건) · `font-size: [5-9]px`(0건) · `background: var(--accent);`(4곳 전수 — base.css `.chat-list__avatar`·`.btn--primary`, ReviewsView 활성 탭·아바타, CustomerHome CTA, LandingView 2곳) · `padding: (1px [578]px|2px 8px|2px 10px|3px 9px);`(11건/6파일 — AdminView 6·HomeView 1·CommunityView 1·ReviewsView 1·OrderCard 1·LandingView 1, 전부 기존 분류 또는 마케팅 화면) · `matchScore|match_reasons`(OrderCard — 앱은 별점 표기).
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·추천 카드 노출 규격), 프로젝트 배지·색상 표준(1px 6px / 10px / 400), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 비노출 / 다크 모드 대비).

## (c) 발견한 개선 포인트
1. **공개 랜딩 Primary CTA의 다크 모드 저대비(신규 발견)**: [LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L274-L279) `.lp-btn--primary`("기사로 무료 시작하기")와 [LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L390-L399) `.lp-card__action`("운행 확인하고 신청하기" — 추천 카드 미리보기)이 `background: var(--accent)` + `color: #ffffff`다. 라이트(`--accent`=#1f1f1f)는 정상이나 **다크에서 `--accent`가 #63e2b7로 바뀌어 흰 글자 대비 ≈1.6:1**로 떨어진다. 게다가 랜딩은 `HeaderBar`가 숨겨지는 공개 화면이라 다크 전환이 가능한 유일한 경로가 이전 방문 시 localStorage 설정뿐이어도, **홍보 링크로 처음 들어온 사람이 보는 첫 화면**이라는 점에서 결함 노출 가치가 크다. 같은 앱은 이 문제를 국소 예외로 이미 해결하고 있다 — [ReviewsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/ReviewsView.vue#L264-L273)(활성 탭)·[home/CustomerHome.vue](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L88-L104)(14:07 적용)·[HomeView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L1079-L1082)(히어로 CTA)가 모두 `html.dark { color: #07120e }`. **같은 규약을 적용.**
2. **랜딩 라우트 배선 자체는 결함 없음**: 비로그인 방문자는 `requiresAuth` 화면 진입 시 `login` 대신 `welcome`으로 보내지고, `welcome`에서 토큰이 있으면 `market`으로 되돌아간다([router/index.js](file:///d:/wwwroot/nowhere/frontend/src/router/index.js#L87-L97)). 랜딩에서는 `HeaderBar` 미노출([App.vue](file:///d:/wwwroot/nowhere/frontend/src/App.vue#L276-L279)) + 하단 내비는 `auth.isAuthenticated` 조건이라 노출되지 않는다([App.vue](file:///d:/wwwroot/nowhere/frontend/src/App.vue#L311)) — 공개 화면 크롬 구성 정상. 마운트/언마운트 시 `overflow` 잠금 해제 처리도 들어가 있음([LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L21-L30)). 수정 없음.
3. **랜딩 화면의 용어·구조 표준 준수**: "운행" 용어만 사용("오더" 0건), 기술 식별자(운행/주문 번호) 노출 0건, 제목 계층 `h1`(히어로) → `h2`(섹션) → `h3`(이유 카드) 1회씩 정상, 장식 SVG에 `aria-hidden="true"` 부여, `.lp-btn--outline` 호버 시 `border-color: var(--text)` 등 토큰만 사용. **수정 없음.**
4. **표준 스캔 신규 위반 0건**: 10px 미만 폰트 0건, "오더"/주문번호 0건, 보조용언 붙여쓰기 0건, 아이콘 전용 버튼 `aria-label` 누락 0건(직전 회차 보완분 유지). `--accent` 채움 4계열 중 base.css `.chat-list__avatar`·`.btn--primary`와 ReviewsView 아바타는 기존 분류(전역 `--on-accent` 토큰 설계 선행 필요) 그대로 유지.
5. **기존 반복 후보(상태 재확인, 유지)**: FAB 흰 글리프 3곳 · `--accent` 아바타 흰 글자 · EmptyState 아이콘 변형색 하드코딩 · `qmp-quick` scoped/전역 충돌 · DashboardView `.settle-item` 상충 재사용 · '정산 대기중' 백엔드 캐노니컬 · `data/levels.js` "드라이버" 타이틀 · MyMarketView FAB '＋' 글리프 · HomeView `.home-more` 공통화 · `formatWon` 명칭 혼재 · OrderFavoritesView eyebrow `--danger` · coin+`원` 이중 표기 · 관리자 화면 상태 칩 규격 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · HeaderBar 라우트 표 데드 엔트리 · 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종 혼재 · 말줄임표/모달 푸터 문구 혼재 — 기존 분류 유지.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/LandingView.vue` | `.lp-demo__caption` 뒤에 다크 전용 예외 블록 1개 추가 — `html.dark .lp-btn--primary, html.dark .lp-card__action { color: #07120e }`(+사유 주석). 라이트는 기존 흰 글자 유지(어두운 채움), 다크만 어두운 글자로 전환. 랜딩 Primary CTA·추천 카드 액션의 다크 대비 1.6:1 → 10:1 수준 |

- 공통: `<script>`·props·emit·import·DOM 구조 무변경(스코프 CSS 규칙 1블록 추가, 5행). 선택자 특이도(`html.dark .lp-*` = 0,2,1)가 기존 단일 클래스 규칙보다 높아 **CSS 선언 순서와 무관하게 적용**되며, 스코프드라 타 화면 파급 0건. 단일 파일·단일 편집이라 원복 위험 없음(그렙으로 지속성 확인).
- 수정 후 확인: `html.dark .lp-btn--primary`/`html.dark .lp-card__action` 2건 잔존([LandingView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/LandingView.vue#L408-L413)), `color: #ffffff` 3건은 `.lp-mark`(로고 그라디언트)·`.lp-btn--primary`·`.lp-card__action` 라이트 기본값으로 유지 — 의도한 상태.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **랜딩 `.lp-score`("조건 96%") 배지 규격(`padding: 2px 8px` / 11px / 700)**: 프로젝트 배지 컨벤션(1px 6px / 10px / 400)과 다르다. 다만 이 칩은 앱 상태 배지가 아니라 **마케팅 페이지의 추천 카드 미리보기 라벨**이고, 같은 마케팅 화면군인 [LoginView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/auth/LoginView.vue)(13:01 전량 검토, 배지 규격 미적용 유지)와 동일 취급이 일관적이다. 랜딩 전체가 40px 히어로·13~17px 본문으로 확대된 타이포 스케일이라 11px/700이 의도된 시각 결정일 수 있어 후보로만 기록. (참고: 실제 앱 카드의 조건 점수는 [OrderCard.vue](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L318-L325)의 `ScoreStars` 별점 + 근거 모달로 노출되며 "조건 %" 배지는 쓰지 않는다 — 랜딩은 `docs/PRODUCT_VISION.md`의 카드 규격을 그대로 따른 목업.)
2. **랜딩 `.lp-mark`/`lp-mark--sm` 로고 마크의 `--brand-gradient` + 흰 글자**: 다크에서 그라디언트가 틸(#63e2b7→#3ca97e)로 바뀌어 "N" 글자 대비가 낮다. 1번과 원인이 같지만, 13:01 보고서에서 "로그인 마크 그라디언트"로 이미 후보 분류된 **동일 계열(마크·그라디언트)** 이며, 아바타·그라디언트 채움 전체를 건드리면 라이트 대비가 역전될 수 있어 전역 `--on-accent` 토큰 설계가 선행돼야 한다. 이번에도 후보 유지(CTA 2곳만 국소 예외).
3. **랜딩 `.lp-eyebrow`의 `text-transform: uppercase`**: 문구가 순수 한글("운행 최적화 플랫폼")이라 대문자 변환이 시각적으로 무효다. 렌더 결과 차이가 없어 UI 개선이 아닌 정리 작업으로 후보.
4. **랜딩 `.lp-card__action`의 버튼형 `div`**: 추천 카드 목업 안의 "운행 확인하고 신청하기"가 실제 버튼처럼 보이지만 클릭 동작이 없는 정적 요소다. 아래 캡션("추천 카드에는 조건 일치율과 근거가 함께 표시됩니다")으로 목업임을 밝히고 있어, 실제 링크로 바꾸면 "한 화면 Primary 버튼 하나" 원칙과 충돌한다. 구조 판단이 필요해 후보.
5. **관리자 화면 상태 칩 6곳(AdminView `.report-card__cat`/`__status`·`.op-badge`·`.op-order__status`·`.v-chip`·`.support-ticket-chip` — 1px 8px·2px 8px / 700)**: 14:07에 기사 화면 2곳을 통일했으나 관리자 화면은 정보 밀도가 높은 운영 도구로 굵은 칩이 의도된 구분일 수 있고, 역대 회차에서 "관리자 화면 배지 잔여분"으로 분류돼 왔다. 기사 앱 화면 우선 원칙에 따라 후보 유지.
6. **`assets/base.css`의 레거시 `.card`·`.btn`·`.btn--primary`·`.btn--ghost`·`.btn--danger`·`.btn--block`(465~534행)**: `class="btn…"`/`class="card"` 사용처 0건으로 확정된 죽은 CSS. 제거는 UI 개선이 아닌 정리 작업이고 동적 클래스 결합 가능성을 완전히 배제할 수 없어 후보 유지.
7. **HeaderBar 라우트 표 정리**: 신규 `welcome`은 `HeaderBar` 제외 목록에 정상 반영됐으나, `showBack` 제외 목록의 `'my-orders'`·`PAGE_LABEL`의 `'match'`는 라우터에 없는 데드 이름이고 `actions`·`settlement`·`registrant-settlement`·`support`·`settings*`·`community-post`는 헤더 라벨 미등록(각 화면 자체 page-head 보유). 노출 결함은 없고 라우터-헤더 계약 정리 건이라 후보.
8. **기존 반복 후보(14:07·13:01 기록분 유지)**: 하위 화면 뒤로가기 중복 · 앰버 팔레트 3종(`#f0a800` vs `--warn` #ffa940 vs `--status-acceptance-pending` #c28a2e) · 말줄임표 ASCII/`…` 혼재 · SettingsVehiclesView 로딩 중 빈 카드(스켈레톤 부재) · 모달 푸터 '닫기'/'취소' 혼재 · AdminView 금액 인라인 포맷 · FAB 3곳 흰 글리프 · HeaderBar `html.dark .hb-btn` 중복 예외 · HomeView `.home-more` 공통화 · SettlementView 계좌 마크업 중복 · `new Date(created_at_iso)` 방어 — 기존 분류 유지.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ built in 1.04s`, 4373 modules, 오류·경고 0건) — 수정 반영 후 실행. 신규 `LandingView` 청크(`LandingView-DeubbLg-.css` 5.94 kB / `LandingView-C9Iy796g.js` 4.76 kB) 정상 생성.
- import 누락 점검: 변경이 스코프 `<style>` 규칙 1블록뿐이라 import·심볼·전역 CSS 영향 0건 → 빌드 통과로 검증 대체. `git status`로 변경 범위가 `LandingView.vue`(신규 파일 내 1블록)임을 확인(그 외 변경은 타 세션 랜딩 배선분·직전 14:07 적용분).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
