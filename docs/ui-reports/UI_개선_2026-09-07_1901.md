# UI 개선 보고서 — 2026-09-07 19:01

## 실행 결과 요약
- **변경 없음(수정 코드 0건)** — 직전 실행(68회차, 09-07 18:01) 보고서 작성 직후 18:03~18:04에 **frontend/src 7개 파일이 변경**되어(역할 기반 가입 기능) 해당 변경분을 우선 검토. 변경 내용의 UI 정합성(RegisterView 가입 유형 선택 UI 등)을 확인했으나 **수정할 명확·소형 UI 결함은 발견되지 않음**. 전역 위반 재스캔(기술 식별자 · '오더/드라이버' 잔존 라벨 · 7~9px 소형 폰트)도 신규 위반 0건 — 기존 반복 후보 상태 변화 없음. 18:01 이후 빌드가 변경 코드를 반영하지 않았으므로 이번 실행에서 재빌드 검증 완료.

## (a) 실행 시각
- 2026-09-07 19:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(18:01 이후 수정분, 7건)**: `frontend/src/data/roles.js`(18:03) · `frontend/src/stores/auth.js`(18:03) · `frontend/src/router/index.js`(18:03) · `frontend/src/views/MoreView.vue`(18:03) · `frontend/src/composables/useProfileSettings.js`(18:04) · `frontend/src/api/auth.js`(18:04) · `frontend/src/views/auth/RegisterView.vue`(18:04) — 전수 읽기 검토
- **변경 성격**: 기사/등록자 역할 선택 가입 기능 — `apiRegister` 역할 파라미터, `auth.register(…, role)`, `ROLE_CUSTOMER` 정의·라벨(등록자) 신설, RegisterView '가입 유형' 2분할 선택 UI 추가
- **전역 위반 감사(grep)**: `frontend/src` 재스캔 —
  - 기술 식별자 템플릿 노출(`주문번호|오더 번호|주문 번호|NO.|#숫자`): **0건**(grep 적중분은 CSS hex 컬러·코드 주석 false positive, SupportView.vue 189행 placeholder 문구는 기존 후보)
  - `오더` 한글 노출: **0건**
  - `드라이버` 템플릿 문구: 0건 — 잔존 17건은 코드 주석·levels.js 레벨 타이틀(기존 후보)이며 화면 노출 아님
  - `font-size: 7~9px`: **0건**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-07_1801.md`(68회차) — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 배지·상태 스펙 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
18:01 이후 신규 변경분(7건) 검토 결과 **수정할 명확·소형 UI 결함 없음**. 확인된 정합 사항:
1. **RegisterView 가입 유형 UI**: 2분할 선택 카드(기사/등록자) — 활성만 brand 테두리+soft 배경, Primary 버튼 '가입하기' 단일, 화면당 스크롤 잠금·카드 내 스크롤은 LoginView와 동일 규격. 기사 기본 선택, 등록자 선택 시 `my-market`, 기사는 `home`으로 분기 — 라벨·설명 모두 한국어·'운행' 용어 준수.
2. **역할 라벨 단일 소스**: `data/roles.js` ROLE_LABELS(등록자)를 RegisterView·MoreView·useProfileSettings가 공용 사용 — 역할 한글 라벨 하드코딩 없음.
3. **용어**: 변경분 UI '드라이버' 문구 0건 — 잔존은 코드 주석·레벨 타이틀뿐.
4. **기술 식별자**: 신규 노출 없음(등록 화면·마이페이지 확인).
5. **반복 후보 상태 변화 없음**(68회차 기록분 그대로, 이번 변경과 무관): 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색 · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버" · 홈 추천 조건% 미노출 · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · 커뮤니티 카테고리 색 · 문의 placeholder 문구(SupportView 189행) 등.

## (d) 실제 적용한 수정
- **없음(변경 없음)** — 18:01 이후 변경분은 기능 작업(역할 선택 가입)이며 UI 규칙 위반 없음. 반복·임의 수정 금지 방침에 따라 기존 후보·신규 관찰 모두 재적용·임의 수정 안 함.

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(68회차 기록분 유지)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView 189행 '(운행 번호·날짜 포함 시…)' — 사용자가 운행 번호를 알 수 없어 안내 실효성 문제, 기존 분류) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계 · App.vue 데드 CSS · ChatView 미사용 `ref` import — 기존 분류 유지(렌더링 영향 없거나 대형·디자인 결정 수반).
- **신규 관찰(이번 실행 기록, 미적용)**: ① 등록자(Customer) 가입 직후 첫 화면이 `my-market` 직행인 반면, 이후 로그인 홈(`/`)은 CustomerHome(등록자용 홈)으로 분기됨 — 첫 화면 정책 정합성은 제품·기획 판단이라 미적용. ② `role === 'Driver'` 리터럴 비교 11곳(App.vue·HomeView·MarketView·MoreView·ProfileView·SettingsView·SettingsVehiclesView·QuickMatchPanel·useDashboardStats·useOrderDetail 등) — `data/roles.js` ROLE_DRIVER 상수 단일 소스 원칙상 상수 치환이 옳으나 전역 일괄 변경(11개 파일)이라 소형 수정 범위 초과, 후보 기록. ③ RegisterView '등록자' 설명 "운행을 등록하고 기사를 관리하는 사업자" — 문구 확인 필요(기사 '관리' 표현이 과함), 제품 문구 결정이라 후보.

## (f) 빌드 결과
- `npm run build`(frontend, vite) — **통과**, `✓ built in 1.26s`, 에러 0건. 18:01 이후 변경 코드(역할 상수·RegisterView·auth store) 포함 전체 모듈 변환 정상(4364 modules), 주요 청크 정상 생성(ChatView 68.74 kB · AdminView 55.10 kB · OrderDetailView 53.63 kB).
- import 누락 점검: 신규 import(`ROLE_CUSTOMER`/`ROLE_DRIVER` 등 data/roles.js 상수) 전부 정상 해석 — 누락 없음. 이번 실행 수정 0건이라 별도 점검 대상 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
