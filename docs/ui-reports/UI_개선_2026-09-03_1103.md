# UI 개선 보고서 — 2026-09-03 11:03

## (a) 실행 시각
- 2026-09-03 11:03 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰: `views/ProfileView.vue`(레벨 프로그레스 바·xp-events 대조), `views/DashboardView.vue`(bar/settle 트랙 dark 쌍 대조 — `.status-list__bar`·`.settle-bar`·`.mini-list__empty`), `views/orders/OrderDetailView.vue`(신청자 카드 푸터 구분선·차량 정보 박스·지도 placeholder dark 쌍 대조), `views/community/UserPageView.vue`(`.user-tabs`·`.post-row__image` dark 쌍 대조), `views/community/CommunityView.vue`·`CommunityPostView.vue`(hover·composer·이미지 dark 쌍 대조), `views/orders/RideHistoryView.vue`(섀도우), `views/auth/LoginView.vue`·`RegisterView.vue`(전체 재검토), `views/NotFoundView.vue`, `views/settings/*`(부분 재확인)
- 컴포넌트: `components/orders/SetGroupCard.vue`(`.set-card__route-dot` dark 쌍 대조), `components/layout/HeaderBar.vue`·`ChatListener.vue`·`NotificationListener.vue`, `components/common/ConfirmDialog.vue`·`SegmentedGroup.vue`·`ScrollTopButton.vue`·`ImageGallery.vue`·`LevelBadge.vue`·`VerifiedBadge.vue`·`BaseIcon.vue`, `components/ui/*`(재확인)
- CSS: `assets/base.css`(전역 토큰·타이포 스케일 — body 12px 기준으로 라벨 10~11px이 의도적 소형 스케일임을 확인)
- 전역 감사(grep): 전체 views/components 대상 `background/border-top: rgba(0,0,0,…)` **light-only 틴트 전수** — dark 쌍 누락 여부 1건 확인 방식으로 대조, 잔존 "드라이버"/"오더" UI 노출 0건(코드 주석·`data/levels.js` 동기 데이터만), mojibake 주석 잔존 0건
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_0101.md` 및 그 이전 보고서 10건(적용 내역 전수 확인 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **다크 모드 프로그레스 트랙 소실 — ProfileView `.level-bar` (신규 발견)**: 레벨 XP 프로그레스 바 트랙이 `rgba(0,0,0,0.06)` 단일 선언 → 다크 모드(표면 #1c1c22)에서 트랙이 거의 안 보임. 동일 역할 요소(DashboardView `.status-list__bar`·`.settle-bar`는 white 0.1, `.mini-list__empty`는 white 0.04)와 같은 파일의 `.xp-events`(white 0.03)는 모두 dark 쌍을 보유하는데 `.level-bar`만 누락된 잔존 건. 지난 01:01 회차의 "18건 전수"는 파일별 특정 요소만 봤고, 이번 전수에서 이 1건이 유일하게 누락임을 확인.
2. **다크 모드 구분선 소실 — OrderDetailView 신청자 카드 푸터 (신규 발견)**: `.detail-claim-card :deep(.n-card__footer)` `border-top: rgba(0,0,0,0.06)` 단일 선언 → 다크 모드에서 카드 본문과 푸터 액션 영역(승인/거절 버튼 행)의 구분선이 사라짐. 같은 카드 블록의 `.detail-claim-item__vehicle`·`.detail-map__placeholder`는 01:01 회차에 dark 쌍이 추가됐으나 푸터 구분선만 잔존.
3. **기타 재확인 결과 클린**: light-only `rgba(0,0,0,…)` 백그라운드/보더 전수(views 29곳·components 16곳) 결과 dark 쌍 누락은 위 2건뿐(나머지는 대응 쌍 보유 또는 미디어 오버레이·섀도우). `html.dark` 쌍 없이 hover만 있는 건 0건. 배지 스펙(1px 6px/10px/400) 잔존 위반 0건, mojibake 주석 0건, "드라이버" UI 노출 0건.
4. **Login/Register 확인**: primary 버튼(로그인/가입하기) 각 1개로 기준 준수. `.login-submit` 11px/600은 body 12px 스케일에서 전체 폭 CTA에 다소 작은 감이 있으나 naive-ui 기본 CTA보다 작게 만든 의도적 톤으로 보임 — 후보로만 기록.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/ProfileView.vue` | `.level-bar`에 dark 오버라이드 추가 — `html.dark .level-bar { background: rgba(255,255,255,0.1); }` (DashboardView `.status-list__bar`·`.settle-bar` dark 쌍과 동일 규칙·동일 높이 10px 트랙). 라이트 값·구조·동작 무변경 |
| `views/orders/OrderDetailView.vue` | `.detail-claim-card :deep(.n-card__footer)`에 dark 오버라이드 추가 — `html.dark ... { border-top-color: rgba(255,255,255,0.08); }` (같은 파일 박스 dark 틴트 매핑 0.04→0.06, 0.02→0.03 규칙의 구분선 연장). 라이트 값·구조·동작 무변경 |

전부 `<style scoped>` CSS 추가(라이트 값 무변경, 다크 전용 오버라이드) — 의미·레이아웃·상호작용 무변경. 다크 틴트 값은 같은 파일/코드베이스의 기존 dark 오버라이드 값 체계(0.06~0.1)를 따름.

## (e) 적용하지 않은 항목과 이유 (후보)
- **Login/Register `.login-submit`(44px full-width CTA) 11px/600**: 전체 폭 주동작의 텍스트가 다소 작아 보이나, naive-ui large input과 버튼 높이를 맞춘 디자인의 일관성·기기별 검증 필요 — 설계 검토 후 재평가(신규 후보).
- **ProfileView `.level-benefit`(3px 10px/11px/600)**: 레벨 혜택 나열 콘텐츠 칩 — 배지 스펙 대상 아님(기존 후보 유지).
- **HomeView 단일·셋트 탭 카운트 vs 표시 개수 불일치**: 표시 상한이 의도적 "선택지 축소"일 가능성 — 방향 확인 필요(기존 후보 유지).
- **ReviewsView·CommunityView 탭/카운트 `1px 7px`·공용 `UiChip`(4px 7px/11px/700)**: 콘텐츠 칩/탭 카운트로 배지 스펙 대상 아님(기존 후보 유지).
- **다크 모드 brand 위 흰 글자(`--on-brand`)·EmptyState 아이콘 5색·의미색 하드코딩(#e5484d 좋아요 등)·커뮤니티 카테고리 색**: 전역 색상 체계/디자인 결정 사항(기존 후보 유지, 반복 미적용).
- **data/levels.js 레벨 타이틀 "드라이버"**: 백엔드 `LevelTable.php`와 동기화된 값 — 교차 변경 필요(기존 후보 유지).
- **SettingsVerificationView "신청" primary 2개 중복**: 주동작 재설계 필요(기존 후보 유지).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 9.61s`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `html.dark .level-bar`(ProfileView)·`html.dark .detail-claim-card :deep(.n-card__footer)`(OrderDetailView) 존재 확인 2/2. views/components의 light-only `rgba(0,0,0,…)` 틴트 중 dark 쌍 누락 0건.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 오버라이드 2건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
