# UI 개선 보고서 — 2026-09-03 01:01

## (a) 실행 시각
- 2026-09-03 01:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰/컴포넌트(직접 읽기): `views/HomeView.vue`(추천 탭·히어로·빈 상태), `views/ChatView.vue`, `views/AdminView.vue`(사용자/기사 탭·빈 상태), `views/orders/OrderDetailView.vue`(신청자 카드·지도 placeholder·스타일 말단 영역), `views/community/CommunityView.vue`·`CommunityPostView.vue`(hover·입력·이미지 배경 dark 대응 대조), `views/MarketView.vue`(필터/검색 모달), `views/ProfileView.vue`·`views/DashboardView.vue`·`views/community/UserPageView.vue`(틴트 배경 대조), `components/common/EmptyState.vue`, `components/layout/HeaderBar.vue`, `components/orders/SetGroupCard.vue`, `assets/base.css`(전체 695줄, 토큰·공용 배지·버튼·스켈레톤·verify·채팅 공용)
- 전역 감사(grep): 배지-유사 padding(`1px 7px`·`2px 6px`·`4px 7px` 등), fallback hex(`var(--x, #…)`), `font-size 11px + weight 600/700` 전수, `type="primary"` 38건(파일별 다중 노출 여부), `color: var(--brand)` 155건(비클릭 노출 분류), UI 용어(드라이버/오더/주문번호/사용자/유저), 영문 placeholder, `text-transform/letter-spacing`, EmptyState 사용처 35건(title·hint 중복 여부), **`rgba(0,0,0,…)` 틴트·hover의 `html.dark` 쌍 보유 여부 18건 전수**
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_0001.md` 및 그 이전 보고서 9건(적용 내역 전수 확인 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **다크 모드 hover 피드백 누락 — 커뮤니티 2종 (신규 발견)**: `.feed-card__more:hover`(피드 카드 더보기)·`.feed-action:hover`(좋아요/댓글 등 액션)가 라이트 전용 `rgba(0,0,0,0.04~0.05)` 단일 선언이라 다크 모드(표면 #1c1c22)에서 hover가 거의 보이지 않음. 같은 파일의 composer input·comments 배경·이미지 배경·focus 등은 모두 `html.dark` 흰 틴트 쌍(0.03~0.08)이 이미 있어 **이 2종만 누락된 잔존 건**. CommunityView·CommunityPostView 두 파일 모두 동일.
2. **다크 모드 박스 구분 누락 — OrderDetailView 2종 (신규 발견)**: `.detail-claim-item__vehicle`(신청자 차량 정보 박스, 0.04)·`.detail-map__placeholder`(지도 미리보기 안내 박스, 0.02)가 라이트 검정 틴트 단일 선언 → 다크 모드에서 표면과 구분이 사라짐. 동일 파일 인접 요소(`.detail-pending-card` 등)와 타 파일(ProfileView `.xp-events`, DashboardView `.mini-list__empty`, UserPageView `.user-tabs`·`.post-row__image`)은 모두 dark 쌍이 있어 잔존 건으로 판단.
3. **AdminView EmptyState 힌트가 타이틀 반복 + 구두점 불일치 (신규 발견)**: 사용자 탭 "사용자가 없습니다 / 등록된 사용자가 없습니다."·기사 탭 "기사가 없습니다 / 등록된 기사가 없습니다." — 타이틀과 힌트가 같은 뜻을 두 줄로 반복. 타 화면 EmptyState(HomeView·ChatView·RideHistoryView 등)는 힌트에 보충 정보를 주고 마침표가 없는 것과 불일치.
4. **기타 재확인 결과 클린**: 낡은 fallback hex 0건, 미정의 CSS 토큰 0건, 배지 스펙(1px 6px/10px/400) 잔존 위반 0건(잔존 `1px 7px`·`4px 7px`·`2px 6px`은 탭 카운트·콘텐츠 칩(UiChip)·클릭 액션 버튼·평점 칩으로 기존 분류 유지), UI 노출 "드라이버"/"오더"/"주문번호"/"유저" 0건(주석·관리자 분류 용어 "사용자"만), 영문 placeholder 0건, `type="primary"` 38건은 모두 서로 다른 모달/다이얼로그/폼에 존재해 동시 노출 아님(설정 인증 화면 2건은 기존 후보), 18건의 `rgba(0,0,0,…)` 배경 중 dark 쌍 누락은 위 4건뿐(나머지는 대응 쌍 보유).
5. **HomeView 탭 카운트 vs 표시 개수 (관찰)**: 단일 탭 칩 카운트는 전체 추천 수인데 목록은 상위 5개만, 셋트는 3개만 표시 — "더 보기" 경로가 없어 수치와 실제 표시가 어긋날 수 있음(왕복 탭은 3개 한도로 카운트와 일치). 판단 피로도 감소 의도로 보이나 의도 확인 필요 — 후보로만 기록.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/community/CommunityView.vue` | `.feed-card__more:hover`·`.feed-action:hover`에 `html.dark` 흰 틴트 오버라이드 추가 (`rgba(255,255,255,0.06)`) 2건 — 다크 모드 hover 피드백 복원. 라이트 값·구조·동작 무변경 |
| `views/community/CommunityPostView.vue` | 동일 2건 추가 (`html.dark .feed-card__more:hover`·`.feed-action:hover`, `rgba(255,255,255,0.06)`) — 상세 화면도 같은 피드 카드 공유 |
| `views/orders/OrderDetailView.vue` | `.detail-claim-item__vehicle`에 dark 오버라이드(`rgba(255,255,255,0.06)`) 추가 — 신청자 차량 정보 박스 다크 대비 |
| `views/orders/OrderDetailView.vue` | `.detail-map__placeholder`에 dark 오버라이드(`rgba(255,255,255,0.03)`) 추가 — 지도 안내 박스 다크 대비 |
| `views/AdminView.vue` | 사용자 탭 EmptyState 힌트 "등록된 사용자가 없습니다." → "계정이 가입하면 이 목록에 표시됩니다" (타이틀 반복 해소 + 마침표 제거) |
| `views/AdminView.vue` | 기사 탭 EmptyState 힌트 "등록된 기사가 없습니다." → "기사 계정이 가입하면 이 목록에 표시됩니다" (동일) |

전부 `<style scoped>` CSS 추가(라이트 값 무변경, 다크 전용 오버라이드) 또는 EmptyState 힌트 문구 2건 — 의미·레이아웃·상호작용 무변경. 다크 틴트 값은 같은 파일/코드베이스의 기존 dark 오버라이드 값(0.03~0.08)과 동일 규칙을 따름.

## (e) 적용하지 않은 항목과 이유 (후보)
- **HomeView 단일·셋트 탭 카운트 vs 표시 개수 불일치**: 표시 상한(5·3)이 의도적 "선택지 축소"일 가능성 — 카운트를 표시 수로 맞출지, "더 보기" 경로를 둘지 방향 확인 필요(신규 후보).
- **ReviewsView·CommunityView 탭/카운트 `1px 7px`**: 활성 탭 위 흰 오버레이 대비 문제 — 탭 디자인과 함께 재검토(기존 후보 유지).
- **공용 `UiChip.vue`(4px 7px/11px/700)·`.detail-claim-item__rating`(4px 8px)·OrderDetailView `.detail-claim-item` 평점 칩**: 콘텐츠 칩/전용 요소로 배지 스펙 대상 아님(기존 후보 유지).
- **data/levels.js 레벨 타이틀 "드라이버"**: 백엔드 `LevelTable.php`와 동기화된 값 — 교차 변경 필요(기존 후보 유지).
- **의미색 하드코딩(#e5484d 좋아요·`#ffa940` 별점·`#f0a020` 평점·confetti 색 배열)**: 토큰화 시 다크 값과 달라 색 변화 수반 — 색상 체계 결정 후 일괄 적용(기존 후보 유지).
- **다크 모드 brand 위 흰 글자(`--on-brand`)·EmptyState 아이콘 5색·커뮤니티 카테고리 6색 grayscale 통일**: 전역 설계/디자인 결정 사항(기존 후보 유지, 반복 미적용).
- **AdminView `.auto-history__status` 라이트 흰 글자·SettingsVerificationView "신청" primary 2개**: 각각 색상 체계/주동작 재설계 필요(기존 후보 유지).
- **AdminView 분류 탭 "사용자"/"기사" 용어**: 관리자 운영 분류명으로 "기사" 통일 대상이 아닌 사용자 탭 명칭 — 운영 화면 설계 사항(후보).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 826ms`, 에러 0건.
- 수정 후 grep 검증: `html.dark .feed-card__more:hover`·`.feed-action:hover`가 커뮤니티 2파일 각 2건, `html.dark .detail-claim-item__vehicle`·`.detail-map__placeholder`가 OrderDetailView 각 1건 존재 확인(6/6). 커뮤니티 2파일에서 dark 쌍 누락 hover 0건, OrderDetailView 박스 틴트 dark 쌍 누락 0건.
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 추가 6건 + EmptyState 힌트 문구 2건뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
