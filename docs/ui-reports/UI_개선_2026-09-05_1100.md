# UI 개선 보고서 — 2026-09-05 11:00

## 실행 결과 요약
- **수정 0건 (변경 없음)** — 01:01(43회차) 이후 `frontend/src` 변경 7건(실측 mtime: api/auth.js·stores/auth.js·useProfileSettings.js·MoreView 10:54 · RegisterView 10:54 · HomeView 10:55 · CustomerHome 10:58)을 전수 재열람·캐너리 감사. 신규 UI 위반·명확한 소형 개선점 미발견으로 코드 수정 없음. 직전 회차(01:01) 적용분(RideHistoryView `countLabel`)은 파일(01:03)에 정상 반영·유지 확인.

## (a) 실행 시각
- 2026-09-05 11:00 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- mtime 스캔(`Get-ChildItem -Recurse`): 01:01 이후 `frontend/src` 변경 7건 — `api/auth.js`(10:54) · `stores/auth.js`(10:54) · `composables/useProfileSettings.js`(10:54) · `views/MoreView.vue`(10:54) · `views/auth/RegisterView.vue`(10:54) · `views/HomeView.vue`(10:55) · `views/home/CustomerHome.vue`(10:58) · (RideHistoryView 01:03은 직전 회차 수정분 반영)
- **직접 열람**: 위 7건 전부(script·템플릿·scoped CSS), `views/auth/LoginView.vue`(RegisterView와 형제 스타일 대조), `components/orders/OrderCard.vue`(추천 카드 필드/배지 재확인), `views/orders/RideHistoryView.vue`(`countLabel` L102-107·템플릿 L385 적용 확인)
- **전역 감사(grep)**: `font-size` 10px 미만 0건 / "주문번호|order_number|오더번호" UI 노출 0건 / 배지 스펙(padding 1px 6px 중 weight≠400) 위반 0건(멀티라인 매치는 전부 스펙 준수 블록의 오탐) / 템플릿 내 영문·"오더·드라이버" 노출 0건(매치는 주석·스크립트뿐) / `match_score|match_reasons` 프론트 사용처 0건(백엔드 생성분 미노출 — 아래 c-3)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-05_0101.md`(43회차)·`0002.md`(42회차) 등 — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·추천 카드 규격), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / 기술 식별자 노출 금지)

## (c) 발견한 개선 포인트
1. **10:54~10:58 변경분(인증·홈 7건) 클린**: stores/auth.js(`register`에 role 파라미터 추가)·api/auth.js(호출 규격)는 UI 문구 없음. RegisterView(역할 선택 2분할 카드 — 활성만 brand 테두리, `가입 유형` 문구, 기사/등록자 설명, 저장 CTA Primary 1개, 오류 n-alert)·MoreView(히어로/통계/메뉴, VIP·레벨 배지 스펙 준수)·HomeView(히어로 퀘스트/추천 탭/빈 상태/스켈레톤)·CustomerHome(등록자 홈 — 히어로 CTA 1개) — 용어("운행")·한국어·배지 스펙·Primary 버튼 1개 원칙 신규 위반 없음.
2. **직전 수정 유지 확인**: RideHistoryView 요약 히어로 건수 라벨이 `총 {{ countLabel }} 건수`로 교정된 상태(정산 탭 → "총 정산 건수"). 내부 상태값 `정산완료` 재노출 없음.
3. **match_score·match_reasons 미노출 (관찰, 설계 결정으로 기록)**: 백엔드(OrderListService L1479-1480 등)는 조건일치율·근거를 생성하나 프론트 사용처 0건. 단, HomeView L1025·OrderCard L511 주석에 "조건%·근거·추천 배지는 사용하지 않음 — 심플하게 노선·시간·금액·태그만"이라는 명시적 결정이 있고 09-02 23:01 보고서도 이 구조를 시안 부합으로 분류 — PRODUCT_VISION L31·L39(조건%+근거 체크리스트 카드 규격)와의 괴리는 후보(e-2)로만 유지.
4. **전역 캐너리 통과**: 10px 미만 폰트 0건, 기술 식별자 노출 0건, 배지 스펙 위반 0건.

## (d) 실제 적용한 수정
- **없음 (변경 없음)** — 이번 회차 신규 위반·명확한 소형 개선점 미발견으로 코드 수정 생략.

## (e) 적용하지 않은 항목과 이유 (후보)
- **HomeView 추천 카드 조건%(match_score)+근거 체크리스트(match_reasons) 미노출**: 코드 주석 2곳에 명시된 "심플 카드" 설계 결정과 충돌하고, 노출 구현은 카드 하단 행·근거 체크 목록 등 신규 기능 구현에 해당 — 적용 대상 아님. PRODUCT_VISION 카드 규격과의 정렬 여부는 제품 결정 필요(후보).
- **LoginView·RegisterView scoped CSS 복제(login-wrap/card/head/mark/title/desc/alert/remember/footer/link/submit ~19개 규칙 동일)**: 공통 auth CSS 추출은 두 파일 동시 리팩터링 — 기존 후보 분류 유지(반복 미적용).
- **기존 반복 후보(HomeView 탭 카운트 불일치·OrderCreateView 중복 CSS/들여쓰기·RideHistoryView/OrderCreateView 상태 탭 CSS 중복·커뮤니티 카테고리 색 체계·DashboardView 하드코딩 틴트·`/v8-noti` 데드 CSS·`.login-submit` CTA 크기 11px/600·data/levels.js "드라이버"·naive 전역 테마·의미색 하드코딩 등)**: 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- 코드 수정 없음(변경 없음) — `npm run build`는 미실행(수정 시에만 실행하는 회차 규칙 준수).
- import 누락 점검: 대상 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
