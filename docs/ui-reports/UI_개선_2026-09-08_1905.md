# UI 개선 보고서 — 2026-09-08 19:05

## 실행 결과 요약
- **수정 2건 적용** — 직전 실행(09-08 18:01) 보고서 이후 `frontend/src` 변경분 **6건**(SettingsProfileView 18:03 · useProfileSettings 18:03 · api/admin.js 18:06 · api/operations.js 18:13 · AdminView 18:17 · MarketView 18:19) 중 vue·composable 4건을 검토했다. 설정 화면 분리 리팩토링에서 **컴포저블 `useProfileSettings`에 `ROLE_CUSTOMER` import 누락**(SettingsProfileView에서 `isCustomer` 평가 시 런타임 ReferenceError 위험)과 **ProfileView가 템플릿에서 쓰는 `isCustomer`를 destructure 목록에서 누락**(등록자 '업체 인증' 카드가 항상 숨김)한 결함 2건을 발견·수정했다. 이 둘은 같은 리팩토링(설정 분리 + data/roles 상수 전환) 과정의 참조 누락으로, 소형·명확한 버그 수정에 해당한다.

## (a) 실행 시각
- 2026-09-08 19:05 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- **mtime 델타 스캔(09-08 18:01 이후 수정분)**: `frontend/src` 전체 재귀 — **6건**: `views/settings/SettingsProfileView.vue`(18:03, 신규 미추적 — 프로필 수정 화면) · `composables/useProfileSettings.js`(18:03) · `api/admin.js`(18:06, API 계층 — vue/CSS 검토 대상 아님, 존재만 확인) · `api/operations.js`(18:13, API 계층 — 동일) · `views/AdminView.vue`(18:17) · `views/MarketView.vue`(18:19).
- **직접 판독(검토 대상)**:
  - `composables/useProfileSettings.js`(전체 153줄) — 18:03 리팩토링분. data/roles 기반 `roleLabel`·`isCustomer` 전환 지점 확인 → **import 누락 결함 발견(아래 (c)-1)**.
  - `views/settings/SettingsProfileView.vue`(전체 101줄, 신규) — 프로필 수정(이름·연락처·업체명) 단일 카드 + Primary '저장' 1개. `isCustomer`를 destructure해 사용(18·35·49·52행) → **위 결함의 실제 크래시 경로 확인**.
  - `views/ProfileView.vue`(1~60행 script + 160~214행 '업체 인증' 카드 영역) — 템플릿 183행 `v-if="isCustomer"` 사용분과 destructure 목록(29~32행) 대조 → **destructure 누락 결함 발견(아래 (c)-2)**.
  - `views/settings/SettingsView.vue`(전체) — 동일 composable 소비처. `ROLE_CUSTOMER`·`ROLE_DRIVER`를 data/roles에서 직접 import해 `isCustomer`/`isDriver`/`canVerify` 자체 정의 → 결함 영향 없음 재확인. 카드 그리드·'로그아웃'(error tertiary) 구조 클린.
  - `views/settings/SettingsNotificationsView.vue`(전체) — 동일 composable 소비처. `notifyEnabled·toggleNotify`만 사용 → 결함 영향 없음 확인.
  - `views/MarketView.vue`(전체 1383줄) — 18:19 수정분. 빠른매칭 FAB(기사 전용)·필터/노선 검색 모달·연결 운행 섹션·그리드·퀵 드로어. **신규 UI 위반 없음**(기존 후보 재확인만, 아래 (e)).
- **전역 규칙 감사(grep)**:
  - 템플릿 기술 식별자(`주문번호|주문 번호|오더번호|운행 번호`): **1건** — SupportView.vue 문의 placeholder(기존 후보 유지) · `font-size: 7~9px`: **0건**
  - `ROLE_CUSTOMER` 참조 vs import: `useProfileSettings.js`만 불일치(이번 수정으로 해소). SettingsView·SettingsVerificationView·RegisterView·AdminView 등은 정상 import.
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-08_1801.md` — 적용 내역·후보 전수 대조, 반복 적용 없음
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙·의미색), 메모리 규칙(한국어 UI / "운행" 용어 / 기술 식별자 노출 금지 / import 누락 런타임 검증)

## (c) 발견한 개선 포인트
1. **useProfileSettings.js `ROLE_CUSTOMER` import 누락(이번 실행 적용)**: 94행 `const isCustomer = computed(() => auth.user?.role === ROLE_CUSTOMER);`가 `../data/roles`에서 `roleLabel`만 import한 채 `ROLE_CUSTOMER`를 미선언 참조. computed 평가 시 **ReferenceError → SettingsProfileView가 `isCustomer`를 템플릿(35·49·52행)에 사용하므로 '프로필 수정' 화면 렌더링 자체가 깨질 수 있는 결함**. (18:03 리팩토링이 문자열 'Customer' 비교를 ROLE_CUSTOMER 상수로 전환하면서 import에 반영을 누락)
2. **ProfileView `isCustomer` destructure 누락(이번 실행 적용)**: 183행 등록자 '업체 인증' 카드가 `v-if="isCustomer"`인데 script destructure 목록(29~32행)에 없어 **항상 미노출(등록자에게 카드가 보이지 않음)**. 16:08 ProfileView 리팩토링에서 composable 반환값 추가분을 destructure에 반영하지 않은 참조 누락.
3. **SettingsProfileView 구조 클린 (신규 확인)**: 뒤로가기 + page-head(제목·역할별 설명) + 단일 n-card(업체명/이름/연락처/이메일) + Primary '저장' 1개. 등록자(업체명·담당자)와 기사(이름) 분기 문구 정상. 기술 식별자·용어 위반 없음. (이번 수정의 결함만 해소)
4. **MarketView 18:19 수정분 — 신규 위반 0건**: 템플릿 전수 판독 결과 기술 식별자·용어·소형 폰트·배지 스펙(`.market-filters__badge` #07120e 채움 표준)·화면당 Primary(모달 내 '적용'/'검색' 각 1건) 모두 준수. 18:01 보고서 후보와 중복되는 신규 결함 없음.
5. **SettingsView·SettingsNotificationsView — 영향 없음**: `isCustomer`/`roleLabel`을 자체 정의 또는 미사용하므로 (c)-1 결함에 노출되지 않음(동일 composable 소비처 전수 확인).

## (d) 실제 적용한 수정
- **`composables/useProfileSettings.js`** — 5행 import에 `ROLE_CUSTOMER` 추가: `import { ROLE_CUSTOMER, roleLabel as roleLabelOf } from '../data/roles';`. (94·117행의 isCustomer 사용처를 정상 바인딩 → SettingsProfileView 런타임 크래시 방지. 동작 변화 없음)
- **`views/ProfileView.vue`** — 29~32행 destructure에 `isCustomer` 추가: `roleLabel, initials, level, formatWon, isCustomer`. (183행 '업체 인증' 카드가 등록자에게 정상 노출. 이 카드 184~204행의 상태 행(`is_business_verified`/`is_account_verified`)과 '관리' 버튼 동작은 기존 그대로)

## (e) 적용하지 않은 항목과 이유 (후보)
- **기존 반복 후보(18:01 기록분 유지, 상태 재확인)**: 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색(그린 vs 확인 필요 옐로우) · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 타입 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버"(data/levels.js↔LevelTable.php) · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩(MarketView 즐겨찾기 #f0a500 포함) · naive 전역 테마 · v-chip/verify-chip 패딩 차이 · 문의 placeholder 문구(SupportView) · MarketView 데드 CSS(`.page-head`/`.page-head__desc` scoped 규칙 — 템플릿 미사용, 렌더링 무영향 정리로 UI 개선 아님) · brand/accent 위 글자색 전역 설계(아바타 포함) · 등록자 가입 직행 `my-market`(제품·기획 판단) · `role === 'Driver'` 리터럴 8곳 상수 치환(ProfileView 24·43행 포함 — 전역 일괄, 소형 범위 초과) · RegisterView '등록자' 설명 문구(제품 문구 결정) · SettingsView 카드 타이포 단차 — 기존 분류 유지.
- **AdminView.vue 18:17 수정(후보 이월)**: 관리자 시스템 재구축 작업분(3천 줄 이상 증감) — 작업 진행 중으로 전수 판독·수정은 보류, 다음 실행 점검 후보.
- **api/admin.js·api/operations.js**: API 계층(18:06·18:13) — vue/CSS 검토 대상 아님.
- 이번 실행 신규 결함은 위 (c) 1·2건이며 모두 적용 완료 — 신규 미적용 항목 없음.

## (f) 빌드 결과
- `npm run build`(frontend) **통과** — 에러 0건, `✓ built in 1.53s`. ProfileView·MarketView·AdminView·SettingsProfileView 등 관련 라우트 청크 정상 생성.
- import 누락 점검: `ROLE_CUSTOMER`는 `data/roles.js`에서 export 확인(`ROLE_CUSTOMER = 'Customer'`) → 추가 import 유효. ProfileView의 `isCustomer`는 `useProfileSettings` 반환 객체(146행)에 포함됨을 확인 → destructure 정상 바인딩. 다른 소비처(SettingsView·SettingsNotificationsView·SettingsProfileView)와 중복 import·미사용 경고 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
