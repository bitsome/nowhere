# UI 개선 보고서 — 2026-09-04 16:01

## 실행 결과 요약
- **변경 없음** — 15:02(34회차) 이후 유일한 소스 변경은 34회차 자체 수정(AdminView 15:06)뿐이라 신규 리뷰 대상이 없었고, 직후 콘텐츠 수정(12:33)이 있었으나 전체 재열람이 밀려 있던 MarketView를 이번에 전수 재열람 + 전역 캐너리 감사 결과 신규 적용 대상 미발견. 32·33회차 적용분 유지 확인(5/5·4/4).

## (a) 실행 시각
- 2026-09-04 16:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 전체 읽기 — **12:33 콘텐츠 수정 후 전체 재열람이 밀려 있던 화면**):
  - `views/MarketView.vue`(전체 1411줄 — 마켓: 서비스 유형/필터/검색/내 운행 칩 행·적용 필터 요약 칩·필터 모달(날짜·시간대·유형·특별 보기·출발/도착 3단 셀렉트·차량·인원·금액·정렬)·노선 검색 모달·연결 운행/전체 운행 섹션·페이지네이션·빠른매칭 FAB + 하단 드로어(QuickMatchPanel) — script L1-683·템플릿 L686-1030·scoped CSS L1031-1367·전역 드로어 CSS L1370-1411 전수)
- 전역 감사(grep): 10px 미만 폰트 0건, "오더/주문번호/order_number" .vue 노출 0건, 낡은 fallback(`var(--brand, #4f6ef2)` 등)·`#4f6ef2` 잔존 0건, 고정 `text-color="#0/1/f/e"` 0건, 배지-유사 padding(`2~5px xpx`) 잔존은 콘텐츠 칩·미디어 오버레이뿐(기존 분류)
- 이전 회차 적용 유지 확인(grep): OrderDetailView `rideBarTextColor`(L115)·`:text-color`(L1217)·`detail-actionbar__step--dark`(L1224·L2299) — 32회차(14:01) 유지 4/4. AdminView `.n-switch--active` deep 규칙(L847-848)·`html.dark .auto-card` reset 규칙(L857-858) — 33회차(15:02) 유지 2/2
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-04_1502.md`(34회차)·`1401.md`(33회차) 등(적용 내역·후보 전수 — 반복 적용 없음. 이번 회차는 15:02 이후 첫 실행)
- 작업 트리 확인: 15:02 이후 `frontend/src` 소스 변경은 AdminView.vue(15:06 — 34회차 자체 수정) 1건뿐. 12:01~15:02 구간 변경 7건은 13:05 보고서 판정대로 하위 폴더 재정리 터치이며, 그중 **유일하게 전체 재열람 이력이 밀린 MarketView(12:33)를 이번에 전수 확인**
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first / brand 채움 위 글자 표준 `#07120e`)

## (c) 발견한 개선 포인트
1. **MarketView 클린 (신규 전체 확인)**: 필터/검색/초기화 칩 행(활성 `brand-soft`+brand·건수 배지 `.market-filters__badge` `#07120e`·1px 6px 계열/10px/400 — 09-03 14:05 적용 유지)·적용 필터 요약 칩(개별 해제)·결과 건수(`b`는 `--text` — 09-02 적용 유지)·필터 모달(푸터 Primary "적용" 1개 + 취소/초기화 보조)·노선 검색 모달(Primary "검색" 1개)·연결/전체 섹션 헤더·EmptyState 문구("가져올 수 있는 운행이 없습니다")·빠른매칭 FAB(`v-if="isDriver"`, 하단 네비 위)·드로어 헤더/닫기·"운행" 용어·한국어 — 전부 배지 스펙·Primary 1개 원칙 준수. 신규 위반 없음. (FAB `var(--brand)`+흰 아이콘은 기존 관용 분류 — 아래 e)
2. **전역 캐너리 통과**: 10px 미만 폰트 0건, "오더/주문번호" 노출 0건, 낡은 hex fallback 0건, 고정 light text-color 0건. 배지-유사 padding 잔존(HomeView L1023·MarketView L1311·ProfileView L645·NotificationsView L760/795·Community*·HeaderBar 등)은 콘텐츠 칩·섹션 헤더 버튼으로 배지 스펙 대상 아님(기존 반복 분류). 흰 글자 잔존(아바타·갤러리 오버레이·VIP/레벨 배지·로그인 CTA·FAB·경고 알림 등) 전부 기존 관용/설계 분류 항목 — 신규 잔존 없음.
3. **32·33회차 적용 유지**: OrderDetailView 하단 진행 바 글자색 로직 4곳, AdminView 스위치 라벨 대비 규칙 2블록 모두 정상 존재.

## (d) 실제 적용한 수정
- 없음 (변경 없음).

## (e) 적용하지 않은 항목과 이유 (후보)
- **MarketView·MyMarketView·CommunityView FAB(var(--brand) 원형 + 흰 아이콘)·ProfileView 아바타 등**: 아이콘/아바타 관용 — "brand 위 글자색" 전역 설계 후보로 기존 분류 유지(반복 미적용).
- **EmptyState 아이콘 variant 5색 하드코딩(#7a5af8·#e8733d 등)·UiChip variant 색·VIP 금색 그라디언트·naive 라이트 primary 초록(#18a058)·App.vue 하단 n-badge naive 기본색**: 의미색·전역 naive 테마 설계 후보 — 기존 분류 유지(반복 미적용).
- **HomeView 탭 카운트 vs 표시 개수 불일치·AdminView `page-head__desc` 좁은 설명·OrderDetailView `.detail-hero` 흰 글자·`.v8-noti` 데드 CSS·RegisterView CSS 복제 등 기존 후보**: 기존 분류 유지(반복 미적용).
- **배지-유사 padding 잔존(MarketView `.market-active__chip` 5px 10px·`.filter-quick__chip` 6px 12px 등)**: 필터 칩 등 클릭형 콘텐츠 칩으로 배지 스펙 대상 아님 — 유지.

## (f) 빌드 결과
- 코드 수정 없음 → `npm run build` 미실행(빌드 불필요, 마지막 빌드는 34회차 15:06 성공 확인분).
- 수정 후 grep 검증: 해당 없음(변경 0건). 32·33회차 적용 유지 grep은 (b)에 기록 — 전부 존재.
- import 누락 점검: 코드 변경 없음 — 해당 없음.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
