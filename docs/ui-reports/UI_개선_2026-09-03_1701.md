# UI 개선 보고서 — 2026-09-03 17:01

## (a) 실행 시각
- 2026-09-03 17:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 읽기): `views/AdminView.vue`(전체 848줄 — 자동 운행 탭·사용자/기사 탭·상태 배지 영역), `views/ChatView.vue`(전체 105줄 — 최근 미검토 소형 화면)
- 컴포넌트/유틸 대조: `views/orders/RideHistoryView.vue`(`statusTextColor` L265-268 — 상태 배지 글자색 표준), `components/chat/ChatThread.vue`(16:03 회차 적용 `orderStatusTextColor` 대조), `utils/colors.js`(`statusColorVar`), `assets/base.css`(`--status-*` 라이트/다크 토큰 L15-23·L46-54)
- 전역 감사(grep): 상태 배지 인라인 스타일(`:style` background + statusColorVar 계열) 전수 — AdminView 1곳만 흰 글자 고정으로 잔존 확인, 배지 스펙(1px 6px/10px/400) 위반 전수 0건, 10px + font-weight 500~700 쌍 전수 0건
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1603.md`·`1501.md`·`1405.md`·`1301.md`·`1207.md`(적용 내역·후보 전수 — 반복 적용 없음. 01:01 회차 후보 "AdminView `.auto-history__status` 라이트 흰 글자"가 16:03 회차에서 확립된 RideHistoryView `statusTextColor` 표준(완료·공개 등 밝은 상태는 `#101418`) 적용 대상으로 판정됨)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **AdminView 자동 등록 운행 이력 상태 배지 — 흰 글자 on 밝은 상태색 (신규 판정·적용)**: 자동 운행 탭의 이력 행 상태 배지 `.auto-history__status`(L763-771)가 배경을 인라인 상태색(`autoStatusColor` → `statusColorVar`, L217)으로 채우면서 글자색은 CSS `#fff` 고정이었다. 라이트 모드에서 `--status-published`(#36adff 파랑)·`--status-driving`(#13c2c2 시안)·`--status-completed`(#18a058 초록)·`--status-trading`(#ffa940 앰버) 등 밝은 배경 위 흰 10px 글자는 저대비(≈1.9~3.4:1). 같은 코드베이스의 동일 용도 상태 배지(RideHistoryView `statusTextColor`·ChatThread `orderStatusTextColor` — 16:03 회차 적용, OrderCard·SetGroupCard 클래스 규칙)는 이미 상태별 글자색 규칙을 갖고 있어 **화면 간 불일치** 상태였다. 01:01 회차에서 후보로 남았으나, 이후 16:03 회차가 ChatThread에 동일 상태 목록의 `#101418` 규칙을 확립해 표준화가 완료됨에 따라 AdminView 잔존 건에 동일 규칙 적용이 일관된 소규모 수정으로 판정.
2. **ChatView 클린 (신규 확인)**: 105줄 소형 화면 — 페이지 셸·스켈레톤·EmptyState(한국어·hint 보충)·목록/대화 전환 구조 단순. 배지·용어("운행")·다크 대응 위반 없음 — 수정 불필요.
3. **AdminView 기타 클린 (신규 확인)**: 사용자/기사 탭 EmptyState 힌트(01:01 회차 적용 문구)·n-tag 역할 색(Super Admin=레드·Admin=앰버는 의미색)·자동 등록 설정 카드 Primary("설정 저장") 1개·배지 스펙 준수 — 위 1건 외 추가 위반 없음.
4. **기타 재확인 클린**: 상태 배지 인라인 배경 전수 결과 흰 글자 잔존 AdminView 외 0건(OrderCard·SetGroupCard는 클래스 규칙, RideHistoryView·ChatThread는 computed 글자색), 배지 스펙(1px 6px/10px/400) 위반 0건.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/AdminView.vue` | 상태 태그 글자색 함수 `autoStatusTextColor` 추가 — `completed/trading/published/driving/acceptance_pending`은 `#101418`, 나머지는 `#ffffff` (RideHistoryView `statusTextColor`·ChatThread `orderStatusTextColor`와 동일 규칙·동일 상태 목록, 주석 명시) |
| `views/AdminView.vue` | 상태 태그 인라인 스타일(L506)에 `color: autoStatusTextColor(order.status)` 추가 — 자동 등록 이력 배지의 밝은 상태색 위 흰 글자 저대비 해소 |
| `views/AdminView.vue` | `.auto-history__status` CSS(L769)에서 고정 `color: #fff` 제거 — 글자색은 상태 밝기에 따라 인라인 지정으로 일원화 |

script 함수 1곳 + 템플릿 인라인 스타일 1곳(신규 import 없음) + scoped CSS 정리. 의미·레이아웃·패딩·동작 무변경 — 글자색만 상태별로 RideHistoryView·ChatThread 표준과 동일하게 일원화.

## (e) 적용하지 않은 항목과 이유 (후보)
- **AdminView 페이지 헤드 설명 문구** "기사 차량·면허 인증을 승인하거나 해제합니다."(L246): 자동 운행 탭이 추가되며 페이지 범위가 넓어졌으나 문구는 인증 업무만 설명 — 운영 문구 재설계 사항(후보).
- **ChatImageSheet 흰 아이콘 11곳**: 사진 위 오버레이 아이콘 관용 패턴 — 기존 후보 유지.
- **채팅 아바타·FAB('＋')·brand-gradient 아이콘 박스·Login/Register 그라디언트 CTA·ProfileView naive 세그먼트/ReviewsView 활성 탭**: 원형 아바타/브랜드·naive 관용 — 전역 "brand 위 글자색"/naive 테마 설계 결정 후 일괄 재평가(기존 후보 유지).
- **라이트 naive-ui primary 초록(#18a058)·다크 primary 흰 글자 전역 불일치**: 전역 naive 테마 오버라이드 필요 — 대형 후보(12:07~16:03 유지).
- **HomeView 탭 카운트 불일치·`.login-submit` CTA 크기(11px/600)·SettingsVerificationView primary 2개·data/levels.js "드라이버"·DashboardView 하드코딩 naive 색**: 기존 후보 유지(반복 미적용).
- **"운행시작·정산완료·운행중" 등 붙여쓰기 상태 키워드**: 상태값/스테퍼 라벨 관행 — 정상 분류(미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 896ms`, 에러 0건.
- 수정 후 grep 검증: `autoStatusTextColor`가 AdminView script(L220)·템플릿 인라인 스타일(L506)에 존재, `.auto-history__status` CSS에서 `color: #fff` 제거 확인 — 상태 배지 인라인 배경 전수 중 흰 글자 잔존 0건.
- import 누락 점검: 이번 변경은 script에 기존 import된 것만 사용하는 함수 추가(`statusColorVar`는 이미 import됨) + 템플릿 인라인 스타일 1곳 + scoped CSS 정리 — 신규 import 추가·제거 없음, 빌드 통과로 런타임 참조 검증 완료. **import 누락 없음.**
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
