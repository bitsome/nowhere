# UI 개선 보고서 — 2026-09-03 18:01

## (a) 실행 시각
- 2026-09-03 18:01 (KST, 로컬 환경)

## (b) 검토한 파일 목록
- 뷰(직접 읽기): `views/orders/OrderDetailView.vue`(운행 진행 스테퍼 — 템플릿 L385-412·CSS L2100-2170·`RIDE_PROCEDURE_STEPS`/`rideStepTimeLabel` L88-121 — 운행 상세 화면, 핵심 노출 요소)
- 컴포넌트(직접 읽기, **기존 회차 전체 읽기 이력이 없는 신규 대상 2건**): `components/orders/OrderDetailChat.vue`(전체 341줄 — 운행 상세 인라인 대화창), `components/community/CommunityPostEditor.vue`(전체 262줄 — 커뮤니티 글 수정 모달)
- 컴포넌트(부분 대조): `components/chat/ChatList.vue`(탭 카운트 칩 `.chat-chip__count` L226-228·안읽음 `n-badge` L205 — 13:01 후보 3곳 묶음 재확인)
- 전역 감사(grep): **10px 미만(`font-size` 7·8·9px) 전수 — OrderDetailView L2149 1곳만 잔존 발견**, 10px + font-weight 500~700 잔존 분류(카운트 칩·강조 라벨), UI 노출 "오더"/"주문번호"/"드라이버" 전수(0건), `background: var(--brand)`+`color:#fff` 인라인 쌍 전수(0건), `n-badge` 사용처 전수(App.vue 하단 네비·ChatList — 색 오버라이드 없음 확인)
- 직전 보고서 대조: `docs/ui-reports/UI_개선_2026-09-03_1701.md`·`1603.md`·`1501.md`·`1301.md`(적용 내역·후보 전수 — 반복 적용 없음)
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 메모리 규칙(한국어 UI / "운행" 용어 / 배지 스펙 1px 6px·10px·400 / v8 상황별 색·grayscale-first)

## (c) 발견한 개선 포인트
1. **운행 진행 스테퍼 단계 시각 9px — 앱 전체 유일한 10px 미만 텍스트 (신규 발견·적용)**: 운행 상세 상단 스테퍼(`.ride-steps__time`, L2148)가 각 단계(시작/장소/탑승/출발/이동/도착) 아래 기록 시각(HH:MM)을 9px로 표시 — 전체 Vue 파일·`base.css` grep 결과 **9px 미만 폰트는 이 1곳뿐**. 나머지 마이크로 텍스트는 전부 10px 이상이라 화면·앱 전체 최소 가독 크기와 어긋남. 수치/한글 혼합이 아닌 tabular HH:MM이어서 위험은 작지만, 상세 히어로의 부가 메타로 반복 노출되는 만큼 최소 크기(10px)로 상향이 안전한 소규모 개선으로 판정. 컨테이너 `.ride-steps`는 `overflow-x:auto` + 항목 `flex-shrink:0`이라 1px 증가로 인한 레이아웃 붕괴 없음.
2. **OrderDetailChat 클린 (신규 전체 확인)**: 등록자-수행자 인라인 대화창 — 헤더 타이틀+상대 이름 칩(`padding 1px 6px/10px/400` 배지 스펙 준수, `--brand-soft`+`--brand` 의미색), EmptyState 한국어 문구("대화를 불러오는 중..."/"아직 대화가 없습니다…"), placeholder ChatThread와 동일 문구, Primary 전송 버튼 1개, 전부 `var(--*)` 토큰 사용 — 수정 불필요.
3. **CommunityPostEditor 클린 (신규 전체 확인)**: 글 수정 모달 — 카테고리 필터 칩(11px/600, 마켓 필 언어 관행)·한국어 placeholder·Primary "저장" 1개+quaternary "취소"·토큰 사용 — 수정 불필요.
4. **기타 재확인 클린**: 10px + bold 잔존은 전원 카운트 칩·강조 라벨(MyMarketView 휴지통 건수·요청 카운트다운, ActionCenterView 섹션 라벨, ChatList 탭 카운트)으로 기존 분류 유지, UI 노출 "오더"/주문번호/드라이버 0건, brand 배경 흰 글자 인라인 0건(17:01 AdminView 수정 후 잔존 없음).

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/orders/OrderDetailView.vue` | `.ride-steps__time` `font-size: 9px` → `10px` + 근거 주석("앱 최소 가독 크기(10px) — 유일한 9px 잔존 제거") — 운행 진행 스테퍼 단계 시각 가독성 상향 |

`<style scoped>` CSS 값 1건 + 주석 1건(템플릿·script·import 무변경). 의미·색·레이아웃·동작 무변경 — 9px→10px 크기 상향만 적용.

## (e) 적용하지 않은 항목과 이유 (후보)
- **ChatList `.chat-chip__count`(1px 5px/10px/600)·활성 탭 white-on-brand**: 탭 카운트 칩 — 13:01 후보(CommunityView·MarketView·ChatList 3곳 묶음) 유지. 일관 처리 방침(글자색·채움) 결정 후 일괄 검토.
- **App.vue 하단 네비·ChatList `n-badge` naive 기본색(라이트 초록)**: 전역 naive 테마 오버라이드 필요 — 대형 후보(12:07~17:01 유지).
- **10px+font-weight 700 잔존(MyMarketView `.status-tabs__trash-count`/`.claim-time__text`/`.claim-time__remain`·ActionCenterView `.actions-detail__label` 등)**: 배지가 아닌 건수/카운트다운/섹션 강조 라벨 — 배지 스펙 대상 아님(정상 분류, 변경 안 함).
- **ChatImageSheet 흰 아이콘·아바타/FAB/brand-gradient·ProfileView naive 세그먼트·라이트 naive 초록 불일치·HomeView 탭 카운트 불일치·`.login-submit` CTA·SettingsVerificationView primary 2개·data/levels.js "드라이버"**: 기존 후보 유지(반복 미적용).
- **OrderDetailChat "대화를 불러오는 중..."·CommunityPostEditor/각 placeholder ASCII `...`**: 앱 전역 일관 관행(ChatThread·ChatImageSheet 등 동일) — 정상 분류(미적용).

## (f) 빌드 결과
- `npm run build` (frontend, vite) **성공** — `✓ built in 959ms`, 4347 modules transformed, 에러 0건.
- 수정 후 grep 검증: `font-size: (7|8|9)px` **0건** — 앱 전체에서 10px 미만 텍스트 제거 확인(1/1 적용).
- import 누락 점검: 이번 변경은 `<style scoped>` CSS 값 1건 + 주석뿐이며 템플릿 구조·script·import 변경 없음 — **import 누락 없음**.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만).
