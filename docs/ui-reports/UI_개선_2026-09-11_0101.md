# UI 개선 보고서 — 2026-09-11 01:01

## 실행 결과 요약
- **코드 수정 1건(1개 파일)** — 이번 회차는 `frontend/src` 의 **알림센터 "지금 처리할 알림" 카드의 상황별 색상 매핑**을 감사해, **`신고(report)` 유형만 색상 클래스가 누락되는 결함**을 찾아 교정했다.
  1. **`priorityCls` 에 `report` 분류 누락 → 신고 알림이 문제(레드)가 아닌 기본 배경으로 표시됨(신규 발견·적용)**: [NotificationsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L113-L121) 의 `priorityCls` 는 우선처리 카드의 상황별 배경/테두리를 정하는데, 매핑에 `offer / request / warning / cancel / price` 6종 중 **`report`(신고) 키가 빠져 있었다**. 그런데 같은 파일의 [KIND](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L48-L49) 는 `report` 를 **"신고/분쟁 — 문제 신호라 레드 톤"** 으로 정의하고 `group: 'request'` 로 묶으며, [TAG_VARIANT](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L91) 도 `report → red` 로 매핑한다. 우선처리 목록 필터는 `kindOf(n).group === 'request'`([L108-L110](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L108-L110))이므로 `report` 는 **반드시 이 카드에 노출되는데**, `:class="priorityCls(n)"` 이 `undefined` 를 반환해 **`.v8-priority-item.danger`(레드 배경·테두리)가 적용되지 않고** 기본 배경 [`.v8-priority-item`](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L461-L472) 의 **연녹색(`#f0f7f3`)** 으로 떨어졌다. 즉 분쟁·신고(가장 시급한 문제 신호)가 우선처리 카드에서 **초록 톤**으로 표시되는 의미색 역전이었다.
- 실행 전 상태 점검: HEAD `f240340`, `frontend/src` 최신 mtime = **00:04:23**(`OrderCard.vue`·`HomeView.vue`, 직전 00:01 회차 적용분) → **직전 회차 이후 타 세션 신규 편집 0건**(안정 상태). 신규 산출물이 없어 이번 회차도 **기존 코드 계약 감사**로 방향을 전환했다(직전 회차 "실시간 카운트다운 숫자 폭"과 중복 없는 신규 감사 축 = "알림 상황별 색상 매핑 완전성").
- 전역 표준 스캔 신규 위반 0건: `오더`/`주문번호` **0건**, 10px 미만 폰트 **0건**, 배지 규격(1px 6px/10px/400) 위반 **0건**, `target="_blank"` rel 누락 **0건**(href 있는 `<a>` 5곳 전부 `rel` 동반).
- `npm run build` 통과(`✓ built in 1.01s`, 오류·경고 0건). 분류 맵 키 1개 추가뿐이라 import·DOM·레이아웃 무변경.
- 서버 배포·빌드·DB 동기화 없음(로컬만).

## (a) 실행 시각
- 2026-09-11 01:01~01:2x (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-11_0001.md`(적용: 카운트다운 `tabular-nums` 5곳) · `2026-09-10_2301.md`(적용: `color-scheme`·`dvh`·`overscroll-behavior`) · `2026-09-10_1901.md`(적용: 반복 펄스 `prefers-reduced-motion`) — 적용 내역·미적용 후보 전수 대조 후 **반복 적용 없음**. 이번 `priorityCls` 누락은 역대 보고서에 등장하지 않는 신규 항목(09-02_0914 회차가 `priorityCls` 를 `request: success→warn` 으로 손댄 이력이 있으나, 그 이후 도입된 `report` 유형은 반영되지 않은 상태였다).
- **실행 전 상태 점검**: `Get-Date`(01:01) + `git log --oneline -3`(HEAD `f240340`) + `git status --short` + `Get-ChildItem frontend/src -Recurse | Sort LastWriteTime -Descending` → 00:04:23(직전 회차 적용분) 이후 **신규 편집 0건** 확인.
- **신규 축 — 알림 상황별 색상 매핑 전수**: [NotificationsView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue) 전량 열람(스크립트 분류 맵 + 템플릿 + scoped CSS 700여 행). 매핑 객체 4종을 각각 전수 대조 — [KIND](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L42-L55)(11종) · [TAG_VARIANT](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L85-L97)(11종) · [priorityCls](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L113-L121)(6종) · [STATUS_TONE](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L122-L132)(9종). CSS 클래스 존재 여부도 대조 — `.v8-priority-item.success/warn/danger/price`(라이트·다크) 전부 정의됨([L473-L507](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L473-L507)), `.v8-icon` 9종 전부 정의됨.
- **분류 함수 대조**: [classify()](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L57-L80) 가 반환할 수 있는 11개 kind 중 `group === 'request'` 인 것 = `offer·request·warning·cancel·price·report` 6종. → [priorityItems 필터](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L108-L110)에 노출 가능한 정확히 그 6종 중 `report` 만 `priorityCls` 에서 누락됨을 특정.
- **공용 위젯 대조**: [UiListRow.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiListRow.vue)(`tag` prop에 `a` 전달 시 href 없이 클릭 핸들러만 붙음 — `.ui-row` 에 `cursor: default`·`background: transparent`·`border: 0`·`font-family: inherit` 리셋이 있어 버튼 태그도 수용 가능) · [UiSection.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiSection.vue) · [UiChip.vue](file:///d:/wwwroot/nowhere/frontend/src/components/ui/UiChip.vue)(variant 7종 `green/yellow/red/blue/purple/teal/default` — `TAG_VARIANT` 값과 전부 대응).
- **신규 파일 검토(수정 없음)**: [RegistrantSettlementView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/RegistrantSettlementView.vue)(입금 대기 히어로·계좌·정산 내역 — `h1` 위계·배지 규격 `1px 6px/10px/400`·`UiSection` 재사용 정합, 결함 없음) · [SharedOrderView.vue](file:///d:/wwwroot/nowhere/frontend/src/views/SharedOrderView.vue)(다크 Primary CTA 예외·safe-area·`dvh` 처리 완료, 결함 없음).
- **공용 토큰/클래스 재확인**: [base.css](file:///d:/wwwroot/nowhere/frontend/src/assets/base.css)(`:root`/`html.dark` 토큰, `.badge` 규격, `.sk-*` 스켈레톤, `prefers-reduced-motion` 블록) — 직전 회차 적용분 유지 확인.
- **모션/뷰포트/네이티브 재확인(교차 검증, 수정 없음)**: `@keyframes` 18건·`animation:` 25건 재판독 → 무한 반복 6곳은 09-10_1901 적용분으로 정지 처리됨, 일회성(`notify-in`·`nav-badge-pop`·`celebration-*`)·스피너(`ig-spin`·`cb-spin`·`img-sheet-*`)는 기존 분류 유지. `vh`/`dvh`·`safe-area-inset` 18건·`overscroll-behavior` 4건 — 직전 회차 적용분 유지.
- **키보드/포커스 계약 확인**: `role="button"` 카드 3곳([HomeView](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L686-L693)·[OrderCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L303-L310)·[SetGroupCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L77))는 `@keydown.enter` 만 처리(Space 미지원) — 09-10_2001·2101 이 "nested interactive + Enter 버블링" 구조 문제로 이미 분류.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀·의미색), 프로젝트 배지 표준(1px 6px/10px/400), 메모리 색 규약(민트=완료/정상, 옐로우=확인 필요, 레드=취소/거절/문제), 용어 규칙("운행"·"기사").

## (c) 발견한 개선 포인트
1. **알림 우선처리 카드의 신고(report) 색상 누락(신규 발견·적용)**: 알림센터는 유형을 11종으로 분류해 `KIND`(아이콘·아이콘색·태그·그룹), `TAG_VARIANT`(칩 색), `STATUS_TONE`(상태 배지 색)에 각각 매핑한다. 그런데 **"지금 처리할 알림" 카드의 배경/테두리를 정하는 `priorityCls` 만 `report` 키가 없어** `undefined` 를 반환했다. 이 카드는 `group === 'request'` 만 모으므로 신고가 반드시 포함되고, 결과적으로 **문제 신호(신고/분쟁)가 가장 눈에 띄어야 할 카드에서 기본 배경인 연녹색 톤으로 표시**됐다. 같은 파일 안에서 `KIND` 주석이 "신고/분쟁 — 문제 신호라 레드 톤"이라 선언하고 `TAG_VARIANT` 가 `red` 를 주는 것과 **규칙 내부 자기모순**이다.
2. **href 없는 `<a>` 로 만든 행(신규 관찰·후보)**: [NotificationsView 우선처리 아이템](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L258-L264) 과 `UiListRow` 에 `tag="a"` 를 넘긴 3곳([NotificationsView](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L339)·[HomeView](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L891)·[CustomerHome](file:///d:/wwwroot/nowhere/frontend/src/views/home/CustomerHome.vue#L40))은 `@click` 핸들러만 있고 `href` 가 없다. href 없는 `<a>` 는 **링크 롤이 없고 키보드 포커스도 받지 못해** 사실상 마우스로만 동작한다. 다만 버튼으로 바꾸면 신고/제안 항목 내부의 "처리할 일에서 처리" 버튼과 **중첩 인터랙티브**가 되고 Enter 버블링으로 함께 열리는 문제가 재현되어(09-10 보고서의 커뮤니티 카드 후보와 동일 계열) **구조 변경**이 필요하다.
3. **우선처리 카드의 목록 축약 표기(신규 관찰·후보)**: 머리말은 `{{ priorityItems.length }}건`(전체), 목록은 `priorityItems.slice(0, 4)`(4건)이라 5건 이상이면 **표시 건수와 실제 행 수가 달라진다**. 하단에 "처리할 일 화면으로 이동" 버튼이 있어 의도된 축약으로 보이나, "외 N건" 표기가 없어 오해 소지가 있다.

## (d) 실제 적용한 수정
| 파일 | 수정 내용 |
| --- | --- |
| `views/NotificationsView.vue` | [priorityCls](file:///d:/wwwroot/nowhere/frontend/src/views/NotificationsView.vue#L113-L121) 에 `report: 'danger'` 추가(L119-120) — 신고 알림이 우선처리 카드에서 `.v8-priority-item.danger`(레드 배경·테두리, 다크 포함)를 받도록 함. `KIND`·`TAG_VARIANT` 와 동일 분류를 유지한다는 이유 주석 1줄 동반 |

- 공통: **분류 맵 키 1개 추가뿐** — 템플릿·컴포넌트 등록·props·emit·import·텍스트·DOM 구조 **무변경**. 이미 CSS에 정의돼 있던 `.v8-priority-item.danger` 규칙(라이트 `#fdeeee`·다크 `#291c1e`)을 그대로 재사용하므로 **새 스타일 정의 0건**.
- 적용 후 확인: `priorityCls` 키가 6종(`offer/request/warning/cancel/price/report`)으로 `group === 'request'` 노출 집합과 정확히 일치 → 폴백(`undefined`) 경로 소거. 소스 그렙으로 `report: 'danger'`(L120) 잔존 확인.

## (e) 적용하지 않은 항목과 이유 (후보)
1. **href 없는 `<a>` 행 4곳(2번)**: 접근성상 버튼/링크가 맞지만, 우선처리 아이템 내부에 "처리할 일에서 처리" 버튼이 있어 버튼 태그로 바꾸면 **중첩 인터랙티브**(HTML 상 유효하지 않음)가 되고 Enter 버블링으로 카드가 함께 열린다. `UiListRow` 행은 현재 중첩 버튼이 없는 곳도 있으나, 3개 호출부의 상호작용 의미(링크 vs 버튼)를 먼저 정해야 해 **구조 변경** → 후보.
2. **`role="button"` 카드의 Space 키 미지원(3곳)**: [HomeView](file:///d:/wwwroot/nowhere/frontend/src/views/HomeView.vue#L686-L693)·[OrderCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/OrderCard.vue#L303-L310)·[SetGroupCard](file:///d:/wwwroot/nowhere/frontend/src/components/orders/SetGroupCard.vue#L77) 는 `@keydown.enter` 만 처리한다. WAI-ARIA button 패턴은 Enter·Space **둘 다**를 요구하지만, Space 키다운은 `preventDefault` 없이는 페이지 스크롤을 유발하고 카드 내부 버튼에 포커스가 있을 때 버블링으로 카드까지 함께 열리는 기존 구조 문제(09-10_2001·2101 분류)와 같은 뿌리라 **임의 추가 보류** → 후보.
3. **우선처리 목록 "외 N건" 표기(3번)**: 표시 4건 고정 대비 머리말 전체 건수 — 의도된 축약(하단 이동 버튼 존재)으로 판단 → 후보.
4. **기존 반복 후보(상태 재확인, 유지)**: FAB 3곳(`var(--brand)` + 흰 글리프, 커뮤니티·내 마켓·마켓) · `--accent` 아바타 흰 이니셜(09-07 "임의 수정 금지" 유지) · `:focus-visible` 미정의 / `outline: none` · UiChip 하드코딩 팔레트(칩 규격 4px·11px/700 은 배지 표준 대상 아님) · 관리자 상태 칩 굵기 잔여분 · base.css 레거시 `.btn`/`.card` 죽은 CSS · `data/levels.js` "드라이버" 타이틀 · PWA 매니페스트 용어("드라이버 오더 마켓") · `theme-color`/매니페스트 색 테마 연동 · `aria-live` 도입 · `user-scalable=no` · `transition: all` · 소수점 폰트(11.5px 등) · `border-radius: 14px` 하드코딩 · `@media` 브레이크포인트 토큰화 · `word-break: break-all`(알림 본문) · `formatWon` 명칭 혼재 · coin+`원` 이중 표기 · MoreView 정산/처리할 일 아이콘 중복 · "내 마켓" 아이콘 표기 불일치 · SettingsVehiclesView 로딩 스켈레톤 부재 · 관리자 모달 `60vh`/`50vh` · MarketView `n-drawer` 80vh — 각각 전역 디자인 결정 또는 순수 정리 작업으로 09-03~09-11 여러 회차에서 분류된 항목 → 기존 분류 유지(반복 미적용).

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, `✓ 4376 modules transformed`, `✓ built in 1.01s`, 오류·경고 0건) — 수정 반영 후 실행. `NotificationsView` 청크(`NotificationsView-AN5f0CUN.js`) 정상 재생성.
- import 누락 점검: 변경이 **분류 맵 키 1개 추가(주석 1줄)뿐**이라 import·심볼·컴포넌트 등록 영향 **0건**. 추가한 값 `'danger'` 는 이미 CSS 클래스 접미사로 정의돼 있던 문자열이고 외부 식별자를 참조하지 않는다.
- 빌드 산출물 검증: `dist/assets/NotificationsView-AN5f0CUN.js` 에서 `priorityItems` 필터 직후 매핑이 `{offer:\`price\`,request:\`warn\`,warning:\`warn\`,cancel:\`danger\`,price:\`price\`,report:\`danger\`}` 로 반영됨을 확인(`report:\`danger\`` 존재).
- 런타임 위험 점검: ① 변경은 `:class` 바인딩이 반환하는 **문자열 값 1개**를 추가하는 것으로, 신고 알림에서만 `danger` 클래스가 붙고 나머지 유형의 클래스·레이아웃·아이콘·텍스트는 완전히 동일하다. ② `.v8-priority-item.danger` 는 라이트 `#fdeeee`·다크 `#291c1e` 배경과 레드 테두리가 이미 정의돼 있어 **다크 대비 재검토 불필요**. ③ 분류 맵 폴백 경로가 사라져 `undefined` 클래스(연녹색 기본 배경) 렌더가 없어진다. 레이아웃·여백·모션 변화 0.
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 워킹트리에 남김(사용자 커밋 대기).
