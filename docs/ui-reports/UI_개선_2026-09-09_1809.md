# UI 개선 보고서 — 2026-09-09 18:09

## 실행 결과 요약
- **코드 수정 6건(6개 파일)** — 직전 실행(17:02) 이후 `frontend/src` 신규 소스 변경은 서비스 워커 개발 해제(main.js, sw.js — UI 아님)뿐. 17:02 보고서의 21건 붙여쓰기 교정과 16:02 적용분(5파일)에 중복되지 않는 신규 항목만 직접 수정했다.
- 신규 발견 3유형: ① 처리할 일 확인 다이얼로그의 조사 오류("취소을 수락할까요?") ② 보조용언 띄어쓰기 전수 감사에서 남아 있던 `알려드릴게요`·`알려드립니다`·`모아뒀어요` 잔존 5건 ③ ReviewsView '내가 쓴 리뷰' 블록의 과다 들여쓰기(편집 잔재).
- `npm run build` 통과. 새 기능·구조 변경 없음.

## (a) 실행 시각
- 2026-09-09 18:09 (시스템 로컬 시간)

## (b) 검토한 파일 목록
- **직전 보고서 대조**: `docs/ui-reports/UI_개선_2026-09-09_1702.md` — 적용 내역(12파일 21건)·후보 전수 대조, 반복 적용 없음. 17:02 이후 변경분은 git 델타 기준 main.js·sw.js(서비스 워커 개발 해제, UI 아님)뿐임을 확인.
- **신규 소스 감사(검색 에이전트 + 직접 판독)**: `MyMarketView.vue` · `DashboardView.vue` · `ActionCenterView.vue` · `ProfileView.vue` · `community/UserPageView.vue` · `settings/` 6개 · `ReviewsView.vue` · `SupportView.vue`.
- **보조용언 띄어쓰기 잔존 감사(grep, `frontend/src`)**: 17:02 전수 교정 이후 잔존한 `~드릴게요/~드립니다/~뒀어요` 붙여쓰기 5건 발굴.
- 기준 문서: `docs/PRODUCT_VISION.md`(UI 미니멀 원칙), 프로젝트 UX 카피 표준(보조용언 '-아/-어 + 주다·보다·드리다' 띄어쓰기).

## (c) 발견한 개선 포인트
1. **채팅 요청 수락·거절 확인 문구 조사 오류(명백한 오류)**: `ActionCenterView.vue`의 확인 다이얼로그가 모든 요청 유형에 '을' 고정. `CHAT_TAG`의 `요금 협의`·`취소`는 받침 없는 단어라 **"요금 협의을 수락할까요?"·"취소을 거절할까요?"** 로 노출됨 → **적용**(받침 판별 헬퍼로 '을/를' 동적 선택).
2. **보조용언 붙여쓰기 잔존 5건(명백한 불일치)**: 17:02 전수 교정 후에도 `알려드릴게요`(SupportView 접수 토스트·SettingsVerificationView 접수 토스트), `알려드립니다`(SettingsNotificationsView 2곳), `모아뒀어요`(ProfileView 바로가기 힌트)가 남음. 동일 파일·동일 클래스에 `알려 드려요`(ReportDialog)·`보내 드려요`(ForgotPasswordView) 등 띄어쓰기 표준 사례가 있어 국소적 아웃라이어 → **적용**.
3. **ReviewsView '내가 쓴 리뷰' 블록 들여쓰기 어긋남(소스 편집 잔재)**: 같은 파일 '받은 리뷰' 블록과 달리 4칸 과다 들여쓰기 + 닫는 태그 정렬 어긋남(마크업 중첩 자체는 정상, 표시 영향 없음) → **적용**(정리).
4. console.log·debugger·중첩 인터랙티브 요소·v-for :key 중복·오더 용어 누출 0건(상태 유지).

## (d) 실제 적용한 수정
1. `views/orders/ActionCenterView.vue` — `particleUl()` 헬퍼(받침 유무로 을/를 판별) 추가, `resolveChatRequest` 확인 문구를 `${label}${particleUl(label)} 수락할까요?` 형태로 교정(2행).
2. `views/SupportView.vue:84` — '알려드릴게요' → '알려 드릴게요'.
3. `views/settings/SettingsVerificationView.vue:131` — '알려드릴게요' → '알려 드릴게요'.
4. `views/settings/SettingsNotificationsView.vue:27·42` — '알려드립니다' → '알려 드립니다' 2건.
5. `views/ProfileView.vue:209` — '모아뒀어요' → '모아 뒀어요'.
6. `views/ReviewsView.vue:205~211` — '내가 쓴 리뷰' 헤드 블록 과다 들여쓰기 정리(받은 리뷰 블록과 동일 깊이로 통일).

## (e) 적용하지 않은 항목과 이유 (후보)
1. **MyMarketView '정산' 탭 빈 상태 문구·CTA**(불확실): 빈 목록일 때 기본 문구 '등록한 운행이 없습니다 + 운행 등록'이 노출되지만, 운행은 있되 정산 건이 0건인 사용자에게 어긋날 수 있음. 탭별 문구를 의도적으로 범용화했을 가능성도 있어 제품 확인 후 적용.
2. **ActionCenterView '가져오기' vs '운행 시작 승인' 용어 불일치**(불확실): claim 건을 '가져오기 요청/승인'과 '운행 시작 승인'으로 달리 표현. 별개 단계를 뜻하는 것일 수 있어 보류.
3. **SupportView/Settings 상태 칩 등 잔여 font-weight 700 필**(기존 후보 유지): 배지 표준(10px·400)과 다른 요소들 전반 정리는 프로젝트 차원 결정 필요.
4. **기존 반복 후보(17:02 기록분 유지, 상태 재확인)**: coin 아이콘+"원" 이중 표기 · UiChip 기본 스타일 배지화 · OrderDetailView `.hero-badge--urgent` · 채팅 목록 탭 선택 초기화 · '지난주' 칩/그룹 라벨 단차 · 승인 요청 칩 색 · HomeView·ChatList 칩 CSS 중복 · ActionCenterView 채팅 요청 칩 색 · DashboardView scoped CSS 중복·데드 규칙 · 레벨 타이틀 "드라이버" · 홈 추천 조건% 미노출(의도된 결정) · LoginView·RegisterView auth CSS 중복 · OrderCard 들여쓰기 · OrderCreateView CSS 중복 · 커뮤니티 카테고리 색 · 의미색 하드코딩 · naive 전역 테마 · 문의 placeholder 문구(SupportView, "운행 번호" 규정 용어) · MarketView 데드 CSS · brand/accent 위 글자색 전역 설계 · 등록자 가입 직행 `my-market` · `role === 'Driver'` 리터럴 7곳 상수 치환 · RegisterView '등록자' 설명 문구 · AdminView.vue 잔여 구간 전수 판독 — 기존 분류 유지.
- 참고: 17:02 이후 커밋되지 않은 main.js(개발 환경 서비스 워커 해제)·sw.js 변경은 UI 개선이 아니므로 이번 보고서 범위에서 제외.

## (f) 빌드 결과
- **`npm run build` 통과** (vite v8.2.1, ✓ built in 1.02s) — 수정 6개 파일 포함 전체 번들 오류 없음.
- import 누락 점검: 4건은 문자열 리터럴 교정(import 영향 없음), ActionCenterView는 파일 내 로컬 함수 추가(신규 심볼·import 없음), ReviewsView는 템플릿 들여쓰기만 조정 → `npm run build` 통과로 검증 대체. 수정 후 그렙 재확인으로 띄어쓰기 표준·particleUl 적용 상태 지속 확인(붙여쓰기 잔존 0건).
- 서버 배포·빌드·DB 동기화는 수행하지 않음(로컬 작업만). 적용분은 미커밋 상태로 워킹트리에 남김(사용자 커밋 대기).
