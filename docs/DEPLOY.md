# DEPLOY

## 기본 절차
1. 의존성 설치
2. 환경 변수 설정
3. 앱 키 생성
4. 마이그레이션 실행
5. 프론트 자산 빌드
6. 웹 서버 연결

## 체크리스트
- `.env` 설정 완료
- `APP_KEY` 생성 완료
- DB 연결 확인
- `php artisan migrate` 실행
- `npm run build` 실행
- 로그 및 권한 확인

## 참고
- Laravel Cloud 또는 일반적인 PHP 호스팅 환경에 배포 가능
- 프론트 변경 사항이 보이지 않으면 빌드 또는 개발 서버 상태를 확인한다.
